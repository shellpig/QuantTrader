# QuantTrader 程式碼設計方針

> **目的：** 本文件是給 AI Agent 或開發者的**實作指引**，依照 `量化交易系統規格書_shellpig版.md` 撰寫。
> 每個 Phase/子階段列出：要建的檔案、類別/函式簽名、資料契約、依賴關係、測試要求。
> **閱讀本文件的 Agent 應可直接動手寫 code，不需要再回頭讀規格書。**

---

## 全域規範（所有 Phase 共用）

### 專案根目錄

```
C:\Users\User\OneDrive\桌面\AI_Work\ProjectClaude\QuantTrader\
```

### Python 版本與套件管理

- **Python 3.12+**
- **套件管理：`uv`**（`pyproject.toml` 定義依賴，`uv sync` 安裝）
- 所有 import 均使用**絕對路徑**：`from src.data.fetcher import FinMindFetcher`

### pyproject.toml 基本結構

```toml
[project]
name = "quanttrader"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2",
    "pandas-ta>=0.3.14b1",
    "duckdb>=1.0",
    "pyarrow>=17.0",
    "plotly>=5.22",
    "streamlit>=1.37",
    "anthropic>=0.34",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
    "requests>=2.32",
    "yfinance>=0.2.40",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.5"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
target-version = "py312"
line-length = 120
```

### 型別標注

- 所有公開函式**必須**有完整的 type hint（參數 + 回傳值）
- 使用 Python 3.12 原生語法：`list[str]`、`dict[str, float]`、`str | None`

### 時區鐵律

- **所有 datetime 必須為 timezone-aware**
- 禁止出現 naive datetime
- Phase 1-8 台股資料使用 `Asia/Taipei`
- Phase 9 起改為 market-aware timezone：台股 `Asia/Taipei`，美股 `America/New_York`
- 常數定義在 `src/core/constants.py`；新增市場時不得把 timezone 寫死在 fetcher、storage、backtest 或 UI 任一層

### 設定檔讀取

- 密鑰：`.env` → 用 `python-dotenv` 讀取
- 參數：`config.yaml` → 用 `pyyaml` 讀取
- 統一透過 `src/core/config.py` 的 `get_config()` 函式存取

### 錯誤處理原則

- 外部 API 呼叫：捕獲 `requests.exceptions.RequestException`，重試最多 3 次（間隔 1/2/4 秒）
- 資料驗證失敗：拋出自定義 `DataQualityError`（定義在 `src/core/exceptions.py`）
- 內部模組間：信任輸入，不做冗餘驗證

### 測試規範

- 框架：`pytest`
- 測試檔命名：`tests/test_{module_name}.py`
- 固定測試資料放 `tests/fixtures/`（小型 CSV 或 Parquet，手工構造已知答案）
- 不 mock 資料庫（DuckDB 用 `:memory:` 模式即可）
- 外部 API 測試用 `@pytest.mark.integration` 標記，預設不跑

### 目錄結構總覽

```
QuantTrader/
├── src/
│   ├── __init__.py
│   ├── core/
│   │   ├── __init__.py
│   │   ├── config.py          # config.yaml + .env 讀取
│   │   ├── constants.py       # TAIPEI_TZ、MARKET_OPEN/CLOSE 等常數
│   │   └── exceptions.py      # 自定義例外
│   ├── data/
│   │   ├── __init__.py
│   │   ├── fetcher.py         # IDataFetcher + FinMindFetcher + YFinanceFetcher
│   │   ├── cleaner.py         # L1/L2/L3 清洗
│   │   ├── storage.py         # ParquetStorage + DuckDBMeta
│   │   └── maintenance.py     # rebuild / validate 指令
│   ├── backtest/
│   │   ├── __init__.py
│   │   ├── base.py            # BacktesterBase ABC
│   │   ├── engine_vec.py      # VectorizedBacktester
│   │   ├── engine_event.py    # EventDrivenBacktester
│   │   ├── account.py         # Account ABC + SimpleAccount
│   │   ├── events.py          # BarEvent, OrderEvent, FillEvent
│   │   ├── cost.py            # 手續費、交易稅、滑價計算
│   │   ├── metrics.py         # 績效指標計算
│   │   ├── batch.py           # 批次策略比較 runner
│   │   ├── sweep.py           # 參數掃描 Grid Search runner
│   │   ├── walk_forward.py    # Walk-Forward Analysis runner
│   │   ├── optimizer.py       # GridSearch（舊版，保留）
│   │   └── report.py          # Tearsheet 生成
│   ├── strategy/
│   │   ├── __init__.py
│   │   ├── base.py            # StrategyBase ABC
│   │   └── examples/
│   │       ├── __init__.py
│   │       ├── ma_cross.py           # 雙均線交叉策略
│   │       ├── dca.py                # 定期定額策略
│   │       ├── rsi.py                # RSI 超買超賣策略
│   │       ├── kd_cross.py           # KD 交叉策略
│   │       ├── macd_cross.py         # MACD 交叉策略
│   │       ├── bollinger_band.py     # 布林通道策略
│   │       ├── bias.py               # 乖離率策略
│   │       └── donchian_breakout.py  # 突破策略（Donchian Channel）
│   ├── indicators/
│   │   ├── __init__.py
│   │   └── calculator.py      # IndicatorEngine（pandas-ta 封裝 + 別名映射）
│   ├── ai/
│   │   ├── __init__.py
│   │   └── advisor.py         # AIAdvisor（LLM Provider Tool Use）
│   └── ui/
│       ├── __init__.py
│       └── app.py             # Streamlit 主程式
├── tests/
│   ├── __init__.py
│   ├── fixtures/              # 測試用固定資料
│   ├── test_fetcher.py
│   ├── test_storage.py
│   ├── test_cleaner.py
│   ├── test_cost.py
│   ├── test_metrics.py
│   ├── test_account.py
│   ├── test_engine_vec.py
│   ├── test_engine_event.py
│   ├── test_consistency.py
│   ├── test_indicators.py
│   ├── test_advisor.py
│   ├── test_strategies.py
│   ├── test_batch.py
│   ├── test_sweep.py
│   └── test_walk_forward.py
├── data/                      # gitignore，執行時自動建立
├── notebooks/
├── config.yaml
├── .env.example
├── .gitignore
└── pyproject.toml
```

---

## Phase 1：台股資料基礎建設

### 1-A 資料源連線驗證

#### 要建的檔案

1. **`src/core/__init__.py`** — 空檔
2. **`src/core/constants.py`**
3. **`src/core/exceptions.py`**
4. **`src/core/config.py`**
5. **`src/data/__init__.py`** — 空檔
6. **`src/data/fetcher.py`**
7. **`tests/test_fetcher.py`**
8. **`config.yaml`**
9. **`.env.example`**
10. **`.gitignore`**
11. **`pyproject.toml`**

#### `src/core/constants.py`

```python
TAIPEI_TZ = "Asia/Taipei"

# 台股集中市場交易時間
MARKET_OPEN = "09:00"
MARKET_CLOSE = "13:30"

# 台股漲跌停幅度（2015/06/01 起）
PRICE_LIMIT_PCT = 0.10

# 標準化欄位名稱
STANDARD_COLUMNS = ["date", "open", "high", "low", "close", "volume", "symbol"]
```

#### `src/core/exceptions.py`

```python
class DataQualityError(Exception): ...
class FetcherError(Exception): ...
class StorageError(Exception): ...
```

#### `src/core/config.py`

```python
import os
from pathlib import Path
import yaml
from dotenv import load_dotenv

_config: dict | None = None

def get_config() -> dict:
    """讀取 config.yaml，合併 .env 密鑰，回傳完整設定 dict。結果快取。"""
    ...

def get_project_root() -> Path:
    """回傳專案根目錄（含 config.yaml 的那層）。"""
    ...

def get_data_dir() -> Path:
    """回傳 data/ 絕對路徑，不存在則自動建立。"""
    ...
```

#### `src/data/fetcher.py` — 類別設計

```python
from abc import ABC, abstractmethod
import pandas as pd

class IDataFetcher(ABC):
    """所有資料源 Fetcher 的抽象介面。"""

    @abstractmethod
    def fetch_daily(self, symbol: str, start: str, end: str) -> pd.DataFrame:
        """
        取得日K資料。

        Args:
            symbol: 台股代碼，純數字字串，如 "2330"
            start: 起始日期 "YYYY-MM-DD"
            end: 結束日期 "YYYY-MM-DD"

        Returns:
            DataFrame，欄位為 STANDARD_COLUMNS，date 為 tz-aware Asia/Taipei
            若無資料回傳空 DataFrame（欄位仍齊全）
        """
        ...

    @abstractmethod
    def fetch_minute(self, symbol: str, start: str, end: str, freq: str = "1") -> pd.DataFrame:
        """
        取得分K資料。freq: "1"=1分K, "5"=5分K, "15", "30", "60"
        回傳格式同 fetch_daily。
        """
        ...


class FinMindFetcher(IDataFetcher):
    """
    FinMind API 資料源。

    實作要點：
    - API base URL: https://api.finmindtrade.com/api/v4/data
    - 認證：header 帶 Authorization: Bearer {token}，token 從 config 讀取
    - 日K dataset: "TaiwanStockPrice"
    - 分K dataset: "TaiwanStockPriceMinute"
    - API 回傳 JSON，取 data 欄位轉 DataFrame
    - 欄位映射：date→date, open→open, max→high, min→low, close→close,
      Trading_Volume→volume, stock_id→symbol
    - 取得後立即呼叫 localize_to_taipei() 轉時區
    - HTTP 錯誤重試 3 次
    """

    def __init__(self, token: str | None = None):
        # token 若為 None，從 config 讀取
        ...


class YFinanceFetcher(IDataFetcher):
    """
    yfinance 備援資料源。

    實作要點：
    - symbol 加 ".TW" 後綴（如 "2330" → "2330.TW"）
    - 使用 yfinance.download() 取得資料
    - 欄位映射：Date→date, Open→open, High→high, Low→low, Close→close, Volume→volume
    - volume 單位為「股」（yfinance 台股已是股數，無需轉換）
    - yfinance 回傳的 date 可能為 UTC 或 naive，需 localize_to_taipei()
    """
    ...


def localize_to_taipei(df: pd.DataFrame, col: str = "date") -> pd.DataFrame:
    """
    將 date 欄時區標準化為 Asia/Taipei。

    關鍵：FinMind 回傳 naive datetime 但語意已是台北時間 → 用 tz_localize（標記）
    若已有時區 → 用 tz_convert（轉換）
    絕對不可對台股來源的 naive datetime 用 tz_convert，否則會多加 8 小時。
    """
    ...
```

#### 測試要求 — `tests/test_fetcher.py`

```python
# === 單元測試（不需網路）===

def test_localize_naive_to_taipei():
    """naive datetime → tz_localize → 值不變，tz 為 Asia/Taipei"""

def test_localize_utc_to_taipei():
    """UTC datetime → tz_convert → 值 +8h，tz 為 Asia/Taipei"""

def test_empty_dataframe_has_standard_columns():
    """空 DataFrame 仍有 STANDARD_COLUMNS 所有欄位"""

# === 整合測試（需網路 + API token）===

@pytest.mark.integration
def test_finmind_daily_2330():
    """FinMind 取 2330 日K → HTTP 200, DataFrame 非空, 欄位齊全, dtype 正確"""

@pytest.mark.integration
def test_yfinance_daily_2330():
    """yfinance 取 2330.TW 日K → DataFrame 非空, 欄位齊全"""

@pytest.mark.integration
def test_both_sources_same_schema():
    """兩個 Fetcher 對同一 symbol 回傳的 DataFrame schema 一致"""
```

---

### 1-B Parquet 存儲結構

#### 要建的檔案

1. **`src/data/storage.py`**
2. **`tests/test_storage.py`**

#### `src/data/storage.py`

```python
from pathlib import Path
import pandas as pd
import duckdb

class ParquetStorage:
    """
    Parquet 檔案讀寫。

    路徑規則：
    - 日K：data/raw/tw/{symbol}/daily.parquet
    - 分K：data/raw/tw/{symbol}/minute.parquet
    - 前復權：data/processed/tw/{symbol}/adj_daily.parquet
    - 回測結果：data/backtest/{backtest_id}.parquet
    """

    def __init__(self, data_dir: Path | None = None):
        # data_dir 預設從 config 讀取
        ...

    def save_daily(self, symbol: str, df: pd.DataFrame) -> Path:
        """
        儲存日K。若檔案已存在，做 upsert（以 date 為 key，去重後合併）。
        回傳寫入的檔案路徑。

        去重邏輯：
        1. 讀取既有 parquet（若存在）
        2. concat 新舊資料
        3. drop_duplicates(subset=["date", "symbol"], keep="last")
        4. sort_values("date")
        5. 寫回 parquet
        """
        ...

    def load_daily(self, symbol: str) -> pd.DataFrame:
        """讀取日K。檔案不存在時回傳空 DataFrame（欄位齊全）。"""
        ...

    def save_minute(self, symbol: str, df: pd.DataFrame) -> Path:
        """儲存分K。去重邏輯同 save_daily。"""
        ...

    def load_minute(self, symbol: str) -> pd.DataFrame:
        ...

    def save_adjusted(self, symbol: str, df: pd.DataFrame) -> Path:
        """儲存前復權資料至 processed/ 路徑。"""
        ...

    def load_adjusted(self, symbol: str) -> pd.DataFrame:
        ...


class DuckDBMeta:
    """
    DuckDB 元資料管理。
    資料庫檔案：data/quant.duckdb

    表結構：
    CREATE TABLE IF NOT EXISTS data_meta (
        symbol      VARCHAR NOT NULL,
        freq        VARCHAR NOT NULL,  -- 'daily' | 'minute'
        source      VARCHAR NOT NULL,  -- 'finmind' | 'yfinance'
        first_date  TIMESTAMP WITH TIME ZONE,
        last_date   TIMESTAMP WITH TIME ZONE,
        row_count   INTEGER,
        updated_at  TIMESTAMP WITH TIME ZONE,
        PRIMARY KEY (symbol, freq)
    );
    """

    def __init__(self, db_path: Path | None = None):
        # db_path 預設 data/quant.duckdb
        ...

    def upsert_meta(self, symbol: str, freq: str, source: str,
                     first_date: pd.Timestamp, last_date: pd.Timestamp,
                     row_count: int) -> None:
        """插入或更新元資料。updated_at 自動填入當前時間。"""
        ...

    def get_meta(self, symbol: str, freq: str) -> dict | None:
        """查詢指定 symbol+freq 的元資料。不存在回傳 None。"""
        ...

    def list_all(self) -> pd.DataFrame:
        """回傳所有元資料列表（用於 UI 顯示）。"""
        ...
```

#### 測試要求 — `tests/test_storage.py`

```python
import tempfile

def test_save_and_load_daily_roundtrip(tmp_path):
    """寫入 → 讀出 → assert_frame_equal"""

def test_upsert_no_duplicates(tmp_path):
    """寫入 10 筆 → 再寫入含重疊 5 筆（共 15 筆中有 5 筆重複 date）→ 結果恰 10+5=15 筆去重後正確筆數"""

def test_load_nonexistent_returns_empty(tmp_path):
    """讀取不存在的檔案 → 回傳空 DataFrame，欄位齊全"""

def test_duckdb_meta_upsert_and_query():
    """用 :memory: DuckDB → upsert → get_meta → 值正確"""

def test_duckdb_meta_upsert_overwrite():
    """同一 symbol+freq upsert 兩次 → get_meta 回傳最後一次的值"""
```

---

### 1-C 資料品質檢核

#### 要建的檔案

1. **`src/data/cleaner.py`**
2. **`tests/test_cleaner.py`**
3. **`tests/fixtures/bad_data.csv`** — 手工構造的壞資料

#### `src/data/cleaner.py`

```python
import pandas as pd
from dataclasses import dataclass

@dataclass
class QualityReport:
    """品質檢核報告。"""
    symbol: str
    total_rows: int
    negative_price_count: int       # L1: 負價格
    zero_price_count: int           # L1: 零價格
    extreme_change_count: int       # L1: 漲跌超過 ±10%
    extreme_change_dates: list[str] # L1: 超限日期列表
    zero_volume_count: int          # 成交量=0 日數
    filled_count: int               # L2: 被前向填充的缺值數
    suspension_ranges: list[tuple[str, str]]  # L2: 停牌區間 [(start, end), ...]

    def summary(self) -> str:
        """回傳人類可讀的摘要文字。"""
        ...


class DataCleaner:
    """
    資料品質檢核與清洗。

    使用方式：
        cleaner = DataCleaner()
        cleaned_df, report = cleaner.clean(raw_df, symbol="2330")
    """

    def clean(self, df: pd.DataFrame, symbol: str) -> tuple[pd.DataFrame, QualityReport]:
        """
        依序執行 L1 → L2 清洗，回傳 (清洗後 DataFrame, 品質報告)。
        不修改原始 df（回傳新物件）。
        """
        ...

    def _l1_detect_anomalies(self, df: pd.DataFrame) -> tuple[pd.DataFrame, dict]:
        """
        L1 異常值偵測：
        1. 價格（open/high/low/close）為負值或 0 → 整列標記移除
        2. 單日漲跌超過 ±10% → 標記（加入 _flagged 欄位），不自動移除
           - 計算方式：(close - prev_close) / prev_close
           - 第一筆無前日資料，跳過
           - 注意：IPO 首五日豁免邏輯在 V1 先不實作，僅標記警告
        3. 成交量 = 0 → 不移除，僅記錄

        回傳：(處理後 df, 統計 dict)
        """
        ...

    def _l2_fill_missing(self, df: pd.DataFrame) -> tuple[pd.DataFrame, dict]:
        """
        L2 缺失值填補：
        1. 日K：前向填充（ffill），連續缺值 > 5 日標記停牌
        2. 識別停牌區間：連續成交量 = 0 且 ≥ 5 個交易日

        回傳：(填補後 df, 統計 dict)
        """
        ...
```

#### 測試用固定資料 — `tests/fixtures/bad_data.csv`

手工構造，包含以下情境（約 20 筆）：

| 情境 | 範例 |
|------|------|
| 負價格 | close = -50.0 |
| 零價格 | open = 0.0 |
| 漲幅 > 10% | 前日 close=100, 今日 close=115（+15%）|
| 正常漲停 | 前日 close=100, 今日 close=110（+10%，正常）|
| 成交量 = 0 | volume = 0（停牌）|
| 連續 3 日缺值 | 三筆 NaN |
| 連續 6 日成交量 0 | 連續停牌，應標記停牌區間 |

#### 測試要求 — `tests/test_cleaner.py`

```python
def test_l1_negative_price_removed():
    """注入負價格 → clean 後該列消失"""

def test_l1_extreme_change_flagged():
    """注入 +15% 漲幅 → report.extreme_change_count > 0, 日期正確"""

def test_l1_normal_limit_up_not_flagged():
    """+10% 漲停 → 不應被標記（邊界值：恰好 10% 不超限）"""

def test_l2_ffill_3_day_gap():
    """3 日 NaN → 填補正確，值等於缺值前最後一筆"""

def test_l2_suspension_detected():
    """連續 6 日成交量 0 → report.suspension_ranges 包含該區間"""

def test_clean_returns_new_dataframe():
    """原始 df 不被修改"""
```

---

### 1-D 除權息前復權調整

#### 要建的檔案

1. **在 `src/data/fetcher.py` 加入**：`FinMindFetcher.fetch_dividends()` 方法
2. **`src/data/cleaner.py` 加入**：`adjust_prices()` 函式（或獨立模組 `adjuster.py`，建議放 cleaner 即可）
3. **`src/data/maintenance.py`**
4. **`tests/test_cleaner.py` 加入**：除權息相關測試
5. **`tests/fixtures/dividends_2330.csv`** — 2330 已知除權息資料

#### 除權息資料取得 — 在 `FinMindFetcher` 加入

```python
class FinMindFetcher(IDataFetcher):
    # ... 既有方法 ...

    def fetch_dividends(self, symbol: str) -> pd.DataFrame:
        """
        取得除權息資料。
        FinMind dataset: "TaiwanStockDividend"

        回傳 DataFrame 欄位：
        - date: 除權息交易日 (tz-aware)
        - cash_dividend: 現金股利（元/股）
        - stock_dividend: 股票股利（元/股，即配股率 × 10）
        - symbol: str
        """
        ...
```

#### 前復權邏輯 — 在 `src/data/cleaner.py` 加入

```python
def compute_adjustment_factors(
    daily_df: pd.DataFrame,
    dividends_df: pd.DataFrame
) -> pd.DataFrame:
    """
    計算調整因子序列。

    邏輯：
    1. 對每個除權息日，計算調整因子：
       factor = 1 - (cash_dividend / ex_date_prev_close)
       （若有股票股利，還需乘以 1 / (1 + stock_dividend/10)）
    2. 從最新日期往回累乘（前復權 = 最新基準日不動，歷史價格向下調整）
    3. 回傳與 daily_df 等長的 adj_factor Series

    回傳 DataFrame：原始 daily_df 加上 adj_factor 欄位
    """
    ...


def adjust_prices(
    df: pd.DataFrame,
    dividends: pd.DataFrame,
    method: str = "forward"
) -> pd.DataFrame:
    """
    套用除權息調整。

    method:
    - "forward": 前復權。OHLC × adj_factor。最新除權息日之後 factor=1（價格不變）。
    - "raw": 不調整，原樣回傳。

    回傳：新的 DataFrame（不修改原始）
    """
    ...
```

#### `src/data/maintenance.py`

```python
class DataMaintenance:
    """
    資料維護工具。

    依賴：IDataFetcher, ParquetStorage, DuckDBMeta, DataCleaner
    透過 __init__ 注入，方便測試時替換。
    """

    def __init__(
        self,
        fetcher: IDataFetcher,
        storage: ParquetStorage,
        meta: DuckDBMeta,
        cleaner: DataCleaner,
    ):
        ...

    def rebuild_symbol(self, symbol: str) -> QualityReport:
        """
        完整重建指定標的：
        1. 重新下載日K 全部歷史
        2. 重新下載除權息資料
        3. 執行 L1/L2 清洗
        4. 計算前復權
        5. 分別存入 raw/ 和 processed/
        6. 更新 DuckDB 元資料
        7. 回傳品質報告
        """
        ...

    def rebuild_adj_factors(self, symbol: str) -> None:
        """僅重算除權息調整因子（不重新下載原始資料）。"""
        ...

    def validate_data(self, symbol: str) -> QualityReport:
        """讀取已存儲的資料，執行 L1/L2/L3 品質驗證，回傳報告。"""
        ...

    def list_stale_symbols(self, days: int = 7) -> list[dict]:
        """從 DuckDB 查出 updated_at 超過 days 天的 symbol 列表。"""
        ...

    def update_daily(self, symbol: str) -> int:
        """
        增量更新：只補抓 last_date+1 至今日的資料。
        回傳新增筆數。
        """
        ...
```

#### 測試要求

```python
# tests/test_cleaner.py（追加）

def test_adj_factor_no_dividend():
    """無除權息 → adj_factor 全為 1.0"""

def test_adj_factor_cash_dividend():
    """已知除息：前日收盤 100, 現金股利 5 → 除息日前 factor < 1"""

def test_forward_adj_latest_price_unchanged():
    """前復權後，最新除權息日之後的收盤價 = 原始收盤價"""

def test_adj_no_gap_across_ex_date():
    """除息日前後調整後收盤價連續（差值 < 0.01）"""

def test_raw_method_returns_unchanged():
    """method="raw" → 回傳的 df 與原始 df 相同"""
```

---

## Phase 2：向量化回測引擎

### 前置依賴

Phase 2 所有模組依賴 Phase 1 的 `ParquetStorage`（讀取資料）和 `cleaner.py`（前復權資料）。

---

### 2-A 訊號生成框架

#### 要建的檔案

1. **`src/strategy/__init__.py`**
2. **`src/strategy/base.py`**
3. **`src/strategy/examples/__init__.py`**
4. **`src/strategy/examples/ma_cross.py`**
5. **`tests/test_engine_vec.py`**（先建骨架）

#### `src/strategy/base.py`

```python
from abc import ABC, abstractmethod
import pandas as pd
from src.backtest.events import BarEvent, OrderEvent
from src.backtest.account import Account

class StrategyBase(ABC):
    """
    策略基類。同時支援向量化引擎與事件驅動引擎。

    向量化引擎呼叫 generate_signals()。
    事件驅動引擎呼叫 on_bar()。
    子類別必須兩者都實作。
    """

    @abstractmethod
    def generate_signals(self, df: pd.DataFrame) -> pd.Series:
        """
        向量化訊號生成。

        Args:
            df: 含 OHLCV 的 DataFrame（已前復權）

        Returns:
            pd.Series，index 與 df 對齊，值為：
            +1 = 買入訊號
            -1 = 賣出訊號
             0 = 無動作

        注意：
        - 訊號在 Bar t 收盤後產生，意味著 signal[t] 的交易在 t+1 開盤執行
        - 不可使用未來資料（signal[t] 只能用 df[:t+1] 的資訊）
        """
        ...

    @abstractmethod
    def on_bar(self, bar: BarEvent, account: Account) -> list[OrderEvent]:
        """事件驅動模式：每根 K 線呼叫一次。"""
        ...

    def on_fill(self, fill, account: Account) -> None:
        """訂單成交後回呼（可選覆寫）。"""
        pass
```

#### `src/strategy/examples/ma_cross.py`

```python
class MACrossStrategy(StrategyBase):
    """
    雙均線交叉策略。

    參數：
    - ma_short: 短期均線天數（預設 20）
    - ma_long: 長期均線天數（預設 60）

    訊號邏輯：
    - 短均線上穿長均線（黃金交叉）→ +1（買）
    - 短均線下穿長均線（死亡交叉）→ -1（賣）
    - 其餘 → 0

    向量化實作：
    - ma_short_series = df["close"].rolling(ma_short).mean()
    - ma_long_series = df["close"].rolling(ma_long).mean()
    - signal = 當 ma_short > ma_long 時為 +1，否則為 -1
    - 用 diff() 偵測交叉點，非交叉日為 0

    事件驅動實作：
    - 內部維護 close 歷史 list
    - 每次 on_bar 時計算最新的短/長均線
    - 偵測交叉並回傳 OrderEvent
    """

    def __init__(self, ma_short: int = 20, ma_long: int = 60):
        self.ma_short = ma_short
        self.ma_long = ma_long
```

#### 測試要求

```python
# tests/fixtures/ma_cross_data.csv
# 手工構造 100 筆日K，其中已知 MA20/MA60 在第 65 筆交叉

def test_ma_cross_signal_golden_cross():
    """在已知交叉日，signal = +1"""

def test_ma_cross_signal_no_lookahead():
    """signal[t] 只依賴 df[:t+1] → 用逐筆比對驗證"""

def test_ma_cross_signal_alignment():
    """signal.index == df.index"""

def test_signal_values_only_minus1_0_plus1():
    """signal 的唯一值集合 ⊆ {-1, 0, +1}"""
```

---

### 2-B 市場摩擦成本計算

#### 要建的檔案

1. **`src/backtest/__init__.py`**
2. **`src/backtest/cost.py`**
3. **`tests/test_cost.py`**

#### `src/backtest/cost.py`

```python
from dataclasses import dataclass

@dataclass
class TradeCost:
    """單筆交易成本明細。"""
    commission: float     # 手續費
    tax: float            # 交易稅
    slippage: float       # 滑價成本（金額）
    total: float          # commission + tax + slippage


class CostCalculator:
    """
    台股交易成本計算器。

    參數來自 config.yaml：
    - commission_rate: 0.001425
    - commission_discount: 0.6
    - tax_rate: 0.003（一般股票）
    - etf_tax_rate: 0.001（ETF）
    - slippage_ticks: 1
    """

    def __init__(
        self,
        commission_rate: float = 0.001425,
        commission_discount: float = 0.6,
        tax_rate: float = 0.003,
        etf_tax_rate: float = 0.001,
        slippage_ticks: int = 1,
    ):
        self.effective_commission_rate = commission_rate * commission_discount
        self.tax_rate = tax_rate
        self.etf_tax_rate = etf_tax_rate
        self.slippage_ticks = slippage_ticks

    def calculate(
        self,
        price: float,
        quantity: int,
        side: str,
        is_etf: bool = False,
    ) -> TradeCost:
        """
        計算單筆交易成本。

        side: "BUY" | "SELL"
        - 手續費：price × quantity × effective_commission_rate（買賣皆收）
          手續費不足 20 元以 20 元計（台股券商規定）
        - 交易稅：僅賣出收。price × quantity × tax_rate（ETF 用 etf_tax_rate）
        - 滑價：get_tick_size(price) × slippage_ticks × quantity
        """
        ...

    @staticmethod
    def get_tick_size(price: float) -> float:
        """
        台股升降單位：
        price < 10       → 0.01
        10 ≤ price < 50  → 0.05
        50 ≤ price < 100 → 0.10
        100 ≤ price < 500 → 0.50
        500 ≤ price < 1000 → 1.00
        price ≥ 1000     → 5.00
        """
        ...

    def apply_slippage(self, price: float, side: str) -> float:
        """
        回傳滑價後的成交價。
        BUY: price + tick_size × slippage_ticks
        SELL: price - tick_size × slippage_ticks
        """
        ...
```

#### 測試要求 — `tests/test_cost.py`

```python
def test_tick_size_boundaries():
    """各價位區間邊界值：9.99→0.01, 10.00→0.05, 49.99→0.05, 50.00→0.10, ..."""

def test_buy_no_tax():
    """買入 → tax = 0"""

def test_sell_has_tax():
    """賣出 1000股 × 100元 → tax = 100000 × 0.003 = 300"""

def test_etf_tax_rate():
    """ETF 賣出 → tax = price × qty × 0.001"""

def test_commission_discount():
    """手續費 = price × qty × 0.001425 × 0.6"""

def test_commission_minimum_20():
    """小金額交易 → 手續費至少 20 元"""

def test_slippage_buy():
    """price=100, BUY → slippage_price = 100 + 0.5 × 1 = 100.5"""

def test_known_trade_total():
    """手算完整交易成本 → 與程式結果誤差 < 0.01"""
```

---

### 2-C 績效指標計算

#### 要建的檔案

1. **`src/backtest/metrics.py`**
2. **`tests/test_metrics.py`**
3. **`tests/fixtures/known_returns.csv`** — 已知報酬序列（手算答案）

#### `src/backtest/metrics.py`

```python
import pandas as pd
from dataclasses import dataclass

@dataclass
class BacktestResult:
    """回測結果容器，供 report.py 使用。"""
    equity_curve: pd.Series          # 每日權益值
    returns: pd.Series               # 每日報酬率
    trades: pd.DataFrame             # 交易明細（entry_date, exit_date, side, pnl, ...）
    total_return: float
    annual_return: float
    max_drawdown: float
    max_drawdown_start: str          # 最大回撤起始日
    max_drawdown_end: str            # 最大回撤結束日
    sharpe_ratio: float
    sortino_ratio: float
    calmar_ratio: float
    win_rate: float
    profit_factor: float
    total_trades: int
    avg_holding_days: float
    max_single_loss: float


def calculate_metrics(
    equity_curve: pd.Series,
    trades: pd.DataFrame,
    risk_free_rate: float = 0.01,
    trading_days: int = 252,
) -> BacktestResult:
    """
    從權益曲線 + 交易明細計算全部績效指標。

    equity_curve: index=date, value=總資產市值（含現金+持倉市值）
    trades: 每筆完成的交易，欄位含 entry_date, exit_date, pnl
    risk_free_rate: 年化無風險利率（台灣定存約 1%）
    trading_days: 台股年交易日數

    計算公式：
    - returns = equity_curve.pct_change().dropna()
    - annual_return = (equity_curve.iloc[-1] / equity_curve.iloc[0]) ^ (252/n_days) - 1
    - sharpe = (returns.mean() - rf_daily) / returns.std() × sqrt(252)
    - sortino = (returns.mean() - rf_daily) / downside_std × sqrt(252)
      downside_std = std of returns[returns < 0]（若全正則 sortino = inf → 用 999.0 代替）
    - calmar = annual_return / abs(max_drawdown)（若 MDD=0 → 999.0）
    - max_drawdown = max(1 - equity_curve / equity_curve.cummax())
    - win_rate = winning_trades / total_trades
    - profit_factor = sum(gains) / abs(sum(losses))（若無虧損 → 999.0）
    """
    ...


def calculate_max_drawdown(equity_curve: pd.Series) -> tuple[float, str, str]:
    """
    回傳 (max_drawdown_pct, peak_date, trough_date)。
    max_drawdown 為正數（如 0.15 表示 15%）。
    """
    ...


def calculate_monthly_returns(equity_curve: pd.Series) -> pd.DataFrame:
    """
    計算月度報酬表，用於熱力圖。

    回傳 DataFrame：
    - index: 年份
    - columns: 1-12 月
    - values: 該月報酬率（小數）
    """
    ...
```

#### 測試要求 — `tests/test_metrics.py`

```python
def test_sharpe_known_returns():
    """已知報酬序列 [0.01, 0.02, -0.01, 0.015, ...] → 手算 Sharpe，程式誤差 < 0.001"""

def test_max_drawdown_simple():
    """equity = [100, 110, 90, 95] → MDD = (110-90)/110 ≈ 0.1818"""

def test_max_drawdown_dates():
    """MDD 的 peak_date 和 trough_date 正確"""

def test_all_winning_edge_case():
    """全贏 → MDD=0, win_rate=1.0, profit_factor=999.0"""

def test_monthly_returns_shape():
    """2 年資料 → 回傳 2 行 × 12 列"""

def test_sortino_no_downside():
    """全正報酬 → sortino = 999.0（避免除以零）"""
```

---

### 2-D Tearsheet 報告生成

#### 要建的檔案

1. **`src/backtest/report.py`**
2. **`tests/test_report.py`**（驗證能正常渲染，不驗證視覺效果）

#### `src/backtest/report.py`

```python
import plotly.graph_objects as go
from plotly.subplots import make_subplots
from src.backtest.metrics import BacktestResult

class TearsheetReport:
    """
    Tearsheet 生成器。輸入 BacktestResult，輸出 Plotly Figure 或 HTML。
    """

    def __init__(self, result: BacktestResult):
        self.result = result

    def create_equity_chart(self) -> go.Figure:
        """
        累積報酬曲線。
        - X 軸：日期
        - Y 軸：權益值
        - 若有 trades，在圖上標記買入（綠三角上）和賣出（紅三角下）
        """
        ...

    def create_drawdown_chart(self) -> go.Figure:
        """回撤曲線（填充面積圖，紅色向下）。"""
        ...

    def create_monthly_heatmap(self) -> go.Figure:
        """
        月度報酬熱力圖。
        - X 軸：月份 (1-12)
        - Y 軸：年份
        - 顏色：紅（負）→ 白（零）→ 綠（正）
        - 每格顯示百分比數字
        """
        ...

    def create_summary_table(self) -> go.Figure:
        """績效數字摘要表（Plotly Table）。"""
        ...

    def create_full_tearsheet(self) -> go.Figure:
        """
        合併所有圖表為一個 multi-subplot Figure。
        佈局：
        Row 1: 累積報酬曲線（佔 40% 高度）
        Row 2: 回撤曲線（佔 20%）
        Row 3: 月度熱力圖（佔 25%）
        Row 4: 摘要表（佔 15%）
        """
        ...

    def save_html(self, filepath: str) -> None:
        """將完整 Tearsheet 存為 HTML 檔。"""
        fig = self.create_full_tearsheet()
        fig.write_html(filepath)

    def get_streamlit_figures(self) -> dict[str, go.Figure]:
        """回傳各子圖的 dict，供 Streamlit 用 st.plotly_chart() 分別顯示。"""
        return {
            "equity": self.create_equity_chart(),
            "drawdown": self.create_drawdown_chart(),
            "monthly": self.create_monthly_heatmap(),
            "summary": self.create_summary_table(),
        }
```

#### `src/backtest/base.py` — Backtester 抽象介面

```python
from abc import ABC, abstractmethod
import pandas as pd
from src.strategy.base import StrategyBase
from src.backtest.metrics import BacktestResult

class BacktesterBase(ABC):
    @abstractmethod
    def run(self, strategy: StrategyBase, data: pd.DataFrame) -> BacktestResult:
        ...
```

#### `src/backtest/engine_vec.py` — 向量化引擎

```python
class VectorizedBacktester(BacktesterBase):
    """
    向量化回測引擎。

    流程：
    1. strategy.generate_signals(data) → signal Series
    2. signal 位移 1 期（signal.shift(1)）→ 模擬「下根 K 線開盤執行」
    3. 計算每日持倉變化（signal diff）
    4. 對每筆交易計算成本（CostCalculator）
    5. 計算每日權益值（equity_curve）
    6. calculate_metrics() → BacktestResult
    """

    def __init__(
        self,
        initial_capital: float = 1_000_000,
        cost_calculator: CostCalculator | None = None,
    ):
        ...

    def run(self, strategy: StrategyBase, data: pd.DataFrame) -> BacktestResult:
        ...
```

#### 測試要求

```python
def test_tearsheet_renders_without_error():
    """給定 mock BacktestResult → create_full_tearsheet() 不拋例外"""

def test_tearsheet_save_html(tmp_path):
    """save_html → 檔案存在且大小 > 0"""

def test_vectorized_backtest_basic():
    """MACross 策略 + 已知資料 → 回傳 BacktestResult，total_trades > 0"""
```

---

## Phase 3：分鐘K 事件驅動回測引擎

### 3-A 事件資料結構定義

#### 要建的檔案

1. **`src/backtest/events.py`**
2. **`tests/test_events.py`**（此檔在 Phase 2 可能已部分存在）

#### `src/backtest/events.py`

```python
from dataclasses import dataclass, asdict
from datetime import datetime

@dataclass(frozen=True)
class BarEvent:
    """
    K 線事件（不可變）。

    frozen=True 確保事件建立後不被策略意外修改。
    """
    symbol: str
    timestamp: datetime          # 必須為 tz-aware
    open: float
    high: float
    low: float
    close: float
    volume: int
    freq: str                    # "1min", "5min", "1day"

    def to_dict(self) -> dict:
        return asdict(self)


@dataclass(frozen=True)
class OrderEvent:
    """下單事件。"""
    symbol: str
    order_type: str              # "MARKET" | "LIMIT"
    side: str                    # "BUY" | "SELL"
    quantity: int                # 正整數（股數，台股 1 張 = 1000 股）
    price: float | None = None   # LIMIT 單必填，MARKET 單為 None

    def __post_init__(self):
        if self.order_type == "LIMIT" and self.price is None:
            raise ValueError("LIMIT order must have a price")
        if self.quantity <= 0:
            raise ValueError("quantity must be positive")
        if self.side not in ("BUY", "SELL"):
            raise ValueError(f"Invalid side: {self.side}")


@dataclass(frozen=True)
class FillEvent:
    """成交事件。"""
    symbol: str
    side: str
    quantity: int
    fill_price: float
    commission: float
    tax: float
    timestamp: datetime

    @property
    def total_cost(self) -> float:
        """成交總金額（含手續費 + 稅）。"""
        base = self.fill_price * self.quantity
        if self.side == "BUY":
            return base + self.commission
        else:
            return base - self.commission - self.tax

    def to_dict(self) -> dict:
        return asdict(self)
```

#### 測試要求

```python
def test_bar_event_frozen():
    """建立後修改屬性 → FrozenInstanceError"""

def test_order_event_limit_requires_price():
    """LIMIT order + price=None → ValueError"""

def test_order_event_invalid_side():
    """side="HOLD" → ValueError"""

def test_order_event_negative_quantity():
    """quantity=-100 → ValueError"""

def test_fill_event_total_cost_buy():
    """BUY 100股×50元, commission=7 → total_cost = 5000 + 7 = 5007"""

def test_fill_event_total_cost_sell():
    """SELL 100股×50元, commission=7, tax=15 → total_cost = 5000 - 7 - 15 = 4978"""

def test_events_serializable():
    """所有事件 to_dict() → 回傳 dict，值可被 json.dumps"""
```

---

### 3-B Account 狀態管理

#### 要建的檔案

1. **`src/backtest/account.py`**
2. **`tests/test_account.py`**

#### `src/backtest/account.py`

```python
from abc import ABC, abstractmethod
from src.backtest.events import FillEvent

class Account(ABC):
    """帳戶抽象介面。"""

    @abstractmethod
    def get_cash(self) -> float: ...

    @abstractmethod
    def get_position(self, symbol: str) -> int:
        """回傳持有股數。無持倉回傳 0。"""
        ...

    @abstractmethod
    def get_positions(self) -> dict[str, int]:
        """回傳所有持倉 {symbol: quantity}。"""
        ...

    @abstractmethod
    def apply_fill(self, fill: FillEvent) -> None:
        """套用成交事件，更新現金與持倉。"""
        ...

    @abstractmethod
    def get_total_value(self, current_prices: dict[str, float]) -> float:
        """回傳帳戶總市值（現金 + 持倉市值）。"""
        ...

    @abstractmethod
    def get_cost_basis(self, symbol: str) -> float:
        """回傳指定標的的平均成本價。無持倉回傳 0.0。"""
        ...


class SimpleAccount(Account):
    """
    V1.0 單一帳戶實作。

    狀態：
    - cash: float（當前現金）
    - positions: dict[str, int]（持倉股數，正數 = 多頭）
    - cost_basis: dict[str, float]（加權平均成本價）

    apply_fill 邏輯：
    BUY:
        total = fill_price × quantity + commission
        cash -= total
        更新 positions[symbol] += quantity
        更新 cost_basis（加權平均）：
            new_cost = (old_cost × old_qty + fill_price × quantity) / (old_qty + quantity)

    SELL:
        total = fill_price × quantity - commission - tax
        cash += total
        positions[symbol] -= quantity
        若 positions[symbol] == 0 → 從 dict 移除，cost_basis 也移除
        若 positions[symbol] < 0 → 拋出 ValueError（V1 不支援放空）
    """

    def __init__(self, initial_capital: float):
        self._initial_capital = initial_capital
        self.cash = initial_capital
        self.positions: dict[str, int] = {}
        self.cost_basis: dict[str, float] = {}
        self._history: list[dict] = []    # 每筆操作紀錄，供事後分析

    def apply_fill(self, fill: FillEvent) -> None:
        ...
        # 每次操作後記錄快照到 _history
        self._history.append({
            "timestamp": fill.timestamp,
            "action": fill.side,
            "symbol": fill.symbol,
            "quantity": fill.quantity,
            "price": fill.fill_price,
            "cash_after": self.cash,
            "positions_after": dict(self.positions),
        })
```

#### 測試要求 — `tests/test_account.py`

```python
def test_initial_state():
    """初始 cash = initial_capital, positions 空"""

def test_buy_reduces_cash():
    """買入 1000 股 × 100 元, commission=85 → cash = 1000000 - 100000 - 85 = 899915"""

def test_sell_increases_cash():
    """賣出 1000 股 × 110 元, commission=94, tax=330 → cash += 110000 - 94 - 330"""

def test_buy_then_sell_position_zero():
    """買 1000 股 → 賣 1000 股 → position = 0, symbol 從 dict 消失"""

def test_oversell_raises():
    """持有 0 股，嘗試賣出 → ValueError"""

def test_cost_basis_weighted_average():
    """買 1000 股 ×100, 再買 1000 股 ×120 → cost_basis = 110"""

def test_total_value():
    """cash=500000, 持有 1000 股, 當前價 120 → total = 500000 + 120000 = 620000"""

def test_unrealized_pnl():
    """cost_basis=100, current_price=120, qty=1000 → 未實現損益 = 20000"""
```

---

### 3-C 事件迴圈核心

#### 要建的檔案

1. **`src/backtest/engine_event.py`**
2. **`tests/test_engine_event.py`**

#### `src/backtest/engine_event.py`

```python
import pandas as pd
from src.backtest.base import BacktesterBase
from src.backtest.account import SimpleAccount
from src.backtest.events import BarEvent, OrderEvent, FillEvent
from src.backtest.cost import CostCalculator
from src.backtest.metrics import BacktestResult, calculate_metrics
from src.strategy.base import StrategyBase

class EventDrivenBacktester(BacktesterBase):
    """
    事件驅動回測引擎。

    核心設計：
    - 逐 Bar 迭代 data DataFrame
    - 每根 Bar：
      1. 若有上一根 Bar 的 pending_orders → 以本根 Bar 開盤價撮合
      2. 呼叫 strategy.on_bar(bar_event, account)
      3. 收到的 OrderEvent 存入 pending_orders（下根 Bar 執行）
      4. 記錄當日權益值（equity_curve）

    這確保「訊號產生於 Bar t，成交於 Bar t+1」，避免未來函數。
    """

    def __init__(
        self,
        initial_capital: float = 1_000_000,
        cost_calculator: CostCalculator | None = None,
    ):
        self.initial_capital = initial_capital
        self.cost_calculator = cost_calculator or CostCalculator()

    def run(self, strategy: StrategyBase, data: pd.DataFrame) -> BacktestResult:
        account = SimpleAccount(self.initial_capital)
        pending_orders: list[OrderEvent] = []
        equity_records: list[dict] = []
        trade_records: list[dict] = []

        bars = list(data.itertuples())

        for i, bar in enumerate(bars):
            bar_event = self._to_bar_event(bar, data)

            # Step 1: 撮合 pending_orders（以本根開盤價）
            for order in pending_orders:
                fill = self._simulate_fill(order, bar_event)
                account.apply_fill(fill)
                strategy.on_fill(fill, account)
                trade_records.append(fill.to_dict())

            pending_orders.clear()

            # Step 2: 策略產生新訂單
            new_orders = strategy.on_bar(bar_event, account)
            if new_orders:
                pending_orders.extend(new_orders)

            # Step 3: 記錄權益
            current_prices = {bar_event.symbol: bar_event.close}
            equity_records.append({
                "date": bar_event.timestamp,
                "equity": account.get_total_value(current_prices),
            })

        return self._build_result(equity_records, trade_records)

    def _to_bar_event(self, bar, data: pd.DataFrame) -> BarEvent:
        """將 DataFrame row 轉為 BarEvent。"""
        ...

    def _simulate_fill(self, order: OrderEvent, bar: BarEvent) -> FillEvent:
        """
        撮合模擬：
        - MARKET 單：以 bar.open ± slippage 成交
        - LIMIT 單：若 bar.low ≤ price ≤ bar.high 則成交，否則不成交
          （V1 先只實作 MARKET 單）
        - 計算 commission + tax
        - 回傳 FillEvent
        """
        ...

    def _build_result(self, equity_records, trade_records) -> BacktestResult:
        ...
```

#### 測試要求 — `tests/test_engine_event.py`

```python
def test_on_bar_called_per_bar():
    """自定義計數策略 → on_bar 呼叫次數 = 資料筆數"""

def test_order_filled_next_bar():
    """Bar t 產生 BUY → FillEvent.timestamp = Bar t+1 的時間"""

def test_market_order_fill_price():
    """市價單 → fill_price = next_bar.open ± slippage"""

def test_no_future_function():
    """策略只能用當前及歷史資料（透過 mock 策略驗證）"""

def test_equity_curve_length():
    """equity_curve 筆數 = 資料筆數"""

def test_last_bar_pending_orders_discarded():
    """最後一根 Bar 產生的 Order 無法執行（無下根 Bar）→ 不影響結果"""
```

---

### 3-D StrategyBase 與範例策略

此階段的 `StrategyBase` 已在 Phase 2-A 建立。這裡補充事件驅動版的 `MACrossStrategy.on_bar()`。

#### 補充 `src/strategy/examples/ma_cross.py` 的 `on_bar` 實作

```python
class MACrossStrategy(StrategyBase):
    # ... generate_signals() 已在 Phase 2 實作 ...

    def __init__(self, ma_short: int = 20, ma_long: int = 60):
        self.ma_short = ma_short
        self.ma_long = ma_long
        self._close_history: list[float] = []

    def on_bar(self, bar: BarEvent, account: Account) -> list[OrderEvent]:
        """
        事件驅動模式邏輯：
        1. 將 bar.close 加入 _close_history
        2. 若歷史不足 ma_long 筆 → 回傳空 list
        3. 計算短/長均線
        4. 偵測交叉：
           - 上穿且無持倉 → BUY（全倉，股數 = cash // price // 1000 × 1000 取整張）
           - 下穿且有持倉 → SELL（全部）
        5. 回傳 [OrderEvent] 或 []
        """
        ...
```

#### 測試要求

```python
def test_strategy_abc_enforcement():
    """未實作 on_bar 的子類 → TypeError on instantiation"""

def test_ma_cross_event_same_entry_points():
    """事件驅動模式的進出場日期，與向量化模式一致"""

def test_strategy_no_modification_needed():
    """同一 MACrossStrategy instance，先跑 VectorizedBacktester，再跑 EventDrivenBacktester，不需修改"""
```

---

### 3-E 雙引擎一致性驗證

#### 要建的檔案

1. **`tests/test_consistency.py`**

#### 測試要求 — `tests/test_consistency.py`

```python
"""
雙引擎一致性測試。

使用 tests/fixtures/ 中的固定資料集（約 500 筆日K），
同一策略分別跑向量化引擎與事件驅動引擎，比對結果。
"""

TOLERANCE = 0.01  # 容許 1% 偏差

def test_total_return_consistency():
    """兩引擎 total_return 偏差 < 1%"""
    vec_result = VectorizedBacktester(...).run(strategy, data)
    evt_result = EventDrivenBacktester(...).run(strategy, data)
    diff = abs(vec_result.total_return - evt_result.total_return)
    assert diff < TOLERANCE, f"Deviation: {diff:.4f}"

def test_trade_count_consistency():
    """兩引擎交易次數相同"""

def test_entry_dates_consistency():
    """兩引擎的進場日期集合一致"""

def test_deviation_report():
    """若偏差 > 1% → 輸出逐日差異明細到 log"""
```

---

## Phase 4：AI 技術分析問答 + Streamlit UI

### 4-A LLM Provider Tool Use 基礎

AI 技術分析需遵守 `config.yaml` 的 `ai.enabled` 開關。當 `enabled=false` 時，不初始化 LLM client、不讀取或驗證 AI API key、不發出外部 LLM API 呼叫；`AIAdvisor` 可回傳明確的停用錯誤物件，或由 UI 在進入聊天流程前攔截。

#### 要建的檔案

1. **`src/ai/__init__.py`**
2. **`src/ai/advisor.py`**
3. **`tests/test_advisor.py`**

#### `src/ai/advisor.py`

```python
from anthropic import Anthropic
import json

SYSTEM_PROMPT = """你是一個台股技術分析助理。你的工作是：
1. 根據使用者提供的工具數據，客觀描述技術指標的現況
2. 解釋技術術語的含義
3. 說明常見的技術型態與歷史統計意義

你不應該：
- 明確建議使用者買進或賣出
- 預測股價未來走勢
- 給予資金配置建議

每次回應的最後，必須加上免責聲明。"""

DISCLAIMER = """
⚠️ 免責聲明：以上分析僅為技術指標數值的客觀陳述，不構成任何投資建議。
技術分析基於歷史數據，不保證未來走勢。AI 無法預測市場，所有投資決策
請自行判斷，並以券商官方行情為準。投資一定有風險，過去績效不代表未來報酬。
"""

# Claude Tool Use 工具定義
TOOLS = [
    {
        "name": "get_price_data",
        "description": "取得指定股票的歷史 K 線資料（日K 或分K）",
        "input_schema": {
            "type": "object",
            "properties": {
                "symbol": {"type": "string", "description": "台股代碼，如 '2330'"},
                "period": {"type": "string", "description": "時間範圍，如 '3mo'、'6mo'、'1y'"},
                "freq": {"type": "string", "enum": ["daily", "60min", "30min", "5min"],
                          "description": "K 線頻率，預設 daily"}
            },
            "required": ["symbol", "period"]
        }
    },
    {
        "name": "calculate_indicators",
        "description": "計算技術指標（支援：RSI_14, MACD, KD, BBANDS_20, MA_5, MA_20, MA_60, EMA_12, ATR_14, OBV, WILLR 等）",
        "input_schema": {
            "type": "object",
            "properties": {
                "symbol": {"type": "string", "description": "台股代碼"},
                "indicators": {
                    "type": "array",
                    "items": {"type": "string"},
                    "description": "指標列表，如 ['RSI_14', 'MACD', 'KD']"
                },
                "period": {"type": "string", "description": "計算期間，預設 '6mo'"}
            },
            "required": ["symbol", "indicators"]
        }
    },
    {
        "name": "get_support_resistance",
        "description": "計算近期支撐與壓力位（基於近期高低點與均線）",
        "input_schema": {
            "type": "object",
            "properties": {
                "symbol": {"type": "string", "description": "台股代碼"},
                "lookback": {"type": "integer", "description": "回溯交易日數，預設 60"}
            },
            "required": ["symbol"]
        }
    }
]


class AIAdvisor:
    """
    Claude Tool Use 整合。

    流程：
    1. 使用者問題 → 送 Claude API（附 TOOLS 定義 + SYSTEM_PROMPT）
    2. Claude 回傳 tool_use → 解析工具名稱與參數
    3. 呼叫本機對應函式（_execute_tool）
    4. 將工具結果送回 Claude API（tool_result）
    5. Claude 生成最終回應
    6. 在回應末尾附加 DISCLAIMER（無論 Claude 是否已附加）
    7. 回傳完整回應文字

    多輪工具呼叫：Claude 可能連續呼叫多個工具（如先取價格再算指標），
    需在 while loop 中持續處理直到 Claude 回傳 stop_reason="end_turn"。
    """

    def __init__(self, model: str = "claude-haiku-4-5-20251001"):
        """
        model 預設用 haiku 省費用。
        API key 從 .env 的 ANTHROPIC_API_KEY 讀取。
        """
        self.client = Anthropic()  # 自動讀取 ANTHROPIC_API_KEY 環境變數
        self.model = model

    def ask(self, question: str) -> str:
        """
        主入口。輸入使用者問題，回傳完整分析文字（含免責聲明）。
        """
        messages = [{"role": "user", "content": question}]

        while True:
            response = self.client.messages.create(
                model=self.model,
                max_tokens=4096,
                system=SYSTEM_PROMPT,
                tools=TOOLS,
                messages=messages,
            )

            # 處理回應...
            # 若有 tool_use block → 執行工具 → 將結果加入 messages → 繼續迴圈
            # 若 stop_reason == "end_turn" → 提取文字 → 附加 DISCLAIMER → return
            ...

    def _execute_tool(self, tool_name: str, tool_input: dict) -> str:
        """
        分發工具呼叫到對應的本機函式。
        回傳 JSON 字串（工具結果）。

        若工具執行失敗 → 回傳 {"error": "描述訊息"}，不拋例外。
        """
        handlers = {
            "get_price_data": self._handle_get_price_data,
            "calculate_indicators": self._handle_calculate_indicators,
            "get_support_resistance": self._handle_get_support_resistance,
        }
        handler = handlers.get(tool_name)
        if not handler:
            return json.dumps({"error": f"Unknown tool: {tool_name}"})
        try:
            result = handler(**tool_input)
            return json.dumps(result, ensure_ascii=False, default=str)
        except Exception as e:
            return json.dumps({"error": str(e)})

    def _handle_get_price_data(self, symbol: str, period: str,
                                freq: str = "daily") -> dict:
        """
        從 ParquetStorage 讀取資料。
        若本機無資料 → 嘗試即時下載。
        回傳最近 N 筆 OHLCV（不超過 60 筆，避免 token 過多）。

        回傳格式：
        {
            "symbol": "2330",
            "freq": "daily",
            "data_count": 60,
            "latest_date": "2026-04-24",
            "data": [{"date": "...", "open": ..., "high": ..., ...}, ...]
        }
        """
        ...

    def _handle_calculate_indicators(self, symbol: str, indicators: list[str],
                                      period: str = "6mo") -> dict:
        """
        使用 IndicatorEngine 計算指標。
        回傳最近 5 筆指標值（供 AI 解讀趨勢）。

        回傳格式：
        {
            "symbol": "2330",
            "indicators": {
                "RSI_14": {"latest": 65.3, "prev_5": [62.1, 63.5, 64.0, 64.8, 65.3]},
                "MACD": {"macd": 1.23, "signal": 0.98, "histogram": 0.25,
                          "cross": "golden_cross_3_days_ago"}
            }
        }
        """
        ...

    def _handle_get_support_resistance(self, symbol: str,
                                        lookback: int = 60) -> dict:
        """
        計算支撐壓力位。

        邏輯：
        - 近 lookback 日的最高價、最低價
        - MA20、MA60 作為動態支撐壓力
        - 近期 swing high / swing low（局部高低點）

        回傳格式：
        {
            "symbol": "2330",
            "current_price": 850.0,
            "support_levels": [{"level": 830.0, "type": "MA20"}, ...],
            "resistance_levels": [{"level": 880.0, "type": "recent_high"}, ...]
        }
        """
        ...
```

#### 測試要求 — `tests/test_advisor.py`

```python
# 不呼叫真實 Claude API，用 mock 測試流程

def test_tool_dispatch():
    """_execute_tool("get_price_data", {...}) → 回傳 dict，不 crash"""

def test_unknown_tool_returns_error():
    """_execute_tool("nonexistent", {}) → {"error": "Unknown tool: ..."}"""

def test_disclaimer_always_appended():
    """模擬完整 ask() 流程（mock API）→ 回應末尾包含 DISCLAIMER 文字"""

def test_invalid_symbol_returns_error():
    """_handle_get_price_data(symbol="9999", ...) → {"error": "..."}"""

@pytest.mark.integration
def test_real_api_call():
    """真實呼叫 Claude API → 回傳包含分析文字 + 免責聲明"""
```

---

### 4-B 技術指標工具實作

#### 要建的檔案

1. **`src/indicators/__init__.py`**
2. **`src/indicators/calculator.py`**
3. **`tests/test_indicators.py`**

#### `src/indicators/calculator.py`

```python
import pandas as pd
import pandas_ta as ta

# 台股慣用名 → pandas-ta 呼叫的映射表
INDICATOR_ALIAS = {
    "KD":         {"func": "stoch",   "kwargs": {"k": 9, "d": 3, "smooth_k": 3}},
    "RSI_14":     {"func": "rsi",     "kwargs": {"length": 14}},
    "MACD":       {"func": "macd",    "kwargs": {"fast": 12, "slow": 26, "signal": 9}},
    "BBANDS_20":  {"func": "bbands",  "kwargs": {"length": 20, "std": 2.0}},
    "ATR_14":     {"func": "atr",     "kwargs": {"length": 14}},
    "OBV":        {"func": "obv",     "kwargs": {}},
    "WILLR":      {"func": "willr",   "kwargs": {"length": 14}},
    "EMA_12":     {"func": "ema",     "kwargs": {"length": 12}},
    "EMA_26":     {"func": "ema",     "kwargs": {"length": 26}},
}

# 動態均線：MA_{n} 格式
# 解析 "MA_5" → sma(length=5), "MA_60" → sma(length=60)


class IndicatorEngine:
    """
    技術指標計算引擎。封裝 pandas-ta，提供台股慣用名介面。
    """

    def calculate(self, df: pd.DataFrame, indicator_name: str) -> pd.DataFrame:
        """
        計算單一指標。

        Args:
            df: 含 open/high/low/close/volume 的 DataFrame
            indicator_name: 如 "RSI_14", "KD", "MACD", "MA_20"

        Returns:
            DataFrame，欄位為該指標的輸出欄
            （如 KD 回傳 K, D 兩欄；MACD 回傳 macd, signal, histogram 三欄）

        Raises:
            ValueError: 不支援的指標名稱
        """
        ...

    def calculate_batch(self, df: pd.DataFrame,
                         indicator_names: list[str]) -> pd.DataFrame:
        """計算多個指標，回傳合併後的 DataFrame。"""
        ...

    def list_supported(self) -> list[str]:
        """回傳所有支援的指標名稱列表。"""
        ...

    def _parse_ma(self, name: str) -> tuple[str, int] | None:
        """
        解析 MA_{n} 格式。
        "MA_5" → ("sma", 5)
        "EMA_12" → ("ema", 12)
        不匹配 → None
        """
        ...
```

#### 測試要求 — `tests/test_indicators.py`

```python
def test_rsi_range():
    """RSI_14 → 所有值介於 0-100"""

def test_macd_three_columns():
    """MACD → 回傳含 macd, signal, histogram 三欄"""

def test_kd_alias():
    """KD → 回傳含 K, D 兩欄（確認 stoch 被正確呼叫）"""

def test_ma_dynamic_parsing():
    """MA_5, MA_20, MA_60 → 分別計算 SMA"""

def test_bbands_three_bands():
    """BBANDS_20 → 上軌、中軌、下軌三欄"""

def test_invalid_indicator_raises():
    """"FOOBAR" → ValueError"""

def test_list_supported():
    """list_supported() 包含 KD, RSI_14, MACD, BBANDS_20 等"""

def test_calculate_batch():
    """多指標一次算 → 欄位全部正確合併"""
```

---

### 4-C Streamlit UI 框架

#### 要建的檔案

1. **`src/ui/__init__.py`**
2. **`src/ui/app.py`** — Streamlit 主程式（多頁面入口）
3. **`src/ui/pages/data_management.py`** — 資料管理頁
4. **`src/ui/pages/backtest.py`** — 回測頁
5. **`src/ui/pages/ai_chat.py`** — AI 問答頁
6. **`src/ui/pages/settings.py`** — 設定頁

#### `src/ui/app.py`

```python
"""
Streamlit 主程式。

啟動方式：uv run streamlit run src/ui/app.py

頁面架構：
- 使用 st.navigation() 實現多頁面切換（Streamlit 1.36+）
- 四個頁面：資料管理 / 回測 / AI 問答 / 設定
- 若 config.ai.enabled=false，可隱藏 AI 問答頁，或顯示停用狀態頁
"""
import streamlit as st

def main():
    st.set_page_config(
        page_title="QuantTrader - shellpig",
        page_icon="📊",
        layout="wide",
    )

    pages = {
        "資料管理": "src/ui/pages/data_management.py",
        "回測": "src/ui/pages/backtest.py",
        "AI 問答": "src/ui/pages/ai_chat.py",
        "設定": "src/ui/pages/settings.py",
    }
    # 使用 st.navigation 或 st.sidebar radio 切換
    ...

if __name__ == "__main__":
    main()
```

#### 各頁面設計要點

**`data_management.py`**
- 顯示 DuckDBMeta.list_all() 的資料表（symbol, freq, 最後更新日, 筆數）
- 「更新」按鈕：選擇 symbol → 呼叫 maintenance.update_daily()
- 「重建」按鈕：呼叫 maintenance.rebuild_symbol()
- 顯示更新結果和品質報告

**`backtest.py`**
- 輸入：股票代碼、日期範圍、策略選擇（下拉）、策略參數
- 「執行回測」按鈕 → 呼叫 BacktesterBase.run()
- 結果：先顯示績效摘要，再顯示 TearsheetReport 的各圖表（st.plotly_chart）
- 績效摘要欄位順序：交易次數、總報酬、年化報酬、最大回撤、Sharpe；「交易次數」必須放在「總報酬」左邊
- 當交易次數為 0 時，摘要仍需顯示 `0`，避免使用者誤以為總報酬、回撤、Sharpe 全為 0 是計算錯誤
- 可選引擎（向量化 / 事件驅動）

**`ai_chat.py`**
- 讀取 `config.ai.enabled`；若為 false，顯示「AI 功能已關閉」，不得初始化 AIAdvisor 或送出 API 請求
- **首次進入**顯示免責聲明確認頁 → 使用者點「我了解」後才能使用
- 使用 st.chat_message / st.chat_input 建構聊天 UI
- 訊息歷史存在 st.session_state
- 呼叫 AIAdvisor.ask() 取得回應
- 顯示回應（markdown 格式）

**`settings.py`**
- AI 功能開關（`ai.enabled`），預設 false，可儲存至 config.yaml
- API Key 輸入（st.text_input type="password"）
- 讀取 / 寫入 .env 檔
- 顯示 config.yaml 的當前值
- 「儲存為預設」按鈕 → 寫回 config.yaml
- 風控參數調整（max_daily_loss_pct, max_position_pct 等）

#### 測試要求

Streamlit UI 不做自動化測試（需要瀏覽器），改為**手動驗收清單**：

- [ ] `uv run streamlit run src/ui/app.py` 無錯啟動
- [ ] 四個頁面可切換，無 ImportError
- [ ] 設定頁 API Key 遮罩顯示
- [ ] 回測頁選擇策略 + 執行 → 顯示績效摘要與 Tearsheet；摘要第一欄為「交易次數」，第二欄為「總報酬」
- [ ] AI 問答頁免責聲明確認機制
- [ ] AI 問答頁可輸入問題並取得回應

---

### 4-D 端到端整合測試

#### 要建的檔案

1. **`tests/test_e2e.py`**

#### 測試要求 — `tests/test_e2e.py`

```python
"""
端到端整合測試。
需要：本機有 2330 日K 資料 + ANTHROPIC_API_KEY 設定。
"""

@pytest.mark.integration
def test_full_backtest_pipeline():
    """
    完整回測流程：
    1. 從 storage 讀取 2330 日K
    2. 建立 MACrossStrategy
    3. 跑 VectorizedBacktester
    4. 產生 BacktestResult
    5. 生成 Tearsheet HTML
    → 全程不 crash，HTML 檔案存在
    """

@pytest.mark.integration
def test_ai_advisor_real_question():
    """
    完整 AI 問答：
    1. 問「2330 的 RSI 是多少」
    2. AIAdvisor.ask() 回傳回應
    3. 回應包含數值 + 免責聲明
    4. 全程 < 30 秒
    """

@pytest.mark.integration
def test_invalid_symbol_graceful():
    """
    輸入不存在的代碼 "9999" → 友善錯誤訊息，不 crash
    """
```

---

## 模組依賴關係總覽

```
Phase 1                      Phase 2                 Phase 3              Phase 4
─────────                    ─────────               ─────────            ─────────
core/constants  ◄──────────── (被所有模組使用)
core/config     ◄──────────── (被所有模組使用)
core/exceptions ◄──────────── (被所有模組使用)

data/fetcher ──► data/storage ──► data/cleaner ──► data/maintenance
                     │                 │
                     ▼                 ▼
              backtest/cost     backtest/metrics ◄── backtest/report
                     │                 │
                     ▼                 ▼
              backtest/engine_vec     backtest/engine_event
                     │                 │
                     ▼                 ▼
              strategy/base    backtest/account
                     │              │
                     ▼              ▼
              strategy/examples/ma_cross
                                    │
                     ┌──────────────┘
                     ▼
              indicators/calculator ──► ai/advisor ──► ui/app
```

---

## 開發順序建議

**嚴格按 Phase 1 → 2 → 3 → 4 順序。每個子階段的驗收測試全過後，才進入下一個。**

Phase 內部的檔案建立順序：先建被依賴的（constants → config → fetcher → storage），再建依賴方。

每完成一個子階段，執行 `uv run pytest tests/ -v --tb=short`（排除 `@pytest.mark.integration`）確認全綠。

---

## 2026-04-26 架構補充：市場與 AI Provider 抽象

### A. 市場抽象先行，Phase 9 擴充美股 US-1

2026-04-26 原則是先建立市場抽象，暫不實作美股。2026-05-11 規格討論後，Phase 9 改為擴充 **美股 US-1**，但範圍只包含日 K、調整後價格、回測與技術分析。後續修資料層與回測層時，仍需避免把 `tw`、`Asia/Taipei`、`.TW`、台股稅率與台股交易單位散落到更多模組。

建議新增或整理（Phase 9-A 已更新此定義，以 9-A `MarketSpec` 為準）：

```python
from dataclasses import dataclass
from typing import Literal

Market = Literal["tw", "us"]

@dataclass(frozen=True)
class MarketSpec:
    market: Market
    label: str
    timezone: str
    currency: str
    lot_size: int
    volume_unit: str
    price_tick: float | None
```

市場常數：

```python
MARKET_SPECS: dict[str, MarketSpec] = {
    "tw": MarketSpec(
        market="tw",
        label="台股",
        timezone="Asia/Taipei",
        currency="TWD",
        lot_size=1000,
        volume_unit="shares",
        price_tick=None,  # 台股使用級距函式
    ),
    "us": MarketSpec(
        market="us",
        label="美股",
        timezone="America/New_York",
        currency="USD",
        lot_size=1,
        volume_unit="shares",
        price_tick=0.01,
    ),
}
```

落地原則：

- `ParquetStorage` 路徑組合應接受 `market="tw"`，並驗證 `market` / `symbol`。
- Phase 9-A 後 `ParquetStorage` 路徑組合需接受 `market="us"`；未知 market 仍需拒絕。
- `symbol` 不可直接拼進路徑；需先 sanitize，再確認 `resolve()` 後仍位於 `data_dir`。
- `CostCalculator` 現階段仍可保留台股模型，但 ETF 判定、稅率與 tick rule 後續應集中到市場設定或 helper。
- Phase 9 可以新增美股日 K fetcher / adapter、美股成本模型與 UI 市場切換；不得超出 US-1 範圍偷做美股即時、分 K、籌碼、財報、匯率或期權。

### B. AIAdvisor 改為 provider-neutral

`AIAdvisor` 應負責投資分析流程與工具調度，不應直接綁定 Claude SDK。建議拆成：

```text
src/ai/
  advisor.py
  providers/
    base.py
    openai_provider.py
    anthropic_provider.py
    gemini_provider.py
  tools.py
```

核心介面建議：

```python
class LLMProvider(Protocol):
    def ask(
        self,
        messages: list[dict],
        tools: list[dict],
        model: str,
        temperature: float,
        max_tokens: int,
    ) -> LLMResponse:
        ...
```

設計原則：

- `AIAdvisor` 只依賴 `LLMProvider`，不直接 import OpenAI / Anthropic / Gemini SDK。
- provider adapter 負責轉換工具格式、解析 tool call、統一錯誤訊息。
- `config.yaml` 管 enabled、provider、model、temperature、max_tokens、timeout。
- `enabled=false` 時不得初始化 provider，不得要求 AI API key，非 AI 功能與 UI 頁面必須照常可用。
- `.env` 只放 API key；key 名稱為 `OPENAI_API_KEY`、`ANTHROPIC_API_KEY`、`GEMINI_API_KEY`。
- model id 不要寫死在核心邏輯，UI 可給預設選項，但需支援手動輸入。

---

## Phase 6：UI/UX 強化

### 6-A 主題切換與 UI 美化

**對應規格：** 規格書 §11 Phase 6-A。

**新增檔案：**

```
src/ui/themes.py
```

**修改檔案：**

```
src/ui/app.py                     # 啟動時注入主題 CSS、依設定切換側邊欄元件
src/ui/pages/settings.py          # 新增「外觀」區塊
src/ui/pages/backtest.py          # 績效摘要改用 metric card（fallback 原生 st.metric）
src/core/config.py                # 確保讀取 ui 區塊缺值時回傳預設
.streamlit/config.toml            # 只保留 layout / runOnSave 等基礎設定
pyproject.toml                    # 新增可選依賴 streamlit-extras、streamlit-option-menu
config.yaml                       # 不主動寫入；使用者於設定頁儲存時才補入 ui 區塊
```

**`src/ui/themes.py` 介面：**

```python
from typing import Literal, TypedDict

ThemeName = Literal["finance_green", "midnight_blue", "arctic_light", "cyberpunk", "warm_sepia", "obsidian_dark"]

class ThemePalette(TypedDict):
    background: str
    surface: str
    primary: str
    text: str
    muted: str
    plotly_template: str  # "plotly_white" | "plotly_dark"

THEMES: dict[ThemeName, ThemePalette] = {...}

def get_theme(name: str) -> tuple[ThemeName, ThemePalette]:
    """回傳合法主題；未知值回退到 arctic_light。"""

def render_theme_css(name: ThemeName) -> str:
    """產生注入用的 <style> 字串，包含 CSS 變數與 Streamlit selector overrides。"""
```

**`config.yaml` 對應結構：**

```yaml
ui:
  theme: arctic_light            # obsidian_dark | finance_green | midnight_blue | arctic_light | cyberpunk | warm_sepia
  use_extras: true
  use_option_menu: true
```

讀取規則：

- `get_config()["ui"]` 缺失時 → 視為空 dict。
- `theme` 缺失或非法 → 回退 `arctic_light`，並在頁面以 `st.warning` 提示一次。
- `use_extras` / `use_option_menu` 缺失 → 預設 `True`。

**`app.py` 啟動順序：**

1. `st.set_page_config(...)`（必須是第一個 Streamlit 呼叫）。
2. 讀取 `config.yaml` 的 `ui` 區塊，呼叫 `themes.get_theme(name)`。
3. `st.markdown(themes.render_theme_css(theme), unsafe_allow_html=True)` 注入 CSS。
4. 依 `use_option_menu` 與 `streamlit-option-menu` 是否安裝，決定側邊欄使用 `option_menu` 或 `st.radio`。

**可選依賴的 import 模式：**

```python
try:
    from streamlit_extras.metric_cards import style_metric_cards
    HAS_EXTRAS = True
except ImportError:
    HAS_EXTRAS = False
```

未安裝時 fallback 到原生元件，不得 raise。設定頁需顯示安裝提示。

**設定頁「外觀」區塊：**

```python
st.subheader("外觀")
theme_options = ["arctic_light", "obsidian_dark", "finance_green", "midnight_blue", "cyberpunk", "warm_sepia"]
theme = st.selectbox("主題", options=theme_options, index=theme_options.index(current_theme))
use_extras = st.toggle("使用 streamlit-extras 元件", value=current_use_extras)
use_option_menu = st.toggle("使用 option_menu 側邊欄", value=current_use_option_menu)
```

「儲存」按鈕的處理：

1. 將 `ui` 區塊寫回 `config.yaml`（採用既有 `yaml.safe_dump`，保留其他區塊）。
2. 呼叫 `clear_config_cache()`。
3. 呼叫 `st.rerun()`。

**Plotly 主題對應：**

- 在繪製 Plotly 圖表時讀取目前主題的 `plotly_template`，傳入 `fig.update_layout(template=...)`。
- 圖表 `paper_bgcolor` / `plot_bgcolor` 設為主題 `surface` 色，避免深色頁面出現白底圖表。

**測試規範：**

- 主題切換無對應自動化測試（UI 為手動驗收）。
- 但 `themes.get_theme()` 與 `config.yaml ui 區塊讀寫`應有單元測試：
  - `test_themes.py`：合法主題回傳對應 palette；非法主題回退 `arctic_light`。
  - `test_config_ui_section.py`：缺 `ui` 區塊時 `get_config()` 不報錯且 `ui` 為空 dict。

**不做的事：**

- 不動 `.streamlit/config.toml` 的 `[theme]` 區塊（不依賴它做執行期切換）。
- 不引入 `streamlit-shadcn-ui`（與 `streamlit-extras` 風格衝突，本階段先不混用）。
- 不對 Phase 1-5 既有功能的資料流或計算邏輯做任何修改；僅限 UI 層調整。

### 6-B 設定頁與側邊欄 UI 小修

**對應規格：** 規格書 §11 Phase 6-B。

**目標：**

1. 側邊欄只保留 QuantTrader 自訂導覽，不再顯示 Streamlit 自動 multipage 導覽。
2. 預設主題改為 `midnight_blue`。
3. 設定頁把「策略以外設定」與「策略 preset」的儲存/恢復流程分離。
4. 策略類型下拉選單支援全部 8 種策略，且顯示中文說明。
5. 策略預設值恢復後，每種策略類型至少有一組 preset。
6. 目前策略清單中的每筆 preset 可單獨清除。

**修改檔案：**

```text
src/ui/app.py                     # 隱藏或移除 Streamlit 自動頁面入口，只保留自訂選單
src/ui/themes.py                  # DEFAULT_THEME / fallback 改為 midnight_blue（若有集中常數）
src/core/config.py                # ui.theme 缺失或非法時回退 midnight_blue
src/core/strategy_config.py       # 提供 DEFAULT_STRATEGY_PRESETS 與 8 種策略類型顯示 helper
src/ui/pages/settings.py          # 分離設定/策略儲存恢復、補齊 8 種策略表單、單筆刪除 preset
tests/test_themes.py              # fallback 預期改 midnight_blue
tests/test_config_ui_section.py   # 缺 ui / 非法 theme 預設驗證
tests/test_strategy_config.py     # 8 種預設 preset 與中文 label 驗證
tests/test_settings_page.py       # 若新增/已有設定頁 helper 測試，覆蓋分離儲存與單筆刪除
```

**預設主題規則：**

```python
DEFAULT_THEME = "midnight_blue"
THEME_OPTIONS = [
    "arctic_light",
    "obsidian_dark",
    "finance_green",
    "midnight_blue",
    "cyberpunk",
    "warm_sepia",
]
```

- `ui` 區塊缺失 → 使用 `DEFAULT_THEME`，不自動改寫 `config.yaml`。
- `ui.theme` 缺失 → 使用 `DEFAULT_THEME`。
- `ui.theme` 非合法值 → 使用 `DEFAULT_THEME`，UI 顯示 warning 一次。
- 保留 lazy migration：只有使用者點 `儲存設定` 才寫入 `ui:`。

**側邊欄規則：**

- `src/ui/app.py` 必須只有一套頁面導覽狀態來源，例如目前自訂的 QuantTrader 選單。
- 若 Streamlit 自動產生的 multipage nav 出現在 sidebar，需以最小改動移除來源或隱藏自動 nav container。
- 自訂選單仍維持四頁：`資料管理`、`回測`、`AI 問答`、`設定`。
- 不得刪除 `src/ui/pages/*.py` 模組；這些是 app 內部匯入的頁面實作，不等於 Streamlit 自動 pages 目錄。

**設定頁責任分離：**

目前 `settings.py` 的 `_save_config_and_env(...)` 同時寫 `ui/ai/risk/backtest/strategies/.env`。6-B 需拆成責任清楚的 helper：

```python
def _save_non_strategy_config_and_env(...) -> None:
    """只寫 ui、ai、risk、backtest 與 .env，不改 strategies / strategy。"""

def _restore_non_strategy_defaults_and_env() -> None:
    """只恢復策略以外設定，不改 strategies / strategy。"""

def _save_strategy_presets(strategies: list[dict[str, Any]]) -> None:
    """只寫 strategies[]，並移除舊版 strategy 區塊；不改其他 config 區塊與 .env。"""

def _restore_strategy_defaults() -> None:
    """只把 strategies[] 寫成 DEFAULT_STRATEGY_PRESETS。"""

def _delete_strategy_preset(name: str) -> None:
    """只刪除指定 preset name，保留其他 preset 與非策略設定。"""
```

按鈕位置與文案：

- 「回測參數」之後、「策略設定」之前：`儲存設定`、`恢復設定預設值`。
- 「策略設定」表單之後：`儲存策略`、`恢復策略預設值`。
- 「目前策略清單」每列前方：單筆清除按鈕（建議 icon 或短文案，如 `清除`）。

**策略類型與顯示：**

策略類型選項不得手寫成 2 筆，需從單一來源取得：

```python
SUPPORTED_STRATEGY_TYPES = list(STRATEGY_META.keys())

def make_strategy_type_label(strategy_type: str) -> str:
    meta = STRATEGY_META.get(strategy_type)
    if not meta:
        return strategy_type
    return f"{strategy_type} ({meta['label']})"
```

`st.selectbox("策略類型", ...)` 應用 `format_func=make_strategy_type_label` 或等價方式，顯示：

- `moving_average_cross (均線交叉)`
- `dollar_cost_averaging (定期定額)`
- `rsi (RSI 超買超賣)`
- `kd_cross (KD 交叉)`
- `macd_cross (MACD 交叉)`
- `bollinger_band (布林通道)`
- `bias (乖離率)`
- `donchian_breakout (突破策略)`

**策略參數表單：**

`settings.py` 的策略編輯表單需支援全部 8 種策略：

| strategy type | 欄位 |
|:---|:---|
| `moving_average_cross` | `short_window`, `long_window` |
| `dollar_cost_averaging` | `monthly_day`, `monthly_amount`, `min_buy_unit`, `non_trading_day_policy`, `buy_price_field` |
| `rsi` | `period`, `oversold`, `overbought` |
| `kd_cross` | `k_period`, `d_period`, `smooth_k` |
| `macd_cross` | `fast`, `slow`, `signal` |
| `bollinger_band` | `period`, `std_dev` |
| `bias` | `ma_period`, `buy_bias`, `sell_bias` |
| `donchian_breakout` | `entry_period`, `exit_period` |

表單驗證應沿用 `src/core/strategy_config.py` normalization 規則，避免 settings page 與回測頁出現不同合法性判定。

**預設策略 preset：**

建議在 `src/core/strategy_config.py` 集中定義，讓 `config.yaml` 預設、設定頁恢復、測試都用同一份資料：

```python
DEFAULT_STRATEGY_PRESETS: list[dict[str, Any]] = [
    {"name": "MA20_MA60", "type": "moving_average_cross", "params": {"short_window": 20, "long_window": 60}},
    {"name": "定期定額", "type": "dollar_cost_averaging", "params": {"monthly_day": 5, "monthly_amount": 10000.0, "min_buy_unit": 1, "non_trading_day_policy": "next_trading_day", "buy_price_field": "close"}},
    {"name": "RSI_14", "type": "rsi", "params": {"period": 14, "oversold": 30.0, "overbought": 70.0}},
    {"name": "KD_Cross", "type": "kd_cross", "params": {"k_period": 9, "d_period": 3, "smooth_k": 3}},
    {"name": "MACD_Cross", "type": "macd_cross", "params": {"fast": 12, "slow": 26, "signal": 9}},
    {"name": "BB_20", "type": "bollinger_band", "params": {"period": 20, "std_dev": 2.0}},
    {"name": "BIAS_20", "type": "bias", "params": {"ma_period": 20, "buy_bias": -10.0, "sell_bias": 10.0}},
    {"name": "Donchian_20_10", "type": "donchian_breakout", "params": {"entry_period": 20, "exit_period": 10}},
]
```

**測試規範：**

- `test_config_missing_ui_section_returns_defaults`：缺 `ui` 時預設 theme 為 `midnight_blue`。
- `test_get_theme_invalid_falls_back_to_midnight_blue`：非法 theme fallback 改為 `midnight_blue`。
- `test_default_strategy_presets_cover_all_strategy_types`：`DEFAULT_STRATEGY_PRESETS` 覆蓋 `STRATEGY_META` 全部 8 種 type。
- `test_strategy_type_labels_include_chinese_metadata`：每個策略 type label 皆包含括號中文說明。
- `test_save_non_strategy_config_preserves_strategies`：儲存一般設定不改 `strategies[]`。
- `test_save_strategy_presets_preserves_non_strategy_sections`：儲存策略不改 `ui/ai/risk/backtest` 與 `.env`。
- `test_delete_strategy_preset_removes_only_one`：單筆清除只刪指定 preset。

**不做的事：**

- 不改策略訊號邏輯、回測引擎、資料讀寫格式。
- 不新增第 9 種策略。
- 不把單筆清除做成「刪除策略類型」；它只刪 preset。
- 不因恢復一般設定而清除 API key 以外未指定內容；API key 是否清空依設定頁既有預設策略處理，但不得動 `strategies[]`。

---

## Phase 7：策略擴充

### 7-A 六種技術分析策略

**對應規格：** 規格書 §11 Phase 7-A。

**新增檔案：**

```
src/strategy/examples/rsi.py
src/strategy/examples/kd_cross.py
src/strategy/examples/macd_cross.py
src/strategy/examples/bollinger_band.py
src/strategy/examples/bias.py
src/strategy/examples/donchian_breakout.py
tests/test_strategies.py
```

**修改檔案：**

```
src/strategy/examples/__init__.py     # 匯出 6 個新策略類別
src/core/strategy_config.py           # 新增 6 種 preset 正規化函式
src/ui/pages/backtest.py              # 新增 6 種策略類型的參數顯示與回測分派
```

---

#### `src/strategy/examples/rsi.py`

```python
class RSIStrategy(StrategyBase):
    def __init__(
        self,
        period: int = 14,
        oversold: float = 30.0,
        overbought: float = 70.0,
    ) -> None: ...

    def generate_signals(self, df: pd.DataFrame) -> pd.Series: ...
    def on_bar(self, bar: BarEvent, account: Account) -> list[OrderEvent]: ...
    def reset_runtime_state(self) -> None: ...
```

**向量化邏輯：**
1. 以 `pandas_ta.rsi(close, length=period)` 計算 RSI。
2. 追蹤 RSI 是否低於 `oversold`，回升到 `oversold` 以上時發 +1。
3. 追蹤 RSI 是否高於 `overbought`，回落到 `overbought` 以下時發 -1。
4. Warm-up 期（前 `period` 筆 RSI 為 NaN）訊號為 0。

**事件驅動邏輯：**
1. 維護 `_close_history: list[float]`，逐 bar 累計。
2. 以 Wilder smoothing 手動計算 RSI（或累積足夠 bar 後用 pandas_ta）。
3. 比較前一 bar 的 RSI 與當前 RSI 判斷上穿/下穿。
4. 買進用 `_BUY_INTENT_QUANTITY`，賣出用 `account.get_position(bar.symbol)`。

**參數驗證：**
- `period > 0`
- `0 <= oversold < overbought <= 100`

---

#### `src/strategy/examples/kd_cross.py`

```python
class KDCrossStrategy(StrategyBase):
    def __init__(
        self,
        k_period: int = 9,
        d_period: int = 3,
        smooth_k: int = 3,
    ) -> None: ...
```

**向量化邏輯：**
1. 以 `pandas_ta.stoch(high, low, close, k=k_period, d=d_period, smooth_k=smooth_k)` 計算 K、D。
2. K 從下方上穿 D → +1（黃金交叉）。
3. K 從上方下穿 D → -1（死亡交叉）。
4. 判斷交叉：`(K_prev <= D_prev) & (K_curr > D_curr)` 為上穿。

**事件驅動邏輯：**
1. 維護 `_high_history`、`_low_history`、`_close_history`。
2. 累積足夠 bar 後計算 Stochastic K 和 D。
3. 比較前一 bar 與當前的 K-D 關係判斷交叉。

**Warm-up：** 前 `k_period + smooth_k + d_period - 2` 筆訊號為 0。

**參數驗證：**
- `k_period > 0, d_period > 0, smooth_k > 0`

---

#### `src/strategy/examples/macd_cross.py`

```python
class MACDCrossStrategy(StrategyBase):
    def __init__(
        self,
        fast: int = 12,
        slow: int = 26,
        signal: int = 9,
    ) -> None: ...
```

**向量化邏輯：**
1. 以 `pandas_ta.macd(close, fast=fast, slow=slow, signal=signal)` 計算 MACD、Signal。
2. MACD 從下方上穿 Signal → +1。
3. MACD 從上方下穿 Signal → -1。

**Warm-up：** 前 `slow + signal - 1` 筆訊號為 0。

**參數驗證：**
- `fast > 0, slow > 0, signal > 0`
- `fast < slow`

---

#### `src/strategy/examples/bollinger_band.py`

```python
class BollingerBandStrategy(StrategyBase):
    def __init__(
        self,
        period: int = 20,
        std_dev: float = 2.0,
    ) -> None: ...
```

**向量化邏輯：**
1. 以 `pandas_ta.bbands(close, length=period, std=std_dev)` 計算上軌、中軌、下軌。
2. 收盤價從上方跌破下軌（`close_prev >= lower_prev` 且 `close_curr < lower_curr`）→ +1。
3. 收盤價從下方突破上軌（`close_prev <= upper_prev` 且 `close_curr > upper_curr`）→ -1。

**Warm-up：** 前 `period` 筆訊號為 0。

**參數驗證：**
- `period > 0, std_dev > 0`

---

#### `src/strategy/examples/bias.py`

```python
class BiasStrategy(StrategyBase):
    def __init__(
        self,
        ma_period: int = 20,
        buy_bias: float = -10.0,
        sell_bias: float = 10.0,
    ) -> None: ...
```

**向量化邏輯：**
1. 計算 `ma = close.rolling(ma_period).mean()`。
2. 計算 `bias = (close - ma) / ma * 100`。
3. BIAS 從高位下穿 `buy_bias` → +1。
4. BIAS 從低位上穿 `sell_bias` → -1。

**Warm-up：** 前 `ma_period` 筆訊號為 0。

**參數驗證：**
- `ma_period > 0`
- `buy_bias < sell_bias`

---

#### `src/strategy/examples/donchian_breakout.py`

```python
class DonchianBreakoutStrategy(StrategyBase):
    def __init__(
        self,
        entry_period: int = 20,
        exit_period: int = 10,
    ) -> None: ...
```

**向量化邏輯：**
1. 計算 `upper = high.rolling(entry_period).max().shift(1)`（前 N 日最高價，不含當日）。
2. 計算 `lower = low.rolling(exit_period).min().shift(1)`（前 M 日最低價，不含當日）。
3. 收盤價突破 `upper` → +1。
4. 收盤價跌破 `lower` → -1。

**Warm-up：** 前 `max(entry_period, exit_period)` 筆訊號為 0。

**參數驗證：**
- `entry_period > 0, exit_period > 0`

---

#### `src/core/strategy_config.py` 修改

在 `_normalize_one_preset()` 的策略類型分派中，新增 6 種 type 的處理：

```python
if strategy_type == "rsi":
    norm_params = _normalize_rsi_params(params)
    ...

if strategy_type == "kd_cross":
    norm_params = _normalize_kd_cross_params(params)
    ...

if strategy_type == "macd_cross":
    norm_params = _normalize_macd_cross_params(params)
    ...

if strategy_type == "bollinger_band":
    norm_params = _normalize_bollinger_band_params(params)
    ...

if strategy_type == "bias":
    norm_params = _normalize_bias_params(params)
    ...

if strategy_type == "donchian_breakout":
    norm_params = _normalize_donchian_breakout_params(params)
    ...
```

各 `_normalize_*_params()` 函式簽名與回傳值與既有 `_normalize_moving_average_params` 一致：
- 回傳 `dict[str, Any] | None`。
- 不合法參數回傳 `None`（preset 被過濾）。
- 缺值用預設值補。

**各函式正規化規則：**

| 函式 | 參數 | 預設值 | 驗證 |
| :--- | :--- | :--- | :--- |
| `_normalize_rsi_params` | `period`, `oversold`, `overbought` | 14, 30, 70 | `period>0`, `0<=oversold<overbought<=100` |
| `_normalize_kd_cross_params` | `k_period`, `d_period`, `smooth_k` | 9, 3, 3 | 全正整數 |
| `_normalize_macd_cross_params` | `fast`, `slow`, `signal` | 12, 26, 9 | 全正整數, `fast<slow` |
| `_normalize_bollinger_band_params` | `period`, `std_dev` | 20, 2.0 | `period>0`, `std_dev>0` |
| `_normalize_bias_params` | `ma_period`, `buy_bias`, `sell_bias` | 20, -10.0, 10.0 | `ma_period>0`, `buy_bias<sell_bias` |
| `_normalize_donchian_breakout_params` | `entry_period`, `exit_period` | 20, 10 | 全正整數 |

---

#### `src/core/strategy_config.py` — 策略中文 metadata

在 `strategy_config.py` 新增 `STRATEGY_META` 字典與查詢函式，為所有策略類型（含既有 2 種 + 新增 6 種）提供中文 metadata。

```python
from typing import TypedDict


class StrategyMeta(TypedDict):
    label: str
    description: str
    buy_hint: str
    sell_hint: str
    param_labels: dict[str, str]


STRATEGY_META: dict[str, StrategyMeta] = {
    "moving_average_cross": {
        "label": "均線交叉",
        "description": "短均線上穿長均線買進，下穿賣出",
        "buy_hint": "短均線 > 長均線（黃金交叉）",
        "sell_hint": "短均線 < 長均線（死亡交叉）",
        "param_labels": {
            "short_window": "短均線週期",
            "long_window": "長均線週期",
        },
    },
    "dollar_cost_averaging": {
        "label": "定期定額",
        "description": "每月固定日期以固定金額買入",
        "buy_hint": "每月指定日自動買入",
        "sell_hint": "不主動賣出（持有至回測結束）",
        "param_labels": {
            "monthly_day": "每月投入日",
            "monthly_amount": "每月投入金額",
            "min_buy_unit": "最小買入單位（股）",
            "non_trading_day_policy": "非交易日處理",
            "buy_price_field": "買入價格欄位",
        },
    },
    "rsi": {
        "label": "RSI 超買超賣",
        "description": "RSI 低於超賣線買進，高於超買線賣出",
        "buy_hint": "RSI < 超賣門檻（如 30）",
        "sell_hint": "RSI > 超買門檻（如 70）",
        "param_labels": {
            "period": "RSI 週期",
            "oversold": "超賣門檻",
            "overbought": "超買門檻",
        },
    },
    "kd_cross": {
        "label": "KD 交叉",
        "description": "K 線上穿 D 線買進，下穿賣出",
        "buy_hint": "K 線上穿 D 線（黃金交叉）",
        "sell_hint": "K 線下穿 D 線（死亡交叉）",
        "param_labels": {
            "k_period": "K 值回看期間",
            "d_period": "D 值平滑期間",
            "smooth_k": "K 值平滑期間",
        },
    },
    "macd_cross": {
        "label": "MACD 交叉",
        "description": "MACD 上穿訊號線買進，下穿賣出",
        "buy_hint": "MACD 線上穿 Signal 線",
        "sell_hint": "MACD 線下穿 Signal 線",
        "param_labels": {
            "fast": "快線 EMA 週期",
            "slow": "慢線 EMA 週期",
            "signal": "訊號線 EMA 週期",
        },
    },
    "bollinger_band": {
        "label": "布林通道",
        "description": "跌破下軌買進，突破上軌賣出",
        "buy_hint": "收盤價跌破下軌（超跌反轉）",
        "sell_hint": "收盤價突破上軌（超漲反轉）",
        "param_labels": {
            "period": "中軌 SMA 週期",
            "std_dev": "標準差倍數",
        },
    },
    "bias": {
        "label": "乖離率",
        "description": "乖離率過低買進，過高賣出",
        "buy_hint": "BIAS < 買進門檻（如 -10%）",
        "sell_hint": "BIAS > 賣出門檻（如 10%）",
        "param_labels": {
            "ma_period": "均線週期",
            "buy_bias": "買進乖離率門檻（%）",
            "sell_bias": "賣出乖離率門檻（%）",
        },
    },
    "donchian_breakout": {
        "label": "突破策略",
        "description": "突破 N 日高點買進，跌破 M 日低點賣出",
        "buy_hint": "收盤價突破前 N 日最高價",
        "sell_hint": "收盤價跌破前 M 日最低價",
        "param_labels": {
            "entry_period": "進場回看天數",
            "exit_period": "出場回看天數",
        },
    },
}


def get_strategy_meta(strategy_type: str) -> StrategyMeta | None:
    """回傳策略類型的中文 metadata，未知類型回傳 None。"""
    return STRATEGY_META.get(strategy_type)


def make_strategy_label(preset: dict[str, Any]) -> str:
    """產生 UI 下拉選單顯示文字。

    改寫既有 make_strategy_label：
    - 有 STRATEGY_META → "{name} ({label})"，例如 "MA20_MA60 (均線交叉)"
    - 無 STRATEGY_META → "{name} ({type})"，維持 fallback
    """
    name = str(preset.get("name", "")).strip() or "Unnamed"
    strategy_type = str(preset.get("type", "")).strip() or "unknown"
    meta = STRATEGY_META.get(strategy_type)
    label = meta["label"] if meta else strategy_type
    return f"{name} ({label})"


def format_param_caption(strategy_type: str, params: dict[str, Any]) -> str:
    """產生中文參數摘要，用於 st.caption。

    例如："RSI 週期=14, 超賣門檻=30, 超買門檻=70"
    無 metadata 時 fallback 為英文 key=value。
    """
    meta = STRATEGY_META.get(strategy_type)
    param_labels = meta["param_labels"] if meta else {}
    parts = []
    for key, value in params.items():
        display_key = param_labels.get(key, key)
        parts.append(f"{display_key}={value}")
    return ", ".join(parts)
```

---

#### `src/ui/pages/backtest.py` 修改

**策略選擇下拉選單：** 使用改寫後的 `make_strategy_label(preset)` 產生中文顯示文字。

**策略參數 caption 顯示：** 選擇策略後，統一用以下模式顯示中文資訊（取代既有的 per-type `st.caption` 硬寫）：

```python
from src.core.strategy_config import get_strategy_meta, format_param_caption

meta = get_strategy_meta(selected_type)
if meta:
    st.caption(f"📋 {meta['description']}")
    st.caption(f"📈 買進：{meta['buy_hint']}　📉 賣出：{meta['sell_hint']}")
    st.caption(f"⚙️ {format_param_caption(selected_type, selected_params)}")
else:
    st.warning(f"此策略類型目前未支援執行：{selected_type}")
```

既有的 `if selected_type == "moving_average_cross"` / `elif selected_type == "dollar_cost_averaging"` 兩段硬寫的 caption 改為走上述統一路徑，不再需要 per-type 分支。

**回測分派：** 在 `_run_backtest()` 中，新增 6 個策略類型的處理分支，邏輯與 `moving_average_cross` 一致：
1. 從 `strategy_params` 讀取參數，建構策略 instance。
2. 依 `engine_name` 選擇 `VectorizedBacktester` 或 `EventDrivenBacktester`。
3. 執行 `engine.run(strategy=strategy, data=data)`。
4. 顯示 Tearsheet、股價走勢、EPS。

**warm-up 處理：** `VectorizedBacktester._drop_warmup_signals()` 需擴充，依策略屬性推算 warm-up 長度。各策略提供 `warmup_period` 屬性（或由引擎從已知參數推算）。

**import 更新：** 從 `src.strategy.examples` 引入 6 個新策略類別。

---

#### 測試規範

**測試檔案：** `tests/test_strategies.py`

**測試結構：** 6 個策略共用同一個測試檔，每個策略包含以下測試：

| 測試類別 | 命名模式 | 驗證內容 |
| :--- | :--- | :--- |
| 參數驗證 | `test_{type}_invalid_params_raises` | 不合法參數拋 `ValueError` |
| 向量化訊號 | `test_{type}_signal_values` | 訊號只含 `{-1, 0, +1}`，warm-up 為 0 |
| 向量化邏輯 | `test_{type}_signal_correctness` | 以已知走勢驗證指標交叉/突破條件產生正確訊號 |
| 事件驅動 | `test_{type}_on_bar_produces_orders` | 以已知 fixture 逐 bar 餵入，驗證 OrderEvent 產生 |
| 狀態重置 | `test_{type}_reset_clears_state` | `reset_runtime_state()` 後 `on_bar` 行為如同新 instance |

**strategy_config 正規化測試：** 在 `tests/test_strategy_config.py` 追加：

| 測試名稱 | 驗證內容 |
| :--- | :--- |
| `test_normalize_rsi_params_defaults` | 缺值補預設 |
| `test_normalize_rsi_params_invalid` | `oversold >= overbought` → `None` |
| `test_normalize_kd_cross_params_defaults` | 缺值補預設 |
| `test_normalize_macd_cross_params_invalid` | `fast >= slow` → `None` |
| `test_normalize_bollinger_band_params_defaults` | 缺值補預設 |
| `test_normalize_bias_params_invalid` | `buy_bias >= sell_bias` → `None` |
| `test_normalize_donchian_breakout_params_defaults` | 缺值補預設 |

**strategy metadata 測試：** 在 `tests/test_strategy_config.py` 追加：

| 測試名稱 | 驗證內容 |
| :--- | :--- |
| `test_strategy_meta_covers_all_types` | `STRATEGY_META` 包含全部 8 種策略類型 |
| `test_strategy_meta_has_required_fields` | 每個 meta 都有 `label`、`description`、`buy_hint`、`sell_hint`、`param_labels` |
| `test_make_strategy_label_chinese` | `moving_average_cross` preset → 包含 `"均線交叉"` |
| `test_format_param_caption_chinese` | `rsi` + `{period:14}` → 包含 `"RSI 週期=14"` |
| `test_format_param_caption_unknown_type_fallback` | 未知 type → 英文 key=value |

**回測頁 UI 測試：** 手動驗收，與既有 MA Cross 驗收方式相同。每種新策略至少對 2330 執行一次向量化回測，確認 Tearsheet 與股價走勢正常顯示。另需確認：
- 策略下拉選單顯示中文名稱
- 選擇策略後顯示中文策略說明、買賣條件、參數說明

**不做的事：**

- 不修改既有策略（MA Cross、DCA）的任何程式碼。
- 不新增策略間的組合邏輯（多策略同時執行、策略 ensemble）。
- 不引入加倉/分批進出機制，維持「全進全出」模式。
- 不修改指標引擎 `IndicatorEngine`，策略內部直接呼叫 `pandas_ta` 函式。

---

### 7-B 策略研究工作台

**對應規格：** 規格書 §11 Phase 7-B。

**新增檔案：**

```
src/backtest/batch.py
src/backtest/sweep.py          # （7-C 使用，7-B 先建空模組預留 import）
tests/test_batch.py
```

**修改檔案：**

```
src/backtest/metrics.py         # BacktestResult 新增 signals 欄位
src/backtest/engine_vec.py      # 暴露 signals Series 到 BacktestResult
src/ui/pages/backtest.py        # tab 重構 + K 線圖 + signal overlay + 指標副圖
```

---

#### `src/backtest/batch.py`

```python
from __future__ import annotations

from dataclasses import dataclass, field

import pandas as pd

from src.backtest.engine_vec import VectorizedBacktester
from src.backtest.metrics import BacktestResult
from src.core.strategy_config import get_strategy_presets, STRATEGY_META


@dataclass
class StrategyRunSummary:
    preset_name: str
    strategy_type: str
    total_return: float
    annual_return: float
    max_drawdown: float
    sharpe_ratio: float
    win_rate: float
    profit_factor: float
    total_trades: int
    error: str | None = None
    result: BacktestResult | None = None


@dataclass
class BatchResult:
    symbol: str
    start_date: str
    end_date: str
    engine: str
    initial_capital: float
    summaries: list[StrategyRunSummary] = field(default_factory=list)


def run_strategy_batch(
    *,
    symbol: str,
    data: pd.DataFrame,
    presets: list[dict],
    initial_capital: float = 1_000_000,
) -> BatchResult:
    """對多個策略 preset 批次執行回測，收集 summary metrics。"""
    ...
```

**核心邏輯：**

1. 遍歷 `presets`，對每個 preset：
   - 依 `type` 建構策略 instance（複用 backtest.py 中的策略建構邏輯，抽成 `_build_strategy(preset)` helper）。
   - 建構 `VectorizedBacktester`，執行 `run(strategy, data)`。
   - 從 `BacktestResult` 提取 summary metrics，建構 `StrategyRunSummary`。
   - 若任何步驟拋例外，捕獲並記錄 `error`，`result=None`，繼續下一個 preset。
2. 打包所有 summaries 為 `BatchResult` 回傳。

**策略建構 helper `_build_strategy(preset)`：**

```python
def _build_strategy(preset: dict) -> StrategyBase:
    """根據 preset 的 type 和 params 建構策略 instance。

    抽出來讓 batch.py、sweep.py、backtest.py UI 三處共用，
    避免策略建構邏輯散布三處。
    """
    strategy_type = preset.get("type", "")
    params = preset.get("params", {})

    builders: dict[str, Callable] = {
        "moving_average_cross": lambda p: MACrossStrategy(**p),
        "rsi": lambda p: RSIStrategy(**p),
        "kd_cross": lambda p: KDCrossStrategy(**p),
        "macd_cross": lambda p: MACDCrossStrategy(**p),
        "bollinger_band": lambda p: BollingerBandStrategy(**p),
        "bias": lambda p: BiasStrategy(**p),
        "donchian_breakout": lambda p: DonchianBreakoutStrategy(**p),
    }

    builder = builders.get(strategy_type)
    if builder is None:
        raise ValueError(f"Unsupported strategy type: {strategy_type}")
    return builder(params)
```

---

#### `src/backtest/metrics.py` 修改

`BacktestResult` 新增可選欄位：

```python
@dataclass
class BacktestResult:
    # ... 既有欄位不變 ...
    signals: pd.Series | None = None  # 向量化引擎的原始訊號（+1/0/-1）
```

- 向量化引擎在 `run()` 完成後，將 `signals` Series 存入 `BacktestResult.signals`。
- 事件驅動引擎不產生 signals Series，填 `None`。
- 此欄位為 7-B 新增，不影響既有 `calculate_metrics()` 的行為。

---

#### `src/backtest/engine_vec.py` 修改

在 `run()` 方法的 `_build_result()` 呼叫處，將計算好的 `signals` Series 傳入 `BacktestResult`：

```python
result = calculate_metrics(equity_curve=equity, trades=trade_df)
result.signals = signals  # 暴露原始訊號供 UI signal overlay 使用
return result
```

---

#### `src/ui/pages/backtest.py` 重構

**Tab 結構：**

```python
def render() -> None:
    st.title("回測")

    tab_single, tab_compare, tab_history = st.tabs(
        ["單次回測", "策略比較", "歷史結果"]
    )

    with tab_single:
        _render_single_backtest_tab()

    with tab_compare:
        _render_comparison_tab()

    with tab_history:
        _render_history_tab()
```

**`_render_single_backtest_tab()`：** 現有 `render()` 的全部內容搬入，行為不變。

**`_render_comparison_tab()`：**

1. 共用 symbol / 日期輸入元件。
2. 「開始比較」按鈕 → 呼叫 `run_strategy_batch()`。
3. 顯示結果 DataFrame（`st.dataframe`），欄位格式化。
4. 提供「匯出結果」按鈕 → 存 CSV 到 `data/backtest/strategy_comparisons/`。
5. `st.selectbox` 選擇某個策略 → 展開該策略的 Tearsheet + K 線圖 + overlay + 指標副圖。

**`_render_history_tab()`：**

1. 掃描 `data/backtest/strategy_comparisons/` 和 `data/backtest/parameter_sweeps/` 目錄。
2. 列出 CSV 檔案清單（`st.selectbox`）。
3. 選中後讀取 CSV → 顯示 DataFrame。

---

#### K 線圖升級

**修改 `_render_price_panel()`：**

```python
def _render_price_panel(
    *,
    price_df: pd.DataFrame,
    trades: pd.DataFrame,
    symbol: str,
    signals: pd.Series | None = None,
    strategy_type: str | None = None,
    strategy_params: dict | None = None,
) -> None:
```

新增參數：`signals`（signal overlay 用）、`strategy_type` + `strategy_params`（指標副圖用）。

**Candlestick 主圖：**

```python
fig = make_subplots(
    rows=2 if needs_subplot else 1,
    cols=1,
    shared_xaxes=True,
    row_heights=[0.7, 0.3] if needs_subplot else [1.0],
    vertical_spacing=0.03,
)

fig.add_trace(
    go.Candlestick(
        x=features["date"],
        open=features["open"],
        high=features["high"],
        low=features["low"],
        close=features["close"],
        increasing_line_color="#d62728",   # 漲 = 紅（台股慣例）
        decreasing_line_color="#2ca02c",   # 跌 = 綠
        name="K 線",
    ),
    row=1, col=1,
)
```

- 若 `price_df` 缺少 `open`/`high`/`low` 欄位，降級為折線圖（保持向後相容）。
- MA5/MA20/MA60 均線保留在主圖上。

**Signal marker 層：**

```python
if signals is not None:
    buy_signal_dates = signals[signals == 1].index
    sell_signal_dates = signals[signals == -1].index

    fig.add_trace(go.Scatter(
        x=buy_signal_dates,
        y=price_df.loc[buy_signal_dates, "close"],
        mode="markers",
        marker={"symbol": "diamond", "size": 8, "color": "rgba(255,127,14,0.5)"},
        name="買進訊號",
    ), row=1, col=1)

    fig.add_trace(go.Scatter(
        x=sell_signal_dates,
        y=price_df.loc[sell_signal_dates, "close"],
        mode="markers",
        marker={"symbol": "diamond", "size": 8, "color": "rgba(23,190,207,0.5)"},
        name="賣出訊號",
    ), row=1, col=1)
```

**Trade marker 層：** 保留既有的三角形標記，實色不透明。

**區分用途：** Signal marker = 策略意圖（半透明菱形），Trade marker = 引擎成交（實色三角形）。向量化引擎有 next-bar 成交語義，兩者不在同一 bar。

---

#### 策略指標副圖

**判斷是否需要副圖：**

```python
_SUBPLOT_STRATEGY_TYPES = {"rsi", "kd_cross", "macd_cross", "bias"}
_MAIN_OVERLAY_TYPES = {"bollinger_band", "donchian_breakout"}

needs_subplot = strategy_type in _SUBPLOT_STRATEGY_TYPES
needs_main_overlay = strategy_type in _MAIN_OVERLAY_TYPES
```

**各策略指標繪製：**

**RSI 副圖：**
```python
rsi = pandas_ta.rsi(price_df["close"], length=params["period"])
fig.add_trace(go.Scatter(x=dates, y=rsi, name="RSI", line={"color": "#8c564b"}), row=2, col=1)
fig.add_hline(y=params["overbought"], line_dash="dash", line_color="red", row=2, col=1)
fig.add_hline(y=params["oversold"], line_dash="dash", line_color="green", row=2, col=1)
```

**KD 副圖：**
```python
stoch = pandas_ta.stoch(high, low, close, k=params["k_period"], d=params["d_period"], smooth_k=params["smooth_k"])
fig.add_trace(go.Scatter(x=dates, y=stoch_k, name="K", line={"color": "#1f77b4"}), row=2, col=1)
fig.add_trace(go.Scatter(x=dates, y=stoch_d, name="D", line={"color": "#ff7f0e"}), row=2, col=1)
```

**MACD 副圖：**
```python
macd_result = pandas_ta.macd(close, fast=params["fast"], slow=params["slow"], signal=params["signal"])
fig.add_trace(go.Scatter(x=dates, y=macd_line, name="MACD", line={"color": "#1f77b4"}), row=2, col=1)
fig.add_trace(go.Scatter(x=dates, y=signal_line, name="Signal", line={"color": "#ff7f0e"}), row=2, col=1)
fig.add_trace(go.Bar(x=dates, y=histogram, name="Histogram",
    marker_color=["#2ca02c" if v >= 0 else "#d62728" for v in histogram]), row=2, col=1)
```

**BIAS 副圖：**
```python
ma = close.rolling(params["ma_period"]).mean()
bias = (close - ma) / ma * 100
fig.add_trace(go.Scatter(x=dates, y=bias, name="BIAS", line={"color": "#9467bd"}), row=2, col=1)
fig.add_hline(y=params["buy_bias"], line_dash="dash", line_color="green", row=2, col=1)
fig.add_hline(y=params["sell_bias"], line_dash="dash", line_color="red", row=2, col=1)
```

**布林通道（主圖 overlay）：**
```python
bbands = pandas_ta.bbands(close, length=params["period"], std=params["std_dev"])
fig.add_trace(go.Scatter(x=dates, y=upper, name="上軌", line={"dash": "dot", "color": "#aaa"}), row=1, col=1)
fig.add_trace(go.Scatter(x=dates, y=lower, name="下軌", line={"dash": "dot", "color": "#aaa"}, fill="tonexty"), row=1, col=1)
```

**Donchian Channel（主圖 overlay）：**
```python
upper_ch = high.rolling(params["entry_period"]).max().shift(1)
lower_ch = low.rolling(params["exit_period"]).min().shift(1)
fig.add_trace(go.Scatter(x=dates, y=upper_ch, name="進場線", line={"dash": "dot", "color": "#17becf"}), row=1, col=1)
fig.add_trace(go.Scatter(x=dates, y=lower_ch, name="出場線", line={"dash": "dot", "color": "#bcbd22"}), row=1, col=1)
```

**MA Cross / DCA：** 不新增額外圖表，MA 線已在主圖、DCA 無技術指標。

---

#### 結果保存

```python
def _save_comparison_csv(batch_result: BatchResult) -> Path:
    """將 BatchResult 的 summaries 存為 CSV。"""
    output_dir = Path("data/backtest/strategy_comparisons")
    output_dir.mkdir(parents=True, exist_ok=True)

    timestamp = pd.Timestamp.now().strftime("%Y%m%dT%H%M%S")
    filename = f"{batch_result.symbol}_{batch_result.start_date}_{batch_result.end_date}_{timestamp}.csv"

    rows = []
    for s in batch_result.summaries:
        rows.append({
            "策略名稱": s.preset_name,
            "策略類型": s.strategy_type,
            "總報酬": s.total_return,
            "年化報酬": s.annual_return,
            "最大回撤": s.max_drawdown,
            "Sharpe": s.sharpe_ratio,
            "勝率": s.win_rate,
            "Profit Factor": s.profit_factor,
            "交易次數": s.total_trades,
            "錯誤": s.error or "",
        })

    df = pd.DataFrame(rows)
    path = output_dir / filename
    df.to_csv(path, index=False, encoding="utf-8-sig")
    return path
```

---

#### 測試規範

**測試檔案：** `tests/test_batch.py`

| 測試名稱 | 驗證內容 |
| :--- | :--- |
| `test_run_strategy_batch_returns_all_summaries` | 傳入 3 個 preset，回傳 3 筆 summary |
| `test_run_strategy_batch_failed_preset_skipped` | 其中 1 個 preset 參數不合法，不中斷、error 欄有值 |
| `test_run_strategy_batch_summary_fields_correct` | summary 的 total_return 等欄位與單獨跑 BacktestResult 一致 |
| `test_batch_result_signals_exposed` | 向量化引擎回傳的 BacktestResult 包含 signals Series |
| `test_save_comparison_csv_creates_file` | 呼叫 `_save_comparison_csv` 後 CSV 檔案存在且欄位正確 |
| `test_build_strategy_all_types` | `_build_strategy` 對 7 種 type（不含 DCA）都能建構成功 |
| `test_build_strategy_unsupported_type_raises` | 未知 type 拋 ValueError |

**UI 測試：** 手動驗收。

**不做的事：**

- 不修改 7-A 的策略程式碼。
- 不在 batch 中支援 DCA 策略（DCA 有專用 `run_dca_backtest` 流程，不適合統一 runner）。
- 不做 equity curve 疊圖（多條 equity curve 重疊），7-B 只做比較表 + 選中展開。
- 不做 Streamlit 原生 row click 互動，改用 selectbox 選擇。

---

### 7-C 參數掃描與防過度最佳化

**對應規格：** 規格書 §11 Phase 7-C。

**新增檔案：**

```
src/backtest/sweep.py
tests/test_sweep.py
```

**修改檔案：**

```
src/ui/pages/backtest.py        # 新增「參數掃描」tab
```

---

#### `src/backtest/sweep.py`

```python
from __future__ import annotations

from dataclasses import dataclass, field
from itertools import product
from typing import Any

import pandas as pd

from src.backtest.batch import _build_strategy
from src.backtest.engine_vec import VectorizedBacktester
from src.backtest.metrics import BacktestResult
from src.core.strategy_config import STRATEGY_META


@dataclass
class SweepRunSummary:
    params: dict[str, Any]
    total_return: float
    annual_return: float
    max_drawdown: float
    sharpe_ratio: float
    win_rate: float
    profit_factor: float
    total_trades: int
    sample_warning: bool = False    # total_trades < 3
    error: str | None = None
    result: BacktestResult | None = None


@dataclass
class SweepResult:
    symbol: str
    start_date: str
    end_date: str
    strategy_type: str
    total_combos: int
    valid_combos: int
    results: list[SweepRunSummary] = field(default_factory=list)


MAX_SWEEP_COMBOS = 200


def generate_param_grid(
    strategy_type: str,
    param_ranges: dict[str, list],
) -> tuple[list[dict[str, Any]], int]:
    """生成參數組合並過濾不合法的。

    回傳 (valid_combos, total_combos_before_filter)。
    使用 _normalize_*_params 判斷合法性。
    """
    ...


def run_parameter_sweep(
    *,
    symbol: str,
    data: pd.DataFrame,
    strategy_type: str,
    param_ranges: dict[str, list],
    initial_capital: float = 1_000_000,
) -> SweepResult:
    """對單一策略類型進行參數 Grid Search。

    Raises:
        ValueError: 合法組合數超過 MAX_SWEEP_COMBOS。
    """
    ...
```

---

#### `generate_param_grid()` 詳細邏輯

```python
def generate_param_grid(
    strategy_type: str,
    param_ranges: dict[str, list],
) -> tuple[list[dict[str, Any]], int]:
    """生成參數組合，過濾不合法的。

    回傳 (合法組合 list, 過濾前總組合數)。
    """
    from src.core.strategy_config import (
        _normalize_rsi_params,
        _normalize_kd_cross_params,
        _normalize_macd_cross_params,
        _normalize_bollinger_band_params,
        _normalize_bias_params,
        _normalize_donchian_breakout_params,
        _normalize_moving_average_params,
    )

    normalizers = {
        "moving_average_cross": _normalize_moving_average_params,
        "rsi": _normalize_rsi_params,
        "kd_cross": _normalize_kd_cross_params,
        "macd_cross": _normalize_macd_cross_params,
        "bollinger_band": _normalize_bollinger_band_params,
        "bias": _normalize_bias_params,
        "donchian_breakout": _normalize_donchian_breakout_params,
    }

    normalizer = normalizers.get(strategy_type)
    if normalizer is None:
        raise ValueError(f"Unsupported strategy type for sweep: {strategy_type}")

    keys = list(param_ranges.keys())
    values = list(param_ranges.values())
    all_combos = [dict(zip(keys, combo)) for combo in product(*values)]
    total = len(all_combos)

    valid = []
    for combo in all_combos:
        if normalizer(combo) is not None:
            valid.append(combo)

    return valid, total
```

---

#### `run_parameter_sweep()` 詳細邏輯

```python
def run_parameter_sweep(...) -> SweepResult:
    valid_combos, total_combos = generate_param_grid(strategy_type, param_ranges)

    if len(valid_combos) > MAX_SWEEP_COMBOS:
        raise ValueError(
            f"合法組合數 {len(valid_combos)} 超過上限 {MAX_SWEEP_COMBOS}，請縮小參數範圍。"
        )

    results = []
    for combo in valid_combos:
        preset = {"type": strategy_type, "params": combo, "name": "sweep"}
        try:
            strategy = _build_strategy(preset)
            engine = VectorizedBacktester(initial_capital=initial_capital)
            bt_result = engine.run(strategy=strategy, data=data)

            summary = SweepRunSummary(
                params=combo,
                total_return=bt_result.total_return,
                annual_return=bt_result.annual_return,
                max_drawdown=bt_result.max_drawdown,
                sharpe_ratio=bt_result.sharpe_ratio,
                win_rate=bt_result.win_rate,
                profit_factor=bt_result.profit_factor,
                total_trades=bt_result.total_trades,
                sample_warning=(bt_result.total_trades < 3),
                result=bt_result,
            )
        except Exception as e:
            summary = SweepRunSummary(
                params=combo,
                total_return=0, annual_return=0, max_drawdown=0,
                sharpe_ratio=0, win_rate=0, profit_factor=0,
                total_trades=0, error=str(e),
            )
        results.append(summary)

    return SweepResult(
        symbol=symbol,
        start_date=str(data.index[0].date()),
        end_date=str(data.index[-1].date()),
        strategy_type=strategy_type,
        total_combos=total_combos,
        valid_combos=len(valid_combos),
        results=results,
    )
```

---

#### `src/ui/pages/backtest.py` — 參數掃描 tab

**Tab 結構更新（7-C 後）：**

```python
tab_single, tab_compare, tab_sweep, tab_history = st.tabs(
    ["單次回測", "策略比較", "參數掃描", "歷史結果"]
)
```

**`_render_sweep_tab()` 流程：**

```python
def _render_sweep_tab() -> None:
    # 1. 共用 symbol / 日期輸入
    symbol, start_date, end_date = _shared_inputs(key_prefix="sweep")

    # 2. 策略類型選擇（排除 DCA）
    sweep_types = [t for t in STRATEGY_META if t != "dollar_cost_averaging"]
    selected_type = st.selectbox("策略類型", sweep_types,
        format_func=lambda t: STRATEGY_META[t]["label"])

    # 3. 根據策略類型動態顯示參數輸入
    meta = STRATEGY_META[selected_type]
    param_ranges = {}
    for param_key, param_label in meta["param_labels"].items():
        raw = st.text_input(f"{param_label}", placeholder="例如：5,10,20", key=f"sweep_{param_key}")
        if raw.strip():
            param_ranges[param_key] = _parse_param_values(raw, param_key)

    # 4. 即時計算合法組合數
    if param_ranges:
        valid_combos, total = generate_param_grid(selected_type, param_ranges)
        st.info(f"總組合數: {total} → 合法組合: {len(valid_combos)}")
        if len(valid_combos) > MAX_SWEEP_COMBOS:
            st.warning(f"合法組合數 {len(valid_combos)} 超過上限 {MAX_SWEEP_COMBOS}，請縮小參數範圍。")
            can_run = False
        else:
            can_run = True
    else:
        can_run = False

    # 5. 開始掃描
    if st.button("開始掃描", disabled=not can_run):
        data = _load_backtest_data(symbol, start_date, end_date)
        with st.spinner(f"掃描中 ({len(valid_combos)} 組)..."):
            sweep_result = run_parameter_sweep(
                symbol=symbol, data=data,
                strategy_type=selected_type,
                param_ranges=param_ranges,
            )
        _render_sweep_results(sweep_result)
```

**參數值解析：**

```python
def _parse_param_values(raw: str, param_key: str) -> list:
    """解析逗號分隔的參數值。

    數值型參數轉為 int 或 float；去重並排序。
    """
    parts = [p.strip() for p in raw.split(",") if p.strip()]
    values = []
    for p in parts:
        try:
            values.append(int(p))
        except ValueError:
            values.append(float(p))
    return sorted(set(values))
```

**結果渲染 `_render_sweep_results()`：**

```python
def _render_sweep_results(sweep_result: SweepResult) -> None:
    # 1. 排序選擇
    sort_col = st.selectbox("排序依據", ["Sharpe", "總報酬", "年化報酬", "最大回撤", "勝率", "Profit Factor"])
    ascending = (sort_col == "最大回撤")

    # 2. 顯示數量
    show_n = st.slider("顯示筆數", min_value=5, max_value=50, value=20)

    # 3. 建構 DataFrame
    rows = _build_sweep_dataframe(sweep_result, sort_col, ascending, show_n)
    st.dataframe(rows, use_container_width=True)

    # 4. 選中某列展開
    selected_idx = st.selectbox("選擇查看詳細", range(len(rows)))
    selected_run = sweep_result.results[selected_idx]
    if selected_run.result:
        # 展開 Tearsheet + K 線圖 + 指標副圖（複用 7-B 的 _render_price_panel）
        ...

    # 5. 匯出
    if st.button("匯出掃描結果"):
        path = _save_sweep_csv(sweep_result)
        st.success(f"已匯出：{path}")
```

**防過度最佳化視覺標記（在 DataFrame 中）：**

```python
def _build_sweep_dataframe(sweep_result, sort_col, ascending, show_n) -> pd.DataFrame:
    rows = []
    for r in sweep_result.results:
        if r.error:
            continue
        row = {**r.params}
        row["總報酬"] = f"{r.total_return:.2%}"
        row["年化報酬"] = f"{r.annual_return:.2%}"
        row["最大回撤"] = f"{r.max_drawdown:.2%}" + (" ⚠ 回撤過深" if r.max_drawdown > 0.5 else "")
        row["Sharpe"] = f"{r.sharpe_ratio:.2f}"
        row["勝率"] = f"{r.win_rate:.2%}"
        pf = "N/A" if r.profit_factor == 999.0 else f"{r.profit_factor:.2f}"
        row["Profit Factor"] = pf
        trades_str = str(r.total_trades)
        if r.total_trades < 3:
            trades_str += " ⚠ 樣本不足"
        row["交易次數"] = trades_str
        rows.append(row)

    df = pd.DataFrame(rows)
    # sort and limit
    ...
    return df.head(show_n)
```

---

#### 掃描結果保存

```python
def _save_sweep_csv(sweep_result: SweepResult) -> Path:
    output_dir = Path("data/backtest/parameter_sweeps")
    output_dir.mkdir(parents=True, exist_ok=True)

    timestamp = pd.Timestamp.now().strftime("%Y%m%dT%H%M%S")
    filename = f"{sweep_result.symbol}_{sweep_result.strategy_type}_{timestamp}.csv"

    rows = []
    for r in sweep_result.results:
        if r.error:
            continue
        row = {**r.params}
        row.update({
            "total_return": r.total_return,
            "annual_return": r.annual_return,
            "max_drawdown": r.max_drawdown,
            "sharpe_ratio": r.sharpe_ratio,
            "win_rate": r.win_rate,
            "profit_factor": r.profit_factor,
            "total_trades": r.total_trades,
            "sample_warning": r.sample_warning,
        })
        rows.append(row)

    df = pd.DataFrame(rows)
    path = output_dir / filename
    df.to_csv(path, index=False, encoding="utf-8-sig")
    return path
```

---

#### 測試規範

**測試檔案：** `tests/test_sweep.py`

| 測試名稱 | 驗證內容 |
| :--- | :--- |
| `test_generate_param_grid_filters_invalid` | MA `short=[5,10,20]` × `long=[10,20,40]` → `short>=long` 的組合被排除 |
| `test_generate_param_grid_rsi_filters` | RSI `oversold=[30,80]` × `overbought=[70,80]` → `oversold>=overbought` 排除 |
| `test_generate_param_grid_total_count` | 回傳的 total_combos 為未過濾前的笛卡兒積大小 |
| `test_run_parameter_sweep_exceeds_limit_raises` | 合法組合 > 200 → 拋 ValueError |
| `test_run_parameter_sweep_returns_results` | MA `short=[5,10]` × `long=[40,60]` → 回傳 4 筆 SweepRunSummary |
| `test_sweep_sample_warning_flag` | `total_trades < 3` 的結果 `sample_warning=True` |
| `test_sweep_failed_combo_has_error` | 策略建構失敗的 combo → error 欄位有值 |
| `test_save_sweep_csv_creates_file` | CSV 檔案存在且欄位正確 |
| `test_parse_param_values_dedup_sort` | `"20,5,10,10"` → `[5, 10, 20]` |
| `test_parse_param_values_float` | `"1.5,2.0,2.5"` → `[1.5, 2.0, 2.5]` |

**UI 測試：** 手動驗收。

**不做的事：**

- 不做 train/test split 或 walk-forward optimization（由 7-D 承接）。
- 不做 heatmap 視覺化（可留後續 UI 優化）。
- 不對 DCA 策略做參數掃描。
- 不做多策略同時掃描（一次只掃一種策略類型）。
- 不自動推薦「最佳參數」，只提供排序後的 Top N。

---

### 7-D Walk-Forward Analysis

**對應規格：** 規格書 §11 Phase 7-D。

**新增檔案：**

```
src/backtest/walk_forward.py
tests/test_walk_forward.py
```

**修改檔案：**

```
src/ui/pages/backtest.py        # 新增「Walk-Forward」tab
```

**實作範圍：**

- 單一 symbol。
- 只支援 `VectorizedBacktester`。
- 只支援 7-C 可掃描策略，排除 `dollar_cost_averaging`。
- 採 rolling calendar window，不做 anchored expanding。
- 不做 portfolio、多標的、event-driven、Monte Carlo、AI 解讀。

**子階段切分：**

| 子階段 | 範圍 | 驗證 |
| :--- | :--- | :--- |
| 7-D-1 核心引擎 | `walk_forward.py`、window splitter、runner、aggregate、parameter stability、CSV export | ✅ 已驗證：`tests/test_walk_forward.py` + `tests/test_sweep.py` = 62 passed, 1 warning |
| 7-D-2 UI tab + 文件 + 回歸 | 回測頁 Walk-Forward tab、中文說明、資料長度檢查、預估回測次數、進度條、summary/window/stability table、CSV 下載、文件更新 | ✅ 已驗收：UI 規格補點通過；Phase 7 回歸 116 passed, 1 warning |

**7-D-1 驗證紀錄（2026-05-09）：**

- 指令：`.\.venv\Scripts\python.exe -m pytest tests/test_walk_forward.py tests/test_sweep.py -v`
- 結果：62 passed, 1 warning。
- 注意：第一次一般權限執行時，pytest `tmp_path` fixture 在 `C:\Users\User\AppData\Local\Temp\pytest-of-User` 遇到 `PermissionError: [WinError 5]`；依專案 Windows/OneDrive 規則，用同一 venv 與同一測試範圍 elevated 重跑後通過。
- `warning_count` 已補 regression test：`test_warning_count_includes_per_window_and_unstable_param`，確認 per-window warnings 與 unstable-parameter aggregate warnings 會一併計數。

**7-D-2 驗收紀錄（2026-05-10）：**

- Walk-Forward tab 已整合至回測頁，且不影響單次回測、策略比較、參數掃描、歷史結果 tabs。
- UI 英文研究術語已補中文說明：Walk-Forward（滾動樣本外驗證）、IS（樣本內／訓練期）、OOS（樣本外／驗證期）、Sharpe Ratio（夏普比率）、CV（變異係數）。
- 回測次數預估已依日期區間、IS/OOS/Step 與合法組合數顯示：`預估 {windows} 段 × {combos} 組合 = 最多 {total} 次回測`。
- UI 驗收補點已通過：Max windows=10 顯示、combo 上限阻止、summary metrics、window warnings、sweep 失敗視窗標記、CSV 匯出入口。
- 指令：`.\.venv\Scripts\python.exe -m py_compile src\ui\pages\backtest.py src\backtest\walk_forward.py`
- 結果：通過。第一次一般權限執行時因 Windows/OneDrive `__pycache__` 寫入權限失敗，elevated 重跑同範圍後通過。
- 指令：`.\.venv\Scripts\python.exe -m pytest tests/test_strategies.py tests/test_strategy_config.py tests/test_batch.py tests/test_sweep.py tests/test_walk_forward.py -v`
- 結果：116 passed, 1 warning。

---

#### `src/backtest/walk_forward.py`

```python
from __future__ import annotations

from dataclasses import dataclass, field
from pathlib import Path
from typing import Any

import pandas as pd

from src.backtest.batch import _build_strategy
from src.backtest.engine_vec import VectorizedBacktester
from src.backtest.metrics import BacktestResult
from src.backtest.sweep import (
    MAX_SWEEP_COMBOS,
    SweepRunSummary,
    generate_param_grid,
    run_parameter_sweep,
)


DEFAULT_IS_MONTHS = 12
DEFAULT_OOS_MONTHS = 6
DEFAULT_STEP_MONTHS = 6
MIN_WFA_WINDOWS = 3
MAX_WFA_WINDOWS = 10
MIN_OOS_TRADES_WARNING = 3
SUPPORTED_OPTIMIZE_METRICS = {"total_return", "annual_return", "sharpe_ratio"}


@dataclass(frozen=True)
class WalkForwardWindow:
    window_id: int
    is_start: pd.Timestamp
    is_end: pd.Timestamp
    oos_start: pd.Timestamp
    oos_end: pd.Timestamp


@dataclass(frozen=True)
class WalkForwardWindowResult:
    window: WalkForwardWindow
    best_params: dict[str, Any] | None
    is_best: SweepRunSummary | None
    oos_result: BacktestResult | None
    degradation: float | None
    skipped: bool
    warnings: list[str] = field(default_factory=list)


@dataclass(frozen=True)
class WalkForwardSummary:
    strategy_type: str
    optimize_metric: str
    windows: list[WalkForwardWindowResult]
    total_window_count: int
    valid_window_count: int
    skipped_window_count: int
    aggregate: dict[str, Any]
    parameter_stability: dict[str, Any]
```

---

#### `generate_walk_forward_windows()`

```python
def generate_walk_forward_windows(
    data: pd.DataFrame,
    *,
    is_months: int = DEFAULT_IS_MONTHS,
    oos_months: int = DEFAULT_OOS_MONTHS,
    step_months: int = DEFAULT_STEP_MONTHS,
    min_windows: int = MIN_WFA_WINDOWS,
    max_windows: int = MAX_WFA_WINDOWS,
) -> list[WalkForwardWindow]:
    """依 calendar month 產生 rolling IS/OOS 視窗。

    Rules:
    - data index 必須是 DatetimeIndex。
    - is_months / oos_months / step_months 必須 > 0。
    - 至少產生 min_windows 段，否則 raise ValueError。
    - MVP hard cap: max_windows 不得超過 MAX_WFA_WINDOWS。
    - 若可產生超過 max_windows 段，只回傳最早 max_windows 段。
    """
    ...
```

**最低資料長度檢查：**

```python
def required_months_for_wfa(
    *,
    is_months: int,
    oos_months: int,
    step_months: int,
    min_windows: int = MIN_WFA_WINDOWS,
) -> int:
    return is_months + oos_months + (min_windows - 1) * step_months
```

UI 執行前需用相同公式阻止資料不足：

```text
資料長度不足：目前資料僅 {actual} 個月，WFA 至少需要 {required} 個月（IS {is_months} + OOS {oos_months} + {min_windows - 1} 段步進 × {step_months}）。
```

---

#### `run_walk_forward_analysis()`

```python
def run_walk_forward_analysis(
    *,
    symbol: str,
    data: pd.DataFrame,
    strategy_type: str,
    param_ranges: dict[str, list],
    optimize_metric: str = "sharpe_ratio",
    initial_capital: float = 1_000_000,
    is_months: int = DEFAULT_IS_MONTHS,
    oos_months: int = DEFAULT_OOS_MONTHS,
    step_months: int = DEFAULT_STEP_MONTHS,
    max_combinations: int = MAX_SWEEP_COMBOS,
) -> WalkForwardSummary:
    """執行 rolling WFA。

    Flow:
    1. 驗證 optimize_metric 屬於 SUPPORTED_OPTIMIZE_METRICS。
    2. 產生 rolling windows。
    3. 每段 IS 呼叫 run_parameter_sweep。
    4. 依 optimize_metric 選出 error is None 且 result is not None 的最佳 summary。
    5. 用最佳 params 建策略，對 OOS data 執行 VectorizedBacktester。
    6. sweep 全失敗則 skipped=True，不計入 aggregate。
    7. 彙總 OOS 指標與參數穩定性。
    """
    ...
```

**選最佳參數：**

```python
def _select_best_sweep_run(
    sweep_runs: list[SweepRunSummary],
    optimize_metric: str,
) -> SweepRunSummary | None:
    candidates = [
        r for r in sweep_runs
        if r.error is None and r.result is not None
    ]
    if not candidates:
        return None
    return max(candidates, key=lambda r: getattr(r, optimize_metric))
```

`profit_factor` 不得放入 `SUPPORTED_OPTIMIZE_METRICS`，因為 sentinel `999.0` 會污染排序。`max_drawdown` 也不放入 MVP，因為屬越低越好指標。

**Degradation：**

```python
def _calculate_degradation(
    is_best: SweepRunSummary,
    oos_result: BacktestResult,
    optimize_metric: str,
) -> float:
    return getattr(oos_result, optimize_metric) - getattr(is_best, optimize_metric)
```

MVP 只做絕對差值；ratio-based degradation 列為 follow-up。

---

#### 參數穩定性

```python
def calculate_parameter_stability(
    window_results: list[WalkForwardWindowResult],
) -> dict[str, Any]:
    """計算有效 window 的 best_params 穩定性。

    skipped=True 或 best_params is None 的視窗不納入。
    對每個 numeric param 計算 min/max/mean/median/std/cv/status。
    """
    ...
```

CV 分類：

```python
cv = std / abs(mean)  # mean == 0 時直接判 unstable

if cv < 0.15:
    status = "stable"
elif cv < 0.40:
    status = "moderate"
else:
    status = "unstable"
```

回傳格式建議：

```python
{
    "overall_status": "stable" | "moderate" | "unstable",
    "params": {
        "short_window": {
            "min": 8,
            "max": 12,
            "mean": 10.0,
            "median": 10.0,
            "std": 1.2,
            "cv": 0.12,
            "status": "stable",
        }
    },
}
```

---

#### Aggregate metrics

```python
def _build_walk_forward_aggregate(
    window_results: list[WalkForwardWindowResult],
) -> dict[str, Any]:
    """只統計 skipped=False 且 oos_result is not None 的有效視窗。"""
    ...
```

至少輸出：

- `oos_win_window_rate`
- `avg_oos_return`
- `median_oos_return`
- `avg_oos_sharpe`
- `worst_oos_drawdown`
- `avg_degradation`
- `warning_count`

若有效視窗數少於 `MIN_WFA_WINDOWS`，aggregate warnings 必須包含「視窗數過少，WFA 可信度有限」。

---

#### CSV export

```python
def save_walk_forward_summary_csv(
    summary: WalkForwardSummary,
    output_dir: Path | str = Path("data/backtest/walk_forward"),
) -> tuple[Path, Path]:
    """輸出 window summary CSV 與 parameter stability CSV。"""
    ...
```

檔名格式：

```text
{strategy_type}_wfa_windows_{timestamp}.csv
{strategy_type}_wfa_stability_{timestamp}.csv
```

window CSV 至少包含：

- `window_id`
- `is_start`
- `is_end`
- `oos_start`
- `oos_end`
- `best_params`
- IS / OOS metrics
- `degradation`
- `skipped`
- `warnings`

---

#### `src/ui/pages/backtest.py` — Walk-Forward tab

新增 tab，不假設既有 tab 數固定。UI 主要顯示中文；必要英文術語需附中文說明。

**控制項：**

- 策略 preset / strategy type
- 參數範圍輸入（沿用 7-C）
- IS months（樣本內月份）
- OOS months（樣本外月份）
- Step months（滾動步進月份）
- Optimize metric（最佳化指標）
- Max combinations（最大參數組合數）
- Run button

**執行前檢查：**

1. 參數組合數 > 0 且 <= 200。
2. 資料長度足以產生至少 3 段 WFA。
3. 預估回測次數顯示：`將進行 {windows} 段 × {combos} 組 = {total} 次回測`。
4. 視窗數不得超過 `MAX_WFA_WINDOWS=10`。

**執行中：**

- 使用 `st.progress` 顯示目前 window 進度。
- 每段 window 文案需顯示中文，例如「正在執行第 2 / 5 段 WFA」。

**結果顯示：**

1. Summary metrics：
   - 有效視窗數
   - OOS 勝率
   - 平均 OOS return
   - 平均 OOS Sharpe
   - 最差 OOS drawdown
   - 平均 degradation
2. Window table：
   - IS/OOS 起訖
   - best params
   - IS/OOS metrics
   - degradation
   - skipped
   - warnings
3. Parameter stability table：
   - param
   - min / median / max
   - cv
   - status
4. CSV download。

---

#### 警告行為

| 條件 | 行為 |
| :--- | :--- |
| WFA window 少於 3 段 | 執行前阻止 |
| OOS trade 少於 3 筆 | 該視窗標記警告，仍計入彙總 |
| 大多數 OOS 報酬為負 | 彙總層警告 |
| 平均 degradation 大幅為負 | 彙總層警告 |
| 任一參數 `status == "unstable"` | 彙總層警告 |
| 某 window IS sweep 全部失敗 | `skipped=True`，不計入彙總 |
| 資料長度不足 | 執行前阻止 |

---

#### 測試規範

**測試檔案：** `tests/test_walk_forward.py`

| 測試名稱 | 驗證內容 |
| :--- | :--- |
| `test_generate_walk_forward_windows_basic` | 依預設產生 rolling IS/OOS 視窗 |
| `test_generate_walk_forward_windows_requires_enough_data` | 資料不足時拋 ValueError |
| `test_generate_walk_forward_windows_max_windows_cap` | 視窗數最多 10 段 |
| `test_required_months_for_wfa_formula` | 最低月數公式正確 |
| `test_run_walk_forward_returns_window_results` | 回傳 window results |
| `test_run_walk_forward_uses_is_for_sweep_and_oos_for_validation` | IS 掃描、OOS 驗證資料切割正確 |
| `test_run_walk_forward_selects_best_params_by_metric` | 依指定 metric 選最佳參數 |
| `test_run_walk_forward_rejects_profit_factor_metric` | `profit_factor` 不可作為 optimize metric |
| `test_run_walk_forward_records_degradation` | degradation = OOS metric - IS metric |
| `test_run_walk_forward_sets_low_trade_warning` | OOS trades < 3 時標記 warning |
| `test_run_walk_forward_skips_failed_sweep_window` | sweep 全失敗視窗 skipped=True |
| `test_walk_forward_valid_window_count_excludes_skipped` | valid/skipped count 正確 |
| `test_parameter_stability_stable` | CV < 0.15 → stable |
| `test_parameter_stability_moderate` | 0.15 <= CV < 0.40 → moderate |
| `test_parameter_stability_unstable` | CV >= 0.40 → unstable |
| `test_walk_forward_excludes_dca` | DCA 不支援 WFA |
| `test_walk_forward_respects_max_combos` | 參數組合 > 200 時阻止 |
| `test_save_walk_forward_csv_creates_files` | 兩份 CSV 建立且欄位正確 |

**不做的事：**

- 不做 anchored expanding window。
- 不做 ratio-based degradation。
- 不把 `profit_factor` / `max_drawdown` 納入 optimize metric。
- 不做 event-driven WFA。
- 不做多標的 WFA。

---

## Phase 8：個股綜合分析儀表板

**對應規格：** 規格書 §11 Phase 8。

**新增目錄：**

```
src/analysis/                    # 新目錄
├── __init__.py
├── technical_summary.py         # 8-A
├── pattern.py                   # 8-B
└── chip_analysis.py             # 8-C

src/data/realtime.py             # 8-D
```

**修改檔案：**

```
src/data/fetcher.py              # 8-C 擴充 fetch_institutional / fetch_margin
src/data/storage.py              # 8-C 擴充 save/load institutional / margin
src/core/constants.py            # 8-C 新增欄位常數
src/ai/advisor.py                # 8-E 新增 generate_stock_dashboard_analysis
src/ui/app.py                    # 8-F 新增「個股分析」入口
src/ui/pages/dashboard.py        # 8-F 新增儀表板頁面
config.yaml                      # 新增 realtime: section
```

**實作範圍：**

- 單一 symbol 個股分析。
- 8-A/B/C/D 四條線互相獨立可平行，8-E 依賴 8-A+8-C，8-F 最後整合。
- 不做即時自動刷新（手動按鈕觸發）。
- 不做多股同時分析。
- 不做實盤下單或自動交易。

**子階段切分：**

| 子階段 | 範圍 | 依賴 | AI | 新管線 |
| :--- | :--- | :--- | :--- | :--- |
| 8-A | 技術面自動判讀引擎 | 無 | 否 | 無 |
| 8-B | K 線型態辨識 + 多週期 | 8-A（TimeframeTrend） | 否 | 無 |
| 8-C | 籌碼分析管線 | 無 | 否 | FinMind 法人 + 融資融券 |
| 8-D | 即時行情接入 | 無 | 否 | TWSE MIS API |
| 8-E | AI 綜合分析與操作劇本 | 8-A, 8-C | 是 | FinMind TaiwanStockInfo |
| 8-F | 儀表板 UI | 8-A~E | 間接 | 無 |
| 8-G | 新手友善說明文字 | 8-F | 否 | 無 |

---

### 8-A 技術面自動判讀引擎

**對應規格：** 規格書 §11 Phase 8-A。

**新增檔案：**

```
src/analysis/__init__.py
src/analysis/technical_summary.py
tests/test_technical_summary.py
```

---

#### `src/analysis/technical_summary.py`

```python
from __future__ import annotations

from dataclasses import dataclass, field

import pandas as pd


@dataclass
class PriceLevel:
    value: float
    label: str       # "近期高點" / "前高" / "MA20" / "MA60" / ...
    kind: str        # "resistance" / "support"


@dataclass
class TechnicalSummary:
    # 技術分析總覽
    trend_direction: str          # "多頭趨勢" / "空頭趨勢" / "盤整"
    ma_status: str                # "多頭排列 (5>20>60)" / "空頭排列" / "糾結"
    kd_status: str                # "KD 高檔鈍化" / "KD 黃金交叉" / ...
    macd_status: str              # "正值收斂" / "負值擴張" / ...
    volume_status: str            # "量能放大" / "量能略縮" / "爆量"
    volume_price_relation: str    # "價漲量增" / "價漲量縮（短線整理）" / ...

    # 短線綜合分數
    short_term_score: float       # 0.0~1.0
    short_term_label: str         # "強勢偏多" / "中等偏多" / "中性" / "偏空"
    short_term_components: dict[str, float]  # {"ma": 0.8, "kd": 0.6, ...}

    # 關鍵價位
    resistance_levels: list[PriceLevel] = field(default_factory=list)
    support_levels: list[PriceLevel] = field(default_factory=list)

    # 量價結構
    volume_price_divergence: str = ""
    ma_bias: str = ""
    chip_behavior: str = ""       # 由 8-C 注入
    operation_observation: str = ""
```

---

#### `generate_technical_summary()`

```python
def generate_technical_summary(
    df: pd.DataFrame,
    *,
    chip_behavior: str = "",
) -> TechnicalSummary:
    """從日 K OHLCV 生成技術面摘要。

    Args:
        df: 日 K DataFrame，需含 open/high/low/close/volume，index 為 DatetimeIndex。
        chip_behavior: 由 8-C chip_analysis 注入的籌碼行為文字。

    Returns:
        TechnicalSummary dataclass。

    Raises:
        ValueError: df 為空或缺少必要欄位。

    Notes:
        - 使用 raw price（非 adjusted），確保與盤中報價同尺度。
        - 資料少於 5 bar 時，MA/KD/MACD 相關欄位標示資料不足。
        - 資料少於 60 bar 時，MA60 相關欄位標示資料不足。
    """
    ...
```

---

#### 判讀規則實作細節

**趨勢方向：**

```python
def _determine_trend(ma5: float, ma20: float, ma60: float | None) -> str:
    if ma60 is None:
        # MA60 算不出來時只看 MA5 vs MA20
        return "多頭趨勢" if ma5 > ma20 else "空頭趨勢" if ma5 < ma20 else "盤整"
    if ma5 > ma20 > ma60:
        return "多頭趨勢"
    if ma5 < ma20 < ma60:
        return "空頭趨勢"
    return "盤整"
```

**短線綜合分數：**

```python
def _calculate_short_term_score(
    *,
    ma_score: float,      # 0.0~1.0
    kd_score: float,      # 0.0~1.0
    volume_price_score: float,  # 0.0~1.0
    breakout_score: float,      # 0.0~1.0
) -> tuple[float, str, dict[str, float]]:
    """加權計算短線綜合分數。

    權重：MA 結構 30%, KD 狀態 25%, 量價關係 25%, 突破狀態 20%。
    """
    score = (
        ma_score * 0.30
        + kd_score * 0.25
        + volume_price_score * 0.25
        + breakout_score * 0.20
    )
    if score >= 0.7:
        label = "強勢偏多"
    elif score >= 0.5:
        label = "中等偏多"
    elif score >= 0.3:
        label = "中性"
    else:
        label = "偏空"
    components = {
        "ma": ma_score,
        "kd": kd_score,
        "volume_price": volume_price_score,
        "breakout": breakout_score,
    }
    return score, label, components
```

**關鍵價位：**

```python
def _find_resistance_levels(df: pd.DataFrame) -> list[PriceLevel]:
    """找壓力區：近 60 日最高價、近 20 日最高價（取不重複的 2 個）。"""
    ...

def _find_support_levels(
    df: pd.DataFrame, ma20: float, ma60: float | None
) -> list[PriceLevel]:
    """找支撐區：近期低點、MA20、MA60（取最近的 2~3 個）。"""
    ...
```

**量能狀態：**

```python
def _assess_volume(today_vol: int, avg_5d_vol: float) -> str:
    ratio = today_vol / avg_5d_vol if avg_5d_vol > 0 else 1.0
    if ratio > 1.5:
        return "量能放大" if ratio < 3.0 else "爆量"
    if ratio < 0.7:
        return "量能略縮"
    return "量能正常"
```

---

#### 測試規範

**測試檔案：** `tests/test_technical_summary.py`

| 測試名稱 | 驗證內容 |
| :--- | :--- |
| `test_bullish_trend_fixture` | 多頭排列 fixture → trend/ma/kd/macd 欄位正確 |
| `test_bearish_trend_fixture` | 空頭排列 fixture → 正確空頭判讀 |
| `test_consolidation_fixture` | 盤整 fixture → 正確盤整判讀 |
| `test_short_term_score_range` | 分數在 0.0~1.0 範圍 |
| `test_short_term_score_weights` | 各組件權重正確加總 |
| `test_resistance_levels_found` | 壓力區正確計算 |
| `test_support_levels_found` | 支撐區正確計算 |
| `test_volume_status_high` | 量 > 1.5x → "量能放大" |
| `test_volume_status_low` | 量 < 0.7x → "量能略縮" |
| `test_insufficient_data_5bars` | 僅 5 bar → MA60/KD/MACD 標示資料不足 |
| `test_all_nan_data` | 全 NaN → 不 crash |
| `test_ma_bias_calculation` | 乖離率計算正確 |
| `test_chip_behavior_injection` | chip_behavior 參數正確注入輸出 |

**不做的事：**

- 不做 AI 分析（由 8-E 承接）。
- 不做即時報價（由 8-D 承接）。
- 不做 Streamlit UI 渲染。

---

### 8-B K 線型態辨識

**對應規格：** 規格書 §11 Phase 8-B。

**新增檔案：**

```
src/analysis/pattern.py
tests/test_pattern.py
```

---

#### `src/analysis/pattern.py`

```python
from __future__ import annotations

from dataclasses import dataclass, field

import pandas as pd


@dataclass
class CandlePattern:
    name: str           # "長紅 K" / "錘子" / "吞噬" / ...
    detected: bool
    description: str    # "多方力道" / "反轉訊號" / ...


@dataclass
class ChartPatternResult:
    pattern_type: str   # "W底（雙底）" / "M頭（雙頂）"
    formed: bool
    description: str    # "近60日形成標準W底型態" / "未形成標準W底型態"
    key_points: list[tuple[str, float]] = field(default_factory=list)
    # [("低點1", 750.0), ("低點2", 755.0), ("頸線", 790.0)]


@dataclass
class TimeframeTrend:
    timeframe: str      # "daily" / "weekly" / "monthly"
    trend_direction: str  # "多頭" / "空頭" / "盤整"
    strength: str       # "強" / "中強" / "中" / "弱"


@dataclass
class MultiTimeframeAnalysis:
    daily: TimeframeTrend
    weekly: TimeframeTrend
    monthly: TimeframeTrend
```

---

#### K 線型態辨識

```python
def detect_candle_patterns(df: pd.DataFrame) -> list[CandlePattern]:
    """辨識當日 K 線型態。

    使用 pandas-ta cdl_pattern + 自定規則。
    只看最後一根 bar（當日）。

    自定規則（pandas-ta 無直接對應）：
    - 長紅 K：body > 2× 近 20 日平均 body 且為陽線
    - 長黑 K：body > 2× 近 20 日平均 body 且為陰線
    - 帶上影線：upper_shadow > 2× body
    - 帶下影線：lower_shadow > 2× body

    pandas-ta 規則：
    - cdl_doji → 十字線
    - cdl_pattern(name=["hammer", "hangingman"]) → 錘子 / 吊人
    - cdl_pattern(name="engulfing") → 吞噬
    - cdl_pattern(name=["morningstar", "eveningstar"]) → 晨星 / 夜星
    """
    ...
```

---

#### 圖表型態偵測

```python
def detect_chart_pattern(
    df: pd.DataFrame,
    *,
    lookback_days: int = 60,
    tolerance_pct: float = 0.03,
) -> list[ChartPatternResult]:
    """偵測 W 底 / M 頭型態。

    保守策略：寧可漏報不要誤報。

    W 底演算法：
    1. 在近 lookback_days 日用手寫鄰近點比較找出局部最低點（不新增 scipy 依賴）
    2. 取最近 2 個相近低點（誤差 < tolerance_pct）
    3. 兩低點之間必須有反彈高點（> 兩低點的平均 + 3%）
    4. 滿足以上條件才判定為 W 底

    M 頭演算法（鏡像邏輯）。

    偵測不到時回傳 formed=False，description="未形成標準 X 型態"。
    """
    ...
```

---

#### 多週期分析

```python
def analyze_multi_timeframe(df: pd.DataFrame) -> MultiTimeframeAnalysis:
    """將日 K resample 為週 K / 月 K 並分析各週期趨勢。

    每個週期判讀：
    - trend_direction：使用 8-A 的 _determine_trend 邏輯
    - strength：依 MA 排列 + RSI 位置
      - RSI > 60 且多頭排列 → "強"
      - RSI 50~60 且多頭排列 → "中強"
      - 盤整或 RSI 40~60 → "中"
      - RSI < 40 或空頭排列 → "弱"

    Resample 規則：
    - 週 K：使用 'W' frequency
    - 月 K：使用 'ME' frequency
    - open=first, high=max, low=min, close=last, volume=sum
    """
    ...
```

---

#### 測試規範

**測試檔案：** `tests/test_pattern.py`

| 測試名稱 | 驗證內容 |
| :--- | :--- |
| `test_detect_long_red_candle` | 長紅 K 正確辨識 |
| `test_detect_long_black_candle` | 長黑 K 正確辨識 |
| `test_detect_doji` | 十字線（cdl_doji）正確辨識 |
| `test_detect_hammer` | 錘子（cdl_pattern）正確辨識 |
| `test_detect_engulfing` | 吞噬（cdl_pattern）正確辨識 |
| `test_no_pattern_detected` | 普通 K 棒不誤報 |
| `test_w_bottom_detected` | W 底 fixture 正確偵測 |
| `test_w_bottom_not_formed` | 非 W 底 → formed=False |
| `test_m_top_detected` | M 頭 fixture 正確偵測 |
| `test_m_top_not_formed` | 非 M 頭 → formed=False |
| `test_tolerance_boundary` | 誤差剛好 3% 的邊界 |
| `test_multi_timeframe_bullish` | 日/週/月多頭 fixture |
| `test_multi_timeframe_mixed` | 日多頭週盤整月空頭 |
| `test_resample_ohlcv_correct` | resample 開高低收量正確 |
| `test_insufficient_data_for_pattern` | 資料不足不 crash |

**不做的事：**

- 不做頭肩型態（選配，不在 MVP）。
- 不做 AI 型態解讀。
- 不做型態歷史回測驗證。

---

### 8-C 籌碼分析管線

**對應規格：** 規格書 §11 Phase 8-C。

**新增檔案：**

```
src/analysis/chip_analysis.py
tests/test_chip_analysis.py
```

**修改檔案：**

```
src/data/fetcher.py        # 新增 fetch_institutional / fetch_margin
src/data/storage.py        # 新增 save/load institutional / margin
src/core/constants.py      # 新增欄位常數
```

---

#### `src/core/constants.py` 擴充

```python
INSTITUTIONAL_COLUMNS = [
    "date", "foreign_buy", "foreign_sell", "foreign_net",
    "trust_buy", "trust_sell", "trust_net",
    "dealer_buy", "dealer_sell", "dealer_net", "symbol",
]

MARGIN_COLUMNS = [
    "date", "margin_buy", "margin_sell", "margin_balance",
    "short_buy", "short_sell", "short_balance", "symbol",
]
```

---

#### `src/data/fetcher.py` 擴充

```python
def fetch_institutional(self, symbol: str, start_date: str) -> pd.DataFrame:
    """三大法人買賣超（FinMind TaiwanStockInstitutionalInvestorsBuySell）。

    原始資料為 long format（每法人一 row），normalize 時 pivot 為 wide format。
    外資 = Foreign_Investor + Foreign_Dealer_Self（加總）。
    自營商 = Dealer_self + Dealer_Hedging（加總）。

    回傳欄位（單位：股）：
        date, foreign_buy, foreign_sell, foreign_net,
        trust_buy, trust_sell, trust_net,
        dealer_buy, dealer_sell, dealer_net, symbol

    FinMind name 值對應：
        Foreign_Investor → foreign_（主）
        Foreign_Dealer_Self → foreign_（合併加總）
        Investment_Trust → trust_
        Dealer_self → dealer_（主）
        Dealer_Hedging → dealer_（合併加總）
    """
    ...

def fetch_margin(self, symbol: str, start_date: str) -> pd.DataFrame:
    """融資融券（FinMind TaiwanStockMarginPurchaseShortSale）。

    原始資料為 wide format（每日一筆 row）。

    回傳欄位（單位：張）：
        date, margin_buy, margin_sell, margin_balance,
        short_buy, short_sell, short_balance, symbol

    FinMind 原始欄位映射：
        MarginPurchaseBuy → margin_buy
        MarginPurchaseSell → margin_sell
        MarginPurchaseTodayBalance → margin_balance
        ShortSaleBuy → short_buy
        ShortSaleSell → short_sell
        ShortSaleTodayBalance → short_balance
    """
    ...
```

---

#### `src/data/storage.py` 擴充

```python
def save_institutional(self, symbol: str, df: pd.DataFrame) -> None:
    """儲存三大法人 → data/raw/tw/{symbol}/institutional.parquet"""
    ...

def load_institutional(self, symbol: str) -> pd.DataFrame:
    """載入三大法人。回傳空 DataFrame 若檔案不存在。"""
    ...

def save_margin(self, symbol: str, df: pd.DataFrame) -> None:
    """儲存融資融券 → data/raw/tw/{symbol}/margin.parquet"""
    ...

def load_margin(self, symbol: str) -> pd.DataFrame:
    """載入融資融券。回傳空 DataFrame 若檔案不存在。"""
    ...
```

---

#### `src/analysis/chip_analysis.py`

```python
from __future__ import annotations

from dataclasses import dataclass

import pandas as pd


@dataclass
class ChipSummary:
    # 三大法人近 N 日（單位：張）
    foreign_net_n_days: int
    trust_net_n_days: int
    dealer_net_n_days: int
    foreign_label: str        # "買超 36 張" / "賣超 52 張"
    trust_label: str
    dealer_label: str

    # 籌碼指標（降級版，用法人資料替代券商分點）
    chip_concentration: str   # "集中" / "分散" / "穩定"
    chip_trend: str           # "中性偏多" / "偏空" / ...
    chip_description: str     # "主力進出互見，籌碼趨於穩定"

    # 融資融券
    margin_balance_change: int
    short_balance_change: int


def generate_chip_summary(
    institutional_df: pd.DataFrame,
    margin_df: pd.DataFrame,
    *,
    n_days: int = 5,
) -> ChipSummary:
    """生成籌碼分析摘要。

    Args:
        institutional_df: 法人資料（單位：股）。
        margin_df: 融資融券資料（單位：張）。
        n_days: 統計天數（預設 5 天）。

    法人資料轉張：顯示時 // 1000。

    籌碼集中度判讀（降級版）：
    - 三大法人連續 N 日同方向 → "集中"
    - 多空交錯 → "穩定"
    - 連續同方向賣出 → "分散"
    """
    ...
```

---

#### 增量補抓邏輯

```python
def fetch_institutional_incremental(
    self, symbol: str, storage: Storage
) -> pd.DataFrame:
    """增量抓取法人資料。

    1. 載入本地 institutional.parquet。
    2. 若本地有資料，取最後日期 + 1 天作為 start_date。
    3. 若本地無資料，使用預設 start_date（6 個月前）。
    4. 呼叫 fetch_institutional(symbol, start_date)。
    5. 合併並去重（以 date 為 key）。
    6. 存回 storage。
    7. 回傳完整 DataFrame。
    """
    ...
```

融資融券同理。

---

#### 測試規範

**測試檔案：** `tests/test_chip_analysis.py`

| 測試名稱 | 驗證內容 |
| :--- | :--- |
| `test_fetch_institutional_returns_wide_format` | pivot 後欄位正確 |
| `test_fetch_institutional_aggregates_foreign` | 外資 = Foreign_Investor + Foreign_Dealer_Self |
| `test_fetch_institutional_aggregates_dealer` | 自營商 = Dealer_self + Dealer_Hedging |
| `test_fetch_institutional_unit_is_shares` | 單位為股 |
| `test_fetch_margin_columns_correct` | 融資融券欄位正確 |
| `test_fetch_margin_unit_is_lots` | 單位為張 |
| `test_save_load_institutional_roundtrip` | 存取一致 |
| `test_save_load_margin_roundtrip` | 存取一致 |
| `test_incremental_fetch_only_missing_days` | 只補抓缺少天數 |
| `test_chip_summary_concentrated` | 連續同方向 → "集中" |
| `test_chip_summary_dispersed` | 連續賣出 → "分散" |
| `test_chip_summary_stable` | 多空交錯 → "穩定" |
| `test_chip_summary_label_format` | label 格式正確（"買超 X 張"） |
| `test_chip_summary_empty_data` | 空資料不 crash |

**不做的事：**

- 不做券商分點分析。
- 不做即時籌碼（盤中法人動態）。
- 不做籌碼歷史趨勢圖。

---

### 8-D 即時行情接入

**對應規格：** 規格書 §11 Phase 8-D。

**新增檔案：**

```
src/data/realtime.py
tests/test_realtime.py
```

**修改檔案：**

```
config.yaml              # 新增 realtime: section
```

---

#### `config.yaml` 新增

```yaml
realtime:
  cache_ttl: 10          # 同一 symbol 快取秒數
  request_timeout: 5     # API 請求逾時秒數
```

---

#### `src/data/realtime.py`

```python
from __future__ import annotations

import time
from dataclasses import dataclass, field

import requests


@dataclass
class RealtimeQuote:
    symbol: str
    name: str
    price: float
    change: float
    change_pct: float
    open: float
    high: float
    low: float
    yesterday_close: float
    volume: int             # 累積成交量（張）
    timestamp: str          # "HH:MM:SS"
    best_bid: list[float] = field(default_factory=list)
    best_ask: list[float] = field(default_factory=list)
    best_bid_vol: list[int] = field(default_factory=list)
    best_ask_vol: list[int] = field(default_factory=list)
    is_market_open: bool = True


@dataclass
class BidAskStructure:
    total_bid_vol: int
    total_ask_vol: int
    bid_ratio: float      # 買方佔比
    ask_ratio: float      # 賣方佔比
    label: str            # "買盤較積極" / "賣壓較重" / "多空均衡"


class RealtimeFetcher:
    """即時報價查詢，統一介面，內部自動路由 tse / otc。"""

    BASE_URL = "https://mis.twse.com.tw/stock/api/getStockInfo.jsp"

    def __init__(self, cache_ttl: int = 10, request_timeout: int = 5):
        self._market_map: dict[str, str] = {}  # symbol → "tse" | "otc"
        self._cache: dict[str, tuple[float, RealtimeQuote]] = {}
        self._cache_ttl = cache_ttl
        self._request_timeout = request_timeout

    def fetch_quote(self, symbol: str) -> RealtimeQuote:
        """統一介面，內部自動路由 tse / otc prefix。

        快取策略：同一 symbol 在 cache_ttl 秒內重複查詢直接回 cache。

        SSL 注意：僅對 mis.twse.com.tw 使用 verify=False，
        並在該請求範圍內 suppress InsecureRequestWarning。

        z == "-" 時表示無成交，price 設為 yesterday_close。

        change / change_pct 由本地計算：
            change = price - yesterday_close
            change_pct = change / yesterday_close * 100
        """
        ...

    def fetch_bid_ask_structure(self, quote: RealtimeQuote) -> BidAskStructure:
        """計算買賣力道估算。

        盤中：五檔買賣量總量比。
        盤後 fallback：(close - open) / (high - low) 近似多空力道。

        Label 規則：
            bid_ratio > 0.55 → "買盤較積極"
            ask_ratio > 0.55 → "賣壓較重"
            else → "多空均衡"
        """
        ...

    def load_market_map(self) -> None:
        """從 FinMind TaiwanStockInfo 載入上市/上櫃分類。

        type 欄位映射：
            twse → "tse"
            tpex → "otc"
            emerging → 不支援（忽略）

        失敗時 fallback：預設所有 symbol 為 "tse"。
        """
        ...
```

---

#### TWSE API 解析細節

```python
def _parse_twse_response(self, data: dict) -> RealtimeQuote:
    """解析 TWSE MIS API 回傳的單一股票資料。

    API key 映射：
        c → symbol
        n → name
        z → price（"-" 時用 y 替代）
        y → yesterday_close
        o → open
        h → high
        l → low
        v → volume（張）
        t → timestamp
        b → best_bid（"_" 分隔）
        a → best_ask（"_" 分隔）
        g → best_bid_vol（"_" 分隔）
        f → best_ask_vol（"_" 分隔）

    五檔解析：
        "_" 分隔 → split("_") → filter 空字串 → 轉型
    """
    ...
```

---

#### 測試規範

**測試檔案：** `tests/test_realtime.py`

| 測試名稱 | 驗證內容 |
| :--- | :--- |
| `test_parse_twse_response_normal` | 正常回傳解析正確 |
| `test_parse_twse_response_no_trade` | z="-" 時 price=yesterday_close |
| `test_parse_five_levels` | 五檔報價解析正確 |
| `test_parse_five_levels_partial` | 不足五檔時不 crash |
| `test_change_calculation` | change/change_pct 計算正確 |
| `test_cache_hit_within_ttl` | TTL 內第二次查詢不打 API |
| `test_cache_miss_after_ttl` | TTL 過期後重新查詢 |
| `test_market_map_tse` | twse symbol → tse_ prefix |
| `test_market_map_otc` | tpex symbol → otc_ prefix |
| `test_market_map_fallback` | 查不到時 fallback tse |
| `test_bid_ask_structure_buy_dominant` | 買方佔比 > 0.55 |
| `test_bid_ask_structure_balanced` | 多空均衡 |
| `test_is_market_open_detection` | 盤中/盤後判斷 |

**不做的事：**

- 不做 WebSocket 即時推播。
- 不做自動刷新 / auto-rerun。
- 不做多股同時查詢。
- 不做盤中 alert / 通知。

---

### 8-E AI 綜合分析與操作劇本

**對應規格：** 規格書 §11 Phase 8-E。

**修改檔案：**

```
src/ai/advisor.py         # 新增 generate_stock_dashboard_analysis
tests/test_advisor.py     # 新增測試
```

---

#### `src/ai/advisor.py` 擴充

```python
@dataclass
class TradingScenario:
    name: str           # "開高走高" / "震盪整理" / "開低回測"
    entry_range: str    # "775 ~ 780"
    stop_loss: float    # 755
    target: str         # "820 / 860"


@dataclass
class DashboardAnalysis:
    industry_overview: list[str]    # 3-5 個 bullet points
    company_overview: list[str]     # 3-5 個 bullet points
    volume_price_analysis: str      # 2-3 句自然語言分析
    scenarios: list[TradingScenario]  # 3 個情境
    conclusion: str                 # 1 句話總結


def generate_stock_dashboard_analysis(
    self,
    symbol: str,
    technical_summary: "TechnicalSummary",
    chip_summary: "ChipSummary | None",
    company_info: dict | None,
    recent_prices: pd.DataFrame,
) -> DashboardAnalysis:
    """用 LLM Tool Use 生成個股綜合分析。

    Prompt 設計原則：
    - structured output（tool_use / JSON mode）
    - system prompt 注入：TechnicalSummary + ChipSummary + 近期價量摘要
    - AI 不得自行編造數字，必須基於已提供數據
    - 操作劇本價位必須基於支撐壓力計算結果
    - 操作劇本定位為「研究情境推演」，禁止「建議買進 / 必買 / 保證」語氣
    - 中文輸出
    - 附短免責：非投資建議，僅供研究與風險控管參考

    降級策略：
    - ai.enabled=false → raise AIDisabledError
    - AI 呼叫失敗 → raise AICallError
    - chip_summary is None → prompt 省略籌碼段
    - UI layer 必須 catch AIDisabledError / AICallError，Tab 4 顯示降級內容，不得留空

    Args:
        symbol: 股票代碼。
        technical_summary: 8-A 技術面摘要。
        chip_summary: 8-C 籌碼摘要（可選）。
        company_info: FinMind TaiwanStockInfo（可選）。
        recent_prices: 近 60 日 OHLCV。

    Returns:
        DashboardAnalysis dataclass。
    """
    ...
```

---

#### 測試規範

新增至 `tests/test_advisor.py`：

| 測試名稱 | 驗證內容 |
| :--- | :--- |
| `test_dashboard_analysis_returns_correct_structure` | mock LLM，回傳 DashboardAnalysis 所有欄位 |
| `test_dashboard_analysis_scenarios_count` | scenarios 恰好 3 個 |
| `test_dashboard_analysis_without_chip` | chip_summary=None 時仍正常 |
| `test_dashboard_analysis_ai_disabled` | ai.enabled=false → raise AIDisabledError |

**不做的事：**

- 不做多模型 ensemble。
- 不做歷史分析快取（每次重新生成）。
- 不做自動交易觸發。

---

### 8-F 個股儀表板 UI

**對應規格：** 規格書 §11 Phase 8-F。

**新增檔案：**

```
src/ui/pages/dashboard.py
tests/test_dashboard_page.py
```

**修改檔案：**

```
src/ui/app.py             # 新增「個股分析」入口
```

---

#### `src/ui/app.py` 修改

在 `option_menu` 新增「個股分析」入口，排在「回測」之後：

```python
# option_menu items 新增
selected = option_menu(
    ...,
    options=["資料管理", "回測", "個股分析", "AI 問答", "設定"],
    ...
)
```

---

#### `src/ui/pages/dashboard.py`

```python
def render_dashboard_page() -> None:
    """個股綜合分析儀表板主頁面。

    頁面進入流程：
    1. 股票代碼輸入（st.text_input）
    2. 「分析」按鈕
    3. 載入流程：日 K → 即時報價 → 技術面 → 籌碼 → AI
    4. Tab 分頁顯示結果

    Tab 規劃：
    - Tab 1 總覽：行情條 + K 線圖 + 技術分析總覽 + 關鍵價位 + 短線分數
    - Tab 2 籌碼與量價：法人 + 融資融券 + 買賣力道 + 量價結構
    - Tab 3 型態與週期：K 線型態表 + W底/M頭 + 多週期
    - Tab 4 AI 劇本：產業/公司 + 量價分析 + 三情境 + 結論

    排版原則：
    - 每個 tab 內 1~2 屏
    - st.columns 2~3 欄
    - st.metric 數字卡片
    - 不注入大量 CSS hack

    主題相容：
    - Plotly 圖表使用 get_plotly_template()

    ai.enabled=false 時：
    - Tab 4 仍顯示，內容降級為「AI 功能未啟用」+ rule-based 結論
    """
    ...
```

---

#### Tab 1：總覽 實作要點

```python
def _render_tab_overview(
    quote: RealtimeQuote | None,
    technical: TechnicalSummary,
    df: pd.DataFrame,
) -> None:
    """
    行情條：st.columns(5) 顯示即時價/漲跌/漲跌幅/成交量/多空力道。
    K 線圖：Plotly candlestick + MA5/20/60 + 支撐壓力標註線。
    技術分析總覽：st.columns(3) × 2 rows，每格一項判讀。
    關鍵價位：st.columns(2) 壓力區/支撐區。
    短線綜合分數：gauge chart 或 st.progress + label。
    「重新整理報價」按鈕：觸發 RealtimeFetcher 重新查詢。
    """
    ...
```

---

#### Tab 2：籌碼與量價 實作要點

```python
def _render_tab_chip(
    chip: ChipSummary | None,
    bid_ask: BidAskStructure | None,
    technical: TechnicalSummary,
) -> None:
    """
    法人近 5/10/20 日：st.dataframe 或 st.columns metric。
    籌碼集中度指標：文字 + 趨勢描述。
    融資融券：metric card（餘額 + 變化）。
    買賣力道估算：bar chart 或 gauge。
    量價結構分析：技術面文字（8-A volume_price_divergence + ma_bias）。

    若 chip is None：顯示 st.info("尚未載入籌碼資料")。
    """
    ...
```

---

#### Tab 3：型態與週期 實作要點

```python
def _render_tab_pattern(
    candle_patterns: list["CandlePattern"],
    chart_patterns: list["ChartPatternResult"],
    mtf: "MultiTimeframeAnalysis",
) -> None:
    """
    K 線型態表：st.dataframe（型態名/是否偵測/說明）。
    型態分析：每個 ChartPatternResult 一段文字。
    多週期分析：st.columns(3) 日/週/月趨勢 + 強度。
    """
    ...
```

---

#### Tab 4：AI 劇本 實作要點

```python
def _render_tab_ai(
    analysis: "DashboardAnalysis | None",
    technical: TechnicalSummary,
    ai_enabled: bool,
) -> None:
    """
    ai_enabled=False 或 analysis is None：
        顯示 st.info("請啟用 AI 功能") + rule-based 結論（technical.operation_observation）。

    ai_enabled=True 且 analysis 可用：
        產業/公司概況：bullet points。
        量價分析：自然語言段落。
        操作劇本：st.dataframe 三情境表（情境/進場區間/停損/目標）。
        整體結論：st.success 或 st.info。
        免責聲明：st.caption。
    """
    ...
```

---

#### 測試規範

**測試檔案：** `tests/test_dashboard_page.py`

| 測試名稱 | 驗證內容 |
| :--- | :--- |
| `test_dashboard_page_imports` | 模組可 import 不 crash |
| `test_dashboard_page_render_no_symbol` | 未輸入代碼時顯示提示 |
| `test_dashboard_tab_overview_renders` | Tab 1 各區塊函式可呼叫 |
| `test_dashboard_tab_chip_no_data` | chip=None 時不 crash |
| `test_dashboard_tab_ai_disabled` | ai.enabled=false 時降級正確 |
| `test_dashboard_option_menu_entry` | app.py 含「個股分析」入口 |

**手動驗收項目（12 項）：**

1. 輸入 2330 → 4 個 tab 正確顯示
2. 輸入上櫃股 6547 → 即時報價正確路由
3. 盤後查詢 → 顯示「盤後資料」
4. 「重新整理報價」按鈕 → 報價更新
5. Tab 2 籌碼 → 法人買賣超正確顯示
6. Tab 3 型態 → K 線型態表正確
7. Tab 4 AI → 三情境劇本正確
8. `ai.enabled=false` → Tab 4 降級正確
9. 6 套主題切換 → 儀表板配色正確
10. 不影響回測頁功能
11. 不影響設定頁功能
12. 不影響 AI 問答頁功能

**不做的事：**

- 不做即時自動刷新。
- 不做多股同時分析。
- 不做個股追蹤清單。
- 不做歷史分析存檔。

---

### 8-G 新手友善說明文字

**對應規格：** 規格書 §11 Phase 8-G。

**修改檔案：**

```
src/ui/pages/dashboard.py        # 新增說明文字常數 + 修改 render 函式
tests/test_dashboard_page.py     # 追加說明文字覆蓋測試
```

**不修改的檔案：**

```
src/analysis/technical_summary.py  # 不動
src/analysis/pattern.py            # 不動
src/analysis/chip_analysis.py      # 不動
src/data/realtime.py               # 不動
src/ai/advisor.py                  # 不動
```

---

#### 說明文字常數定義

在 `dashboard.py` 模組頂部新增兩個 dict 常數：

```python
_HELP_TEXTS: dict[str, str] = {
    # ── Tab 1 總覽：技術分析總覽 6 metrics ──
    "trend_direction": (
        "根據 5 日、20 日、60 日移動平均線（MA）的排列判定。"
        "MA5 > MA20 > MA60 表示短中長期均價依序遞增，為「多頭趨勢」；"
        "反之為「空頭趨勢」；交錯排列為「盤整」。"
        "移動平均線是將過去 N 日收盤價取平均，用來平滑短期波動、觀察價格趨勢方向。"
    ),
    "ma_status": (
        "觀察三條均線（MA5、MA20、MA60）的相對位置。"
        "「多頭排列 (5>20>60)」代表短、中、長期均價皆上升，多方力道強勁；"
        "「空頭排列」反之；「均線糾結」表示三條均線互相交纏，市場方向不明，"
        "常出現在趨勢轉折或盤整階段。"
    ),
    "kd_status": (
        "KD 指標（隨機指標）衡量收盤價在近期最高最低價區間中的相對位置，"
        "由 K 值和 D 值組成，範圍 0~100。"
        "K 值由下往上穿越 D 值稱為「黃金交叉」，是偏多訊號；反之為「死亡交叉」，偏空。"
        "K、D 皆 > 80 稱「高檔鈍化」，暗示可能過熱；皆 < 20 稱「低檔鈍化」，暗示可能超跌。"
        "本系統使用 K=9, D=3, smooth=3 參數。"
    ),
    "macd_status": (
        "MACD 由 DIF 線（12 日與 26 日 EMA 之差）和 DEA 線（DIF 的 9 日 EMA）組成。"
        "DIF > 0 且 DIF > DEA 稱「正值擴張」，多方力道增強；"
        "DIF > 0 但 DIF ≤ DEA 稱「正值收斂」，多方力道減弱；"
        "DIF < 0 時同理判斷空方力道。零軸附近震盪表示多空膠著。"
    ),
    "volume_status": (
        "比較今日成交量與近 5 日平均成交量的倍數。"
        "> 1.5 倍為「量能放大」（市場參與度升高），> 3 倍為「爆量」（可能有重大事件）；"
        "0.7~1.5 倍為「量能正常」；< 0.7 倍為「量能略縮」（市場觀望）。"
        "成交量是價格變動的燃料，量能變化常領先價格變化。"
    ),
    "volume_price_relation": (
        "結合價格漲跌方向與量能變化的組合判讀。"
        "「價漲量增」是最健康的上漲型態，代表上漲有買盤支撐；"
        "「價漲量縮」暗示追價力道不足，可能只是短線整理或上漲動能衰竭；"
        "「價跌量增」代表賣壓沉重；"
        "「價跌量縮」可能是恐慌賣壓消退或下跌接近尾聲。"
    ),

    # ── Tab 1 總覽：壓力區/支撐區 ──
    "resistance": (
        "壓力區是股價向上可能遇到較大賣壓的價位。"
        "本系統取近 60 日最高價與近 20 日最高價作為壓力參考。"
        "當股價接近壓力區時，前波高點的套牢賣壓可能湧出；突破壓力區則代表多方力道強勁。"
    ),
    "support": (
        "支撐區是股價向下可能獲得較大買盤支撐的價位。"
        "本系統取近期低點、20 日均線（MA20）、60 日均線（MA60）作為支撐參考。"
        "當股價接近支撐區時，低接買盤可能進場；跌破支撐區則代表空方佔優。"
    ),

    # ── Tab 1 總覽：短線綜合分數 ──
    "short_term_score": (
        "短線綜合分數由四個面向加權計算："
        "均線結構（30%）+ KD 指標位置與交叉（25%）+ 量價關係（25%）+ 突破狀態（20%）。"
        "分數 70% 以上為「強勢偏多」，50% 以上且未滿 70% 為「中等偏多」，"
        "30% 以上且未滿 50% 為「中性」，未滿 30% 為「偏空」。"
        "此分數僅反映技術面短期狀態，不構成投資建議。"
    ),

    # ── Tab 2 籌碼與量價：三大法人 ──
    "foreign": (
        "外資全稱「外國機構投資人」，是台股最大的法人買賣力量。"
        "外資買超通常被視為看好該股票，賣超則相反。"
        "但外資操作也可能包含避險、ETF 調倉等非方向性交易。"
    ),
    "trust": (
        "投信全稱「投資信託公司」，即台灣的共同基金管理公司。"
        "投信買超通常代表基金經理人看好該股票，且投信通常是波段操作。"
    ),
    "dealer": (
        "自營商是券商用自有資金進行交易的部門。"
        "自營商操作較短線，有時是為了避險而非看好方向。"
    ),

    # ── Tab 2 籌碼與量價：籌碼集中度 ──
    "chip_concentration": (
        "觀察近 N 日三大法人每日淨買賣方向的一致性。"
        "若連續多日同向買入，籌碼呈「集中」狀態，代表法人看法一致偏多；"
        "連續同向賣出為「分散」；多空交錯為「穩定」。"
    ),

    # ── Tab 2 籌碼與量價：融資融券 ──
    "margin_balance": (
        "融資是投資人向券商借錢買股票。"
        "融資餘額增加代表散戶看多、積極加碼；減少則可能是獲利了結或認賠出場。"
        "融資通常被視為散戶指標，融資大增而股價不漲有時是警訊。"
    ),
    "short_balance": (
        "融券是投資人向券商借股票來賣（放空），預期股價下跌後買回歸還獲利。"
        "融券餘額增加代表看空力道增加；"
        "融券大量回補（餘額減少）有時被稱為「軋空」，可能推動股價上漲。"
    ),

    # ── Tab 2 籌碼與量價：買賣力道 ──
    "bid_ask": (
        "根據盤中五檔掛單量估算多空力道。"
        "買方五檔總掛量佔比越高，代表買盤越積極。"
        "注意：掛單可能隨時被撤銷，此指標僅為即時參考，非精確的內外盤統計。"
    ),

    # ── Tab 2 籌碼與量價：量價結構分析 3 行 ──
    "volume_price_divergence": (
        "量價背離（如「價漲量縮」或「價跌量縮」）代表價格方向缺乏成交量配合。"
        "價漲量縮暗示上漲動能不足，可能只是技術性反彈；"
        "價跌量縮可能是賣壓消退。"
        "量價同步（如「價漲量增」）代表趨勢方向有成交量支持，較為可靠。"
    ),
    "ma_bias": (
        "乖離率 = (收盤價 - MA20) / MA20 × 100%，"
        "衡量股價偏離 20 日均線的程度。"
        "乖離率 > +3% 為偏高，表示短期漲幅較大，有拉回均線的可能；"
        "< -3% 為偏低，表示短期跌幅較大，有反彈向均線靠攏的可能。"
        "乖離率是均值回歸的觀察工具。"
    ),
    "operation_observation": (
        "綜合趨勢方向、量價關係、乖離率與籌碼面的 rule-based 判讀結論。"
        "此文字由系統自動產生，非 AI 生成，不構成投資建議。"
    ),

    # ── Tab 3 型態與週期：多週期趨勢 ──
    "timeframe_daily": (
        "以每日 K 棒計算的趨勢方向與強度，反映短期（數日至數週）的價格走勢。"
    ),
    "timeframe_weekly": (
        "將每週的日 K 棒彙總為一根週 K"
        "（開=該週第一個有效交易日開盤, 高=週內最高, 低=週內最低, "
        "收=該週最後一個有效交易日收盤, 量=週總量），"
        "反映中期（數週至數月）趨勢。"
    ),
    "timeframe_monthly": (
        "將每月日 K 棒彙總為一根月 K，反映長期（數月至數年）趨勢。"
        "當日線、週線、月線趨勢一致時，趨勢可靠度較高。"
    ),
    "timeframe_strength": (
        "強度由均線排列與 RSI（相對強弱指標）綜合判斷。"
        "RSI > 60 且多頭排列為「強」；RSI 50~60 且多頭為「中強」；"
        "RSI < 40 或空頭排列為「弱」；其餘為「中」。"
    ),
}


_PATTERN_DETAILS: dict[str, str] = {
    "長紅 K": (
        "當日實體（收盤價 - 開盤價）超過近 20 日平均實體的 2 倍，"
        "且為陽線（收盤 > 開盤）。代表多方在當日佔明顯優勢，買盤力道強勁。"
        "若出現在低檔區域，可能暗示反轉向上。"
    ),
    "長黑 K": (
        "當日實體超過近 20 日平均實體的 2 倍，"
        "且為陰線（收盤 < 開盤）。代表空方在當日佔明顯優勢，賣壓沉重。"
        "若出現在高檔區域，可能暗示反轉向下。"
    ),
    "十字線": (
        "開盤價與收盤價幾乎相同（實體極小），形成十字形狀。"
        "代表當日多空勢均力敵、拉鋸激烈。"
        "出現在趨勢末端時常被視為反轉訊號，需配合後續走勢確認。"
    ),
    "錘子": (
        "下影線長度超過實體 2 倍以上，上影線極短。"
        "出現在下跌趨勢中為「錘子」，暗示下方有買盤接手，可能反轉向上。"
        "其形態像一把錘子正在「打底」。"
    ),
    "吊人": (
        "形態與錘子相同（長下影線、短上影線），但出現在上漲趨勢中。"
        "暗示盤中有大幅下殺但最終收回，然而多方力道可能已在消耗，需警覺反轉風險。"
    ),
    "吞噬": (
        "今日 K 棒的實體完全覆蓋前一日 K 棒的實體。"
        "多頭吞噬（前陰後陽且今日實體包住昨日）暗示多方反攻；"
        "空頭吞噬（前陽後陰）暗示空方壓制。是較強的反轉確認訊號。"
    ),
    "晨星": (
        "三根 K 棒組合：第一根長黑 K → 第二根小實體（跳空或窄幅）→ 第三根長紅 K，"
        "且收盤超過第一根實體中點。"
        "出現在下跌趨勢中，暗示空方力竭、多方接手，是強烈的底部反轉訊號。"
    ),
    "夜星": (
        "晨星的相反型態：長紅 K → 小實體 → 長黑 K。"
        "出現在上漲趨勢中，暗示多方力竭、空方介入，是強烈的頂部反轉訊號。"
    ),
    "帶上影線": (
        "上影線長度超過實體 2 倍以上。"
        "代表盤中買方曾推高股價，但遇到賣壓而回落。"
        "上影線越長，上檔壓力越重，可能暗示短期有高點。"
    ),
    "帶下影線": (
        "下影線長度超過實體 2 倍以上。"
        "代表盤中賣方曾壓低股價，但遇到買盤接手而回升。"
        "下影線越長，下檔支撐越強，可能暗示短期有底。"
    ),
    "W底（雙底）": (
        "股價在相近價位形成兩個低點（誤差 < 3%），中間有一段反彈，形成 W 字形。"
        "當股價突破兩低點之間的反彈高點（頸線）時確認型態成立。"
        "上漲目標約為頸線到低點距離的等幅上漲。"
    ),
    "M頭（雙頂）": (
        "股價在相近價位形成兩個高點（誤差 < 3%），中間有一段回落，形成 M 字形。"
        "當股價跌破兩高點之間的回落低點（頸線）時確認型態成立。"
        "下跌目標約為高點到頸線距離的等幅下跌。"
    ),
}
```

所有字串使用括號內多段隱式拼接（implicit string concatenation），避免單行過長。

---

#### `_render_tab_overview()` 修改

```python
def _render_tab_overview(
    quote: RealtimeQuote | None,
    technical: TechnicalSummary,
    df: pd.DataFrame,
) -> bool:
    # ... 行情條不變 ...

    # 技術分析總覽：6 個 metric 加 help
    st.markdown("**技術分析總覽**")
    row1 = st.columns(3)
    row1[0].metric("趨勢方向", technical.trend_direction, help=_HELP_TEXTS["trend_direction"])
    row1[1].metric("均線狀態", technical.ma_status, help=_HELP_TEXTS["ma_status"])
    row1[2].metric("KD 狀態", technical.kd_status, help=_HELP_TEXTS["kd_status"])
    row2 = st.columns(3)
    row2[0].metric("MACD 狀態", technical.macd_status, help=_HELP_TEXTS["macd_status"])
    row2[1].metric("量能狀態", technical.volume_status, help=_HELP_TEXTS["volume_status"])
    row2[2].metric("量價關係", technical.volume_price_relation, help=_HELP_TEXTS["volume_price_relation"])

    # 壓力區/支撐區加 caption 說明
    c1, c2 = st.columns(2)
    with c1:
        st.markdown("**壓力區**")
        st.caption(_HELP_TEXTS["resistance"])
        # ... 壓力點列表不變 ...
    with c2:
        st.markdown("**支撐區**")
        st.caption(_HELP_TEXTS["support"])
        # ... 支撐點列表不變 ...

    # 短線綜合分數加 caption 說明
    st.markdown("**短線綜合分數**")
    score_pct = max(0.0, min(1.0, float(technical.short_term_score)))
    st.progress(score_pct, text=f"{technical.short_term_label}（{score_pct * 100:.1f}%）")
    st.caption(_HELP_TEXTS["short_term_score"])
    # ... 重新整理按鈕不變 ...
```

---

#### `_render_tab_chip()` 修改

```python
def _render_tab_chip(
    chip: ChipSummary | None,
    bid_ask: BidAskStructure | None,
    technical: TechnicalSummary,
    chip_recent_df: pd.DataFrame | None = None,
    chip_error: str | None = None,
) -> None:
    st.subheader("籌碼與量價")
    # ... chip_error 處理不變 ...

    if chip is not None:
        # 三大法人 metric 加 help
        c1, c2, c3 = st.columns(3)
        c1.metric("外資近N日", chip.foreign_label, help=_HELP_TEXTS["foreign"])
        c2.metric("投信近N日", chip.trust_label, help=_HELP_TEXTS["trust"])
        c3.metric("自營商近N日", chip.dealer_label, help=_HELP_TEXTS["dealer"])

        # 籌碼集中度下方加 caption
        st.write(f"籌碼集中度：{chip.chip_concentration} / 趨勢：{chip.chip_trend}")
        st.caption(_HELP_TEXTS["chip_concentration"])
        st.caption(chip.chip_description)

        # 融資融券 metric 加 help
        c4, c5 = st.columns(2)
        c4.metric("融資餘額變化(張)", f"{chip.margin_balance_change:+d}",
                  help=_HELP_TEXTS["margin_balance"])
        c5.metric("融券餘額變化(張)", f"{chip.short_balance_change:+d}",
                  help=_HELP_TEXTS["short_balance"])
        # ... 近 5 交易日法人表格不變 ...

    # 買賣力道 metric 加 help
    if bid_ask is not None:
        st.metric("買賣力道估算", bid_ask.label, help=_HELP_TEXTS["bid_ask"])
        # ...

    # 量價結構分析：每行下方加 caption
    st.markdown("**量價結構分析**")
    st.write(technical.volume_price_divergence or "資料不足")
    st.caption(_HELP_TEXTS["volume_price_divergence"])
    st.write(technical.ma_bias or "資料不足")
    st.caption(_HELP_TEXTS["ma_bias"])
    st.write(technical.operation_observation or "資料不足")
    st.caption(_HELP_TEXTS["operation_observation"])
```

---

#### `_render_tab_pattern()` 修改

```python
def _render_tab_pattern(
    candle_patterns: list[CandlePattern],
    chart_patterns: list[ChartPatternResult],
    mtf: MultiTimeframeAnalysis,
) -> None:
    st.subheader("型態與週期")

    detected = [p for p in candle_patterns if p.detected]
    not_detected = [p for p in candle_patterns if not p.detected]

    if detected:
        st.markdown("**偵測到的型態：**")
        for p in detected:
            st.success(f"**{p.name}** — {p.description}")
            detail = _PATTERN_DETAILS.get(p.name)
            if detail:
                st.caption(detail)

    if not_detected:
        with st.expander("未偵測到的型態", expanded=False):
            rows = [
                {"型態": p.name, "說明": p.description, "詳細": _PATTERN_DETAILS.get(p.name, "")}
                for p in not_detected
            ]
            st.dataframe(pd.DataFrame(rows), width="stretch", hide_index=True)

    # W底/M頭加 caption
    for pattern in chart_patterns:
        if pattern.formed:
            st.success(f"{pattern.pattern_type}：{pattern.description}")
            if pattern.key_points:
                st.caption(", ".join(
                    f"{name}={value:.2f}" for name, value in pattern.key_points if name != "索引區間"
                ))
        else:
            st.info(f"{pattern.pattern_type}：{pattern.description}")
        detail = _PATTERN_DETAILS.get(pattern.pattern_type)
        if detail:
            st.caption(detail)

    # 多週期趨勢 metric 加 help + 強度說明
    st.caption(_HELP_TEXTS["timeframe_strength"])
    c1, c2, c3 = st.columns(3)
    c1.metric("日線", f"{mtf.daily.trend_direction} / {mtf.daily.strength}",
              help=_HELP_TEXTS["timeframe_daily"])
    c2.metric("週線", f"{mtf.weekly.trend_direction} / {mtf.weekly.strength}",
              help=_HELP_TEXTS["timeframe_weekly"])
    c3.metric("月線", f"{mtf.monthly.trend_direction} / {mtf.monthly.strength}",
              help=_HELP_TEXTS["timeframe_monthly"])
```

---

#### 測試規範

**修改測試檔：** `tests/test_dashboard_page.py`（追加）

| 測試名稱 | 驗證內容 |
| :--- | :--- |
| `test_help_texts_has_all_required_keys` | `_HELP_TEXTS` dict 包含所有必要 key（23 個），無遺漏 |
| `test_pattern_details_has_all_10_candle_patterns` | `_PATTERN_DETAILS` dict 包含 10 種 K 棒型態名稱 |
| `test_pattern_details_has_chart_patterns` | `_PATTERN_DETAILS` dict 包含 W底（雙底）和 M頭（雙頂） |
| `test_help_texts_values_are_nonempty_strings` | 所有 help text 值為非空字串 |
| `test_pattern_details_values_are_nonempty_strings` | 所有 `_PATTERN_DETAILS` 值為非空字串 |
| `test_tab_overview_renders_with_help_texts` | Tab 1 render 函式呼叫不 crash（含 help 參數） |
| `test_tab_chip_renders_with_help_texts` | Tab 2 render 函式呼叫不 crash（含 help 參數） |
| `test_tab_pattern_renders_with_details` | Tab 3 render 函式呼叫不 crash（含 detail） |

**Fixture 構造要點：**

- 重用既有 8-F 測試的 TechnicalSummary / CandlePattern / ChipSummary fixture
- 只驗證 dict 完整性與 render 不 crash，不 assert UI 文字內容（Streamlit 單元測試限制）

**通過標準：** 含既有 dashboard 測試一同通過。

**手動驗收項目（6 項）：**

1. Tab 1 → 6 個 metric 的 `?` tooltip hover 後顯示完整說明
2. Tab 1 → 壓力區/支撐區/短線綜合分數下方有 caption 說明
3. Tab 2 → 三大法人/融資融券 metric tooltip 顯示正確
4. Tab 2 → 量價結構分析 3 行下方各有 caption 說明
5. Tab 3 → 偵測到的型態有 caption 詳細說明；未偵測到型態表有「詳細」欄
6. Tab 3 → 日/週/月線 metric tooltip 顯示正確、上方有強度說明 caption

**不做的事：**

- 不擴充 `CandlePattern` dataclass 增加 `detail` 欄位。
- 不修改 analysis 模組的任何計算邏輯。
- 不新增獨立的 help 模組（文字量不大，集中在 dashboard.py 即可）。

---

## Phase 9：美股 US-1 支援

**對應規格：** 規格書 §11 Phase 9。

**總目標：** 在不破壞台股既有流程的前提下，新增美股第一版支援。9-A 到 9-F 的 US-1 只做美股日 K、調整後價格、回測與技術分析；9-G 擴充 yfinance 1m intraday 盤中快照與分 K 圖。Phase 9 仍不做籌碼、財報、匯率換算、期權、WebSocket、買一 / 賣一、五檔、逐筆或實盤級即時報價。

**實作總原則：**

- `market` 是所有市場差異的第一層分流，預設值為 `tw`。
- 台股既有功能不得因美股擴充改變預設行為。
- 美股所有日期以 `America/New_York` timezone 儲存，且必須 timezone-aware。
- 美股回測與報表以 USD 呈現，不換算 TWD。
- 美股成交量顯示 shares，不轉台股「張」。
- 美股資料源第一版只用 `yfinance`，定位為個人研究用途。
- 9-G 盤中價使用 yfinance 最新 1 分 K raw close，漲跌基準使用前一紐約交易日 raw close；必須標示可能延遲且僅供研究分析。
- 美股 UI 放在既有回測頁、個股分析頁與資料管理頁，以市場切換控制，不新增獨立美股頁。
- 單一執行流程只能有一個 `market` context；US-1 禁止混合台股與美股 DataFrame 做 concat、join、align 或同一份 payload 分析。

---

### 9-A 多市場基礎架構

#### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `src/core/market.py` | 新增 | 集中定義 `MarketSpec`、市場常數、symbol 正規化與驗證 |
| `src/core/constants.py` | 修改 | 保留台股常數；新增或搬移 market-aware 設定入口 |
| `src/data/storage.py` | 修改 | `_validate_market` 支援 `tw` / `us`，所有 save/load 保留 `market` 參數 |
| `src/data/storage.py` normalize 函式群 | 修改 | `_normalize_market_df`、`_normalize_splits_df`、`_normalize_institutional_df`、`_normalize_margin_df`、`_empty_*_dataframe` 改為接受 timezone 參數，不再 hardcode `TAIPEI_TZ` |
| `src/data/maintenance.py` | 修改 | `rebuild_symbol`、`update_daily`、`validate_data` 支援 `market` |
| `src/data/storage.py` `DuckDBMeta` | 修改 | `data_meta` schema 加 `market`，主鍵改 `(market, symbol, freq)` |
| `src/core/market.py` | 新增 | 提供 `assert_single_market(...)` 或同等 helper，防止混合市場資料進入同一流程 |
| `tests/test_storage.py` | 修改 / 追加 | 多市場路徑、meta migration、未知 market 拒絕 |
| `tests/test_maintenance.py` | 修改 / 追加 | maintenance 傳遞 market，不破壞台股預設 |

#### TAIPEI_TZ 引用盤點

`src/` 下共 61 處 `TAIPEI_TZ` / `Asia/Taipei` 引用，橫跨 7 個檔案。9-A 必須決定每處的處置方式：

| 檔案 | 出現次數 | 9-A 處置 |
|:---|---:|:---|
| `storage.py` | 17 | 9-A 必改：normalize 函式群與 empty dataframe 改為從 market timezone 取值 |
| `fetcher.py` | 19 | 9-B 改：YFinanceFetcher 依 market 選擇 timezone |
| `dca.py` | 11 | 9-C 改：DCA 依 market 選擇 timezone |
| `backtest.py (UI)` | 6 | 9-C 改：回測頁依 market 選擇 timezone |
| `realtime.py` | 5 | 保留：realtime 只服務台股，TAIPEI_TZ 正確 |
| `dashboard.py` | 2 | 9-D 改：dashboard 依 market 選擇 timezone |
| `constants.py` | 1 | 保留：`TAIPEI_TZ` 常數本身不刪，台股仍使用 |

#### MarketSpec 資料契約

```python
from dataclasses import dataclass
from typing import Literal

Market = Literal["tw", "us"]

@dataclass(frozen=True)
class MarketSpec:
    market: Market
    label: str
    timezone: str
    currency: str
    lot_size: int
    volume_unit: str
    price_tick: float | None
```

建議常數：

```python
MARKET_SPECS: dict[str, MarketSpec] = {
    "tw": MarketSpec(
        market="tw",
        label="台股",
        timezone="Asia/Taipei",
        currency="TWD",
        lot_size=1000,
        volume_unit="shares",
        price_tick=None,  # 台股使用級距函式
    ),
    "us": MarketSpec(
        market="us",
        label="美股",
        timezone="America/New_York",
        currency="USD",
        lot_size=1,
        volume_unit="shares",
        price_tick=0.01,
    ),
}
```

#### Symbol 正規化

```python
def normalize_symbol(symbol: str, market: str) -> str:
    ...

def validate_symbol(symbol: str, market: str) -> str:
    ...
```

規則：

- 共同安全規則（最先執行）：拒絕空字串、`..`、`/`、`\`、絕對路徑、drive anchor。
- 台股：沿用現有規則，允許 `^[0-9A-Z]{4,6}$`。
- 美股正規化順序（依序執行）：
  1. 轉大寫：`aapl` → `AAPL`
  2. class share `.` 轉 `-`：`BRK.B` → `BRK-B`
  3. 拒絕非美股 suffix：含 `.L`、`.TO`、`.HK`、`.T` 等的 ticker 拒絕
  4. regex 驗證：`^[A-Z0-9]+(-[A-Z0-9]+)?$`

#### Market context 隔離

所有跨層 payload 或 DataFrame bundle 必須攜帶 `market`。建議新增最小 helper：

```python
def assert_single_market(markets: Iterable[str]) -> str:
    normalized = {normalize_market(m) for m in markets}
    if len(normalized) != 1:
        raise ValueError(f"Mixed market context is not supported in US-1: {sorted(normalized)}")
    return next(iter(normalized))
```

使用位置：

- 回測頁：單次 run 的 symbol、storage load、maintenance update、cost calculator 必須同一 market。
- 個股分析頁：daily / adjusted / dashboard payload / AI prompt 必須同一 market。
- 資料管理頁：單次更新 / 重建必須同一 market。

US-1 禁止：

- `pd.concat([tw_df, us_df])`
- 台股 benchmark 對齊美股標的
- 台美 portfolio 同時計算
- 將 `Asia/Taipei` 與 `America/New_York` 資料混入同一指標或回測流程

#### DuckDBMeta migration

新 schema：

```sql
CREATE TABLE IF NOT EXISTS data_meta (
    market      VARCHAR NOT NULL DEFAULT 'tw',
    symbol      VARCHAR NOT NULL,
    freq        VARCHAR NOT NULL,
    source      VARCHAR NOT NULL,
    first_date  TIMESTAMP WITH TIME ZONE,
    last_date   TIMESTAMP WITH TIME ZONE,
    row_count   INTEGER,
    updated_at  TIMESTAMP WITH TIME ZONE,
    PRIMARY KEY (market, symbol, freq)
);
```

若既有 DB 缺少 `market` 欄位，採 drop-and-rebuild 方案（metadata 可從 parquet 檔案重建，無資料遺失風險）：

1. 檢測 `data_meta` 是否有 `market` 欄位。
2. 若無：DROP TABLE `data_meta`，以新 schema（含 `market` 欄位與新 PK）重新 CREATE。
3. 掃描 `data/raw/tw/` 下既有 parquet，重建 metadata 並寫入 `market='tw'`。
4. 首次啟動後即完成遷移，後續不再觸發。

#### 9-A 不做

- 不新增美股 fetcher 實際抓資料。
- 不修改 UI 呈現美股選項，除非為了 plumbing 測試使用隱藏參數。
- 不修改回測成本模型。

---

### 9-B 美股日 K 資料管線

#### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `src/data/fetcher.py` | 修改 | `YFinanceFetcher` 支援 `market="tw" | "us"`；美股 ticker 不加 `.TW` |
| `src/data/cleaner.py` | 視需要修改 | 清洗流程不可假設台股 timezone |
| `src/data/maintenance.py` | 修改 | 美股 rebuild/update 產生 raw + adjusted；美股批次更新需節流 |
| `src/data/storage.py` | 使用 9-A 能力 | 存入 `raw/us` 與 `processed/us` |
| `tests/test_fetcher.py` | 追加 | 美股 normalize、timezone、ticker mapping、Adj Close 調整、split-adjusted volume |
| `tests/test_maintenance.py` | 追加 | 美股 rebuild/update daily |

#### YFinanceFetcher 介面

```python
class YFinanceFetcher(IDataFetcher):
    def __init__(
        self,
        downloader: Callable[..., pd.DataFrame] | None = None,
        market: str = "tw",
    ) -> None: ...

    def fetch_daily(self, symbol: str, start: str, end: str) -> pd.DataFrame: ...
    def fetch_minute(self, symbol: str, start: str, end: str, freq: str = "1") -> pd.DataFrame: ...
```

Ticker mapping：

- `market="tw"`：`2330` → `2330.TW`，沿用現有行為。
- `market="us"`：`AAPL` → `AAPL`，`BRK.B` → `BRK-B`。

美股 `fetch_minute` 第一版不支援，應明確 raise：

```python
raise FetcherError("US-1 does not support US minute data.")
```

#### 美股 adjusted daily 產生規則

yfinance raw 欄位若包含 `Adj Close`：

```python
price_ratio = raw["Adj Close"] / raw["Close"]
adjusted["open"] = raw["Open"] * price_ratio
adjusted["high"] = raw["High"] * price_ratio
adjusted["low"] = raw["Low"] * price_ratio
adjusted["close"] = raw["Adj Close"]
```

成交量必須做 split-adjust，不可保留 raw volume 直接進 adjusted daily：

```python
adjusted["volume"] = raw["Volume"] * cumulative_split_factor
```

重點：

- `price_ratio = Adj Close / Close` 可能包含 dividend adjustment，只能用來調 OHLC。
- `cumulative_split_factor` 必須只來自 split events，不能混入 dividend adjustment。
- 若 yfinance downloader 支援 `actions=True` 並回傳 `Stock Splits`，優先用該欄位建立 cumulative split factor。
- 若實作改走 `Ticker.splits`，需確保 split 日期與 daily index 以 `America/New_York` 對齊。
- 對 4-for-1 split，split 前的歷史 volume 應乘 4，與 split-adjusted price 對齊。

- 若 `Close <= 0` 或 ratio 無法計算，該列應丟棄或 fallback，不能產生 inf / NaN 價格。
- 若整批資料沒有 `Adj Close`，可用 raw OHLCV 存 adjusted，但 caller 需要拿到狀態或 log warning：「使用未調整價格」。
- 若無法取得 split events 或無法確認 split factor，不能宣稱 volume 已完成 split-adjust。Dashboard 的 OBV、量能放大 / 縮小、量價結構需顯示資料限制提示，或改用 raw volume 並明確標示限制。

#### yfinance 節流

單檔更新不需額外 sleep。批次更新 `market="us"` 時，maintenance 層必須在每個 symbol request 間 sleep 至少 1 秒：

```python
US_YFINANCE_REQUEST_INTERVAL_SECONDS = 1.0
```

US-1 可先用常數，不必加入 `config.yaml`。遇到 429 / timeout 時需回報外部資料源限制，不得把 symbol 標成永久無資料。

#### 時區

- 美股日 K naive index / date 進入標準化時，localize 到 `America/New_York`。
- 若 yfinance 回傳 timezone-aware index，轉成 `America/New_York`。
- 禁止轉成 `Asia/Taipei` 後儲存。

#### 9-B 不做

- 9-B 不支援美股分 K；9-G 另以明確規格開放 yfinance 1m intraday，不得在 9-B 偷做。
- 不接 Polygon / Alpha Vantage。
- 不抓財報、dividend 明細、splits 明細到獨立表；但 9-B 必須讀取 split events 作為 adjusted volume 計算依據。
- 不修改回測頁正式啟用美股。

---

### 9-C 美股回測支援

#### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `src/backtest/cost.py` | 修改 | 既有 `CostCalculator` 改名 `TWCostCalculator`；新增 `USCostCalculator`；新增 `create_cost_calculator(market)` factory |
| `src/backtest/_helpers.py` | 修改 | ETF 判斷與 cost builder 不可硬套台股 |
| `src/backtest/dca.py` | 修改 | 美股 DCA 使用 1 股單位，幣別 USD |
| `src/ui/pages/backtest.py` | 修改 | 新增市場切換、讀取 market data、USD 顯示 |
| `tests/test_cost.py` | 追加 | 美股成本模型 |
| `tests/test_backtest_page.py` | 追加 | 美股市場切換與資料載入 |
| `tests/test_dca_backtest.py` | 視需要追加 | 美股 1 股 DCA |

#### 成本模型

採方案 A：台美成本邏輯分離，不合併為單一 class。

既有 `CostCalculator` 改名 `TWCostCalculator`，保留原有台股邏輯（commission_rate + discount、`max(..., 20.0)` 最低手續費、證交稅、ETF 稅率、台股級距 tick）。對外 import 相容性由 `create_cost_calculator` factory 處理。

新增 `USCostCalculator`：

```python
class USCostCalculator:
    def __init__(
        self,
        commission_per_trade: float = 0.0,
        slippage_ticks: int = 1,
        tick_size: float = 0.01,
    ) -> None: ...
```

新增 factory：

```python
def create_cost_calculator(market: str = "tw", **kwargs) -> TWCostCalculator | USCostCalculator:
    if market == "tw":
        return TWCostCalculator(**kwargs)
    if market == "us":
        return USCostCalculator(**kwargs)
    raise ValueError(f"Unknown market: {market}")
```

US-1 預設：

- commission = 0
- tax = 0
- tick size = 0.01
- slippage = `0.01 * slippage_ticks * quantity`
- quantity 最小單位 = 1 股
- US-1 成本參數硬編碼，不開放 config.yaml 調整

不得沿用：

- 台股 20 元最低手續費
- 台股證交稅
- 台股 ETF 稅率
- 台股價格級距 tick
- 台股「張」顯示

#### 回測 UI

- 市場 selector 預設 `台股`。
- 選美股時 symbol placeholder：`AAPL / MSFT / SPY / BRK.B`。
- 自動資料準備呼叫 `market="us"`。
- 回測資料優先讀 `processed/us/{symbol}/adj_daily.parquet`。
- 績效與交易紀錄顯示 USD。
- 美股 DCA 區塊顯示 warning：`US-1 DCA 最小買入單位為 1 整股，不支援碎股。若每月投入金額低於股價，該期可能不會買進。`

#### 9-C 不做

- 不做匯率。
- 不做 SEC fee / TAF。
- 不做美股 benchmark。
- 不做多幣別 portfolio。

---

### 9-D 美股技術分析儀表板

#### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `src/ui/pages/dashboard.py` | 修改 | 市場切換、美股 payload、停用即時 / 籌碼 |
| `src/analysis/technical_summary.py` | 視需要修改 | 不得假設台股 timezone 或成交量單位為張 |
| `src/analysis/pattern.py` | 視需要修改 | 確認 market-neutral |
| `src/ai/advisor.py` | 修改 | symbol 驗證改為 market-aware，AI prompt 帶市場與幣別，並強制繁體中文輸出 |
| `tests/test_dashboard_page.py` | 追加 | 美股 dashboard 不呼叫 TWSE / FinMind chip |
| `tests/test_advisor.py` | 追加 | 美股 symbol 驗證與 subject info |

#### 美股 dashboard payload

美股啟用：

- 技術面 summary
- price chart
- K 棒型態
- chart pattern
- 多週期趨勢
- AI 劇本

美股停用：

- `RealtimeFetcher`
- 買一 / 賣一
- 盤中量
- `generate_chip_summary`
- institutional / margin fetch

以上為 9-D 的 US-1 日線 dashboard 規則。9-G 可新增 yfinance intraday snapshot，但不得復用台股 `RealtimeFetcher`，也不得顯示美股買一 / 賣一。

UI 顯示規則：

- 成交量 label 使用 `成交量(shares)` 或 `Volume(shares)`。
- 日期 caption：`美股日期以紐約交易日為準`。
- 籌碼 tab 可隱藏；若保留 tab，內容需顯示「US-1 尚未支援美股籌碼資料」。

#### AI prompt 語言不變式

不論 `market`、`symbol`、`currency` 或資料來源為何，AIAdvisor 對使用者輸出固定使用繁體中文。美股 prompt 結尾必須包含：

```text
You must reply entirely in Traditional Chinese (zh-TW).
```

測試只需確認 prompt builder 包含該硬約束；不依賴真實 LLM 回應。

---

### 9-E 資料管理頁美股支援

#### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `src/ui/pages/data_management.py` | 修改 | 新增市場切換，美股只顯示日 K 操作 |
| `src/ui/stock_selector.py` | 修改 | market-aware 輸入：台股維持股號+股名搜尋；美股只接受 ticker 輸入（US-1 不提供美股股名自動完成） |
| `tests/test_data_management_page.py` | 可新增 | 若目前無測試，補最小 render / helper 測試 |

#### UI 規則

- 台股模式維持現有功能。
- 美股模式顯示：
  - 日 K 更新
  - 日 K 重建
  - raw / adjusted 資料狀態
  - source = yfinance
  - timezone = America/New_York
- 美股模式停用：
  - 分 K 更新
  - 籌碼抓取
  - TWSE / TPEX 市場路由

停用功能需用明確文案，不得讓使用者以為操作失敗：

```text
US-1 尚未支援美股分 K。
US-1 尚未支援美股籌碼資料。
```

---

### 9-F 整合回歸與文件收束

#### 文件

Phase 9 實作或驗收完成後才更新：

- `PROJECT_BRIEF.md`
- `驗證後已知問題.md`（只記錄驗證中發現的問題，不當規格主文件）

Phase 9 規格討論稿先更新：

- `量化交易系統規格書_shellpig版.md`
- `開發設計方針.md`
- `測試指南.md`

#### 最終手動驗收

1. 回測頁：`市場=美股`，`AAPL`，MA 策略可跑完。
2. 回測頁：`市場=美股`，`SPY`，DCA 可跑完且以 USD 顯示。
3. 個股分析頁：`市場=美股`，`AAPL` 可顯示技術分析與 K 線。
4. 個股分析頁：美股模式不呼叫 TWSE realtime，不抓 FinMind 籌碼。
5. 資料管理頁：`BRK.B` 可正規化並更新到 `raw/us/BRK-B`。
6. 台股回測、台股個股分析、台股資料管理三條主流程不退化。

---

### 9-G 美股 yfinance 盤中快照與分 K 圖

#### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `src/data/fetcher.py` | 修改 | 新增美股 intraday 抓取能力；保留台股既有 yfinance fallback 行為 |
| `src/data/maintenance.py` | 視需要修改 | 若 intraday 需要短期 cache，需明確區分 daily update 與 intraday snapshot |
| `src/ui/pages/dashboard.py` | 修改 | 美股 dashboard 盤中優先使用 intraday snapshot，失敗時降級 adjusted daily |
| `src/ai/advisor.py` | 視需要修改 | 若 AI payload 納入 intraday snapshot，必須明確標示資料語意 |
| `tests/test_fetcher.py` | 追加 | yfinance 1m intraday schema、timezone、ticker mapping、prepost 參數 |
| `tests/test_dashboard_page.py` | 追加 | 美股盤中快照顯示、漲跌計算、失敗降級 |

#### 資料模型

9-G 建議新增最小 dataclass，避免把 intraday bar 誤當 daily row：

```python
@dataclass(frozen=True)
class USIntradaySnapshot:
    symbol: str
    price: float
    previous_raw_close: float
    change: float
    change_pct: float
    volume: int
    timestamp: pd.Timestamp  # America/New_York, timezone-aware
    source: str = "yfinance"
    interval: str = "1m"
```

Intraday OHLCV 可沿用 `STANDARD_COLUMNS`，但語意是「分 K」，不是 daily：

- `date`：bar timestamp，timezone-aware `America/New_York`
- `open/high/low/close`：該 minute bar OHLC
- `volume`：該 minute bar volume，單位 shares
- `symbol`：正規化 ticker

`volume` 在 dataclass 中使用 Python `int`。從 pandas Series 加總後需明確 cast，避免把 numpy / pandas scalar 帶進 payload：

```python
volume = int(intraday_df["volume"].fillna(0).sum())
```

#### YFinanceFetcher intraday 介面

既有 `fetch_minute(..., market="us")` 在 US-1 會拋錯。9-G 實作必須保留此行為，並新增專用方法，例如：

```python
def fetch_us_intraday(
    self,
    symbol: str,
    period: str = "1d",
    interval: str = "1m",
    prepost: bool = False,
) -> pd.DataFrame: ...
```

採專用 API 的原因：台股分 K 與美股 yfinance intraday 的資料來源、限制、cache 策略與語意不同，不應混在同一 public API。9-B 的 `test_yfinance_us_minute_is_not_supported` 必須保留。

呼叫規則：

```python
yf.download(
    ticker,
    period="1d",
    interval="1m",
    prepost=False,
    auto_adjust=False,
    progress=False,
    group_by="column",
    threads=False,
)
```

正規化要求：

- ticker mapping 沿用 9-B：`BRK.B` / `BRK-B` → `BRK-B`。
- index 若為 naive，localize 到 `America/New_York`；若為 timezone-aware，轉為 `America/New_York`。
- 只保留 regular session；第一版 `prepost=False`。
- 判斷最新 bar 是否屬於「今日」時，一律使用 `pd.Timestamp.now(tz="America/New_York").date()`，不得使用 UTC 日期或台北日期。
- 空資料、429、timeout、欄位缺漏必須回報外部資料源限制，不得覆蓋或刪除既有 daily data。

#### Dashboard 顯示規則

美股個股分析按下「分析」時：

1. 先沿用 9-D 流程準備 daily 資料；盤中行情 metrics 需取得前一紐約交易日 raw daily close，技術分析仍使用 adjusted daily。
2. 再嘗試抓 yfinance 1m intraday。
3. 若 intraday 最新 bar 屬於今日紐約交易日，使用最新 bar 的 raw `close` 作為近似盤中價。
4. `change = latest_1m_raw_close - previous_raw_daily_close`。
5. `change_pct = change / previous_raw_daily_close * 100`。
6. 成交量使用今日 regular session 1m volume 加總，單位 shares。
7. 顯示最新 bar timestamp，標註紐約時間。

盤中行情 metrics 必須維持 raw 尺度，不得用 raw intraday close 對 adjusted daily close 計算漲跌。原因是除息或調整因子會讓 adjusted close 偏離市場 raw price，造成漲跌與漲跌幅誤導。

UI 文案必須避免「即時成交價」或「最後成交價」這類過度保證。建議：

```text
美股盤中價使用 yfinance 最新 1 分 K 收盤價，可能延遲，僅供研究分析。
```

狀態欄建議顯示：

```text
近似盤中價
```

或：

```text
盤中分K資料
```

若 intraday 不可用：

- 不阻斷 dashboard。
- 降級使用 9-D adjusted daily 顯示。
- 顯示提示：「目前無法取得美股盤中分 K，已改用最新日線資料。」

#### 分 K 圖

- 在總覽 tab 顯示日內分 K 圖，與既有日 K 圖分開，且位置放在日 K 圖之前。
- 預設 `1m`，可預留 interval selector：`1m / 5m / 15m / 60m`。
- 圖表 x 軸使用紐約時間。
- 不把 intraday K 線餵進既有日線型態偵測、多週期趨勢與回測流程。

#### AI payload

9-G 預設 AI 劇本仍使用 adjusted daily。若後續要納入 intraday snapshot，只能作為附加欄位：

```python
payload["intraday_snapshot"] = {
    "source": "yfinance",
    "interval": "1m",
    "semantic": "latest_1m_close_as_approx_intraday_price",
    ...
}
```

AI prompt 必須明確說明 intraday snapshot 可能延遲，且不是正式交易所即時報價。

#### 9-G 不做

- 不接 yfinance `WebSocket` / `AsyncWebSocket`。
- 不顯示美股買一 / 賣一、五檔、order book。
- 不做逐筆、秒級或 tick data。
- 不做 intraday 回測。
- 不做盤前 / 盤後；`prepost=False`。
- 不採購 Polygon / Alpha Vantage 等付費資料源。

---

## Phase 10：前端架構重構 — Streamlit → Next.js

**對應規格：** 規格書 Phase 10。

**總目標：** 從 Streamlit 遷移至 Next.js + FastAPI，解決佈局、手機適配、效能與互動性限制。核心演算法 (`src/analysis/`, `src/backtest/`, `src/data/`, `src/strategy/`, `src/core/`, `src/ai/`, `src/indicators/`) 不重寫；新增 `src/services/` 服務層、`api/` 後端、`web/` 前端。

**實作總原則：**

- API 只做轉接：router 不放業務邏輯，呼叫 `src/services/`，服務層呼叫 `src/` 核心模組。
- 服務層函式回傳結果物件或錯誤物件（dataclass / dict / Exception），不直接操作 UI。
- JSON 序列化使用 `dataclasses.asdict()` 或 Pydantic model。
- 統一回應 envelope：`{ "data": {...}, "meta": {...} }` 或 `{ "error": { "code": "...", "message": "..." } }`。
- Envelope 例外：`GET /api/health`、`POST /api/jobs`、`GET /api/jobs/{id}/events` (SSE)。
- CORS 開發環境允許 `localhost:3000`。
- Secrets 安全：GET 永不回傳 API key 值；寫入走 write-only endpoint；config 更新走 schema whitelist。
- 前端使用 client-side fetching (SWR)，不做 Server Components streaming。

---

### 10-A 服務層抽離 + FastAPI 後端骨架

#### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `src/services/__init__.py` | 新增 | 服務層模組 |
| `src/services/dashboard_service.py` | 新增 | 從 `src/ui/pages/dashboard.py` 抽離非渲染邏輯 |
| `src/services/backtest_service.py` | 新增 | 從 `src/ui/pages/backtest.py` 抽離非渲染邏輯 |
| `src/services/data_service.py` | 新增 | 從 `src/ui/pages/data_management.py` 抽離非渲染邏輯 |
| `src/services/config_service.py` | 新增 | 從 `src/ui/pages/settings.py` 抽離非渲染邏輯 |
| `api/__init__.py` | 新增 | API 模組 |
| `api/main.py` | 新增 | FastAPI app 入口、CORS middleware |
| `api/deps.py` | 新增 | 共用依賴注入（Config、Storage 實例） |
| `api/job_manager.py` | 新增 | in-memory Job manager |
| `api/routers/__init__.py` | 新增 | 路由模組 |
| `api/routers/config.py` | 新增 | `/api/config/*` 端點 |
| `api/routers/data.py` | 新增 | `/api/data/*` 端點 |
| `api/routers/jobs.py` | 新增 | `/api/jobs/*` 端點 |
| `src/ui/pages/dashboard.py` | 修改 | 改為呼叫 `src/services/dashboard_service` |
| `src/ui/pages/backtest.py` | 修改 | 改為呼叫 `src/services/backtest_service` |
| `src/ui/pages/data_management.py` | 修改 | 改為呼叫 `src/services/data_service` |
| `src/ui/pages/settings.py` | 修改 | 改為呼叫 `src/services/config_service` |
| `pyproject.toml` | 修改 | 加入 `fastapi>=0.115`、`uvicorn[standard]>=0.30`、`sse-starlette>=2.0` |
| `run_api.bat` | 新增 | FastAPI 啟動腳本 |
| `tests/test_services/test_dashboard_svc.py` | 新增 | 服務層測試 |
| `tests/test_services/test_backtest_svc.py` | 新增 | 服務層測試 |
| `tests/test_services/test_data_svc.py` | 新增 | 服務層測試 |
| `tests/test_services/test_config_svc.py` | 新增 | 服務層測試 |
| `tests/test_api/test_config_api.py` | 新增 | API 端點測試 |
| `tests/test_api/test_jobs_api.py` | 新增 | API 端點測試 |

#### 服務層抽離規則

現有 Streamlit 頁面函式（如 `_run_backtest`）內含 `st.error()`、`st.warning()`、`st.session_state` 等 UI 呼叫。抽離至服務層時：

```python
# src/services/dashboard_service.py

@dataclass
class DashboardPayload:
    daily_df: pd.DataFrame
    technical: TechnicalSummary
    patterns: list[dict]
    chip_data: dict | None       # None for market="us"
    intraday: dict | None        # None if unavailable
    realtime: dict | None        # None for market="us"

@dataclass
class DashboardError:
    code: str                    # e.g. "SYMBOL_NOT_FOUND", "DATA_STALE"
    message: str

def build_dashboard_payload(
    symbol: str,
    market: str = "tw",
    bars: int = 250,
) -> DashboardPayload | DashboardError:
    """組裝儀表板完整 payload。不含任何 UI 操作。"""
    ...
```

Streamlit 頁面改為：

```python
result = build_dashboard_payload(symbol, market)
if isinstance(result, DashboardError):
    st.error(result.message)
    return
# 正常渲染 result
```

API router 改為：

```python
@router.get("/api/dashboard/payload")
def get_dashboard_payload(symbol: str, market: str = "tw", bars: int = 250):
    result = build_dashboard_payload(symbol, market, bars)
    if isinstance(result, DashboardError):
        raise HTTPException(status_code=400, detail={"error": {"code": result.code, "message": result.message}})
    return {"data": serialize(result), "meta": {"market": market, "symbol": symbol, ...}}
```

#### FastAPI app 結構

```python
# api/main.py
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from api.routers import config, data, jobs

app = FastAPI(title="QuantTrader API")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000"],
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(config.router)
app.include_router(data.router)
app.include_router(jobs.router)

@app.get("/api/health")
def health():
    return {"status": "ok"}
```

#### Job manager 設計

```python
# api/job_manager.py

@dataclass
class Job:
    id: str                      # UUID
    type: str                    # "backtest_run" | "data_fetch" | ...
    status: str                  # "running" | "complete" | "error" | "cancelled"
    progress: float              # 0.0 ~ 1.0
    message: str                 # 進度文字
    result: dict | None          # 完成後的結果
    error: dict | None           # 錯誤資訊
    created_at: datetime
    ttl_seconds: int = 1800      # 30 分鐘

class JobManager:
    def __init__(self):
        self._jobs: dict[str, Job] = {}
        self._write_lock = asyncio.Lock()

    async def create_job(self, job_type: str, params: dict) -> Job:
        """建立 job。寫入型 job 必須取得 write lock。"""
        ...

    async def acquire_write_lock(self) -> bool:
        """嘗試取得寫入鎖，失敗回傳 False。"""
        ...

    def get_job(self, job_id: str) -> Job | None: ...
    async def cancel_job(self, job_id: str) -> bool: ...
    def cleanup_expired(self) -> None: ...
```

**寫入型 job 判定：** `data_fetch`、`data_rebuild`、`backtest_run`、`backtest_batch`、`backtest_sweep`、`backtest_wfa` 均為寫入型。

**同步 REST 端點的 write lock：** `DELETE /api/data/{market}/{symbol}` 與 `GET /api/dashboard/payload` 須取得 write lock。Lock 忙碌時立即回傳 `409 Conflict`：`{ "error": { "code": "WRITE_LOCK_BUSY", "message": "目前有其他資料操作正在進行，請稍後再試" } }`。

#### config_service 安全規則

```python
# src/services/config_service.py

CONFIG_UPDATE_WHITELIST = {"ui", "ai", "risk", "backtest.initial_capital"}

def read_config(mask_secrets: bool = True) -> dict:
    """讀取 config.yaml。mask_secrets=True 時 API key 欄位顯示 "***configured***" 或 null。"""
    ...

def update_config(patch: dict, whitelist: set[str] = CONFIG_UPDATE_WHITELIST) -> None:
    """只允許 whitelist 內的 key 更新。"""
    ...

def update_secrets(keys: dict[str, str]) -> None:
    """Write-only。將 API key 寫入 .env，不回傳值。"""
    ...

def get_secrets_status() -> dict[str, bool]:
    """只回傳各 key 是否已設定，不回傳實際值。"""
    ...
```

#### 測試要求

- 服務層測試：驗證 payload 組裝、市場分流、錯誤回傳、secrets 遮蔽。
- API 測試：使用 `httpx.AsyncClient` (FastAPI TestClient)，驗證 HTTP 請求 → 正確呼叫服務層 → 正確 JSON 回傳。不重測核心邏輯。
- 舊 Streamlit 測試必須全數通過。

---

### 10-B Next.js 前端骨架

#### 新增檔案

| 檔案 | 說明 |
|:---|:---|
| `web/package.json` | Next.js + React + TypeScript + Tailwind + shadcn/ui + Lightweight Charts + SWR |
| `web/tsconfig.json` | TypeScript 設定 |
| `web/tailwind.config.ts` | Tailwind CSS v4 設定 |
| `web/next.config.ts` | Next.js 設定 |
| `web/components.json` | shadcn/ui 設定 |
| `web/src/app/layout.tsx` | Root layout（sidebar + theme provider） |
| `web/src/app/page.tsx` | 首頁（重導至 /dashboard） |
| `web/src/app/dashboard/page.tsx` | 空殼頁 |
| `web/src/app/backtest/page.tsx` | 空殼頁 |
| `web/src/app/data/page.tsx` | 空殼頁 |
| `web/src/app/ai/page.tsx` | 空殼頁 |
| `web/src/app/settings/page.tsx` | 空殼頁 |
| `web/src/components/sidebar.tsx` | PC 左側 Sidebar / 手機底部 Tab Bar |
| `web/src/components/theme-provider.tsx` | Dark / Light 主題切換 Context |
| `web/src/components/market-switcher.tsx` | 台股/美股切換元件 |
| `web/src/components/stock-selector.tsx` | 股票代碼輸入（Enter 送出、BRK.B 正規化） |
| `web/src/lib/api-client.ts` | 封裝 fetch：統一 base URL、錯誤處理、TypeScript 泛型 |
| `web/src/lib/utils.ts` | shadcn/ui `cn()` 等 |
| `web/src/lib/formatters.ts` | 數字/日期/貨幣格式化 |
| `web/src/types/analysis.ts` | 對應後端 dataclass 的 TypeScript 型別 |
| `web/src/types/backtest.ts` | 回測型別 |
| `web/src/types/market.ts` | 市場型別 |
| `web/src/types/config.ts` | 設定型別 |
| `run_web.bat` | `cd web && pnpm dev` |
| `run_dev.bat` | 同時啟動 API + Web |

#### api-client.ts 設計

```typescript
// web/src/lib/api-client.ts

const BASE_URL = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000";

interface ApiResponse<T> {
  data: T;
  meta: { market: string; symbol?: string; timestamp: string };
}

interface ApiError {
  error: { code: string; message: string };
}

export async function apiFetch<T>(path: string, options?: RequestInit): Promise<T> {
  const res = await fetch(`${BASE_URL}${path}`, {
    ...options,
    headers: { "Content-Type": "application/json", ...options?.headers },
  });
  if (!res.ok) {
    const body = await res.json().catch(() => null);
    throw new ApiClientError(res.status, body?.error?.code, body?.error?.message);
  }
  return res.json();
}
```

#### Sidebar 設計

```tsx
// web/src/components/sidebar.tsx

const NAV_ITEMS = [
  { label: "個股分析", href: "/dashboard", icon: BarChart3 },
  { label: "回測研究", href: "/backtest", icon: TrendingUp },
  { label: "資料管理", href: "/data", icon: FolderOpen },
  { label: "AI 問答",  href: "/ai",        icon: Bot },
  { label: "設定",     href: "/settings",  icon: Settings },
];

// PC (lg+): 左側固定 Sidebar，寬度 240px
// Mobile (<lg): 底部固定 Tab Bar，5 個 icon
```

#### 主題系統

- CSS 變數架構，Dark 為預設。
- `--chart-up` / `--chart-down` 依市場切換：台股 red/green、美股 green/red。
- shadcn/ui 原生 dark/light class 切換。

#### OneDrive 注意事項

pnpm 預設用 hardlink/symlink，OneDrive 同步可能衝突。處理順序：

1. `web/.npmrc` 加 `package-import-method=copy`
2. 若仍有問題，退回 `npm`，更新 `run_web.bat`、`run_dev.bat`

---

### 10-C 資料管理頁

10-C **拆為兩階段交付**。10-C-1 為前端可獨立驗收的 MVP（後端 API 已全部就緒）；10-C-2 補後端 Job dispatcher 與 SSE 進度，再落地更新／重建／新增。

---

### 10-C-1：列表 + DELETE（不需擴充後端）

#### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `api/routers/data.py` | 既有 | `GET /symbols`、`GET /status`、`DELETE` 三條已實作（10-A 階段完成），10-C-1 不動 |
| `web/src/app/data/page.tsx` | 修改 | 由空殼改為完整資料管理頁（移植 [web/_design/data-mockup.tsx](web/_design/data-mockup.tsx) 的視覺、tokens、配色） |
| `web/src/types/data.ts` | 新增 | `SymbolStatus`、`SymbolRow`、`DataListResponse` 等型別，對應後端 `GET /status` 回傳 |
| `web/src/lib/hooks/useDataList.ts` | 新增 | SWR 包裝 `GET /api/data/symbols` + 為每個 symbol 逐筆 fetch `GET /status`，組合成 `SymbolRow[]` |
| `web/src/components/data/DataTable.tsx` | 新增 | 列表元件（含 sticky 表頭，依 1440px 設計） |
| `web/src/components/data/StatusBadge.tsx` | 新增 | 三態 badge：fresh / stale / missing |
| `web/src/components/data/DeleteConfirmDialog.tsx` | 新增 | 單步確認 Dialog（警示文案 + 取消／確認刪除） |
| `tests/test_api/test_data_api.py` | 既有/補強 | 確認 `GET /symbols`、`GET /status`、`DELETE` 既有測試覆蓋 404 / 409 / 成功路徑 |

#### DELETE 端點規格（既有）

| 項目 | 規格 |
|:---|:---|
| 刪除範圍 | 該 symbol 下所有 parquet + DuckDB metadata |
| 前端確認 | 單步確認 Dialog（**單步**：警示文案 + 取消／確認刪除；不需輸入代碼解鎖） |
| 可復原性 | 本機快取，可重新下載恢復 |
| 回測結果 | 不刪除 `data/backtest/` |
| Write lock | 必須取得 write lock，被佔用回傳 `409 Conflict` |
| 錯誤碼 | 不存在 `404`、I/O 錯誤 `500` |

#### 狀態 badge 三態判定（前端計算）

後端 `GET /status` 回的是 `start_date / end_date / row_count / available`，**最近交易日由前端自己判斷**（避免後端時鐘耦合）：

```ts
function judgeStatus(row: SymbolStatus, latestTradeDate: Date): "fresh" | "stale" | "missing" {
  if (!row.available) return "missing";
  const end = new Date(row.end_date);
  const daysBehind = businessDaysBetween(end, latestTradeDate);
  if (daysBehind === 0) return "fresh";
  if (daysBehind <= 5) return "stale";
  return "missing";
}
```

最近交易日：台股取 `Asia/Taipei` 今天（若為週六/日往前推到週五）；美股取 `America/New_York` 今天類推。可放在 `web/src/lib/trading-calendar.ts`。

#### 美股 raw + adjusted 顯示規則

舊 Streamlit 對美股展開兩列；本輪選擇**單列 + `raw+adj` 標記**（mockup 註解 ① 折衷做法）：

```tsx
{row.market === "us" && (
  <span className="ml-2 text-[10px] uppercase tracking-wider text-slate-500">
    raw+adj
  </span>
)}
```

並在表格上方加 callout：

```tsx
{market === "us" && (
  <Callout>
    美股僅支援日 K（資料來源：yfinance）；US-1 範圍不含分 K 與籌碼資料。
    同一標的會同時保留 raw 與 adjusted 兩份。
  </Callout>
)}
```

#### stage-2 按鈕 disabled 邏輯（10-C-1 須做）

```tsx
<button
  disabled
  title="Phase 10-C-2 開發中"
  className="... opacity-50 cursor-not-allowed"
>
  全部更新
</button>
```

對應四類按鈕：「全部更新」「全部重建」「動作欄·更新」「+ 新增標的」。Tooltip 用 `title` 屬性或 Radix Tooltip 皆可。

#### 動作欄按鈕配色（mockup 註解 ⑥）

- **更新**：outline + 藍色語意（`border-sky-500/30 bg-sky-500/10 text-sky-300`）
- **刪除**：destructive + 紅色語意（`border-rose-500/30 bg-rose-500/10 text-rose-300`）

---

### 10-C-2：更新 / 重建 / 新增（需擴充後端 Job dispatcher）

#### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `api/job_manager.py` | 修改 | dispatcher 區塊新增 `data_update` / `data_rebuild` 兩個 job type 的處理 |
| `api/routers/jobs.py` | 修改 | （若 dispatcher 邏輯放這）對應更新；SSE 進度推送 |
| `web/src/components/data/AddSymbolDialog.tsx` | 新增 | 「+ 新增標的」彈窗，**複用 `StockSelector` 元件**（代碼或名稱皆可） |
| `web/src/components/data/RebuildConfirmDialog.tsx` | 新增 | 「全部重建」二次確認（比照 DELETE 風格） |
| `web/src/components/data/ProgressBar.tsx` | 新增 | 全局進度條（顯示 current/total + 當前處理的 symbol） |
| `web/src/lib/hooks/useDataJob.ts` | 新增 | 建立 job + 訂閱 SSE，回傳 `{ status, current, total, current_symbol, succeeded, failed }` |
| `tests/test_api/test_data_jobs_api.py` | 新增 | `POST /api/jobs` type=`data_update`/`data_rebuild`、SSE 訊息格式、單檔失敗繼續、批次 vs 單檔模式 |

#### Job dispatcher 擴充樣板

```python
# api/job_manager.py（或 routers/jobs.py）

async def _run_data_job(job: Job, manager: JobManager) -> None:
    job_type = job.type  # "data_update" or "data_rebuild"
    params = job.params or {}
    market = params.get("market", "tw")

    if params.get("all"):
        symbols = list_symbols(market=market)
    else:
        symbols = params.get("symbols") or []

    if not symbols:
        manager.fail_job(job.id, error={"code": "NO_TARGETS", "message": "未指定任何 symbol"})
        return

    succeeded: list[str] = []
    failed: list[dict] = []
    total = len(symbols)

    for i, symbol in enumerate(symbols, 1):
        manager.push_event(job.id, "progress", {
            "current": i, "total": total,
            "current_symbol": symbol, "status": "updating",
        })
        try:
            if job_type == "data_update":
                update_daily(symbol, market=market)
            else:
                rebuild_symbol(symbol, market=market)
            succeeded.append(symbol)
            manager.push_event(job.id, "progress", {
                "current": i, "total": total,
                "current_symbol": symbol, "status": "done",
            })
        except Exception as e:
            failed.append({"symbol": symbol, "error": str(e)})
            manager.push_event(job.id, "progress", {
                "current": i, "total": total,
                "current_symbol": symbol, "status": "failed",
                "error": str(e),
            })
            # 不中斷，繼續下一檔

    manager.complete_job(job.id, result={
        "succeeded": succeeded, "failed": failed,
    })
    manager.push_event(job.id, "result", {
        "succeeded": succeeded, "failed": failed,
    })
```

關鍵設計：
- **單檔失敗不中斷整批**（迴圈內 try/except 包單一 symbol）
- 進度 event 推送頻率：每完成一檔推一次「updating」+ 一次「done|failed」（共兩次/檔，足夠 UI 顯示）
- 最終結果同時放 `complete_job` 的 result 與獨立 `result` event（前端任一管道都能讀）

#### SSE 訊息規格

| 事件 | 觸發時機 | data 欄位 |
|:---|:---|:---|
| `progress` | 每檔開始處理 + 每檔完成 | `current`, `total`, `current_symbol`, `status` (`"updating" \| "done" \| "failed"`), `error?` |
| `result` | 全部完成 | `succeeded: string[]`, `failed: { symbol, error }[]` |

#### 「動作欄·更新」單檔模式

呼叫 `POST /api/jobs`：
```json
{
  "type": "data_update",
  "params": { "market": "tw", "symbols": ["2330"] }
}
```
進度 SSE 仍走相同管道，`current/total` 都是 1。

#### 「全部重建」二次確認 Dialog

破壞性操作，比照 DELETE 視覺風格但措辭更強：

```tsx
<Dialog>
  <Title>確認重建全部 {market === "tw" ? "台股" : "美股"} 本機資料</Title>
  <Description>
    此動作將清除所有 {market} 標的的本機快取後重新下載，過程可能耗時數分鐘。
    重建期間其他資料操作將被鎖定。
  </Description>
  <Footer>
    <CancelButton />
    <ConfirmButton variant="destructive">確認重建</ConfirmButton>
  </Footer>
</Dialog>
```

#### 「+ 新增標的」彈窗設計（複用 StockSelector）

```tsx
<AddSymbolDialog
  market={market}
  onSubmit={async (symbol) => {
    const jobId = await createJob({
      type: "data_update",
      params: { market, symbols: [symbol] },
    });
    subscribeSSE(jobId);  // 進度走相同 SSE 管道
  }}
>
  <StockSelector
    market={market}
    placeholder={market === "tw" ? "輸入代碼或名稱（如 2330 / 台積電）" : "輸入代碼（如 AAPL）"}
  />
</AddSymbolDialog>
```

#### Write lock 互斥

`data_update` / `data_rebuild` 都是寫入型 job，依既有 `_WRITE_JOB_TYPES` 機制納入 write lock 範圍。同一時間只允許一個 data job 進行；被佔回 `409`。

---

### 10-D 個股分析儀表板

#### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `api/routers/analysis.py` | 新增 | `/api/analysis/*` + `/api/dashboard/payload` 端點 |
| `api/routers/realtime.py` | 新增 | `/api/realtime/*` 端點 |
| `api/routers/ai.py` | 新增 | `/api/ai/analyze` 端點 |
| `web/src/app/dashboard/page.tsx` | 修改 | 完整儀表板實作 |
| `web/src/components/charts/candlestick-chart.tsx` | 新增 | Lightweight Charts K 線圖 wrapper |
| `web/src/components/charts/volume-chart.tsx` | 新增 | 成交量圖 |
| `web/src/components/charts/indicator-chart.tsx` | 新增 | 技術指標副圖 |
| `web/src/components/charts/chart-container.tsx` | 新增 | 多圖垂直同步容器 |
| `web/src/components/metric-card.tsx` | 新增 | 指標卡片 |
| `web/src/hooks/use-stock-data.ts` | 新增 | SWR hook |
| `web/src/hooks/use-realtime.ts` | 新增 | 行情 polling hook |
| `tests/test_api/test_analysis_api.py` | 新增 | 分析端點測試 |

#### API 端點清單

| 方法 | 路徑 | 用途 |
|:---|:---|:---|
| GET | `/api/dashboard/payload?symbol=&market=&bars=` | 聚合端點（初載） |
| GET | `/api/analysis/technical?symbol=&market=` | 技術面（局部刷新） |
| GET | `/api/analysis/pattern?symbol=&market=` | 型態（局部刷新） |
| GET | `/api/analysis/chip?symbol=` | 籌碼（台股限定） |
| GET | `/api/analysis/daily?symbol=&market=&bars=` | 日 K 資料 |
| GET | `/api/realtime/tw?symbol=` | 台股即時 |
| GET | `/api/realtime/us/intraday?symbol=` | 美股 intraday |
| POST | `/api/ai/analyze` | AI 分析 |

#### 圖表時間格式

| 資料類型 | API 回傳 | Lightweight Charts 時間類型 |
|:---|:---|:---|
| 台股/美股日 K | `"YYYY-MM-DD"` 字串 | `UTCTimestamp` as business day string |
| 美股 1m intraday | `{ timestamp_utc: number, exchange_tz: "America/New_York" }` | `UTCTimestamp` (numeric) |
| 台股即時行情 | ISO 8601 with timezone | display only |

日 K 設定 `timeScale.timeVisible: false` 消除週末空隙。美股 intraday 前端用 `exchange_tz` 轉紐約時間顯示。

#### Lightweight Charts React wrapper 模式

```tsx
// web/src/components/charts/candlestick-chart.tsx

import { createChart, ColorType, IChartApi } from "lightweight-charts";
import { useEffect, useRef } from "react";

interface CandlestickChartProps {
  data: { time: string; open: number; high: number; low: number; close: number }[];
  market: "tw" | "us";
}

export function CandlestickChart({ data, market }: CandlestickChartProps) {
  const containerRef = useRef<HTMLDivElement>(null);
  const chartRef = useRef<IChartApi | null>(null);

  useEffect(() => {
    if (!containerRef.current) return;

    const upColor = market === "tw" ? "#ef4444" : "#22c55e";
    const downColor = market === "tw" ? "#22c55e" : "#ef4444";

    const chart = createChart(containerRef.current, {
      width: containerRef.current.clientWidth,
      height: 400,
      layout: { background: { type: ColorType.Solid, color: "transparent" } },
      crosshair: { mode: 0 },
      timeScale: { timeVisible: false },
    });

    const series = chart.addCandlestickSeries({
      upColor, downColor, wickUpColor: upColor, wickDownColor: downColor,
    });
    series.setData(data);
    chart.timeScale().fitContent();
    chartRef.current = chart;

    const handleResize = () => {
      chart.applyOptions({ width: containerRef.current!.clientWidth });
    };
    window.addEventListener("resize", handleResize);

    return () => {
      window.removeEventListener("resize", handleResize);
      chart.remove();
    };
  }, [data, market]);

  return <div ref={containerRef} className="w-full" />;
}
```

#### chart-container 同步邏輯

```tsx
// web/src/components/charts/chart-container.tsx
// 管理多個 Lightweight Charts 實例的時間軸同步
// 使用 chart.timeScale().subscribeVisibleLogicalRangeChange() 同步各圖表的可見範圍
// 子圖表透過 ref 註冊到容器，容器負責 crosshair sync
```

---

### 10-E 回測研究工作台

10-E 拆 4 段交付：**10-E-1（單次）→ 10-E-2（批次）→ 10-E-3（掃描）→ 10-E-4（WFA）**。本節依此順序拆寫；10-E-1 給完整實作細節，10-E-2~4 給中等密度（params / result / 關鍵元件清單 / SSE 訊息格式 / 驗收骨架）。

#### 10-E 對 10-G-1 基礎設施的依賴（前置條件）

**10-E 開工前 10-G-1 必須完成**。10-E 4 段共用 10-G-1 提供的 4 種基礎設施，本節詳細規格屬 10-G-1（V2.6 補入），這裡只列 10-E 對其的使用介面：

| 基礎設施 | 預期 API（由 10-G-1 提供） | 10-E 使用場景 |
|:---|:---|:---|
| **Toast 系統** | `useToast()` hook，回 `{ success(msg), error(msg), info(msg), dismiss() }`；shadcn `sonner` 為底 | 4 段 job complete / cancel / error；CSV 下載成功；heatmap cell 複製；`OVER_MAX_COMBOS` / `INSUFFICIENT_DATA_FOR_WFA` 等 422 錯誤 |
| **Error Boundary** | `<ErrorBoundary fallback={...}>` 元件；放在各 page 主內容內，不包 sidebar | `BacktestPage` 主內容包一層；只接 React render / lifecycle / hook 例外。SSE 中斷、fetch 失敗、invalid JSON 由 hook 設為 `status="error"`、顯示頁內錯誤區並走 toast |
| **Loading Skeleton** | shadcn `Skeleton` 基礎元件 + 預製變體（`CardSkeleton` / `ChartSkeleton` / `TableSkeleton`） | job running 期間：tearsheet 4 卡 skeleton + K 線 ChartSkeleton + equity ChartSkeleton + trades TableSkeleton；SSE result 到後同步替換 |
| **Command Palette** | `<CommandPalette>` 元件（Ctrl+K 開啟）；註冊 entries 用 `useCommandPaletteEntry({ id, label, action })` hook | 10-E-1 落地時註冊 4 個 entry：「回測：單次 / 策略比較 / 參數掃描 / Walk-Forward」，action 為 `router.push("/backtest?tab=...")` |

實作端注意：若 10-G-1 尚未驗收完成、實作者開了 10-E-1 分支，**應該停下來等 10-G-1 完成再續**；強行寫 inline banner 替代會導致後續 10-G-1 落地後需回頭重構 4 個 sub-stage（即「為什麼這樣排」的核心理由）。

#### 10-E 共用基礎建設（10-E-1 必須完成、10-E-2~4 重用）

##### 新增 / 修改檔案（10-E-1）

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `api/routers/jobs.py` | 修改 | 新增 `_run_backtest_run_job` dispatcher 分支；`_WRITE_JOB_TYPES` 加 `backtest_run` |
| ~~`api/routers/backtest.py`~~ | ~~新增~~ | ~~`GET /api/backtest/strategies`~~ — **取消**：前端直接使用 `GET /api/config` 取 `strategies[]`，不另設端點 |
| `api/routers/jobs.py` | 修改 | `GET /api/jobs/{id}/result` 加 `format=csv` / `part=window|stability` query 分支（10-E-2 起用） |
| `src/services/backtest_service.py` | 修改 | 既有 `run_backtest_job()` 加 cancellation token 與 progress callback；新增 `run_backtest_run_job(manager, job, params)` async wrapper |
| `web/src/app/backtest/page.tsx` | 修改 | 由 placeholder 殼改為 4 tab 框架（單次可用、其他 disabled + tooltip） |
| `web/src/components/backtest/SingleRunTab.tsx` | 新增 | 單次回測 tab 主元件 |
| `web/src/components/backtest/StrategyPresetSelect.tsx` | 新增 | 策略 preset 下拉（讀 `GET /api/config`） |
| `web/src/components/backtest/DateRangePicker.tsx` | 新增 | 日期區間（shadcn Calendar + Popover） |
| `web/src/components/backtest/EngineSelect.tsx` | 新增 | 引擎下拉（vectorized / event_driven） |
| `web/src/components/backtest/TearsheetCards.tsx` | 新增 | 5 metric card |
| `web/src/components/backtest/CandleChartWithMarkers.tsx` | 新增 | Lightweight Charts K 線 + MA + buy/sell markers |
| `web/src/components/backtest/EquityCurveChart.tsx` | 新增 | Lightweight Charts 線圖（支援單線與多線） |
| `web/src/components/backtest/TradesTable.tsx` | 新增 | shadcn Table，sortable + paginated |
| `web/src/components/backtest/BacktestProgressBar.tsx` | 新增 | 進度條（沿用 10-C-2 `ProgressBar` 視覺） |
| `web/src/hooks/use-backtest-job.ts` | 新增 | Job + SSE hook，回 `{ status, progress, result, error, cancel() }` |
| `tests/test_api/test_backtest_api.py` | 新增 | 回測端點測試（10-E-1 提供 ≥ 8 cases） |

##### Job lifecycle 共用合約

所有 4 段共用：

```
POST /api/jobs { type: "backtest_run" | "backtest_batch" | "backtest_sweep" | "backtest_wfa", params: {...} }
  → 201 { job_id, type, status: "running", progress: 0.0, ... }

GET /api/jobs/{job_id}/events
  → SSE event types per sub-stage:
       backtest_run    : progress (running → done) + result
       backtest_batch  : progress (per preset) + result
       backtest_sweep  : progress (per combo, throttled) + result
       backtest_wfa    : window_progress + sweep_progress (nested) + result

GET /api/jobs/{job_id}/result → 200 { data: <result schema 見規格書>, meta: { job_id, status } }
  - status == "complete"：回完整 result
  - status == "cancelled" 且 job.result 不為空：回 partial result
  - 其他狀態：409 JOB_NOT_COMPLETE
GET /api/jobs/{job_id}/result?format=csv[&part=window|stability] → CSV blob (10-E-2 起；由 api/routers/jobs.py 處理)
POST /api/jobs/{job_id}/cancel → 200 { status: "cancelled" }
```

##### 取消行為（共用樣板）

服務層 `run_backtest_*_job` 在每個迴圈點檢查：

```python
async def run_backtest_batch_job(manager, job, params):
    # ...
    for i, preset in enumerate(presets, 1):
        current = manager.get_job(job.id)
        if current and current.status == "cancelled":
            break  # 已完成的 summaries 保留，未跑的略過
        # ... run one preset
        manager.push_event(job.id, "progress", {...})

    # 結束時推 result（含已完成部分）
    partial_result = {...}
    manager.push_event(job.id, "result", partial_result)
    current = manager.get_job(job.id)
    if current and current.status == "cancelled":
        manager.finish_cancelled_job(job.id, result=partial_result)
    else:
        manager.complete_job(job.id, result=partial_result)
```

關鍵：**取消後不丟棄已完成的部分結果**（讓使用者看到中途進度，不是清空），且 status 保持 `cancelled`，不改回 `complete`。

##### JobManager 取消完成 helper

**前置修改（10-E-1 開工前）**：現有 `cancel_job()` 方法必須修改——移除其中的 `_close_event_queue()` 呼叫，改為**只設 status**。原因：10-E 的取消流程是 `cancel_job()` 設 status → service 迴圈檢查到 cancelled 後 break → push partial result event → `finish_cancelled_job()` 關 queue。若 `cancel_job()` 提前關 queue，partial result 就推不進去。

修改後的 `cancel_job()`：

```python
async def cancel_job(self, job_id: str) -> bool:
    job = self._jobs.get(job_id)
    if job is None:
        return False
    if job.status in ("complete", "error"):
        return False
    job.status = "cancelled"
    # 不在此處關閉 event queue；由 finish_cancelled_job() 統一關閉
    return True
```

新增 `finish_cancelled_job()` 方法：

```python
def finish_cancelled_job(self, job_id: str, result: dict[str, Any]) -> None:
    """Store partial result for a cancelled job and close the SSE stream."""
    self.update_job(
        job_id,
        status="cancelled",
        result=result,
        message="已取消",
    )
    self._close_event_queue(job_id)
```

`GET /api/jobs/{job_id}/result` 需允許 `status == "cancelled"` 且 `job.result` 存在時回傳 partial result；`meta.status` 帶 `"cancelled"` 供前端顯示取消提示。`cancelled` 且 `job.result` 為 None 時回 409 `JOB_NOT_COMPLETE`。

##### `_WRITE_JOB_TYPES` 擴充

[api/job_manager.py](api/job_manager.py) 中：

```python
_WRITE_JOB_TYPES: frozenset[str] = frozenset({
    "data_update", "data_rebuild",
    "backtest_run", "backtest_batch", "backtest_sweep", "backtest_wfa",
})
```

理由：回測 job 走 `load_backtest_data()`，會 auto-sync 寫入 Parquet / DuckDB，需取 write lock。

#### 10-E-1：單次回測

##### `run_backtest_run_job` dispatcher 樣板

```python
# api/routers/jobs.py
async def _run_backtest_run_job(manager: JobManager, job: Job, params: dict[str, Any]) -> None:
    from src.services.backtest_service import (
        BacktestServiceError, run_backtest_job, list_strategy_presets
    )

    # Validation
    presets = list_strategy_presets()
    idx = params.get("strategy_preset_index")
    if not isinstance(idx, int) or idx < 0 or idx >= len(presets):
        manager.fail_job(job.id, error={"code": "INVALID_PARAMS",
                                         "message": "strategy_preset_index out of range"})
        return

    # Check cancellation BEFORE long operation
    if manager.get_job(job.id).status == "cancelled":
        return

    manager.push_event(job.id, "progress", {"status": "running", "phase": "loading_data"})

    # Run (off main thread)
    result = await asyncio.to_thread(
        run_backtest_job,
        symbol=params["symbol"],
        start_ts=pd.Timestamp(params["start_date"]),
        end_exclusive=pd.Timestamp(params["end_date"]) + pd.Timedelta(days=1),
        strategy_preset=presets[idx],
        engine=params.get("engine", "vectorized"),
        market=params.get("market", "tw"),
    )

    if isinstance(result, BacktestServiceError):
        manager.fail_job(job.id, error={"code": result.code, "message": result.message})
        return

    # Serialize result to JSON-safe dict
    payload = _serialize_backtest_result(result)
    manager.push_event(job.id, "result", payload)
    manager.complete_job(job.id, result=payload)
```

##### `_serialize_backtest_result()` 規格

把 `BacktestJobResult` 轉成規格書定義的 JSON schema。重點：

- `BacktestResult.equity_curve`（pd.Series）→ `[{date, value}]`
- `BacktestResult.trades`（pd.DataFrame）→ `[{entry_date, exit_date, side, entry_price, exit_price, shares, pnl, return_pct}]`
- `data`（pd.DataFrame）→ `price_data: [{date, open, high, low, close, volume}]`（可取 trades 周圍 ±N 天減量；10-E-1 先全給）
- `signals`：從 trades 派生 `[{date, side: "buy"|"sell", price}]`
- DCA 走 `dca_result` 分支：
  - `metrics.total_trades` = `null`（DCA 無交易次數概念）；`metrics.win_rate` / `profit_factor` / `sharpe_ratio` = `null`
  - `metrics.total_return` / `annual_return` / `max_drawdown` 從 `DcaBacktestResult` 計算
  - `equity_curve`：從 `dca_result.records` 的 `cumulative_market_value` 逐日派生 `[{date, value}]`
  - `trades` = `[]`（空陣列，前端 TradesTable 不 mount）
  - `signals` = `[]`
  - `dca_warning` 帶入（美股碎股提示或 null）
- **`initial_capital`**：預設 `1000000`，由 params 傳入、注入引擎的 `initial_capital` 參數。`backtest_service.run_backtest_job()` 需新增 `initial_capital` 參數（見下方 dispatcher 樣板）

##### `CandleChartWithMarkers` 元件規格

基於 10-D 的 `CandlestickChart` 擴充：

```typescript
// web/src/components/backtest/CandleChartWithMarkers.tsx
interface Props {
  priceData: Array<{ date: string; open: number; high: number; low: number; close: number; volume: number }>;
  market: "tw" | "us";
  signals: Array<{ date: string; side: "buy" | "sell"; price: number }>;
  maOverlay?: Array<{ name: string; period: number; color: string }>;  // 從 strategy_type 推導
  height?: number;
}

// 內部用 createChart + addCandlestickSeries + addLineSeries (MA) + series.setMarkers(...)
//
// Markers mapping:
const markers = signals.map(s => ({
  time: s.date,
  position: s.side === "buy" ? "belowBar" : "aboveBar",
  color: s.side === "buy" ? "var(--chart-up)" : "var(--chart-down)",
  shape: s.side === "buy" ? "arrowUp" : "arrowDown",
  text: s.side === "buy" ? "B" : "S",
}));
```

依 strategy_type 推導 MA overlay：

| strategy_type | MA overlays |
|:---|:---|
| `moving_average_cross` | `[{ name: "MA短", period: params.short_window }, { name: "MA長", period: params.long_window }]` |
| `bollinger_band` | `[{ name: "MA", period: params.period }]` + upper/lower 帶（10-E-1 可先省略 band，只畫中線） |
| `bias` | `[{ name: "MA", period: params.ma_period }]` |
| `donchian_breakout` | 不畫 MA（畫通道線可選，10-E-1 先省略） |
| `rsi`, `kd_cross`, `macd_cross` | 不畫主圖 MA（指標另開副圖；10-E-1 先**不做副圖**） |

##### `TearsheetCards` 元件規格

```typescript
interface Props {
  metrics: {
    total_trades: number;
    total_return: number;       // 0.345 = 34.5%
    annual_return: number;
    max_drawdown: number;       // negative
    sharpe_ratio: number;
  };
  currency: "TWD" | "USD";
  isDca?: boolean;
}

// 5 cards in grid-cols-5:
//   交易次數 | 總報酬率 | 年化報酬率 | 最大回撤 | Sharpe
// 副標：「幣別：TWD」
// 顏色：正報酬綠 / 負報酬紅 / 最大回撤永遠紅（依 chart-up / chart-down market-aware）
```

##### `TradesTable` 元件規格

```typescript
interface Props {
  trades: Array<{
    entry_date: string;
    exit_date: string;
    side: "long" | "short";
    entry_price: number;
    exit_price: number;
    shares: number;
    pnl: number;
    return_pct: number;
  }>;
  currency: "TWD" | "USD";
}

// shadcn <Table> 包 <TableHeader> + <TableBody>
// 欄位（6 欄）：
//   日期區間（entry → exit）| 方向 | 進場價 | 出場價 | 數量（股）| PnL（含 return_pct 副標）
//   注意：引擎回傳的 shares 單位為「股」，台股與美股統一顯示「股」，不轉換為「張」（與舊 Streamlit 回測頁一致）
// 每頁 20 筆，<Pagination> 元件
// 預設按 entry_date DESC
```

##### `use-backtest-job.ts` hook 規格

```typescript
interface UseBacktestJobReturn<TResult> {
  status: "idle" | "running" | "complete" | "error" | "cancelled";
  progress: { current: number; total: number; phase?: string; meta?: any } | null;
  result: TResult | null;
  error: { code: string; message: string } | null;
  start: (type: string, params: Record<string, any>) => Promise<void>;
  cancel: () => Promise<void>;
}

// 內部：
//   1. POST /api/jobs { type, params } → job_id
//   2. open EventSource on /api/jobs/{job_id}/events
//   3. parse SSE events: progress / result / error
//   4. cancel(): POST /api/jobs/{job_id}/cancel
//   5. unmount cleanup: close EventSource
//   6. status 轉 "complete" / "cancelled" / "error" 時呼叫 useToast() 的對應 method
//      (10-G-1 toast 系統的整合點)
//   7. fetch 失敗、SSE 中斷、invalid JSON：set status="error" + error message；不丟給 ErrorBoundary
//   8. 切換市場時 reset state（清空已有回測結果，避免殘留前一市場資料）
```

##### WFA 特化 progress interface（10-E-4 擴充）

WFA 的 SSE 推送 `window_progress` 和 `sweep_progress` 兩種 event type，hook 需特化處理：

```typescript
interface WfaProgress {
  windowId: number;
  totalWindows: number;
  phase: "is_sweep" | "oos_validate" | "done";
  bestParams?: Record<string, any>;   // phase=done 時帶
  oosSharpe?: number;                  // phase=done 時帶
  sweepCurrent?: number;               // is_sweep 期間帶
  sweepTotal?: number;                 // is_sweep 期間帶
}

// 10-E-4 實作時，use-backtest-job.ts 對 WFA job type 走特化分支：
// - 監聽 "window_progress" 與 "sweep_progress" 兩種 event type
// - 回傳 wfaProgress: WfaProgress | null（取代通用 progress）
// - 其餘 status / result / error / start / cancel 介面不變
```

##### `BacktestPage` 整體結構樣板（含 10-G-1 整合）

```tsx
// web/src/app/backtest/page.tsx
import { ErrorBoundary } from "@/components/error-boundary";  // 來自 10-G-1
import { useCommandPaletteEntry } from "@/hooks/use-command-palette";  // 來自 10-G-1
import { useRouter } from "next/navigation";

export default function BacktestPage() {
  const router = useRouter();

  // 註冊 4 個 Command Palette entries（10-G-1 提供 hook）
  useCommandPaletteEntry({ id: "bt-single", label: "回測：單次", action: () => router.push("/backtest?tab=single") });
  useCommandPaletteEntry({ id: "bt-batch",  label: "回測：策略比較", action: () => router.push("/backtest?tab=batch") });
  useCommandPaletteEntry({ id: "bt-sweep",  label: "回測：參數掃描", action: () => router.push("/backtest?tab=sweep") });
  useCommandPaletteEntry({ id: "bt-wfa",    label: "回測：Walk-Forward", action: () => router.push("/backtest?tab=wfa") });

  return (
    <ErrorBoundary fallback={<BacktestErrorFallback />}>
      <BacktestTabs />
    </ErrorBoundary>
  );
}
```

##### `SingleRunTab` skeleton 切換模式（10-E-1）

```tsx
// web/src/components/backtest/SingleRunTab.tsx
import { CardSkeleton, ChartSkeleton, TableSkeleton } from "@/components/skeletons";  // 來自 10-G-1
import { useToast } from "@/hooks/use-toast";  // 來自 10-G-1
import { useBacktestJob } from "@/hooks/use-backtest-job";

export function SingleRunTab() {
  const { status, progress, result, error, start, cancel } = useBacktestJob<BacktestRunResult>();
  const toast = useToast();

  // 由 use-backtest-job 內部觸發 toast（status 變化時）
  // 此處不需重複呼叫

  return (
    <div className="space-y-4">
      <SingleRunForm onSubmit={(params) => start("backtest_run", params)} />

      {status === "idle" && (
        <p className="text-muted-foreground">設定參數後按「開始回測」</p>
      )}

      {status === "running" && (
        <>
          <BacktestProgressBar current={progress?.current} total={progress?.total} onCancel={cancel} />
          <div className="grid grid-cols-5 gap-2">
            {[1,2,3,4,5].map(i => <CardSkeleton key={i} />)}
          </div>
          <ChartSkeleton height={460} />
          <ChartSkeleton height={200} />
          <TableSkeleton rows={5} />
        </>
      )}

      {status === "complete" && result && (
        <>
          <TearsheetCards metrics={result.metrics} currency={result.currency} isDca={result.strategy_type === "dollar_cost_averaging"} />
          <CandleChartWithMarkers priceData={result.price_data} signals={result.signals} market={result.market} maOverlay={...} />
          <EquityCurveChart series={[{ name: "Equity", data: result.equity_curve, color: "var(--chart-primary)" }]} />
          <TradesTable trades={result.trades} currency={result.currency} />
        </>
      )}

      {status === "error" && error && (
        <div className="rounded-lg border border-destructive bg-destructive/10 p-4 text-destructive">
          <p className="font-semibold">執行失敗</p>
          <p className="text-sm">{error.message}（代碼：{error.code}）</p>
        </div>
      )}

      {status === "cancelled" && result && (
        // 取消後仍渲染 partial result（與 complete 同邏輯，只是上方加一條提示）
        <CancelledResultBanner />
      )}
    </div>
  );
}
```

##### Toast 訊息文案規範（全 10-E 共用）

| 場景 | Toast 類型 | 文案 |
|:---|:---|:---|
| 單次回測 complete | success | `回測完成（{strategy_name}）` |
| 單次回測 cancel | info | `回測已取消` |
| 單次回測 error | error | `回測失敗：{error.message}` |
| 批次比較 complete | success | `比較完成（{succeeded} 成功 / {failed} 失敗）` |
| 批次比較 cancel | info | `比較已取消（已完成 {done}/{total}）` |
| 參數掃描 complete | success | `掃描完成（{valid_combos} 個合法組合）` |
| 參數掃描 cancel | info | `掃描已取消（已完成 {done}/{total}）` |
| 參數掃描 over_max | error | `合法組合數 {n} 超過上限 200，請縮小參數範圍` |
| Heatmap 複製 | success | `參數已複製：{param_str}` |
| WFA complete | success | `WFA 分析完成（{n_windows} 段視窗）` |
| WFA cancel | info | `WFA 已取消（已完成 {done}/{total} 段視窗）` |
| WFA insufficient_data | error | `資料不足：需要 {required} 個月、目前 {actual} 個月` |
| CSV 下載 | success | `已下載：{filename}` |

##### 表單元件對應

| 老 Streamlit 元件 | shadcn / 新元件 |
|:---|:---|
| `st.selectbox("市場")` | 既有 `MarketSwitcher` |
| `st.text_input("股票代碼")` + 搜尋 | 既有 `StockSelector` |
| `st.date_input` | 新 `DateRangePicker`（shadcn Calendar + Popover） |
| `st.selectbox("回測引擎")` | 新 `EngineSelect`（shadcn Select） |
| `st.selectbox("策略")` | 新 `StrategyPresetSelect`（shadcn Select；讀 `/api/config`） |
| `st.number_input("初始資金")` | shadcn `Input type="number"` |
| `st.button("開始回測")` | shadcn `Button` |
| `st.spinner` + `st.progress` | 新 `BacktestProgressBar`（重用 10-C-2 樣式） |

#### 10-E-2：策略比較（批次）

##### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `src/services/backtest_service.py` | 修改 | 新增 `run_batch_backtest_job(manager, job, params)`；包裝既有 `run_strategy_batch()` 並插 progress callback |
| `api/routers/jobs.py` | 修改 | dispatcher 加 `backtest_batch` 分支 |
| `api/routers/jobs.py` | 修改 | `GET /api/jobs/{id}/result?format=csv` 對 batch job 走 `save_batch_result_csv()` |
| `web/src/components/backtest/BatchCompareTab.tsx` | 新增 | 比較 tab 主元件 |
| `web/src/components/backtest/StrategyMultiSelect.tsx` | 新增 | 策略多選（shadcn Checkbox + Card） |
| `web/src/components/backtest/ComparisonTable.tsx` | 新增 | 10 欄比較表 + sortable + row 展開 |
| `web/src/components/backtest/MultiEquityChart.tsx` | 新增 | 多線 equity 疊圖（lightweight-charts 多 LineSeries + 顏色 palette + legend） |

##### `MultiEquityChart` 色票

```typescript
const STRATEGY_COLORS = [
  "#3b82f6", "#ef4444", "#10b981", "#f59e0b",
  "#8b5cf6", "#06b6d4", "#ec4899", "#84cc16",
];  // 8 高對比色，對應最多 8 個 preset
```

#### 10-E-3：參數掃描

##### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `src/services/backtest_service.py` | 修改 | 新增 `run_sweep_backtest_job()`；包裝 `run_parameter_sweep()` 並插 progress callback；throttle 邏輯（每 5 個 combo 推一次 progress，當 valid_combos > 50） |
| `api/routers/jobs.py` | 修改 | dispatcher 加 `backtest_sweep` 分支 |
| `web/src/components/backtest/SweepTab.tsx` | 新增 | 掃描 tab 主元件 |
| `web/src/components/backtest/ParamGridForm.tsx` | 新增 | 動態渲染策略參數的逗號分隔輸入欄；解析 + 預設值 + 合法組合數提示 |
| `web/src/components/backtest/SweepRankingTable.tsx` | 新增 | Top N 排名表（含 sample_warning icon） |
| `web/src/components/backtest/SweepHeatmap.tsx` | 新增 | 自製 CSS Grid heatmap（僅 2 參數時 mount） |
| `web/src/lib/sweep-defaults.ts` | 新增 | 從 [src/ui/pages/backtest.py:349-357](src/ui/pages/backtest.py:349) `_SWEEP_DEFAULTS` 移植過來 |

##### `sweep-defaults.ts` 完整內容

```typescript
// web/src/lib/sweep-defaults.ts
// 移植自 src/ui/pages/backtest.py:349-357 _SWEEP_DEFAULTS

export const SWEEP_DEFAULTS: Record<string, Record<string, string>> = {
  moving_average_cross: { short_window: "5,10,20", long_window: "40,60,120" },
  rsi:                  { period: "7,14,21", oversold: "20,30", overbought: "70,80" },
  kd_cross:             { k_period: "9,14", d_period: "3,5", smooth_k: "3,5" },
  macd_cross:           { fast: "8,12", slow: "20,26", signal: "7,9" },
  bollinger_band:       { period: "10,20,30", std_dev: "1.5,2.0,2.5" },
  bias:                 { ma_period: "10,20,30", buy_bias: "-15,-10,-5", sell_bias: "5,10,15" },
  donchian_breakout:    { entry_period: "10,20,55", exit_period: "5,10,20" },
};

// 參數型別（用於前端驗證：int 欄位剔除小數、float 欄位允許小數）
export const PARAM_TYPES: Record<string, Record<string, "int" | "float">> = {
  moving_average_cross: { short_window: "int", long_window: "int" },
  rsi:                  { period: "int", oversold: "float", overbought: "float" },
  kd_cross:             { k_period: "int", d_period: "int", smooth_k: "int" },
  macd_cross:           { fast: "int", slow: "int", signal: "int" },
  bollinger_band:       { period: "int", std_dev: "float" },
  bias:                 { ma_period: "int", buy_bias: "float", sell_bias: "float" },
  donchian_breakout:    { entry_period: "int", exit_period: "int" },
};
```

DCA（`dollar_cost_averaging`）**不支援參數掃描**；前端策略類型下拉不包含 DCA。

##### `SweepHeatmap` 色階規則

```typescript
// 將 Sharpe 值映射到色階：
function sharpeToColor(sharpe: number): string {
  // -2.0 ~ 2.0 線性映射
  // < 0       : red 飽和度依絕對值升高
  // 0 ~ 0.5  : yellow-amber
  // 0.5 ~ 1.0: lime
  // 1.0 ~ 2.0: green 飽和度升高
  // 用 tailwind 既有色階：red-{600,500,400,300,200} / amber-{300,400} / lime-{300,400} / green-{400,500,600,700}
}
```

```tsx
<div className="grid" style={{ gridTemplateColumns: `repeat(${nXValues + 1}, minmax(0, 1fr))` }}>
  {/* corner cell */}
  <div></div>
  {/* X axis header */}
  {xValues.map(x => <div key={x} className="text-xs">{x}</div>)}
  {/* rows */}
  {yValues.map(y => (
    <>
      <div className="text-xs">{y}</div>
      {xValues.map(x => {
        const cell = matrix[y][x];
        return (
          <div
            className="p-2 text-xs cursor-pointer hover:ring-2"
            style={{ backgroundColor: sharpeToColor(cell.sharpe) }}
            onClick={() => copyParamsToClipboard(cell.params)}
            title={`Sharpe: ${cell.sharpe.toFixed(2)} | 總報酬: ${(cell.totalReturn*100).toFixed(1)}%`}
          >
            {cell.sharpe.toFixed(2)}
          </div>
        );
      })}
    </>
  ))}
</div>
```

##### Throttle 邏輯

```python
# src/services/backtest_service.py
async def run_sweep_backtest_job(manager, job, params):
    # ...
    valid_combos = generate_param_grid(strategy_type, candidates)
    n = len(valid_combos)
    throttle_step = 5 if n > 50 else 1

    results = []
    for i, combo in enumerate(valid_combos, 1):
        if manager.get_job(job.id).status == "cancelled":
            break

        # ... run one combo
        results.append(summary)

        if i % throttle_step == 0 or i == n:
            manager.push_event(job.id, "progress", {
                "current": i, "total": n,
                "current_params": combo,
                "status": "running" if i < n else "done",
            })

    partial_result = {...}
    manager.push_event(job.id, "result", partial_result)
    current = manager.get_job(job.id)
    if current and current.status == "cancelled":
        manager.finish_cancelled_job(job.id, result=partial_result)
    else:
        manager.complete_job(job.id, result=partial_result)
```

#### 10-E-4：Walk-Forward Analysis

##### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `src/services/backtest_service.py` | 修改 | 新增 `run_walk_forward_backtest_job()`；包裝 `run_walk_forward_analysis()` 並插 nested progress callback |
| `api/routers/jobs.py` | 修改 | dispatcher 加 `backtest_wfa` 分支 |
| `api/routers/jobs.py` | 修改 | `GET /api/jobs/{id}/result?format=csv&part=window|stability` 兩種 CSV |
| `web/src/components/backtest/WalkForwardTab.tsx` | 新增 | WFA tab 主元件 |
| `web/src/components/backtest/WfaSummaryCards.tsx` | 新增 | 5 metric card + degradation chip |
| `web/src/components/backtest/WfaWindowTable.tsx` | 新增 | window 表 |
| `web/src/components/backtest/WfaStabilityTable.tsx` | 新增 | stability 表（含 CV 標籤） |

##### 巢狀進度 SSE 事件規格

兩種 event type 區分：

```python
# 外層 window 進度
manager.push_event(job.id, "window_progress", {
    "window_id": w.window_id,
    "total_windows": total_w,
    "phase": "is_sweep" | "oos_validate" | "done",
    "best_params": w.best_params,  # 僅 phase=done 時帶
    "oos_sharpe": ...,              # 僅 phase=done 時帶
})

# 內層 sweep 進度（IS 期內的 grid search）
manager.push_event(job.id, "sweep_progress", {
    "window_id": w.window_id,
    "current": i, "total": n,
    "current_params": combo,
})
```

前端 `use-backtest-job.ts` 對 WFA 走特化分支，回傳 `{ windowProgress, sweepProgress, ... }`。

##### CV 穩定性標籤規則

```typescript
function stabilityLabel(cv: number): { text: string; color: string } {
  if (cv < 0.2) return { text: "穩定", color: "text-green-500" };
  if (cv < 0.5) return { text: "中等", color: "text-amber-500" };
  return { text: "不穩定", color: "text-red-500" };
}
```

##### CSV 匯出端點

```python
# api/routers/jobs.py
@router.get("/jobs/{job_id}/result")
def get_backtest_csv(
    job_id: str,
    format: str | None = None,
    part: str | None = None,
    manager: JobManager = Depends(get_manager),
):
    job = manager.get_job(job_id)
    # ... validate complete

    if format == "csv":
        if job.type == "backtest_batch":
            blob = build_batch_csv_blob(job.result)
            filename = f"batch_{symbol}_{ts}.csv"
        elif job.type == "backtest_sweep":
            blob = build_sweep_csv_blob(job.result)
            filename = f"sweep_{symbol}_{strategy_type}_{ts}.csv"
        elif job.type == "backtest_wfa":
            if part == "window":
                blob = build_wfa_window_csv_blob(job.result)
                filename = f"wfa_window_{symbol}_{ts}.csv"
            elif part == "stability":
                blob = build_wfa_stability_csv_blob(job.result)
                filename = f"wfa_stability_{symbol}_{ts}.csv"
        return StreamingResponse(blob, media_type="text/csv",
                                 headers={"Content-Disposition": f'attachment; filename="{filename}"'})
    # default JSON
    return {"data": job.result, "meta": {"job_id": job_id}}
```

CSV 內容沿用既有 `save_batch_result_csv()` / `save_sweep_result_csv()` / `save_walk_forward_summary_csv()` 邏輯（移植到 `build_*_csv_blob()`，回 io.StringIO 而非寫檔）。

**函式位置**：`build_batch_csv_blob()` / `build_sweep_csv_blob()` / `build_wfa_window_csv_blob()` / `build_wfa_stability_csv_blob()` 放在 `src/services/backtest_service.py`（service 層負責產生 CSV blob，router 只負責 wrap 成 `StreamingResponse`）。

---

### 10-F AI 問答頁

10-F 拆成兩階段：**10-F-1 UI shell + 後端 lock（不接 LLM）** 與 **10-F-2 接 LLM（延後）**。本節依此順序拆寫。

#### 10-F-1：UI shell + 後端 lock

##### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `api/routers/ai.py` | 修改 | 新增 `GET /api/ai/status` 與 `POST /api/ai/chat`（兩者均為 lock 骨架，**不引入 sse-starlette**） |
| `web/src/app/ai/page.tsx` | 修改 | 由 placeholder 殼改為實際頁面，掛上 `ChatPageClient` |
| `web/src/components/ai/chat-page-client.tsx` | 新增 | client component，握有訊息 state、組合 disclaimer-gate / message-list / chat-input |
| `web/src/components/ai/disclaimer-gate.tsx` | 新增 | 免責聲明卡 + 「我了解」按鈕；localStorage key `ai_chat.disclaimer_accepted_v1` |
| `web/src/components/ai/message-bubble.tsx` | 新增 | user / assistant 兩種樣式；內容以 `react-markdown` + `remark-gfm` 渲染 |
| `web/src/components/ai/chat-input.tsx` | 新增 | 輸入框 + 送出按鈕（Enter 送、Shift+Enter 換行可選） |
| `web/src/hooks/use-mock-chat.ts` | 新增 | 純前端 mock：送出 → 立即 push user message → setInterval 逐 char push 至 assistant placeholder |
| `web/src/hooks/use-ai-status.ts` | 新增 | SWR 讀 `/api/ai/status`，回 `{ available, reason, message }` |
| `web/src/components/sidebar.tsx` | 修改 | 「AI 問答」項目旁加灰色徽章「後續開放」 |
| `pyproject.toml` | 修改 | version `0.1.0` → `0.2.0` |
| `web/package.json` | 修改 | version `0.10.0` → `0.2.0`（與 py 對齊） |

##### 套件依賴新增（web/）

```bash
pnpm add react-markdown remark-gfm
```

##### `/api/ai/status` 與 `/api/ai/chat`（lock 骨架）

```python
# api/routers/ai.py 追加（與既有 /analyze 同檔）
from fastapi import HTTPException
from pydantic import BaseModel

class ChatRequest(BaseModel):
    messages: list[dict] = []  # 10-F-1 不驗 schema，10-F-2 再嚴謹化

@router.get("/status")
def get_ai_status() -> dict:
    return {
        "available": False,
        "reason": "feature_locked",
        "message": "AI 功能尚未開放，將於後續版本啟用。",
    }

@router.post("/chat")
def post_ai_chat(_: ChatRequest) -> None:
    raise HTTPException(
        status_code=503,
        detail={"error": {"code": "AI_DISABLED", "message": "AI 功能尚未開放。"}},
    )
```

**注意：** 10-F-1 端點都是同步（無 SSE）；待 10-F-2 才會引入 `sse-starlette.EventSourceResponse` 並把 `post_ai_chat` 改成 async generator。

##### Mock 逐字串流（`use-mock-chat.ts`）

```typescript
const MOCK_REPLY =
  "AI 串接尚未開放（這是 UI 預覽）。\n\n本訊息為模擬輸出，待 Phase 10-F-2 接上真實 LLM 後將改為串流逐字回應。";
const CHAR_DELAY_MS = 25;  // 20-40ms 區間中位數

function sendMockMessage(userText: string) {
  setMessages(prev => [...prev, { role: "user", content: userText }]);
  setMessages(prev => [...prev, { role: "assistant", content: "" }]);
  let i = 0;
  const timer = setInterval(() => {
    i += 1;
    setMessages(prev => {
      const next = [...prev];
      next[next.length - 1] = { role: "assistant", content: MOCK_REPLY.slice(0, i) };
      return next;
    });
    if (i >= MOCK_REPLY.length) clearInterval(timer);
  }, CHAR_DELAY_MS);
}
```

未來 10-F-2 時，整個 mock 串流邏輯只要把 `setInterval` 換成 EventSource onmessage handler，訊息結構與動畫節奏不必改。

##### 訊息歷史策略

- 純 React state（`useState<Message[]>`）
- **不**持久化（不寫 localStorage）；F5 重整後重置為 `INITIAL_MESSAGES`（一筆 greeting）
- 與未來 10-F-2 行為一致；若 10-F-2 要 localStorage，再另外設計

#### 10-F-2：接 LLM（延後實作）

##### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `src/ai/advisor.py` | 修改 | 新增 `AIAdvisor.stream_chat(messages)` async generator |
| `src/ai/advisor.py` 三 adapter | 修改 | `AnthropicAdapter` / `OpenAIAdapter` / `GeminiAdapter` 各加 `stream_complete()` |
| `api/routers/ai.py` | 修改 | `POST /api/ai/chat` 改 async + `EventSourceResponse`，`GET /api/ai/status` 改為依 config 動態回傳 |
| `web/src/hooks/use-mock-chat.ts` | 改名 / 取代 | 改成 `use-ai-chat.ts`，接真實 SSE |
| `pyproject.toml` | 修改 | 確認 `sse-starlette` 依賴已在（10-A 應已加） |

##### SSE 串流（規格未來實作參考）

```python
# api/routers/ai.py（10-F-2 版本）
from sse_starlette import EventSourceResponse
import json

@router.post("/chat")
async def post_ai_chat(request: ChatRequest):
    advisor = get_advisor()  # 從 deps 注入
    async def generate():
        try:
            async for chunk in advisor.stream_chat(request.messages):
                yield {"event": "token", "data": json.dumps({"text": chunk})}
            yield {"event": "done", "data": "{}"}
        except Exception as exc:
            yield {"event": "error", "data": json.dumps({"message": str(exc)})}
    return EventSourceResponse(generate())
```

前端使用 `EventSource` 或 `fetch` + `ReadableStream` 逐字接收。

##### 待決問題（啟動 10-F-2 前必須回答）

1. Chat 模式是否啟用 tool use？現有 `ask()` 跑 6 輪 tool（get_price / calc_indicators / get_support_resistance）。串流 + tool 是兩種典範。
2. Messages payload 格式 — OpenAI 標準 `[{role, content}]` 還是 provider-native？
3. 取消（abort）行為與 SSE error event 規格
4. 多輪歷史是否改為 localStorage 持久化
5. Markdown 渲染與 token 串流的協調（部分 token 未閉合時的中間態）

##### 時程備註

- 10-F-2 **不卡 10-G / 10-H** 收尾
- 10-H 移除 `src/ui/` 時 `src/ai/advisor.py` 必須保留（10-F-2 與 dashboard `/api/ai/analyze` 都仍會用）

---

### 10-G 設定頁 + 全局整合

10-G 拆 2 段交付：**10-G-1（基礎設施先行）→ 10-E（4 段）→ 10-G-2（設定頁主功能）**。10-G-1 提供 10-E 所需的全局元件；10-G-2 才做設定頁本身。

#### 10-G-1：基礎設施（Toast / Error Boundary / Skeleton / Command Palette）

##### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `web/package.json` | 修改 | 新增 `sonner` + `cmdk`；移除 `@radix-ui/react-toast` |
| `web/src/components/providers.tsx` | 新增 | 全局 Provider wrapper：Sonner `<Toaster>` + CommandPalette mount + `<ThemeProvider>` 預留位（10-G-2 才裝 next-themes） |
| `web/src/app/layout.tsx` | 修改 | `<body>` 內最外層改用 `<Providers>` 包住 |
| `web/src/hooks/use-toast.ts` | 新增 | 封裝 sonner 為 `{ success, error, info, dismiss }`；預設 duration 3000ms |
| `web/src/components/error-boundary.tsx` | 新增 | React class 元件 + `<DefaultErrorFallback />` |
| `web/src/components/skeletons/index.tsx` | 新增 | `CardSkeleton` / `ChartSkeleton` / `TableSkeleton` 三個變體（shadcn `Skeleton` 為底） |
| `web/src/components/command-palette.tsx` | 新增 | cmdk 包裝 + Dialog overlay + 群組（pages / stocks） |
| `web/src/hooks/use-command-palette.ts` | 新增 | `useCommandPaletteEntry({ id, label, action, group? })` + Zustand-like in-memory store（不引入 Zustand，自製） |
| `web/src/components/data/data-page-client.tsx` | 修改 | 5 處 banner 改 `useToast()` |
| `web/src/components/sidebar.tsx` | 修改 | mount 時註冊 5 個頁面跳轉 entry |
| `web/src/components/stock-selector.tsx` | 修改 | 註冊「股票搜尋」群組（debounce 200ms 查 `/api/data/symbols`） |
| `web/src/tests/hooks/use-toast.test.tsx` | 新增 | 驗證 success/error/info 呼叫進入 sonner queue、duration 預設 3000 |
| `web/src/tests/components/error-boundary.test.tsx` | 新增 | render 內 throw 時顯示 fallback、reset 後回正常 |
| `web/src/tests/components/skeletons.test.tsx` | 新增 | 三變體可渲染、accept className |
| `web/src/tests/components/command-palette.test.tsx` | 新增 | Ctrl+K 開啟、/ 開啟並 focus、Esc 關閉、選擇 entry 觸發 action |
| `web/src/tests/hooks/use-command-palette.test.tsx` | 新增 | 註冊 / 註銷、同 id 去重 |

##### 套件依賴

```bash
cd web && pnpm add sonner cmdk
cd web && pnpm remove @radix-ui/react-toast   # 10-D 連帶裝入但未使用，Tooltip 不受影響
```

##### `Providers` 樹結構

```tsx
// web/src/components/providers.tsx
"use client";
import { Toaster } from "sonner";
import { CommandPalette } from "@/components/command-palette";

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <>
      {/* 10-G-2 才會在這裡加 <ThemeProvider attribute="class" defaultTheme="dark" enableSystem={false}> */}
      {children}
      <Toaster position="bottom-right" duration={3000} richColors closeButton />
      <CommandPalette />
    </>
  );
}
```

##### `useToast()` hook（最終形）

```typescript
// web/src/hooks/use-toast.ts
import { toast } from "sonner";

export type ToastApi = {
  success: (msg: string, opts?: { duration?: number }) => void;
  error:   (msg: string, opts?: { duration?: number }) => void;
  info:    (msg: string, opts?: { duration?: number }) => void;
  dismiss: (id?: string | number) => void;
};

const DEFAULT_DURATION = 3000;

export function useToast(): ToastApi {
  return {
    success: (msg, opts) => toast.success(msg, { duration: opts?.duration ?? DEFAULT_DURATION }),
    error:   (msg, opts) => toast.error(msg,   { duration: opts?.duration ?? DEFAULT_DURATION }),
    info:    (msg, opts) => toast.info(msg,    { duration: opts?.duration ?? DEFAULT_DURATION }),
    dismiss: (id) => toast.dismiss(id),
  };
}
```

##### Error Boundary

```tsx
// web/src/components/error-boundary.tsx
"use client";
import React from "react";

type Props = {
  children: React.ReactNode;
  fallback?: React.ReactNode | ((error: Error, reset: () => void) => React.ReactNode);
};
type State = { error: Error | null };

export class ErrorBoundary extends React.Component<Props, State> {
  state: State = { error: null };

  static getDerivedStateFromError(error: Error): State {
    return { error };
  }

  componentDidCatch(error: Error, info: React.ErrorInfo) {
    // dev 時 console.error；production 可送遙測（目前不送）
    if (process.env.NODE_ENV !== "production") console.error("[ErrorBoundary]", error, info);
  }

  reset = () => this.setState({ error: null });

  render() {
    if (this.state.error) {
      const { fallback } = this.props;
      if (typeof fallback === "function") return fallback(this.state.error, this.reset);
      return fallback ?? <DefaultErrorFallback error={this.state.error} reset={this.reset} />;
    }
    return this.props.children;
  }
}

export function DefaultErrorFallback({ error, reset }: { error: Error; reset: () => void }) {
  return (
    <div className="rounded-lg border border-destructive bg-destructive/10 p-4 text-destructive">
      <p className="font-semibold">執行發生錯誤</p>
      <p className="text-sm mt-1">{error.message}</p>
      {process.env.NODE_ENV !== "production" && (
        <pre className="mt-2 text-xs overflow-auto max-h-40">{error.stack}</pre>
      )}
      <button onClick={reset} className="mt-3 px-3 py-1 rounded bg-destructive text-destructive-foreground text-sm">
        重置
      </button>
    </div>
  );
}
```

##### Skeleton 變體

```tsx
// web/src/components/skeletons/index.tsx
import { cn } from "@/lib/utils";

function Skeleton({ className }: { className?: string }) {
  return <div className={cn("animate-pulse rounded-md bg-muted", className)} />;
}

export function CardSkeleton({ className }: { className?: string }) {
  return (
    <div className={cn("rounded-lg border p-3 space-y-2", className)}>
      <Skeleton className="h-3 w-16" />
      <Skeleton className="h-7 w-24" />
    </div>
  );
}

export function ChartSkeleton({ height = 400, className }: { height?: number; className?: string }) {
  return <Skeleton className={cn("w-full", className)} style={{ height }} />;
}

export function TableSkeleton({ rows = 5, columns = 4, className }: { rows?: number; columns?: number; className?: string }) {
  return (
    <div className={cn("space-y-2", className)}>
      {Array.from({ length: rows }).map((_, i) => (
        <div key={i} className="grid gap-2" style={{ gridTemplateColumns: `repeat(${columns}, minmax(0, 1fr))` }}>
          {Array.from({ length: columns }).map((_, j) => <Skeleton key={j} className="h-6" />)}
        </div>
      ))}
    </div>
  );
}
```

##### Command Palette（cmdk 包裝）

```tsx
// web/src/components/command-palette.tsx
"use client";
import { Command } from "cmdk";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { useCommandEntries } from "@/hooks/use-command-palette";

export function CommandPalette() {
  const [open, setOpen] = useState(false);
  const router = useRouter();
  const entries = useCommandEntries();

  useEffect(() => {
    const onKeyDown = (e: KeyboardEvent) => {
      if ((e.key === "k" && (e.metaKey || e.ctrlKey)) || e.key === "/") {
        // 避免在 input/textarea 內按 `/` 也觸發
        const tag = (e.target as HTMLElement)?.tagName;
        if (e.key === "/" && (tag === "INPUT" || tag === "TEXTAREA")) return;
        e.preventDefault();
        setOpen((v) => !v);
      }
      if (e.key === "Escape") setOpen(false);
    };
    window.addEventListener("keydown", onKeyDown);
    return () => window.removeEventListener("keydown", onKeyDown);
  }, []);

  return (
    <Command.Dialog open={open} onOpenChange={setOpen} label="全域指令">
      <Command.Input autoFocus placeholder="輸入頁面或股票代碼..." />
      <Command.List>
        <Command.Empty>找不到結果</Command.Empty>
        {/* 內部依 group 分組渲染 entries；點擊呼叫 entry.action 後關閉 */}
      </Command.List>
    </Command.Dialog>
  );
}
```

##### `useCommandPaletteEntry` hook + store

```typescript
// web/src/hooks/use-command-palette.ts
"use client";
import { useEffect } from "react";

export type CommandEntry = {
  id: string;
  label: string;
  group?: "pages" | "stocks";
  action: () => void;
};

let entries: CommandEntry[] = [];
const listeners = new Set<() => void>();

function emit() { listeners.forEach((fn) => fn()); }

import { useSyncExternalStore } from "react";

function subscribe(fn: () => void) {
  listeners.add(fn);
  return () => { listeners.delete(fn); };
}
function getSnapshot() { return entries; }

export function useCommandEntries(): CommandEntry[] {
  return useSyncExternalStore(subscribe, getSnapshot, getSnapshot);
}

export function useCommandPaletteEntry(entry: CommandEntry) {
  useEffect(() => {
    entries = [...entries.filter((e) => e.id !== entry.id), entry];
    emit();
    return () => {
      entries = entries.filter((e) => e.id !== entry.id);
      emit();
    };
  }, [entry.id, entry.label, entry.group, entry.action]);
}
```

> 實作端注意：上方 store 為示意；正式版用 React 19 `useSyncExternalStore` + 普通 module-scope state 即可，不引入 Zustand。

##### Sidebar 註冊 5 個頁面 entry

```tsx
// web/src/components/sidebar.tsx 內某處
useCommandPaletteEntry({ id: "nav-dashboard", group: "pages", label: "個股分析", action: () => router.push("/dashboard") });
useCommandPaletteEntry({ id: "nav-data",      group: "pages", label: "資料管理", action: () => router.push("/data") });
useCommandPaletteEntry({ id: "nav-backtest",  group: "pages", label: "回測",     action: () => router.push("/backtest") });
useCommandPaletteEntry({ id: "nav-ai",        group: "pages", label: "AI 問答",  action: () => router.push("/ai") });
useCommandPaletteEntry({ id: "nav-settings",  group: "pages", label: "設定",     action: () => router.push("/settings") });
```

> 10-E-1 額外註冊 4 個 backtest tab entry（見 [10-E-1 BacktestPage 樣板](開發設計方針.md:7172)）。

##### 10-C-2 banner → toast 遷移清單

| 觸發點 | 既有 inline | 改為 toast |
|:---|:---|:---|
| 「全部更新」完成 | 綠色 banner「N 個成功、M 個失敗（{失敗清單}）」 | `toast.success("更新完成：{succeeded} 成功 / {failed} 失敗")`；失敗 > 0 用 `toast.error(...)` |
| 「全部重建」完成 | 同上 | 同上 |
| 「+ 新增標的」成功 | 關 Dialog 即可（無訊息） | `toast.success("已新增標的：{symbol}")` |
| 「動作欄·更新」成功 | inline tick icon | `toast.success("已更新：{symbol}")` |
| DELETE 確認後 | inline 訊息 | `toast.success("已刪除：{symbol}")` |

##### 鍵盤快捷鍵約定

| 按鍵 | 行為 |
|:---|:---|
| `Ctrl+K` / `Cmd+K` | 開啟 / 關閉 Command Palette |
| `/` | 開啟 Command Palette 並 focus input（若當前 focus 在 INPUT / TEXTAREA 則不攔截，讓使用者繼續打字） |
| `Esc` | 關閉 Command Palette / Dialog |

##### Error Boundary 使用慣例

- **不**在 `app/layout.tsx` 全局包一層 —— 否則整頁掛掉時 sidebar 也消失。改在各 page 內包；如 `BacktestPage` 內包 `<BacktestTabs />`、`DashboardPage` 內包主內容
- Default fallback 已足；只在文案需特化時才傳自訂 `fallback`
- 不接 fetch / SSE 失敗 —— 那些走 toast 並由 hook 自行 setState

---

#### 10-G-2：設定頁主功能（API Key / 策略 preset / 主題 / AI toggle）

##### 新增 / 修改檔案

| 檔案 | 動作 | 說明 |
|:---|:---|:---|
| `api/routers/config.py` | 修改 | 新增 `POST /api/config/strategies`、`DELETE /api/config/strategies/{name}`、`POST /api/config/strategies/restore` |
| `src/services/config_service.py` | 修改 | 新增 `delete_strategy_preset_by_name(name)` |
| `web/package.json` | 修改 | 新增 `next-themes` |
| `web/src/components/providers.tsx` | 修改 | 在最外層加 `<ThemeProvider attribute="class" defaultTheme="dark" enableSystem={false}>` |
| `web/src/app/settings/page.tsx` | 修改 | 由 19 行 placeholder 改為四分區實作 |
| `web/src/components/settings/secrets-section.tsx` | 新增 | 5 provider 區（openai / anthropic / gemini / finmind / google） |
| `web/src/components/settings/strategy-presets-section.tsx` | 新增 | preset 列表 + 新增 / 編輯 / 刪除 Dialog + 重置預設 |
| `web/src/components/settings/strategy-preset-dialog.tsx` | 新增 | 新增 / 編輯 共用 Dialog（form 含 name / strategy / params / market） |
| `web/src/components/settings/theme-section.tsx` | 新增 | Dark / Light 切換 toggle |
| `web/src/components/settings/ai-toggle-section.tsx` | 新增 | AI 開關（永遠 disabled + tooltip） |
| `web/src/hooks/use-config.ts` | 新增 | SWR hook 讀 `/api/config` / `/api/config/secrets/status` / `/api/config/strategies` |
| `tests/test_api/test_config_api.py` | 修改 | 補 POST upsert / DELETE by name / POST restore / INVALID_PRESET 422 / idempotent 測試 |
| `tests/test_services/test_config_svc.py` | 修改 | 補 `delete_strategy_preset_by_name` + idempotent |
| `web/src/tests/components/settings/*.test.tsx` | 新增 | 4 個分區元件測試 + preset Dialog 表單驗證 |
| `web/src/tests/hooks/use-config.test.ts` | 新增 | SWR mock 驗證 |

##### 套件依賴

```bash
cd web && pnpm add next-themes
```

##### 後端策略 preset 端點

```python
# api/routers/config.py（追加）
from src.services.config_service import (
    upsert_strategy_preset,
    delete_strategy_preset_by_name,   # 新增
    restore_strategy_defaults,
)

class StrategyPresetUpsertRequest(BaseModel):
    preset: dict[str, Any]

@router.post("/strategies", status_code=201)
def upsert_strategy(request: StrategyPresetUpsertRequest) -> dict[str, Any]:
    try:
        upsert_strategy_preset(request.preset)
    except ValueError as exc:
        raise HTTPException(
            status_code=422,
            detail={"error": {"code": "INVALID_PRESET", "message": str(exc)}},
        ) from exc
    name = str(request.preset.get("name", "")).strip()
    return {"data": {"upserted": True, "name": name}, "meta": {}}

@router.delete("/strategies/{name}", status_code=204)
def delete_strategy(name: str) -> None:
    delete_strategy_preset_by_name(name)  # idempotent；name 不存在不 raise
    return None

@router.post("/strategies/restore")
def restore_strategies() -> dict[str, Any]:
    restore_strategy_defaults()
    count = len(get_strategy_presets_config())
    return {"data": {"count": count}, "meta": {}}
```

##### `delete_strategy_preset_by_name` service 方法

```python
# src/services/config_service.py（追加，補既有 by_index 版的對應 by_name 版）
def delete_strategy_preset_by_name(name: str) -> None:
    """Delete preset matching name (case-sensitive trimmed).  Idempotent."""
    existing = get_strategy_presets_config()
    target = str(name).strip()
    updated = [p for p in existing if str(p.get("name", "")).strip() != target]
    if len(updated) != len(existing):
        _save_strategy_presets(updated)
    # else: no-op（idempotent）
```

##### 主題切換接線

```tsx
// web/src/components/providers.tsx（10-G-2 改造）
"use client";
import { ThemeProvider } from "next-themes";
import { Toaster } from "sonner";
import { CommandPalette } from "@/components/command-palette";

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <ThemeProvider attribute="class" defaultTheme="dark" enableSystem={false}>
      {children}
      <Toaster position="bottom-right" duration={3000} richColors closeButton />
      <CommandPalette />
    </ThemeProvider>
  );
}
```

```tsx
// web/src/components/settings/theme-section.tsx
"use client";
import { useTheme } from "next-themes";

export function ThemeSection() {
  const { theme, setTheme } = useTheme();
  return (
    <Section title="外觀">
      <div className="flex items-center gap-3">
        <span className="text-sm">主題</span>
        <button onClick={() => setTheme("dark")}  data-active={theme === "dark"}>Dark</button>
        <button onClick={() => setTheme("light")} data-active={theme === "light"}>Light</button>
      </div>
    </Section>
  );
}
```

##### 設定頁分區結構

```tsx
// web/src/app/settings/page.tsx
import { SecretsSection } from "@/components/settings/secrets-section";
import { StrategyPresetsSection } from "@/components/settings/strategy-presets-section";
import { ThemeSection } from "@/components/settings/theme-section";
import { AiToggleSection } from "@/components/settings/ai-toggle-section";

export default function SettingsPage() {
  return (
    <div className="space-y-6 max-w-3xl">
      <h1 className="text-2xl font-bold">設定</h1>
      <SecretsSection />
      <StrategyPresetsSection />
      <ThemeSection />
      <AiToggleSection />
    </div>
  );
}
```

##### Secrets 區安全規則

- 輸入框 `type="password"` + `autocomplete="off"` + `value=""`（不預填）
- 儲存按鈕 → `PUT /api/config/secrets` body `{ keys: { openai: "...", ... } }`
- 成功 → toast「API Key 已更新」、SWR `mutate` 重抓 `/api/config/secrets/status`
- **永不**呼叫 `GET /api/config/secrets`（無此端點），只透過 `GET /api/config/secrets/status` 取 boolean

##### 策略 preset CRUD UI

```
StrategyPresetsSection
├── 列表（從 GET /api/config/strategies 取，SWR cache）
│   每列：{name} {strategy 中文名} {market} {params summary} [編輯] [刪除]
├── [+ 新增策略] 按鈕 → StrategyPresetDialog（mode="create"）
├── [編輯] 按鈕 → StrategyPresetDialog（mode="edit", initial={preset}）
├── [刪除] 按鈕 → 二次確認 Dialog → DELETE /api/config/strategies/{name}
└── [重置為預設] 按鈕（右上次要） → 二次確認 Dialog → POST /api/config/strategies/restore
```

`StrategyPresetDialog`：
- 表單欄位：name（text）/ strategy（select：8 個選項）/ market（select：tw / us）/ params（依 strategy 動態渲染：MA 兩個 number / RSI 兩個 number / KD 三個 number / ...）
- 提交：`POST /api/config/strategies` body `{ preset: {...} }`
- 422 INVALID_PRESET → 表單欄位錯誤訊息（不 toast）
- 成功 → toast「已新增策略：{name}」/「已更新策略：{name}」+ SWR mutate

##### Toast 文案規範（10-G-2 部分）

| 場景 | 類型 | 文案 |
|:---|:---|:---|
| API Key 儲存成功 | success | `API Key 已更新` |
| Preset 新增成功 | success | `已新增策略：{name}` |
| Preset 更新成功 | success | `已更新策略：{name}` |
| Preset 刪除成功 | success | `已刪除策略：{name}` |
| Preset 重置成功 | success | `已重置為預設 {count} 組策略` |
| API 422（除 preset 表單內） | error | `操作失敗：{error.message}` |
| 主題切換 | （無 toast，靜默） | — |

##### AI toggle

```tsx
// web/src/components/settings/ai-toggle-section.tsx
"use client";
import { Switch } from "@/components/ui/switch";
import { Tooltip } from "@/components/ui/tooltip";
import { useConfig } from "@/hooks/use-config";

export function AiToggleSection() {
  const { config } = useConfig();
  return (
    <Section title="AI 功能">
      <Tooltip content="AI 功能尚未開放（等待 10-F-2 接 LLM）">
        <Switch disabled checked={false} aria-label="AI 開關" />
      </Tooltip>
      <p className="text-xs text-muted-foreground">
        後端設定：ai.enabled = {String(config?.ai?.enabled ?? false)}（read-only）
      </p>
    </Section>
  );
}
```

##### 不做（明確邊界）

- 不做 i18n、字體大小、緊湊模式、自訂主題色、自訂鍵盤快捷鍵
- 不做 preset 拖曳排序、複製、匯入匯出（編 `config.yaml` 即可）
- 不做 secrets 區的「測試連線」按鈕（10-F-2 才有意義）

---

### 10-H 舊 UI 移除與收尾

**V2.8 拆段：** 10-H 拆為 **10-H-1（收尾前置補強）+ 10-H-2（實際移除與回歸）**。動作清單中的「刪檔 + 改 toml + 跑 regression」屬機械操作，但同段隱含 Playwright E2E、手機底部 Tab Bar、theme CSS 變數測試三項新建工。先在 10-H-1 把先決條件補齊，10-H-2 才按下刪除按鈕，避免破壞舊 UI 後回頭工。

#### 測試遷移檢查表（10-H-1 必須打勾，10-H-2 才能刪檔）

每個被刪的 Streamlit test case 必須先在 service 層/API/前端測試中有對應替代：

| 舊 Streamlit test file | 涵蓋的非 UI 邏輯 | 替代測試位置 | 10-H-1 狀態 |
|:---|:---|:---|:---:|
| `test_dashboard_page.py` | 自動補抓、美股 adjusted daily、停用 realtime/chip、shares 轉換 | `test_services/test_dashboard_svc.py` + `test_api/test_analysis_api.py` | ✅ |
| `test_backtest_page.py` | 回測資料同步、auto sync market、USD caption | `test_services/test_backtest_svc.py` + `test_api/test_backtest_api.py` | ✅ |
| `test_data_management_page.py` | 美股不支援訊息、update_daily market=us | `test_services/test_data_svc.py` + `test_api/test_data_api.py` | ✅ |
| `test_stock_selector.py` | ticker 正規化、BRK.B→BRK-B | `test_services/` 或現有 `test_market.py` | ✅ |
| `test_themes.py` | 主題定義完整性 | **新增** `web/src/tests/lib/theme-vars.test.ts`（10-H-1 補） | ✅ |
| `test_config_ui_section.py` | config 讀寫格式 | `test_services/test_config_svc.py` | ✅ |
| `test_settings_page.py` | 策略 preset CRUD | `test_services/test_config_svc.py` | ✅ |

#### 10-H-1：收尾前置補強

**前置條件：** 10-G-2 通過驗收。

**1. Playwright E2E smoke 套件（`web/tests/e2e/`）**

新增測試檔（建議拆分）：

| 檔案 | 場景 | viewport |
|:---|:---|:---|
| `web/tests/e2e/navigation.spec.ts` | 5 頁可達（sidebar / 底部 Tab Bar / Command Palette 三種跳轉方式） | desktop + mobile |
| `web/tests/e2e/backtest-single.spec.ts` | 發 backtest job → SSE progress → tearsheet 5 metric card → equity chart 顯示 | desktop |
| `web/tests/e2e/backtest-cancel.spec.ts` | 取消 job → status=cancelled → partial result 仍可讀 | desktop |
| `web/tests/e2e/csv-download.spec.ts` | 批次比較 CSV `<a download>` 觸發、toast「CSV 已下載」 | desktop |
| `web/tests/e2e/mobile-tabbar.spec.ts` | 底部 Tab Bar 5 icon 可點、active state | mobile only |

`web/playwright.config.ts` 既有規格 [規格書 §3139] 沿用，不改；新增 mobile project：

```typescript
projects: [
  { name: "desktop-chromium", use: { ...devices["Desktop Chrome"], viewport: { width: 1280, height: 800 } } },
  { name: "mobile-chromium", use: { ...devices["Pixel 5"] } },  // 375×667 等價
],
```

CI 不跑（個人專案），手動本機跑：`cd web && pnpm exec playwright test`。

**2. 手機 <768px 底部 Tab Bar**

修改 [web/src/components/sidebar.tsx](web/src/components/sidebar.tsx) — 拆為兩個元件：

- `<DesktopSidebar>`（既有實作，`hidden lg:flex`）
- `<MobileTabBar>`（新增，`lg:hidden`，固定貼底）

`<MobileTabBar>` 結構：

```tsx
<nav className="fixed bottom-0 left-0 right-0 z-50 h-14 bg-background border-t border-border lg:hidden">
  <div className="grid grid-cols-5 h-full">
    {NAV_ITEMS.map((item) => (
      <Link key={item.href} href={item.href} className="flex flex-col items-center justify-center gap-0.5">
        <item.Icon className={cn("h-5 w-5", isActive(item.href) ? "text-primary" : "text-muted-foreground")} />
        <span className={cn("text-[10px]", isActive(item.href) ? "text-primary" : "text-muted-foreground")}>{item.shortLabel}</span>
      </Link>
    ))}
  </div>
</nav>
```

`NAV_ITEMS` 新增 `shortLabel` 欄位（4 字內）；既有 `label` 留給 desktop 用。

頁面內容需加 `pb-14 lg:pb-0` 避免被 Tab Bar 蓋住。

**3. `test_themes.py` → Vitest CSS 變數替代**

新增 `web/src/tests/lib/theme-vars.test.ts`：

```typescript
import { describe, it, expect, beforeEach } from "vitest";

const REQUIRED_VARS = [
  "--background", "--foreground", "--primary", "--border",
  "--chart-up", "--chart-down",
];

function readVars(): Record<string, string> {
  const styles = getComputedStyle(document.documentElement);
  return Object.fromEntries(REQUIRED_VARS.map((v) => [v, styles.getPropertyValue(v).trim()]));
}

describe("theme CSS variables", () => {
  beforeEach(() => {
    document.documentElement.classList.remove("dark", "light");
  });

  it("dark theme defines all required vars", () => {
    document.documentElement.classList.add("dark");
    const vars = readVars();
    for (const v of REQUIRED_VARS) {
      expect(vars[v], `${v} should be defined under .dark`).not.toBe("");
    }
  });

  it("light theme defines all required vars", () => {
    document.documentElement.classList.add("light");
    const vars = readVars();
    for (const v of REQUIRED_VARS) {
      expect(vars[v], `${v} should be defined under .light`).not.toBe("");
    }
  });
});
```

`vitest.config.ts` 確認 `environment: "happy-dom"` 已開啟（10-B 應已設定，若否補上）。

**驗收條件：**

1. `pnpm exec playwright test` 全綠（desktop + mobile project）
2. `375×667` viewport：底部 Tab Bar 顯示、5 個 icon 各自連到正確路由、active state 正確
3. `pnpm test` 新增 `theme-vars.test.ts` 通過
4. 既有 vitest / pytest 不退化（10-H-1 階段不准刪舊測試）
5. 7 行測試遷移檢查表全部 ✅
6. `run_quanttraderV2.bat` 在 mobile viewport 模擬下可達 5 頁

#### 10-H-2：實際移除與全專案回歸

**前置條件：** 10-H-1 通過驗收，檢查表 7 行 ✅。

**動作：**

1. 移除 `src/ui/`（`app.py` / `themes.py` / `pages/dashboard.py` / `pages/backtest.py` / `pages/data_management.py` / `pages/ai_chat.py` / `pages/settings.py`）
2. 移除 `run_quanttrader.bat`（保留 `run_quanttraderV2.bat` / `run_api.bat` / `run_web.bat` / `run_dev.bat`）
3. `pyproject.toml` 移除：
   - `streamlit`
   - `streamlit-extras`
   - `streamlit-option-menu`
4. 移除 7 個 Streamlit 測試檔：
   - `tests/test_dashboard_page.py`
   - `tests/test_backtest_page.py`
   - `tests/test_data_management_page.py`
   - `tests/test_stock_selector.py`
   - `tests/test_themes.py`
   - `tests/test_config_ui_section.py`
   - `tests/test_settings_page.py`
5. 更新四份文件：
   - `量化交易系統規格書_shellpig版.md`：修訂歷史加 V2.9 標記 10-H 完成
   - `開發設計方針.md`：10-H 區段加完成戳記、Phase 4 區段加註「`ui/` 已於 10-H-2 移除」
   - `測試指南.md`：檢查表 7 行 ✅、總測試數欄位重算（扣 7 個檔的 case 數，加 svc + API + Playwright + theme-vars 的 case 數）
   - `PROJECT_BRIEF.md`：Phase 進度表 10-H-1 / 10-H-2 標 ✅、主線移除 Streamlit 文字、目錄結構刪 `src/ui/` 區塊
6. `.\.venv\Scripts\python.exe -m pytest tests/ -v -m "not integration"` 全綠
7. `pnpm test`（web vitest）全綠
8. `pnpm exec playwright test`（E2E）全綠

**`src/ai/advisor.py` 保留規則：** 10-H-2 不得刪 `src/ai/advisor.py`。理由：
- 10-F-2 接 LLM 時需要 `AIAdvisor.stream_chat()` 的 host class
- Dashboard `/api/ai/analyze` 端點仍呼叫 `AIAdvisor.analyze_dashboard()`

**驗收條件：**

1. `src/ui/` 整個目錄不存在
2. `pyproject.toml` 不含 streamlit 三套件；重跑 `uv sync` 不再拉入
3. `run_quanttrader.bat` 不存在
4. 7 個舊 pytest 測試檔不存在
5. 全專案 pytest 通過（測試總數 ≥ 舊狀態扣 7 個檔的 case 後）
6. 前端 vitest + Playwright E2E 全綠
7. 四份文件已更新並 commit
8. `驗證後已知問題.md` 補「[P10-H] 完成」條目，記錄邊界決定（保留 `advisor.py`、E2E 不接 CI 等）

**範圍邊界（10-H-2 結束後）：**

- 不再支援 Streamlit；任何「請打開舊 UI」的指令皆無效
- `src/ai/advisor.py`、`data/`、`config.yaml`、`.env`：**保留**
- 不做：Streamlit 主題 6 套還原、`bat` 啟動腳本恢復舊形態

---

## Phase 11：Dashboard 基本面與事件擴充

**對應規格：** 規格書 Phase 11。

### 11.0 實作原則

Phase 11 只擴充 Next.js dashboard 與 FastAPI / service / data 層，不回復 Streamlit，不改回測引擎，不擴大美股。實作順序固定：

```text
11-A -> 11-B -> 11-C -> 11-D
```

11-B / 11-C 不可並行，因為兩者都會改：

- `web/src/components/dashboard/dashboard-page-client.tsx`
- `web/src/components/dashboard/tooltip-text.ts`
- `src/services/dashboard_service.py`
- `api/routers/analysis.py`

所有新增 dashboard P11 API 必須走 `/api/analysis/p11/*`，並放在 `@router.get("/api/analysis/{section}")` 動態路由之前。新增 API 時必須同步補 regression 測試，確認 `/api/analysis/p11/*` 不會被 `{section}` 吃掉。

美股處理：

- Service 層 market=`us` 時 P11 method 回 `NotImplementedError` 或 API 轉成 501。
- 前端 market=`us` 時隱藏 P11 下方兩塊，不顯示「尚未支援」文案。

### 11.1 共通前端設計

#### Tooltip 與文字

檔案：

- `web/src/components/dashboard/help-tooltip.tsx`：沿用既有元件，不另建 tooltip 系統。
- `web/src/components/dashboard/tooltip-text.ts`：新增 `P11_` 前綴常數。

規則：

- 本益比、股價淨值比、殖利率、年增率、月增率直接用中文。
- EPS、TTM、VWAP 等保留縮寫時必須加 `HelpTooltip`。
- 新增 `web/src/tests/lib/tooltip-coverage.test.ts` 時，需白名單常見不可避免字詞，避免測試過度脆弱。

#### 色彩

台股慣例：

- 正向漲幅 / 年增 / 月增 / 法人浮盈：紅。
- 負向：綠。
- 倒數 < 7 天：紅；< 30 天：黃；>= 30 天：灰。

使用既有 Tailwind 色系，不新增主題 token，除非同一顏色在多個 P11 元件重複到需要抽象。

### 11.2 11-A 版面調整

修改檔案：

- `web/src/components/dashboard/candlestick-chart.tsx`
- `web/src/components/dashboard/dashboard-page-client.tsx`
- `web/src/components/dashboard/tooltip-text.ts`

實作：

1. `candlestick-chart.tsx`：
   - chart option `height: 400` 改 `height: 300`。
   - 外層容器 `h-[400px]` 改 `h-[300px]`。
   - 確認 skeleton 或 loading fallback 也同步改高度。
2. `dashboard-page-client.tsx`：
   - 只在左欄 chart 下方新增 P11 grid。
   - 中欄 `TechnicalPanel / LevelsPanel / ChipPanel / AiPanel` 不動。
   - 右欄 `PatternsPanel / MultiTimeframePanel` 不動。
   - 新增 `P11PlaceholderPanel` 小元件，避免 6 格重複 JSX。
3. market=`us` 時不渲染 P11 grid。

建議元件：

```tsx
function P11PlaceholderPanel({
  title,
  tooltip,
  children,
  action,
}: {
  title: string;
  tooltip: string;
  children: React.ReactNode;
  action?: React.ReactNode;
}) {
  return (
    <section className="rounded-lg border border-dashed border-slate-700 p-3">
      <div className="mb-2 flex items-center justify-between gap-2">
        <div className="flex items-center gap-1">
          <h3 className="text-sm font-semibold text-slate-100">{title}</h3>
          <HelpTooltip text={tooltip} />
        </div>
        {action}
      </div>
      <div className="text-sm italic text-slate-500">{children}</div>
    </section>
  );
}
```

11-A 不新增 hook、不打 API。

### 11.3 11-B 資料層

#### constants

檔案：`src/core/constants.py`

新增：

```python
PER_COLUMNS = ["date", "per", "pbr", "dividend_yield", "symbol"]
MONTHLY_REVENUE_COLUMNS = ["date", "revenue", "revenue_month", "revenue_year", "symbol"]
EPS_COLUMNS = ["date", "year", "quarter", "eps", "symbol"]
```

`DIVIDENDS_COLUMNS` 若已存在，沿用；若只在 fetcher/storage 區域內定義，提升到 constants，避免 schema 分散。

#### fetcher

檔案：`src/data/fetcher.py`

`IDataFetcher` 新增抽象方法：

```python
def fetch_per(self, symbol: str, start: str, end: str) -> pd.DataFrame: ...
def fetch_monthly_revenue(self, symbol: str, start: str, end: str) -> pd.DataFrame: ...
```

`FinMindFetcher`：

- `fetch_per` 使用 `TaiwanStockPER`，normalized 欄位為 `date/per/pbr/dividend_yield/symbol`。
- `fetch_monthly_revenue` 使用 `TaiwanStockMonthRevenue`，normalized 欄位為 `date/revenue/revenue_month/revenue_year/symbol`。
- `revenue` 保留 FinMind 原始單位「元」。
- `date` 轉 `Asia/Taipei` tz-aware。

`YFinanceFetcher`：

```python
raise NotImplementedError("US not supported in P11.")
```

既有 `FinMindFetcher.fetch_dividends()` / `fetch_eps()` 已存在，不重寫；11-B 只補 storage / maintenance 落地鏈。

#### storage

檔案：`src/data/storage.py`

新增 path helper 與 roundtrip：

```python
save_per(df, symbol, market="tw", source="finmind") -> None
load_per(symbol, market="tw") -> pd.DataFrame

save_monthly_revenue(df, symbol, market="tw", source="finmind") -> None
load_monthly_revenue(symbol, market="tw") -> pd.DataFrame

save_dividends(df, symbol, market="tw", source="finmind") -> None
load_dividends(symbol, market="tw") -> pd.DataFrame

save_eps(df, symbol, market="tw", source="finmind") -> None
load_eps(symbol, market="tw") -> pd.DataFrame
```

路徑：

- `data/raw/tw/{symbol}/per.parquet`
- `data/raw/tw/{symbol}/monthly_revenue.parquet`
- `data/raw/tw/{symbol}/dividends.parquet`
- `data/raw/tw/{symbol}/eps.parquet`

`save_eps` 特別規則：

```python
if "date" not in df.columns and "report_date" in df.columns:
    df = df.copy()
    df["date"] = df["report_date"]
```

最終欄位包含 `EPS_COLUMNS + ["report_date"]`。`data_meta` 的 `first_date / last_date` 取 `date`，不是 `year/quarter`。

#### maintenance

檔案：`src/data/maintenance.py`

`rebuild_symbol` 台股流程新增：

1. fetch/save PER，freq=`per`
2. fetch/save monthly revenue，freq=`monthly_revenue`
3. fetch/save dividends，freq=`dividends`
4. fetch/save EPS，freq=`eps`

起始日：

- PER / 月營收 / EPS：`2015-01-01`
- dividends fetcher 可維持既有 `2000-01-01`，落地時切至 `2015-01-01` 以符合 P11 顯示需求。

`update_symbol` 若不想第一輪處理增量，可先呼叫同一 fetch range + merge 去重；但必須保留既有 daily/institutional/margin 行為。

### 11.4 11-B Service 與 API

檔案：

- `src/services/dashboard_service.py`
- `api/routers/analysis.py`

新增 service methods：

```python
def get_valuation(symbol: str, market: str = "tw") -> dict: ...
def get_monthly_revenue(symbol: str, months: int = 12, market: str = "tw") -> dict: ...
def get_dividend_history_with_pe(symbol: str, count: int = 5, market: str = "tw") -> dict: ...
def get_industry_per_table(symbol: str, market: str = "tw") -> dict: ...
```

`get_valuation`：

- 讀 `load_per(symbol)` 最新一筆。
- 產業從 `stock_info_tw.parquet` 或 `data_service` 既有 symbol name cache 取得；若查不到回 `industry=null`。

`get_monthly_revenue`：

- 讀 `load_monthly_revenue(symbol)`。
- 用 `pct_change(12)` / `pct_change(1)` 計算 yoy/mom。
- 回傳尾端 `months` 筆；不足 12 個月時 yoy 為 `null`。

`get_dividend_history_with_pe`：

- 讀 `load_dividends`、`load_daily`、`load_eps`。
- 對近 N 次現金除息日，取除息日前已公告最近 4 季 EPS。
- EPS 不足 4 季或除息日無 close 時 `ttm_pe=null`。

`get_industry_per_table`：

- peer 清單來自 `stock_info_tw.parquet`。
- cache path：`data/cache/industry_per/{slug(industry)}_{YYYY-MM-DD}.parquet`。
- cache miss 使用 `ThreadPoolExecutor(max_workers=8)`。
- 每個 worker 建立獨立 `FinMindFetcher`，不得共用 `requests.Session`。
- 個別 peer fetch 失敗時記錄 `per/pbr/dividend_yield=null`，不要 raise 整體錯誤。
- 加 industry-level lock，避免同一時間兩個請求重複打同產業。

API endpoints：

```python
@router.get("/api/analysis/p11/valuation")
def get_p11_valuation(...): ...

@router.get("/api/analysis/p11/monthly-revenue")
def get_p11_monthly_revenue(...): ...

@router.get("/api/analysis/p11/dividend-history")
def get_p11_dividend_history(...): ...

@router.get("/api/analysis/p11/industry-per")
def get_p11_industry_per(...): ...
```

這四個必須放在 `/api/analysis/{section}` 之前。

### 11.5 11-B 前端

建議新增目錄：

```text
web/src/components/dashboard/p11/
  valuation-panel.tsx
  monthly-revenue-panel.tsx
  dividend-history-panel.tsx
  industry-per-modal.tsx
```

建議 hooks：

```text
web/src/hooks/use-p11-valuation.ts
web/src/hooks/use-p11-monthly-revenue.ts
web/src/hooks/use-p11-dividend-history.ts
web/src/hooks/use-p11-industry-per.ts
```

或若專案習慣集中在 `web/src/lib/hooks/`，沿用既有 colocate 風格，不為 P11 硬開兩套風格。

`industry-per-modal.tsx` loading UX：

- Dialog 開啟立即顯示 peer skeleton。
- API pending 時，表格區外層 `relative`。
- 上方覆蓋：

```tsx
<div className="absolute inset-0 z-10 flex items-center justify-center bg-slate-950/55 backdrop-blur-[1px]">
  <div className="rounded-lg border border-slate-700 bg-slate-900 px-5 py-4 text-center shadow-xl">
    <p className="text-sm font-semibold text-slate-100">資料讀取中，正在整理同產業本益比...</p>
    <p className="mt-1 text-xs text-slate-400">首次載入約 8-25 秒，完成後會一次更新</p>
  </div>
</div>
```

API 完成後一次替換表格並移除遮罩；不做逐列填值。

### 11.6 11-C 股東會資料層

#### TWSEFetcher

檔案：`src/data/fetcher.py`

新增獨立 class，不繼承 `IDataFetcher`：

```python
class TWSEFetcher:
    TWSE_URL = "https://openapi.twse.com.tw/v1/opendata/t187ap41_L"
    TPEX_URL = "https://www.tpex.org.tw/openapi/v1/t187ap41_O"

    def fetch_shareholder_meeting(self, session: requests.Session | None = None) -> pd.DataFrame:
        ...
```

欄位轉換：

- `公司代號` -> `symbol`
- `開會日期` 民國 `yyyMMdd` -> 西元 tz-aware `date`
- `股東常(臨時)會` -> `meeting_type`，值 normalize 為 `常會` / `臨時會`
- `source = "auto"`
- `updated_at = now`

TWSE / TPEx 任一失敗時，`fetch_shareholder_meeting` 應 raise，讓 maintenance 決定不寫部分結果。

#### storage

檔案：`src/data/storage.py`

股東會是全市場單一 parquet：

```python
save_shareholder_meeting(df) -> None
load_shareholder_meeting() -> pd.DataFrame
load_shareholder_meeting_meta() -> dict
save_shareholder_meeting_meta(meta: dict) -> None
```

路徑：

- `data/raw/tw/shareholder_meeting.parquet`
- `data/raw/tw/shareholder_meeting.meta.json`

股東會不進 `DuckDBMeta`。不得寫 sentinel row。不得改 `data_meta` schema。

#### update auto layer

自動層寫入時需與既有 parquet 比對：

```python
def merge_shareholder_meeting_auto(new_rows: pd.DataFrame, existing: pd.DataFrame, now: pd.Timestamp) -> pd.DataFrame:
    # 新 symbol -> updated_at = now
    # date / meeting_type 相同 -> 保留 existing.updated_at
    # date / meeting_type 變動 -> updated_at = now
    # existing 有、new_rows 沒有 -> 保留 existing row
```

這個函式要獨立測，不要藏在大型 maintenance method 裡。

#### once-per-day guard

放在 `data_update` / `data_rebuild` job 尾端，所有 symbol loop 完成後跑一次。

```python
def should_update_shareholder_meeting(meta_path: Path, now: pd.Timestamp) -> bool:
    if not meta_path.exists():
        return True
    meta = json.loads(meta_path.read_text(encoding="utf-8"))
    last = pd.Timestamp(meta["last_updated_at"]).tz_convert(TAIPEI_TZ).normalize()
    today = now.tz_convert(TAIPEI_TZ).normalize()
    return last < today
```

Guard 通過時：

1. `TWSEFetcher.fetch_shareholder_meeting()`
2. `merge_shareholder_meeting_auto`
3. 寫 parquet
4. 寫 meta json

任一步失敗：log warning，保留既有 parquet/meta，不更新 `last_updated_at`。

### 11.7 11-C Service 與 API

新增 service methods：

```python
def get_institutional_cost(symbol: str, days: int = 30, market: str = "tw") -> dict: ...
def get_event_calendar(symbol: str, market: str = "tw") -> dict: ...
def upsert_shareholder_meeting_override(symbol: str, date: str, meeting_type: str) -> dict: ...
def delete_shareholder_meeting_override(symbol: str) -> dict: ...
```

法人持股成本：

- 外資欄位採既有 institutional schema 中外資淨買欄位。
- 投信、自營商同理。
- 只用淨買 > 0 日期計算成本。
- 若無正淨買，該法人回 `null`。
- `current_price` 取最新 daily close。

事件行事曆：

- 除息資料來自 `load_dividends`。
- 股東會資料來自 auto parquet + manual CSV。
- Manual CSV 不存在時自動視為空，不應讓 API 500。
- P3 合併以 row-level `updated_at` 比較，不做欄位級 merge。

API endpoints：

```python
@router.get("/api/analysis/p11/institutional-cost")
def get_p11_institutional_cost(...): ...

@router.get("/api/analysis/p11/event-calendar")
def get_p11_event_calendar(...): ...

@router.post("/api/analysis/p11/shareholder-meeting/override")
def post_shareholder_meeting_override(...): ...

@router.delete("/api/analysis/p11/shareholder-meeting/override")
def delete_shareholder_meeting_override(...): ...
```

`meeting_type` 只接受 `常會` / `臨時會`；日期必須為 `YYYY-MM-DD`，且不可早於今天前一年。

### 11.8 11-C 前端

新增：

```text
web/src/components/dashboard/p11/institutional-cost-panel.tsx
web/src/components/dashboard/p11/event-calendar-panel.tsx
web/src/components/dashboard/p11/shareholder-meeting-edit-dialog.tsx
```

法人持股成本 panel：

- 顯示外資 / 投信 / 自營商三列。
- 欄位：成本、浮盈虧。
- `null` 顯示 `—`。
- 成本欄 tooltip 說明「以法人 N 日內每日淨買股數 × 該日加權均價（最高+最低+收盤）÷3 加權計算」。

事件行事曆 panel：

- 顯示「即將」與「上次」兩段。
- 除息顯示日期、現金股息、倒數。
- 股東會顯示日期、常會/臨時會、倒數、編輯 icon。
- source=`manual` 時加 `[手動設定]` chip。
- 無資料顯示「撈不到資料，需要手動填入」與編輯 icon。
- 上次股東會不顯示倒數。

股東會 edit dialog：

- Radix Dialog。
- 日期 input 可用 HTML5 `type="date"`。
- 會議類型 radio。
- 顯示目前生效資料來源。
- 有 manual override 時才顯示「清除手動」。
- 儲存與清除後，mutate event calendar hook。

### 11.9 11-D 佔位

11-A 先保留第三格 placeholder，標題暫用「散戶多空比」。11-D 前不得實作演算法，也不得擴充資料層。若 11-C 完成後使用者改決定第三格內容，只需換掉該 placeholder 與後續規格。
