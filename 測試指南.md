# QuantTrader 測試指南

> **目的：** 本文件定義每個 Phase / 子階段的完整測試流程。
> 包含：環境準備、執行指令、預期輸出、通過/失敗判定標準、故障排除。
> **每個子階段的測試全數通過，才能進入下一個子階段。**

---

## 測試環境準備（一次性，Phase 1-A 開始前）

### Step 1：安裝 uv

```powershell
# PowerShell（管理員不必要）
irm https://astral.sh/uv/install.ps1 | iex
```

驗證：

```bash
uv --version
# 預期：uv 0.x.x（任何版本即可）
```

### Step 2：安裝套件

```bash
cd "C:\Users\User\OneDrive\桌面\AI_Work\ProjectClaude\QuantTrader"
uv sync --extra dev
```

驗證：

```bash
uv run python -c "import pandas, duckdb, pyarrow, plotly, yaml; print('ALL OK')"
# 預期輸出：ALL OK
```

### Step 3：設定 .env

```bash
# 複製範本
copy .env.example .env
```

編輯 `.env`，填入：

```
FINMIND_TOKEN=你的_FinMind_Token
ANTHROPIC_API_KEY=sk-ant-你的_API_Key
OPENAI_API_KEY=sk-你的_OpenAI_API_Key
GEMINI_API_KEY=你的_Gemini_API_Key
```

> **沒有 FINMIND_TOKEN / AI API Key？**
> 單元測試不需要。整合測試（標記 `@pytest.mark.integration`）才需要。
> 先跳過 `.env`，只跑單元測試也可以推進開發。

### Step 4：設定 pytest marker

確認 `pyproject.toml` 包含：

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "integration: 需要外部 API 或網路連線的測試",
]
```

這樣 `@pytest.mark.integration` 才不會出現 `PytestUnknownMarkWarning`。

---

## 測試指令速查表

| 用途 | 指令 |
|------|------|
| 跑全部單元測試（不含整合） | `uv run pytest tests/ -v -m "not integration"` |
| 跑全部測試（含整合） | `uv run pytest tests/ -v` |
| 跑指定子階段的測試 | `uv run pytest tests/test_fetcher.py -v` |
| 跑指定單一測試 | `uv run pytest tests/test_fetcher.py::test_localize_naive_to_taipei -v` |
| 顯示詳細錯誤訊息 | 加 `--tb=long` |
| 顯示簡短結果 | 加 `--tb=short` |
| 產出覆蓋率報告 | `uv run pytest tests/ --cov=src --cov-report=term-missing` |
| 只看有哪些測試（不執行） | `uv run pytest tests/ --collect-only` |

---

## Phase 1：台股資料基礎建設

---

### 1-A 資料源連線驗證

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/core/constants.py` | （無獨立測試，被其他模組間接驗證） |
| `src/core/config.py` | （無獨立測試，被其他模組間接驗證） |
| `src/data/fetcher.py` | `tests/test_fetcher.py` |

#### 前置條件

- [x] `pyproject.toml` 已建立且 `uv sync --extra dev` 成功
- [x] `src/core/constants.py`、`src/core/config.py`、`src/core/exceptions.py` 已建立
- [x] `src/data/fetcher.py` 已實作 `IDataFetcher`、`FinMindFetcher`、`YFinanceFetcher`、`localize_to_taipei`
- [x] `config.yaml` 已建立

#### 執行指令

**第一關：單元測試（不需網路 / 不需 API Key）**

```bash
uv run pytest tests/test_fetcher.py -v -m "not integration"
```

預期輸出：

```
tests/test_fetcher.py::test_localize_naive_to_taipei       PASSED
tests/test_fetcher.py::test_localize_utc_to_taipei         PASSED
tests/test_fetcher.py::test_empty_dataframe_has_standard_columns  PASSED

========================= 3 passed =========================
```

**第二關：整合測試（需網路 + .env 已設定 FINMIND_TOKEN）**

```bash
uv run pytest tests/test_fetcher.py -v -m integration
```

預期輸出：

```
tests/test_fetcher.py::test_finmind_daily_2330             PASSED
tests/test_fetcher.py::test_yfinance_daily_2330            PASSED
tests/test_fetcher.py::test_both_sources_same_schema       PASSED

========================= 3 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 3/3 單元測試 PASSED | **必須** |
| 3/3 整合測試 PASSED | **必須**（沒有 API Token 時可暫緩，但進 1-B 前必須補跑） |
| 無 WARNING（除 DeprecationWarning） | 建議 |

#### 各測試的驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_localize_naive_to_taipei` | naive datetime 經 `tz_localize` 後，時區為 `Asia/Taipei`，數值不變（不能偏移 8 小時） | 誤用 `tz_convert` 代替 `tz_localize` |
| `test_localize_utc_to_taipei` | UTC datetime 經 `tz_convert` 後，小時 +8 | 未判斷 `dt.tz is None` 的分支 |
| `test_empty_dataframe_has_standard_columns` | 空 DataFrame 的 `columns.tolist()` == `STANDARD_COLUMNS`（7 欄） | Fetcher 遇到空資料時回傳了 `pd.DataFrame()` 而非帶欄位的空 DataFrame |
| `test_finmind_daily_2330` | FinMind API 回傳 2330 日K，DataFrame 非空、欄位齊全、`date` 為 `datetime64[ns, Asia/Taipei]`、`close` 為 `float64` | FINMIND_TOKEN 過期或未填；API URL 錯誤；欄位映射（max→high, min→low）遺漏 |
| `test_yfinance_daily_2330` | yfinance 取 `2330.TW` 日K，DataFrame 非空 | 網路不通；yfinance 版本過舊；忘記加 `.TW` 後綴 |
| `test_both_sources_same_schema` | 兩個 Fetcher 回傳的 `df.dtypes` 完全一致 | 某一方的 `volume` 是 float 而非 int；`date` 時區不一致 |

#### 故障排除

| 問題 | 解法 |
|------|------|
| `ModuleNotFoundError: No module named 'src'` | 確認 `pyproject.toml` 有 `pythonpath = ["."]`；確認從專案根目錄執行 pytest |
| `requests.exceptions.ConnectionError` | 檢查網路；FinMind API 是否在維護 |
| `FINMIND_TOKEN not found` | 檢查 `.env` 是否存在、token 值是否正確、`config.py` 是否呼叫 `load_dotenv()` |
| yfinance 回傳空 DataFrame | yfinance 對台股偶爾不穩定，重試幾次；確認 symbol 格式為 `"2330.TW"` |

---

### 1-B Parquet 存儲結構

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/data/storage.py` | `tests/test_storage.py` |

#### 前置條件

- [x] Phase 1-A 全部測試通過
- [x] `src/data/storage.py` 已實作 `ParquetStorage`、`DuckDBMeta`

#### 執行指令

```bash
uv run pytest tests/test_storage.py -v
```

預期輸出：

```
tests/test_storage.py::test_save_and_load_daily_roundtrip    PASSED
tests/test_storage.py::test_upsert_no_duplicates             PASSED
tests/test_storage.py::test_load_nonexistent_returns_empty    PASSED
tests/test_storage.py::test_duckdb_meta_upsert_and_query     PASSED
tests/test_storage.py::test_duckdb_meta_upsert_overwrite     PASSED

========================= 5 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 5/5 測試 PASSED | **必須** |
| 所有測試使用 `tmp_path` 或 `:memory:`，不污染真實 `data/` 目錄 | **必須** |

#### 各測試的驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_save_and_load_daily_roundtrip` | 寫入 Parquet → 讀出 → `pd.testing.assert_frame_equal` 完全一致（含 dtype、時區） | Parquet 寫入時丟失時區資訊；`pyarrow` 版本太舊不支援 tz-aware |
| `test_upsert_no_duplicates` | 先寫 10 筆（日期 Day1-10），再寫 8 筆（Day6-13），讀出後恰好 13 筆，Day6-10 的值為第二次寫入的版本 | `drop_duplicates` 的 `keep` 參數錯誤；未按 `date` 排序 |
| `test_load_nonexistent_returns_empty` | 讀取不存在的 symbol → 回傳空 DataFrame，但 `columns` 完整 | 拋出 FileNotFoundError 而非回傳空 DataFrame |
| `test_duckdb_meta_upsert_and_query` | DuckDB `:memory:` → 建表 → upsert → query → 值正確 | SQL 語法錯誤；`TIMESTAMP WITH TIME ZONE` 型別不相容 |
| `test_duckdb_meta_upsert_overwrite` | 同一 `(symbol, freq)` upsert 兩次 → 後者覆蓋前者 | INSERT 未處理衝突（需 `ON CONFLICT ... DO UPDATE`） |

#### Review 補充待加入測試（2026-04-26）

| 測試名稱 | 驗證什麼 | 為何需要 |
|----------|---------|----------|
| `test_storage_rejects_path_traversal_symbol` | `symbol` 含 `..`、路徑分隔符或絕對路徑時必須拒絕；合法 symbol 的輸出路徑 `resolve()` 後仍在 `data_dir` 內 | 規格要求資料只能落在 `data/raw/tw/{symbol}` / `data/processed/tw/{symbol}`；避免外部輸入造成任意路徑寫入 |
| `test_storage_uses_default_tw_market_path` | 未指定 market 時仍寫入 `data/raw/tw/{symbol}`；指定 `market="tw"` 行為相同 | 先保留未來美股擴充接口，但不能破壞現有台股預設路徑 |
| `test_storage_rejects_unknown_market` | 傳入尚未支援的 market，例如 `us`，應明確拒絕或標示未支援 | 本階段不實作美股；避免使用者誤以為美股已可用 |

#### 故障排除

| 問題 | 解法 |
|------|------|
| `pyarrow.lib.ArrowInvalid: Casting from timestamp[ns] to timestamp[ns, tz=Asia/Taipei]` | 確認寫入前 DataFrame 的 `date` 已是 tz-aware |
| DuckDB `Catalog Error: Table 'data_meta' does not exist` | `__init__` 中要執行 `CREATE TABLE IF NOT EXISTS` |
| `assert_frame_equal` 失敗但肉眼看數值一樣 | 通常是 dtype 不同（int64 vs Int64）或時區不同 |

---

### 1-C 資料品質檢核

#### 測試對象

| 模組 | 測試檔 | 固定資料 |
|------|--------|---------|
| `src/data/cleaner.py` | `tests/test_cleaner.py` | `tests/fixtures/bad_data.csv` |

#### 前置條件

- [x] Phase 1-B 全部測試通過
- [x] `tests/fixtures/bad_data.csv` 已建立（手工構造壞資料）
- [x] `src/data/cleaner.py` 已實作 `DataCleaner`、`QualityReport`

#### 測試資料說明

`tests/fixtures/bad_data.csv` 需包含約 20 筆手工構造的資料：

```csv
date,open,high,low,close,volume,symbol
2024-01-02,100.0,105.0,99.0,103.0,1000000,TEST
2024-01-03,-50.0,105.0,99.0,103.0,1000000,TEST     # 負價格
2024-01-04,100.0,105.0,99.0,0.0,1000000,TEST       # 零價格
2024-01-05,100.0,120.0,100.0,115.0,1000000,TEST    # +11.65%（超過 ±10%）
2024-01-08,115.0,126.5,115.0,126.5,1000000,TEST    # +10.0%（恰好漲停，不超限）
2024-01-09,126.5,130.0,125.0,128.0,0,TEST          # 成交量=0
...（含 NaN 缺值、連續 6 日成交量 0 等）
```

#### 執行指令

```bash
uv run pytest tests/test_cleaner.py -v -m "not integration"
```

預期輸出：

```
tests/test_cleaner.py::test_l1_negative_price_removed       PASSED
tests/test_cleaner.py::test_l1_extreme_change_flagged        PASSED
tests/test_cleaner.py::test_l1_normal_limit_up_not_flagged   PASSED
tests/test_cleaner.py::test_l2_ffill_3_day_gap               PASSED
tests/test_cleaner.py::test_l2_suspension_detected           PASSED
tests/test_cleaner.py::test_clean_returns_new_dataframe      PASSED

========================= 6 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 6/6 測試 PASSED | **必須** |
| `test_clean_returns_new_dataframe` 確認原始 df 不被修改 | **必須**（防止副作用 bug） |

#### 各測試的驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_l1_negative_price_removed` | 含負價格的列在 clean 後消失；`report.negative_price_count >= 1` | 只檢查了 `close` 沒檢查 `open/high/low` |
| `test_l1_extreme_change_flagged` | +15% 漲幅 → `report.extreme_change_count >= 1`，且 `extreme_change_dates` 包含正確日期 | 漲跌幅公式分母用錯（用了今日 close 而非前日 close） |
| `test_l1_normal_limit_up_not_flagged` | +10.0% 恰好漲停 → **不應**出現在 `extreme_change_dates` 中 | 邊界條件：用 `>` 而非 `>=` 判斷 10% 門檻（應為 `> 0.10` 或 `> PRICE_LIMIT_PCT`） |
| `test_l2_ffill_3_day_gap` | 3 日 NaN → 填補後值等於缺值前最後一筆的 close | ffill 的 `limit` 參數設太小；或 ffill 前未先按 date 排序 |
| `test_l2_suspension_detected` | 連續 6 日 volume=0 → `report.suspension_ranges` 包含 `("起始日", "結束日")` | 停牌偵測用了錯誤的連續日數閾值 |
| `test_clean_returns_new_dataframe` | 呼叫 clean 前後，原始 df 的 `id()` 或 `.values` 不變 | 直接在原始 df 上 `drop` 或 `fillna(inplace=True)` |

---

### 1-D 除權息前復權調整

#### 測試對象

| 模組 | 測試檔 | 固定資料 |
|------|--------|---------|
| `src/data/cleaner.py`（adjust 函式） | `tests/test_cleaner.py`（追加） | `tests/fixtures/dividends_2330.csv` |
| `src/data/fetcher.py`（fetch_dividends） | `tests/test_fetcher.py`（追加） | — |
| `src/data/maintenance.py` | `tests/test_maintenance.py` | — |

#### 前置條件

- [x] Phase 1-C 全部測試通過
- [x] `tests/fixtures/dividends_2330.csv` 已建立（2330 已知除權息事件，含日期、現金股利金額）
- [x] `adjust_prices()`、`compute_adjustment_factors()` 已實作

#### 測試資料說明

`tests/fixtures/dividends_2330.csv`：

```csv
date,cash_dividend,stock_dividend,symbol
2024-06-26,3.5,0.0,2330
2024-09-18,4.0,0.0,2330
```

搭配手工構造的日K（10 筆左右），確保除息日前後有足夠資料可驗證。

#### 執行指令

```bash
# 除權息相關單元測試
uv run pytest tests/test_cleaner.py -v -k "adj"

# maintenance 測試（若有獨立測試檔）
uv run pytest tests/test_maintenance.py -v -m "not integration"
```

預期輸出：

```
tests/test_cleaner.py::test_adj_factor_no_dividend           PASSED
tests/test_cleaner.py::test_adj_factor_cash_dividend         PASSED
tests/test_cleaner.py::test_forward_adj_latest_price_unchanged PASSED
tests/test_cleaner.py::test_adj_no_gap_across_ex_date        PASSED
tests/test_cleaner.py::test_raw_method_returns_unchanged     PASSED

========================= 5 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 5/5 除權息測試 PASSED | **必須** |
| 整合測試：fetch_dividends 能從 FinMind 取得 2330 除權息資料 | 建議（可在 1-D 結束時補跑） |

#### 各測試的驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_adj_factor_no_dividend` | 無除權息時 → `adj_factor` 全為 1.0 | 函式在空 dividends DataFrame 時 crash |
| `test_adj_factor_cash_dividend` | 已知除息日前日 close=100, cash=5 → 除息日之前的 `adj_factor < 1` | 調整因子計算公式反了（向上調而非向下調） |
| `test_forward_adj_latest_price_unchanged` | 最新交易日的 adj close == raw close | 累乘方向錯誤（應從最新往回乘） |
| `test_adj_no_gap_across_ex_date` | 除息日前一天 adj_close 與除息日 adj_close 的差值 < 0.01 元 | 調整因子只套用在除息日之前，但除息日當天的開盤參考價未納入計算 |
| `test_raw_method_returns_unchanged` | `adjust_prices(df, divs, method="raw")` 回傳值 == 原始 df | method 判斷邏輯遺漏 |

---

### Phase 1 全階段回歸測試

**當 1-D 全部通過後，執行 Phase 1 完整回歸：**

```bash
uv run pytest tests/test_fetcher.py tests/test_storage.py tests/test_cleaner.py -v -m "not integration"
```

預期結果：**全部 PASSED**（至少 19 個測試）。

```bash
# 含整合測試的完整回歸
uv run pytest tests/test_fetcher.py tests/test_storage.py tests/test_cleaner.py -v
```

預期結果：**全部 PASSED**（至少 22 個測試）。

> Phase 1 完成。可進入 Phase 2。

---

## Phase 2：向量化回測引擎

---

### 2-A 訊號生成框架

#### 測試對象

| 模組 | 測試檔 | 固定資料 |
|------|--------|---------|
| `src/strategy/base.py` | `tests/test_engine_vec.py` | `tests/fixtures/ma_cross_data.csv` |
| `src/strategy/examples/ma_cross.py` | 同上 | 同上 |

#### 前置條件

- [x] Phase 1 全階段回歸通過
- [x] `tests/fixtures/ma_cross_data.csv` 已建立（100 筆日K，已知 MA20/MA60 交叉日）

#### 測試資料說明

`tests/fixtures/ma_cross_data.csv`：手工構造 100 筆日K。

- Day 1-60：close 穩定在 100 附近（MA20 ≈ MA60 ≈ 100）
- Day 61-65：close 急拉到 120（MA20 上穿 MA60 → 黃金交叉應出現在約 Day 65）
- Day 66-90：close 穩定在 120
- Day 91-100：close 急跌到 95（MA20 下穿 MA60 → 死亡交叉）

> 手工計算交叉日並記錄在測試的註解中。

#### 執行指令

```bash
uv run pytest tests/test_engine_vec.py -v -k "signal"
```

預期輸出：

```
tests/test_engine_vec.py::test_ma_cross_signal_golden_cross    PASSED
tests/test_engine_vec.py::test_ma_cross_signal_no_lookahead    PASSED
tests/test_engine_vec.py::test_ma_cross_signal_alignment       PASSED
tests/test_engine_vec.py::test_signal_values_only_minus1_0_plus1 PASSED

========================= 4 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 4/4 測試 PASSED | **必須** |
| signal 不包含 NaN | **必須**（rolling 前 N-1 筆應為 0，不是 NaN） |

#### 各測試的驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_ma_cross_signal_golden_cross` | 在手算的交叉日，`signal` 值為 +1 | rolling mean 的 min_periods 設定；交叉判定用 `>` vs `>=` |
| `test_ma_cross_signal_no_lookahead` | 逐筆驗證：`signal[t]` 只使用 `df[:t+1]` 的資料 | 忘記 shift(1) 或在 signal 生成中使用了 future close |
| `test_ma_cross_signal_alignment` | `signal.index` 與 `df.index` 完全相同 | rolling 產生的 Series 長度少了 |
| `test_signal_values_only_minus1_0_plus1` | `set(signal.unique()) <= {-1, 0, 1}` | rolling 前 N-1 筆為 NaN 未處理 |

---

### 2-B 市場摩擦成本計算

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/backtest/cost.py` | `tests/test_cost.py` |

#### 前置條件

- [x] Phase 2-A 全部測試通過
- [x] `src/backtest/cost.py` 已實作 `CostCalculator`、`TradeCost`

#### 執行指令

```bash
uv run pytest tests/test_cost.py -v
```

預期輸出：

```
tests/test_cost.py::test_tick_size_boundaries       PASSED
tests/test_cost.py::test_buy_no_tax                 PASSED
tests/test_cost.py::test_sell_has_tax                PASSED
tests/test_cost.py::test_etf_tax_rate                PASSED
tests/test_cost.py::test_commission_discount         PASSED
tests/test_cost.py::test_commission_minimum_20       PASSED
tests/test_cost.py::test_slippage_buy                PASSED
tests/test_cost.py::test_known_trade_total           PASSED

========================= 8 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 8/8 測試 PASSED | **必須** |
| 所有金額斷言使用 `pytest.approx(expected, abs=0.01)` | **必須**（浮點精度） |

#### 各測試的驗證重點

| 測試名稱 | 手算驗證值 | 失敗常見原因 |
|----------|-----------|-------------|
| `test_tick_size_boundaries` | 9.99→0.01, 10.00→0.05, 49.99→0.05, 50.00→0.10, 99.99→0.10, 100.00→0.50, 499.99→0.50, 500.00→1.00, 999.99→1.00, 1000.00→5.00 | 邊界值的 `<` vs `<=` |
| `test_buy_no_tax` | BUY → `cost.tax == 0.0` | 未區分 BUY/SELL |
| `test_sell_has_tax` | SELL 1000 股 × 100 元 → tax = 300.0 | 乘錯稅率 |
| `test_etf_tax_rate` | ETF SELL → tax = 100000 × 0.001 = 100.0 | `is_etf` 參數未生效 |
| `test_commission_discount` | 1000 股 × 100 元 → commission = 100000 × 0.001425 × 0.6 = 85.5 | 未套用 discount |
| `test_commission_minimum_20` | 1 股 × 10 元 → raw commission = 0.00855 → 取 max(0.00855, 20) = 20.0 | 未實作 minimum 20 元邏輯 |
| `test_slippage_buy` | price=100, BUY → 100 + 0.50 × 1 = 100.50（tick size for 100 = 0.50） | tick size 查表錯誤 |
| `test_known_trade_total` | BUY 1000 股 × 500 元：commission=427.5, tax=0, slippage=1000×1×1=1000 → total=1427.5 | total 計算邏輯 |

---

### 2-C 績效指標計算

#### 測試對象

| 模組 | 測試檔 | 固定資料 |
|------|--------|---------|
| `src/backtest/metrics.py` | `tests/test_metrics.py` | `tests/fixtures/known_returns.csv` |

#### 前置條件

- [x] Phase 2-B 全部測試通過
- [x] `tests/fixtures/known_returns.csv` 已建立

#### 測試資料說明

`tests/fixtures/known_returns.csv`：10 筆已知日報酬率，手算好各指標答案。

```csv
date,equity
2024-01-02,1000000
2024-01-03,1010000
2024-01-04,1025000
2024-01-05,1015000
2024-01-08,1030000
2024-01-09,1020000
2024-01-10,1040000
2024-01-11,1035000
2024-01-12,1050000
2024-01-15,1045000
```

手算：
- total_return = (1045000 - 1000000) / 1000000 = 0.045
- max peak = 1050000 (Jan 12), trough after peak = 1045000 (Jan 15)
- MDD = (1050000 - 1045000) / 1050000 ≈ 0.00476

#### 執行指令

```bash
uv run pytest tests/test_metrics.py -v
```

預期輸出：

```
tests/test_metrics.py::test_sharpe_known_returns      PASSED
tests/test_metrics.py::test_max_drawdown_simple       PASSED
tests/test_metrics.py::test_max_drawdown_dates        PASSED
tests/test_metrics.py::test_all_winning_edge_case     PASSED
tests/test_metrics.py::test_monthly_returns_shape     PASSED
tests/test_metrics.py::test_sortino_no_downside       PASSED

========================= 6 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 6/6 測試 PASSED | **必須** |
| Sharpe 手算誤差 < 0.001 | **必須** |
| 邊界條件（全贏、全正報酬）不 crash | **必須** |

---

### 2-D Tearsheet 報告生成

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/backtest/report.py` | `tests/test_report.py` |
| `src/backtest/engine_vec.py` | `tests/test_engine_vec.py`（追加） |

#### 前置條件

- [x] Phase 2-C 全部測試通過
- [x] `src/backtest/report.py`、`src/backtest/base.py`、`src/backtest/engine_vec.py` 已實作

#### 執行指令

```bash
uv run pytest tests/test_report.py tests/test_engine_vec.py -v
```

預期輸出：

```
tests/test_report.py::test_tearsheet_renders_without_error  PASSED
tests/test_report.py::test_tearsheet_save_html              PASSED
tests/test_engine_vec.py::test_vectorized_backtest_basic     PASSED
（加上 2-A 的 4 個測試也應繼續 PASSED）

========================= 7 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| Tearsheet 2/2 PASSED | **必須** |
| 向量化回測 1/1 PASSED | **必須** |
| HTML 檔案可用瀏覽器開啟 | **建議**（手動檢查） |

#### 手動驗證（建議）

```bash
# 生成 HTML 後用瀏覽器開啟
uv run python -c "
from src.backtest.report import TearsheetReport
from tests.test_report import make_mock_result  # 或自行構造
report = TearsheetReport(make_mock_result())
report.save_html('data/backtest/test_tearsheet.html')
print('Saved to data/backtest/test_tearsheet.html')
"
```

開啟 `data/backtest/test_tearsheet.html`，肉眼確認：
- [ ] 累積報酬曲線正常顯示
- [ ] 回撤曲線紅色向下
- [ ] 月度熱力圖有正確的顏色漸變
- [ ] 摘要表數字與 BacktestResult 一致

---

### Phase 2 全階段回歸測試

```bash
uv run pytest tests/test_cost.py tests/test_metrics.py tests/test_report.py tests/test_engine_vec.py -v
```

預期結果：**全部 PASSED**（至少 21 個測試）。

再跑 Phase 1 + Phase 2 全回歸，確認無破壞：

```bash
uv run pytest tests/ -v -m "not integration" --ignore=tests/test_engine_event.py --ignore=tests/test_account.py --ignore=tests/test_events.py --ignore=tests/test_consistency.py --ignore=tests/test_indicators.py --ignore=tests/test_advisor.py --ignore=tests/test_e2e.py
```

> Phase 2 完成。可進入 Phase 3。

---

## Phase 3：分鐘K 事件驅動回測引擎

---

### 3-A 事件資料結構定義

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/backtest/events.py` | `tests/test_events.py` |

#### 前置條件

- [x] Phase 2 全階段回歸通過
- [x] `src/backtest/events.py` 已實作 `BarEvent`、`OrderEvent`、`FillEvent`

#### 執行指令

```bash
uv run pytest tests/test_events.py -v
```

預期輸出：

```
tests/test_events.py::test_bar_event_frozen               PASSED
tests/test_events.py::test_order_event_limit_requires_price PASSED
tests/test_events.py::test_order_event_invalid_side        PASSED
tests/test_events.py::test_order_event_negative_quantity   PASSED
tests/test_events.py::test_fill_event_total_cost_buy       PASSED
tests/test_events.py::test_fill_event_total_cost_sell      PASSED
tests/test_events.py::test_events_serializable             PASSED

========================= 7 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 7/7 測試 PASSED | **必須** |

#### 各測試的驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_bar_event_frozen` | 修改 `bar.close = 999` → `FrozenInstanceError` | 忘記 `frozen=True` |
| `test_order_event_limit_requires_price` | `OrderEvent(order_type="LIMIT", price=None)` → `ValueError` | `__post_init__` 未實作 |
| `test_order_event_invalid_side` | `side="HOLD"` → `ValueError` | 驗證清單只有 BUY/SELL |
| `test_order_event_negative_quantity` | `quantity=-100` → `ValueError` | 驗證用 `<= 0` 而非 `< 0` |
| `test_fill_event_total_cost_buy` | BUY 100 × 50, comm=7 → total=5007 | `total_cost` property 計算邏輯 |
| `test_fill_event_total_cost_sell` | SELL 100 × 50, comm=7, tax=15 → total=4978 | SELL 要減去 comm 和 tax |
| `test_events_serializable` | `json.dumps(event.to_dict())` 不拋例外 | datetime 無法 serialize（需 `default=str`） |

---

### 3-B Account 狀態管理

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/backtest/account.py` | `tests/test_account.py` |

#### 前置條件

- [x] Phase 3-A 全部測試通過
- [x] `src/backtest/account.py` 已實作 `Account`、`SimpleAccount`

#### 執行指令

```bash
uv run pytest tests/test_account.py -v
```

預期輸出：

```
tests/test_account.py::test_initial_state                PASSED
tests/test_account.py::test_buy_reduces_cash             PASSED
tests/test_account.py::test_sell_increases_cash           PASSED
tests/test_account.py::test_buy_then_sell_position_zero   PASSED
tests/test_account.py::test_oversell_raises               PASSED
tests/test_account.py::test_cost_basis_weighted_average   PASSED
tests/test_account.py::test_total_value                   PASSED
tests/test_account.py::test_unrealized_pnl                PASSED

========================= 8 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 8/8 測試 PASSED | **必須** |
| 所有金額斷言使用 `pytest.approx` | **必須** |

#### 各測試的手算驗證值

| 測試 | 設定 | 手算預期值 |
|------|------|-----------|
| `test_buy_reduces_cash` | 初始 1,000,000；BUY 1000 股 × 100 元，comm=85 | cash = 1,000,000 - 100,000 - 85 = **899,915** |
| `test_sell_increases_cash` | 接上，SELL 1000 股 × 110 元，comm=94, tax=330 | cash += 110,000 - 94 - 330 = **+109,576** |
| `test_cost_basis_weighted_average` | BUY 1000 × 100, BUY 1000 × 120 | cost_basis = (100×1000 + 120×1000) / 2000 = **110.0** |
| `test_total_value` | cash=500,000, 持有 1000 股, 市價 120 | total = 500,000 + 120,000 = **620,000** |

---

### 3-C 事件迴圈核心

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/backtest/engine_event.py` | `tests/test_engine_event.py` |

#### 前置條件

- [x] Phase 3-B 全部測試通過
- [x] `src/backtest/engine_event.py` 已實作 `EventDrivenBacktester`

#### 輔助工具：計數策略

測試中需要一個「什麼都不做但計數」的 helper 策略：

```python
class CountingStrategy(StrategyBase):
    """測試用：不下單，只計算 on_bar 被呼叫的次數。"""
    def __init__(self):
        self.call_count = 0

    def generate_signals(self, df):
        return pd.Series(0, index=df.index)

    def on_bar(self, bar, account):
        self.call_count += 1
        return []
```

#### 執行指令

```bash
uv run pytest tests/test_engine_event.py -v
```

預期輸出：

```
tests/test_engine_event.py::test_on_bar_called_per_bar          PASSED
tests/test_engine_event.py::test_order_filled_next_bar          PASSED
tests/test_engine_event.py::test_market_order_fill_price        PASSED
tests/test_engine_event.py::test_no_future_function             PASSED
tests/test_engine_event.py::test_equity_curve_length            PASSED
tests/test_engine_event.py::test_last_bar_pending_orders_discarded PASSED

========================= 6 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 6/6 測試 PASSED | **必須** |
| `test_order_filled_next_bar` 是最關鍵的 — 驗證無未來函數 | **必須** |

#### 各測試的驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_on_bar_called_per_bar` | CountingStrategy 的 call_count == len(data) | 迭代邏輯跳過了某些 bar |
| `test_order_filled_next_bar` | Bar[5] 產生 BUY → fill.timestamp == Bar[6].timestamp | pending_orders 在同一根 Bar 就撮合了（未延遲） |
| `test_market_order_fill_price` | MARKET BUY → fill_price == next_bar.open + tick_size | slippage 加減方向搞反 |
| `test_no_future_function` | 用 mock 策略驗證：on_bar 收到的 bar_event 不含未來資料 | 引擎在呼叫 on_bar 前就更新了價格 |
| `test_equity_curve_length` | equity 記錄數 == data 行數 | 最後一根 Bar 漏記 |
| `test_last_bar_pending_orders_discarded` | 最後一根 Bar 產生的 Order 不被執行 | 迴圈結束後還嘗試撮合 pending orders |

#### Review 補充待加入測試（2026-04-26）

| 測試名稱 | 驗證什麼 | 為何需要 |
|----------|---------|----------|
| `test_event_engine_accepts_minute_frequency_aliases` | 用 `pd.date_range(..., freq="min")` 與 `freq="5min"` 的資料跑 `EventDrivenBacktester` 不應因 `BarEvent.freq` 驗證失敗 | 規格明定事件驅動引擎以分鐘 K 為最小事件單位；pandas 新版回傳 `min` / `5min`，必須轉成 canonical `1min` / `5min` |
| `test_event_engine_short_index_freq_fallback` | 少於 3 根 bar 的資料不應讓 `pd.infer_freq()` 例外外漏；應穩定 fallback 或給出明確錯誤 | 小樣本 smoke test 與 UI 預覽常會使用 1-2 筆資料，不能因頻率推斷中斷整個回測 |

---

### 3-D StrategyBase 與範例策略

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/strategy/base.py` | `tests/test_engine_event.py`（追加） |
| `src/strategy/examples/ma_cross.py` | 同上 |

#### 前置條件

- [x] Phase 3-C 全部測試通過
- [x] `MACrossStrategy.on_bar()` 已實作

#### 執行指令

```bash
uv run pytest tests/test_engine_event.py -v -k "strategy"
```

預期輸出：

```
tests/test_engine_event.py::test_strategy_abc_enforcement        PASSED
tests/test_engine_event.py::test_ma_cross_event_same_entry_points PASSED
tests/test_engine_event.py::test_strategy_no_modification_needed  PASSED

========================= 3 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 3/3 測試 PASSED | **必須** |

#### 各測試的驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_strategy_abc_enforcement` | 不完整子類 → `TypeError` on `__init__` | 忘記在 StrategyBase 加 `@abstractmethod` |
| `test_ma_cross_event_same_entry_points` | 事件驅動模式與向量化模式的進場日期集合一致 | on_bar 內部的均線計算與 generate_signals 使用了不同視窗 |
| `test_strategy_no_modification_needed` | 同一 instance 先跑向量化、再跑事件驅動 | on_bar 修改了 instance 內部狀態（如 `_close_history`）導致第二次跑結果不同。需在 run() 開頭 reset |

#### Review 補充待加入測試（2026-04-26）

| 測試名稱 | 驗證什麼 | 為何需要 |
|----------|---------|----------|
| `test_buy_quantity_reserves_transaction_costs` | 現金剛好接近滿倉時，買進數量需預留 commission / slippage / 最低手續費；向量化引擎不得產生負現金，事件策略不得送出會讓 `SimpleAccount` 拒絕的 BUY | 規格中的回測資金模型不應隱含槓桿；目前已有帳戶層現金不足防護，但 sizing 邏輯也需要防回歸 |

---

### 3-E 雙引擎一致性驗證

#### 測試對象

| 測試檔 | 固定資料 |
|--------|---------|
| `tests/test_consistency.py` | `tests/fixtures/consistency_data.csv`（約 500 筆日K） |

#### 前置條件

- [x] Phase 3-D 全部測試通過
- [x] `tests/fixtures/consistency_data.csv` 已建立（可用真實 2330 日K 截取 500 筆）

#### 執行指令

```bash
uv run pytest tests/test_consistency.py -v
```

預期輸出：

```
tests/test_consistency.py::test_total_return_consistency   PASSED
tests/test_consistency.py::test_trade_count_consistency    PASSED
tests/test_consistency.py::test_entry_dates_consistency    PASSED
tests/test_consistency.py::test_deviation_report           PASSED

========================= 4 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| total_return 偏差 < 1% | **必須** |
| 交易次數一致 | **必須** |
| 進場日期集合一致 | **必須** |

#### 偏差解讀

若偏差在 0.1% ~ 1% 之間，屬**正常可接受**，來源通常是：
- 向量化引擎用 close price 估算，事件驅動用 next open + slippage
- 股數取整差異（向量化可能用浮點部位，事件驅動取整張）

若偏差 > 1%：
1. 先確認交易次數是否一致
2. 印出逐筆交易比對（`test_deviation_report` 會輸出到 log）
3. 找出第一筆分歧的交易日，逐步 debug

---

### Phase 3 全階段回歸測試

```bash
uv run pytest tests/test_events.py tests/test_account.py tests/test_engine_event.py tests/test_consistency.py -v
```

預期結果：**全部 PASSED**（至少 28 個測試）。

Phase 1 + 2 + 3 全回歸：

```bash
uv run pytest tests/ -v -m "not integration" --ignore=tests/test_indicators.py --ignore=tests/test_advisor.py --ignore=tests/test_e2e.py
```

> Phase 3 完成。可進入 Phase 4。

---

## Phase 4：AI 技術分析問答 + Streamlit UI

---

### 4-A LLM Provider Tool Use 基礎

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/ai/advisor.py` | `tests/test_advisor.py` |

#### 前置條件

- [x] Phase 3 全階段回歸通過
- [x] `src/ai/advisor.py` 已實作 `AIAdvisor`

#### 執行指令

**第一關：單元測試（不需 API Key，用 mock）**

```bash
uv run pytest tests/test_advisor.py -v -m "not integration"
```

預期輸出：

```
tests/test_advisor.py::test_tool_dispatch              PASSED
tests/test_advisor.py::test_unknown_tool_returns_error PASSED
tests/test_advisor.py::test_disclaimer_always_appended PASSED
tests/test_advisor.py::test_invalid_symbol_returns_error PASSED

========================= 4 passed =========================
```

**第二關：整合測試（需選定 provider 的 API key）**

```bash
uv run pytest tests/test_advisor.py -v -m integration
```

預期輸出：

```
tests/test_advisor.py::test_real_api_call              PASSED

========================= 1 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 4/4 單元測試 PASSED | **必須** |
| 1/1 整合測試 PASSED | **必須**（進 4-B 前） |
| 整合測試回應包含免責聲明文字 | **必須** |

#### 各測試的驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_tool_dispatch` | `_execute_tool("get_price_data", {"symbol": "2330", "period": "3mo"})` → 回傳 dict 或 JSON str | handler 函式簽名與 tool_input 不匹配 |
| `test_unknown_tool_returns_error` | 不存在的工具名 → `{"error": "Unknown tool: ..."}` | `handlers.get()` 未處理 None |
| `test_disclaimer_always_appended` | mock LLM provider 的回應後，最終 `ask()` 結果包含 `"免責聲明"` | DISCLAIMER 附加邏輯遺漏 |
| `test_invalid_symbol_returns_error` | symbol="9999" → 回傳 error dict，不 crash | 函式內部 raise 但外層未 catch |
| `test_real_api_call` | 真實呼叫選定 provider → 回傳含分析 + 免責聲明 | API Key 錯誤；model 名稱拼錯；多輪 tool_use 迴圈寫錯 |

#### Provider-neutral 補充待加入測試（2026-04-26）

| 測試名稱 | 驗證什麼 | 為何需要 |
|----------|---------|----------|
| `test_ai_provider_selected_from_config` | `config.yaml` 設定 `provider=openai/anthropic/gemini` 時，`AIAdvisor` 會建立對應 provider adapter | AI 技術分析不應再寫死 Claude，需可由使用者選擇 ChatGPT / Claude / Gemini |
| `test_ai_model_passed_to_provider` | 使用者設定的 `model` 會原樣傳給 provider，不被核心邏輯覆寫 | model id 變動頻繁，核心邏輯不可硬編碼固定模型 |
| `test_ai_disabled_does_not_require_provider_or_api_key` | `config.yaml` 設定 `ai.enabled=false` 時，`AIAdvisor` 不建立 provider、不檢查 API key，並回傳清楚停用狀態 | AI 是可選功能；關閉時不能阻塞資料、回測與非 AI UI 流程 |
| `test_ai_provider_missing_api_key_returns_error` | 選定 provider 但缺少對應 API key 時，回傳清楚錯誤，不 crash | 三家 provider 的 key 不一定都設定；UI 需能友善提示 |
| `test_ai_tool_call_normalized_across_providers` | OpenAI / Anthropic / Gemini mock 回傳的 tool call 都會轉成同一種內部格式 | 各家工具呼叫格式不同，必須在 adapter 層消化差異，避免污染 `AIAdvisor` |

---

### 4-B 技術指標工具實作

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/indicators/calculator.py` | `tests/test_indicators.py` |

#### 前置條件

- [x] Phase 4-A 全部測試通過
- [x] `src/indicators/calculator.py` 已實作 `IndicatorEngine`

#### 測試資料

使用 Phase 2-A 的 `tests/fixtures/ma_cross_data.csv`（或任何 100+ 筆的日K 資料）。
需至少 100 筆才能讓 MA60 之類的長週期指標有足夠資料計算。

#### 執行指令

```bash
uv run pytest tests/test_indicators.py -v
```

預期輸出：

```
tests/test_indicators.py::test_rsi_range             PASSED
tests/test_indicators.py::test_macd_three_columns    PASSED
tests/test_indicators.py::test_kd_alias              PASSED
tests/test_indicators.py::test_ma_dynamic_parsing    PASSED
tests/test_indicators.py::test_bbands_three_bands    PASSED
tests/test_indicators.py::test_invalid_indicator_raises PASSED
tests/test_indicators.py::test_list_supported        PASSED
tests/test_indicators.py::test_calculate_batch       PASSED

========================= 8 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 8/8 測試 PASSED | **必須** |

#### 各測試的驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_rsi_range` | `RSI_14` 所有非 NaN 值介於 [0, 100] | pandas-ta 版本差異；NaN 未排除就 assert |
| `test_macd_three_columns` | 回傳 DataFrame 包含 macd, signal, histogram 三欄 | 欄位重新命名映射漏了 |
| `test_kd_alias` | `"KD"` → 回傳 K, D 兩欄 | INDICATOR_ALIAS 字典 key 拼錯；stoch 參數名 `k`/`d` 不對 |
| `test_ma_dynamic_parsing` | `"MA_5"` → SMA(5)、`"MA_60"` → SMA(60) | `_parse_ma` 正則解析失敗 |
| `test_bbands_three_bands` | 三欄：upper, middle, lower | pandas-ta bbands 回傳欄名含 length 和 std（如 `BBL_20_2.0`） |
| `test_invalid_indicator_raises` | `"FOOBAR"` → `ValueError` | 缺少最後的 fallback raise |
| `test_list_supported` | 回傳 list 包含 `"KD"`, `"RSI_14"`, `"MACD"`, `"BBANDS_20"` | 未合併 INDICATOR_ALIAS keys + 動態 MA 範例 |
| `test_calculate_batch` | `["RSI_14", "MACD", "KD"]` → 所有欄位都在結果 DataFrame 中 | 合併時 index 不對齊 |

---

### 4-C Streamlit UI 框架

#### 測試方式：手動驗收（無自動化測試）

Streamlit 無法用 pytest 測試 UI 渲染，改為手動驗收清單。

#### 前置條件

- [x] Phase 4-B 全部測試通過
- [x] 所有 `src/ui/` 檔案已建立
- [x] 若 `ai.enabled=true`，`.env` 已設定選定 provider 的 API Key；若 `ai.enabled=false`，不需要 AI API Key

#### 啟動

```bash
uv run streamlit run src/ui/app.py
```

預期：瀏覽器自動開啟 `http://localhost:8501`。

#### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 | 通過? |
|---|---------|------|---------|-------|
| 1 | 無錯啟動 | 執行啟動指令 | 終端無 traceback，瀏覽器顯示頁面 | [ ] |
| 2 | 頁面切換 | 點擊左側 sidebar 的四個頁面 | 每個頁面都能正常顯示，無 ImportError | [ ] |
| 3 | 設定頁 — API Key | 進入設定頁，輸入 API Key | 輸入框顯示為遮罩（`****`），不明文顯示 | [ ] |
| 4 | 設定頁 — 儲存 | 修改一個參數，點「儲存為預設」 | `.env` 或 `config.yaml` 被正確寫入 | [ ] |
| 5 | 設定頁 — AI 開關 | 將 `ai.enabled` 關閉並儲存 | AI 問答頁隱藏，或顯示「AI 功能已關閉」且無法送出問題 | [ ] |
| 6 | 設定頁 — AI 開啟 | 將 `ai.enabled` 開啟並選擇 provider/model | 若缺少對應 API key，顯示友善提示，不 crash | [ ] |
| 7 | 資料管理頁 — 列表 | 進入資料管理頁 | 顯示 DuckDB 中已有的 symbol 列表（初次為空表） | [ ] |
| 8 | 資料管理頁 — 下載 | 輸入 `2330`，點「更新」 | 進度條顯示 → 完成後表格新增 2330 | [ ] |
| 9 | 回測頁 — 執行 | 選策略 MACross、股票 2330、日期範圍 → 點「執行回測」 | 顯示績效摘要與 Tearsheet 圖表；摘要第一欄為「交易次數」，第二欄為「總報酬」 | [ ] |
| 10 | 回測頁 — 切換引擎 | 選「事件驅動引擎」→ 重新執行 | 結果正常顯示，與向量化引擎數字接近 | [ ] |
| 11 | AI 問答頁 — 免責聲明 | `ai.enabled=true` 時首次進入 AI 問答頁 | 顯示免責聲明確認頁，有「我了解」按鈕 | [ ] |
| 12 | AI 問答頁 — 確認 | 點「我了解」 | 進入聊天介面 | [ ] |
| 13 | AI 問答頁 — 問答 | 輸入「2330 的 RSI 是多少？」 | AI 回應含指標數值 + 底部免責聲明 | [ ] |
| 14 | AI 問答頁 — 錯誤 | 輸入「9999 的技術面」 | 友善錯誤訊息，不 crash | [ ] |

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| #1-#2 通過 | **必須** |
| #3-#6 通過 | **必須** |
| #7-#10 通過 | **必須** |
| #11-#14 通過 | `ai.enabled=true` 時必須 |

---

### 4-D 端到端整合測試

#### 測試對象

| 測試檔 |
|--------|
| `tests/test_e2e.py` |

#### 前置條件

- [x] Phase 4-C 手動驗收全部通過
- [x] `.env` 已設定 FINMIND_TOKEN；若 `ai.enabled=true`，另需設定選定 AI provider 的 API key
- [x] 本機 `data/` 中已有 2330 日K 資料

#### 執行指令

```bash
uv run pytest tests/test_e2e.py -v -m integration
```

預期輸出：

```
tests/test_e2e.py::test_full_backtest_pipeline     PASSED
tests/test_e2e.py::test_ai_advisor_real_question   PASSED
tests/test_e2e.py::test_invalid_symbol_graceful    PASSED

========================= 3 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| `test_full_backtest_pipeline` PASSED | **必須** |
| `test_ai_advisor_real_question` 回應時間 < 30 秒 | `ai.enabled=true` 時必須 |
| 回應包含數值 + 免責聲明 | `ai.enabled=true` 時必須 |
| `ai.enabled=false` 時非 AI e2e 不需 AI API key | **必須** |

#### 各測試的驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_full_backtest_pipeline` | Storage → Strategy → Backtest → Report → HTML 全程不 crash | 某模組間的資料格式不相容（如 tz-naive 的 equity_curve 傳入 report） |
| `test_ai_advisor_real_question` | 問「2330 的 RSI 是多少」→ 回應含數值 + 免責聲明 + 時間 < 30s | LLM provider 回傳的 tool_use/function calling 格式解析錯誤；工具回傳的 JSON 太大超 token |
| `test_invalid_symbol_graceful` | 輸入 "9999" → 不 crash，回傳包含 "error" 或 "找不到" 字樣 | 工具內部拋例外未被 catch |

---

### Phase 4 全階段回歸測試

```bash
uv run pytest tests/test_advisor.py tests/test_indicators.py -v -m "not integration"
```

預期結果：**12 passed**。

---

## Phase 6：UI/UX 強化

### 6-A 主題切換與 UI 美化（手動驗收 + 單元測試）

**單元測試：**

```bash
uv run pytest tests/test_themes.py tests/test_config_ui_section.py -v
```

預期：

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_get_theme_legal_values` | 6 套主題（arctic_light / obsidian_dark / finance_green / midnight_blue / cyberpunk / warm_sepia）皆能回傳完整 palette | THEMES 字典缺鍵 |
| `test_get_theme_invalid_falls_back_to_arctic_light` | 傳入未知主題名 → 回傳 `arctic_light` | 未實作 fallback |
| `test_render_theme_css_contains_required_selectors` | 產生的 CSS 含 `body`、`stApp`、`stSidebar` 對應規則 | selector 名稱錯誤 |
| `test_config_missing_ui_section_returns_defaults` | `config.yaml` 無 `ui` 區塊時 `get_config()` 不報錯 | 缺 lazy default |
| `test_save_ui_section_preserves_other_sections` | 儲存 `ui` 區塊不破壞 `ai`、`risk`、`backtest`、`strategies` | yaml dump 覆寫整檔 |

**手動驗收（共 8 項）：**

1. **預設主題啟動**：`config.yaml` 無 `ui` 區塊時，`uv run streamlit run src/ui/app.py` 啟動為 `arctic_light` 主題，無錯誤訊息。
2. **6 套主題切換**：在設定頁依序切換 `arctic_light` → `obsidian_dark` → `finance_green` → `midnight_blue` → `cyberpunk` → `warm_sepia`，每次儲存後立即套用，無需重啟 Streamlit；側邊欄、主內容區、按鈕、輸入框配色皆隨主題變化。
3. **lazy migration 驗證**：啟動時 `config.yaml` 未自動新增 `ui` 區塊；只有點「儲存」後 `ui:` 才被寫入。
4. **既有設定不受影響**：儲存外觀設定後，`ai`、`risk`、`backtest`、`strategies` 區塊內容完全不變（diff 確認）。
5. **Plotly 圖表配色**：在 `obsidian_dark`、`finance_green`、`midnight_blue`、`cyberpunk` 等深色主題下進入回測頁，股價走勢圖與 Tearsheet 權益曲線底色與頁面 `surface` 色一致，文字可讀。
6. **option_menu 切換**：在設定頁切換「使用 option_menu 側邊欄」開關，儲存後側邊欄在 `option_menu` 與 `st.radio` 間切換；未安裝 `streamlit-option-menu` 時自動 fallback 並顯示提示。
7. **metric card 切換**：在設定頁切換「使用 streamlit-extras 元件」開關，儲存後回測頁績效摘要在 metric card 與 `st.metric` 間切換；未安裝 `streamlit-extras` 時自動 fallback 並顯示提示。
8. **非法主題回退**：手動將 `config.yaml` 的 `ui.theme` 改為 `neon_pink`，重新整理頁面，系統回退到 `arctic_light` 並顯示警告，不 crash。

**通過條件：** 5 項單元測試全綠 + 8 項手動驗收全部通過。

### 6-B 設定頁與側邊欄 UI 小修（手動驗收 + 單元測試）

**單元測試：**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_themes.py tests/test_config_ui_section.py tests/test_strategy_config.py -v
```

若新增設定頁 helper 測試，追加：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_settings_page.py -v
```

預期：

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_config_missing_ui_section_returns_defaults` | `config.yaml` 無 `ui` 區塊時預設 theme 為 `midnight_blue` | 還沿用 `arctic_light` |
| `test_get_theme_invalid_falls_back_to_midnight_blue` | 非法 theme 回退 `midnight_blue` | fallback 常數未同步 |
| `test_default_strategy_presets_cover_all_strategy_types` | 預設策略 preset 覆蓋 8 種策略類型 | 預設清單漏掉 7-A 新策略 |
| `test_strategy_type_labels_include_chinese_metadata` | 策略類型選項皆有 `{type} ({中文說明})` | settings page 手寫選項或沒用 `STRATEGY_META` |
| `test_save_non_strategy_config_preserves_strategies` | `儲存設定` 不改 `strategies[]` | 一般設定與策略儲存仍共用舊 helper |
| `test_save_strategy_presets_preserves_non_strategy_sections` | `儲存策略` 不改 `ui`、`ai`、`risk`、`backtest`、`.env` | 策略儲存覆寫整份 config |
| `test_delete_strategy_preset_removes_only_one` | 單筆清除只刪指定 preset | 刪除邏輯用 type 或 index 誤刪多筆 |

**手動驗收（共 10 項）：**

1. **側邊欄只留自訂選單**：啟動 `.\.venv\Scripts\python.exe -m streamlit run src/ui/app.py`，左側 sidebar 不得出現 `app`、`ai chat`、`backtest`、`data management`、`settings` 這組 Streamlit 自動頁面入口。
2. **自訂功能頁面完整**：sidebar 仍顯示 QuantTrader 區塊與 `資料管理`、`回測`、`AI 問答`、`設定` 四個入口，四頁皆能切換。
3. **預設外觀**：移除或暫時清空 `config.yaml` 的 `ui` 區塊後啟動，畫面預設為 `midnight_blue`，且不自動寫回 `ui:`。
4. **非法外觀 fallback**：手動將 `ui.theme` 設為 `neon_pink`，重新整理後 fallback 到 `midnight_blue` 並顯示警告，不 crash。
5. **儲存設定不動策略**：修改外觀、AI、風控或回測參數後點 `儲存設定`，確認 `ui` / `ai` / `risk` / `backtest` / `.env` 更新，`strategies[]` diff 不變。
6. **恢復設定預設值不動策略**：點 `恢復設定預設值`，確認策略清單完全保留，只有策略以外設定恢復預設。
7. **策略類型補齊**：設定頁「策略類型」可選 8 種：`moving_average_cross`、`dollar_cost_averaging`、`rsi`、`kd_cross`、`macd_cross`、`bollinger_band`、`bias`、`donchian_breakout`。
8. **策略類型中文說明**：每個策略類型選項後方都有括號中文說明，格式與「編輯策略」欄位一致。
9. **恢復策略預設值**：點 `恢復策略預設值` 後，`strategies[]` 至少有 8 筆，且每種策略類型各一筆；`ui`、`ai`、`risk`、`backtest` 與 `.env` diff 不變。
10. **單筆清除 preset**：在目前策略清單點任一 preset 前方清除按鈕，只移除該筆；其他 preset 與策略以外設定不變。

**通過條件：** 6-B 相關單元測試全綠 + 10 項手動驗收全部通過。

---

## Phase 7：策略擴充

### 7-A 六種技術分析策略

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/strategy/examples/rsi.py` | `tests/test_strategies.py` |
| `src/strategy/examples/kd_cross.py` | 同上 |
| `src/strategy/examples/macd_cross.py` | 同上 |
| `src/strategy/examples/bollinger_band.py` | 同上 |
| `src/strategy/examples/bias.py` | 同上 |
| `src/strategy/examples/donchian_breakout.py` | 同上 |
| `src/core/strategy_config.py`（追加） | `tests/test_strategy_config.py` |

#### 前置條件

- [ ] Phase 6 全階段回歸通過
- [ ] 6 個策略類別已實作 `generate_signals()` 與 `on_bar()`
- [ ] `strategy_config.py` 已新增 6 種 `_normalize_*_params()` 函式
- [ ] `backtest.py` 已新增 6 種策略的回測分派

#### 執行指令

**策略單元測試：**

```bash
uv run pytest tests/test_strategies.py -v
```

預期輸出：

```
tests/test_strategies.py::test_rsi_invalid_params_raises              PASSED
tests/test_strategies.py::test_rsi_signal_values                      PASSED
tests/test_strategies.py::test_rsi_signal_correctness                 PASSED
tests/test_strategies.py::test_rsi_on_bar_produces_orders             PASSED
tests/test_strategies.py::test_rsi_reset_clears_state                 PASSED
tests/test_strategies.py::test_kd_cross_invalid_params_raises         PASSED
tests/test_strategies.py::test_kd_cross_signal_values                 PASSED
tests/test_strategies.py::test_kd_cross_signal_correctness            PASSED
tests/test_strategies.py::test_kd_cross_on_bar_produces_orders        PASSED
tests/test_strategies.py::test_kd_cross_reset_clears_state            PASSED
tests/test_strategies.py::test_macd_cross_invalid_params_raises       PASSED
tests/test_strategies.py::test_macd_cross_signal_values               PASSED
tests/test_strategies.py::test_macd_cross_signal_correctness          PASSED
tests/test_strategies.py::test_macd_cross_on_bar_produces_orders      PASSED
tests/test_strategies.py::test_macd_cross_reset_clears_state          PASSED
tests/test_strategies.py::test_bollinger_band_invalid_params_raises   PASSED
tests/test_strategies.py::test_bollinger_band_signal_values           PASSED
tests/test_strategies.py::test_bollinger_band_signal_correctness      PASSED
tests/test_strategies.py::test_bollinger_band_on_bar_produces_orders  PASSED
tests/test_strategies.py::test_bollinger_band_reset_clears_state      PASSED
tests/test_strategies.py::test_bias_invalid_params_raises             PASSED
tests/test_strategies.py::test_bias_signal_values                     PASSED
tests/test_strategies.py::test_bias_signal_correctness                PASSED
tests/test_strategies.py::test_bias_on_bar_produces_orders            PASSED
tests/test_strategies.py::test_bias_reset_clears_state                PASSED
tests/test_strategies.py::test_donchian_invalid_params_raises         PASSED
tests/test_strategies.py::test_donchian_signal_values                 PASSED
tests/test_strategies.py::test_donchian_signal_correctness            PASSED
tests/test_strategies.py::test_donchian_on_bar_produces_orders        PASSED
tests/test_strategies.py::test_donchian_reset_clears_state            PASSED

========================= 30 passed =========================
```

**strategy_config 正規化 + metadata 測試：**

```bash
uv run pytest tests/test_strategy_config.py -v
```

預期輸出（追加的 12 個測試）：

```
tests/test_strategy_config.py::test_normalize_rsi_params_defaults              PASSED
tests/test_strategy_config.py::test_normalize_rsi_params_invalid               PASSED
tests/test_strategy_config.py::test_normalize_kd_cross_params_defaults         PASSED
tests/test_strategy_config.py::test_normalize_macd_cross_params_invalid        PASSED
tests/test_strategy_config.py::test_normalize_bollinger_band_params_defaults   PASSED
tests/test_strategy_config.py::test_normalize_bias_params_invalid              PASSED
tests/test_strategy_config.py::test_normalize_donchian_breakout_params_defaults PASSED
tests/test_strategy_config.py::test_strategy_meta_covers_all_types             PASSED
tests/test_strategy_config.py::test_strategy_meta_has_required_fields          PASSED
tests/test_strategy_config.py::test_make_strategy_label_chinese                PASSED
tests/test_strategy_config.py::test_format_param_caption_chinese               PASSED
tests/test_strategy_config.py::test_format_param_caption_unknown_type_fallback PASSED

========================= 12 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 30/30 策略測試 PASSED | **必須** |
| 7/7 strategy_config 測試 PASSED | **必須** |
| 所有 signal 值只含 `{-1, 0, +1}` | **必須** |
| warm-up 期間訊號全為 0 | **必須** |

#### 各策略測試的驗證重點

**RSI 策略：**

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_rsi_invalid_params_raises` | `period=0` 或 `oversold>=overbought` → `ValueError` | `__init__` 遺漏驗證 |
| `test_rsi_signal_values` | `set(signal.unique()) <= {-1, 0, 1}` | RSI NaN 未處理為 0 |
| `test_rsi_signal_correctness` | 構造 V 字走勢（先跌後漲），RSI 從 <30 回升 → +1 出現 | RSI 計算用錯 period；穿越判斷用 `<` 而非前一 bar 比較 |
| `test_rsi_on_bar_produces_orders` | 逐 bar 餵入 V 字走勢，在 RSI 超賣回升 bar 產生 BUY OrderEvent | `_close_history` 長度不足時 crash |
| `test_rsi_reset_clears_state` | reset 後再跑 on_bar，行為與新 instance 一致 | 忘記清 `_prev_rsi` |

**KD 交叉策略：**

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_kd_cross_invalid_params_raises` | `k_period=0` → `ValueError` | 參數驗證遺漏 |
| `test_kd_cross_signal_values` | 訊號只含 `{-1, 0, 1}` | stoch 回傳 NaN 位置未歸零 |
| `test_kd_cross_signal_correctness` | 構造先跌後漲走勢，K 上穿 D → +1 | K/D 欄位映射錯誤（pandas-ta 回傳 `STOCHk_*` / `STOCHd_*`）|
| `test_kd_cross_on_bar_produces_orders` | 逐 bar 餵入，交叉 bar 產生 OrderEvent | 手動計算 stoch 公式有誤 |
| `test_kd_cross_reset_clears_state` | reset 後狀態歸零 | 未清 `_prev_k` / `_prev_d` |

**MACD 交叉策略：**

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_macd_cross_invalid_params_raises` | `fast>=slow` → `ValueError` | 參數關係驗證遺漏 |
| `test_macd_cross_signal_values` | 訊號只含 `{-1, 0, 1}` | MACD 前 slow-1 筆 NaN 未歸零 |
| `test_macd_cross_signal_correctness` | 構造急漲走勢，MACD 上穿 Signal → +1 | pandas-ta MACD 欄位名含參數（`MACD_12_26_9`），prefix 解析錯 |
| `test_macd_cross_on_bar_produces_orders` | 逐 bar 餵入，交叉 bar 產生 OrderEvent | EMA 手動計算的 smoothing 因子錯 |
| `test_macd_cross_reset_clears_state` | reset 後狀態歸零 | 未清 EMA 累積值 |

**布林通道策略：**

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_bollinger_band_invalid_params_raises` | `period=0` 或 `std_dev=0` → `ValueError` | 缺驗證 |
| `test_bollinger_band_signal_values` | 訊號只含 `{-1, 0, 1}` | bbands NaN 未歸零 |
| `test_bollinger_band_signal_correctness` | 構造急跌走勢穿越下軌 → +1 | 跌破判斷用 `close < lower` 沒做穿越（需前一 bar >= lower）|
| `test_bollinger_band_on_bar_produces_orders` | 逐 bar 餵入，穿越 bar 產生 OrderEvent | rolling std 手動計算錯 |
| `test_bollinger_band_reset_clears_state` | reset 後狀態歸零 | 未清 `_close_history` |

**乖離率策略：**

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_bias_invalid_params_raises` | `buy_bias>=sell_bias` → `ValueError` | 參數關係驗證遺漏 |
| `test_bias_signal_values` | 訊號只含 `{-1, 0, 1}` | MA NaN → BIAS NaN 未歸零 |
| `test_bias_signal_correctness` | 構造急跌走勢使 BIAS < -10 → +1 | BIAS 公式分母錯（用 close 而非 MA）|
| `test_bias_on_bar_produces_orders` | 逐 bar 餵入，BIAS 穿越門檻 bar 產生 OrderEvent | 手動 MA 計算的 window 不對 |
| `test_bias_reset_clears_state` | reset 後狀態歸零 | 未清 `_close_history` |

**突破策略（Donchian）：**

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_donchian_invalid_params_raises` | `entry_period=0` → `ValueError` | 參數驗證遺漏 |
| `test_donchian_signal_values` | 訊號只含 `{-1, 0, 1}` | rolling max/min NaN 未歸零 |
| `test_donchian_signal_correctness` | 構造突破前 20 日最高價走勢 → +1 | 忘記 `shift(1)` 導致含當日最高價 → 永遠不突破 |
| `test_donchian_on_bar_produces_orders` | 逐 bar 餵入，突破 bar 產生 OrderEvent | `_high_history` 長度計算錯 |
| `test_donchian_reset_clears_state` | reset 後狀態歸零 | 未清 `_high_history` / `_low_history` |

#### strategy_config 正規化測試驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_normalize_rsi_params_defaults` | 空 dict → `{period:14, oversold:30, overbought:70}` | `params.get()` 預設值打錯 |
| `test_normalize_rsi_params_invalid` | `oversold=80, overbought=70` → `None` | 驗證邏輯顛倒 |
| `test_normalize_kd_cross_params_defaults` | 空 dict → `{k_period:9, d_period:3, smooth_k:3}` | 預設值打錯 |
| `test_normalize_macd_cross_params_invalid` | `fast=30, slow=20` → `None` | 缺 `fast < slow` 檢查 |
| `test_normalize_bollinger_band_params_defaults` | 空 dict → `{period:20, std_dev:2.0}` | 預設值打錯 |
| `test_normalize_bias_params_invalid` | `buy_bias=10, sell_bias=-10` → `None` | 缺 `buy_bias < sell_bias` 檢查 |
| `test_normalize_donchian_breakout_params_defaults` | 空 dict → `{entry_period:20, exit_period:10}` | 預設值打錯 |

#### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 |
|---|---------|------|---------|
| 1 | RSI 策略回測 | 選 RSI_14 策略、2330、3 年、向量化引擎 → 執行 | Tearsheet 顯示，有交易紀錄 |
| 2 | KD 交叉策略回測 | 選 KD_Cross 策略、2330、3 年 → 執行 | Tearsheet 顯示，有交易紀錄 |
| 3 | MACD 交叉策略回測 | 選 MACD_Cross 策略、2330、3 年 → 執行 | Tearsheet 顯示，有交易紀錄 |
| 4 | 布林通道策略回測 | 選 BB_20 策略、2330、3 年 → 執行 | Tearsheet 顯示，有交易紀錄 |
| 5 | 乖離率策略回測 | 選 BIAS_20 策略、2330、3 年 → 執行 | Tearsheet 顯示，有交易紀錄 |
| 6 | 突破策略回測 | 選 Donchian_20_10 策略、2330、3 年 → 執行 | Tearsheet 顯示，有交易紀錄 |
| 7 | 事件驅動引擎 | 任選一個新策略切換為事件驅動 → 執行 | 結果正常，與向量化結果接近 |
| 8 | 既有策略不受影響 | 選 MA20_MA60 策略 → 執行 | 結果與 Phase 6 一致 |
| 9 | DCA 策略不受影響 | 選定期定額策略 → 執行 | 結果與 Phase 5-B 一致 |

#### 故障排除

| 問題 | 解法 |
|------|------|
| `ValueError: ma_short must be positive` 等非預期錯誤 | 確認 strategy_config 正規化函式回傳正確的 key 名稱，與策略 `__init__` 參數名一致 |
| warm-up 期間出現買賣訊號 | 確認 `generate_signals` 中 NaN 位置歸零；確認 `_drop_warmup_signals` 已適配新策略 |
| 事件驅動回測 crash at `Insufficient cash for BUY` | 確認 `on_bar` 買入量使用 `_BUY_INTENT_QUANTITY`，由執行層 sizing |
| Tearsheet 績效全為 0 | 確認策略在該區間有產生交易；嘗試縮短 period 或擴大回測區間 |
| `ImportError: cannot import name 'RSIStrategy'` | 確認 `src/strategy/examples/__init__.py` 已更新 |

---

### 7-B 策略研究工作台

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/backtest/batch.py` | `tests/test_batch.py` |
| `src/backtest/metrics.py`（signals 欄位） | `tests/test_batch.py` |
| `src/backtest/engine_vec.py`（signals 暴露） | `tests/test_batch.py` |
| `src/ui/pages/backtest.py`（tab 重構 + 圖表） | 手動驗收 |

#### 前置條件

- [ ] Phase 7-A 全階段回歸通過
- [ ] `src/backtest/batch.py` 已實作 `run_strategy_batch()` 和 `_build_strategy()`
- [ ] `BacktestResult` 已新增 `signals` 欄位
- [ ] `backtest.py` 已改為 tab 結構

#### 執行指令

**batch 單元測試：**

```bash
uv run pytest tests/test_batch.py -v
```

預期輸出：

```
tests/test_batch.py::test_run_strategy_batch_returns_all_summaries     PASSED
tests/test_batch.py::test_run_strategy_batch_failed_preset_skipped     PASSED
tests/test_batch.py::test_run_strategy_batch_summary_fields_correct    PASSED
tests/test_batch.py::test_batch_result_signals_exposed                 PASSED
tests/test_batch.py::test_save_comparison_csv_creates_file             PASSED
tests/test_batch.py::test_build_strategy_all_types                     PASSED
tests/test_batch.py::test_build_strategy_unsupported_type_raises       PASSED

========================= 7 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 7/7 batch 測試 PASSED | **必須** |
| `BacktestResult.signals` 為 pd.Series 且值只含 `{-1, 0, +1}` | **必須** |
| 批次回測中某策略失敗不影響其餘 | **必須** |

#### 各測試驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_run_strategy_batch_returns_all_summaries` | 傳入 3 個 preset → 回傳 3 筆 summary | `_build_strategy` 策略 type 映射不完整 |
| `test_run_strategy_batch_failed_preset_skipped` | 1 個不合法 preset → error 欄有值，其餘 2 筆正常 | except 範圍太窄，未捕獲 ValueError |
| `test_run_strategy_batch_summary_fields_correct` | summary 的 total_return 與單獨跑 BacktestResult 一致 | 批次模式下的 initial_capital 或 data 與單獨跑不同 |
| `test_batch_result_signals_exposed` | result.signals 不為 None 且長度 == len(data) | engine_vec 沒把 signals 寫入 BacktestResult |
| `test_save_comparison_csv_creates_file` | CSV 存在，欄位含策略名稱/總報酬等 9 欄 | 目錄建立失敗（用 `tmp_path`）；encoding 問題 |
| `test_build_strategy_all_types` | 7 種 type（不含 DCA）全部建構成功 | import 遺漏或參數名不匹配 |
| `test_build_strategy_unsupported_type_raises` | `type="unknown"` → ValueError | 未做 type 檢查 |

#### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 |
|---|---------|------|---------|
| 1 | Tab 切換 | 開啟回測頁，點選四個 tab | 四個 tab 可正常切換，互不干擾（單次回測 / 策略比較 / 參數掃描 / 歷史結果） |
| 2 | 單次回測不退化 | 在「單次回測」tab 選 MA20_MA60、2330、3 年 → 執行 | 結果與 7-A 完全一致 |
| 3 | 策略比較 | 在「策略比較」tab 輸入 2330、3 年 → 「開始比較」 | 比較表顯示所有 preset 的 metrics |
| 4 | 比較表欄位 | 檢查比較表 | 9 欄全有：名稱、類型、總報酬、年化、回撤、Sharpe、勝率、PF、交易次數 |
| 5 | 選中展開 | 在比較表下方選某策略 | 展開該策略 Tearsheet + K 線圖 + overlay |
| 6 | K 線圖 | 查看圖表 | 顯示 candlestick（漲紅跌綠），非折線 |
| 7 | Signal/Trade marker | 在單次回測中執行向量化引擎 | 菱形 signal marker（半透明）+ 三角形 trade marker 同圖顯示 |
| 8 | 指標副圖（RSI） | 單次回測選 RSI 策略 | 主圖下方出現 RSI 曲線 + 超買超賣虛線 |
| 9 | 指標副圖（MACD） | 單次回測選 MACD 策略 | 副圖有 MACD 線 + Signal 線 + Histogram 柱狀圖 |
| 10 | 布林通道主圖 overlay | 單次回測選 Bollinger 策略 | 主圖上顯示上軌/下軌/中軌，區間填色 |
| 11 | 匯出 CSV | 策略比較 → 「匯出結果」 | CSV 檔案建立，可開啟且內容正確 |
| 12 | 歷史結果 | 切換到「歷史結果」tab | 列出之前匯出的 CSV，選中後表格顯示 |

#### 故障排除

| 問題 | 解法 |
|------|------|
| K 線圖只顯示折線 | 確認 `price_df` 包含 `open`/`high`/`low` 欄位；缺少時降級為折線 |
| Signal marker 沒出現 | 確認 `BacktestResult.signals` 非 None；事件驅動引擎不顯示 signal marker |
| 指標副圖空白 | 確認 `strategy_type` 和 `strategy_params` 正確傳入 `_render_price_panel` |
| 匯出 CSV 亂碼 | 使用 `encoding="utf-8-sig"`（Excel 需要 BOM） |
| 策略比較跑很久 | 向量化引擎批次跑 8 個 preset 應在數秒內完成；若慢，檢查資料量 |

---

### 7-C 參數掃描與防過度最佳化

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/backtest/sweep.py` | `tests/test_sweep.py` |
| `src/ui/pages/backtest.py`（參數掃描 tab） | 手動驗收 |

#### 前置條件

- [ ] Phase 7-B 全階段回歸通過
- [ ] `src/backtest/sweep.py` 已實作 `generate_param_grid()` 和 `run_parameter_sweep()`
- [ ] `backtest.py` 已新增「參數掃描」tab

#### 執行指令

**sweep 單元測試：**

```bash
uv run pytest tests/test_sweep.py -v
```

預期輸出：

```
tests/test_sweep.py::test_parse_param_values_integers                              PASSED
tests/test_sweep.py::test_parse_param_values_floats                                PASSED
tests/test_sweep.py::test_parse_param_values_negative                              PASSED
tests/test_sweep.py::test_parse_param_values_single                                PASSED
tests/test_sweep.py::test_parse_param_values_empty_string                          PASSED
tests/test_sweep.py::test_parse_param_values_invalid                               PASSED
tests/test_sweep.py::test_parse_param_values_whitespace                            PASSED
tests/test_sweep.py::test_parse_param_values_dedup_sort                            PASSED
tests/test_sweep.py::test_parse_param_values_sort_only                             PASSED
tests/test_sweep.py::test_generate_param_grid_ma_cross_all_valid                   PASSED
tests/test_sweep.py::test_generate_param_grid_ma_cross_filters_invalid             PASSED
tests/test_sweep.py::test_generate_param_grid_rsi_filters_invalid                  PASSED
tests/test_sweep.py::test_generate_param_grid_macd_cross_filters_invalid           PASSED
tests/test_sweep.py::test_generate_param_grid_bias_filters_invalid                 PASSED
tests/test_sweep.py::test_generate_param_grid_unknown_strategy_returns_all         PASSED
tests/test_sweep.py::test_generate_param_grid_max_combos_constant                  PASSED
tests/test_sweep.py::test_sweep_param_specs_excludes_dca                           PASSED
tests/test_sweep.py::test_run_parameter_sweep_exceeds_limit_raises                 PASSED
tests/test_sweep.py::test_run_parameter_sweep_correct_combo_count                  PASSED
tests/test_sweep.py::test_run_parameter_sweep_filters_invalid_combos               PASSED
tests/test_sweep.py::test_run_parameter_sweep_captures_errors                      PASSED
tests/test_sweep.py::test_run_parameter_sweep_result_fields                        PASSED
tests/test_sweep.py::test_run_parameter_sweep_sample_warning_set_when_few_trades   PASSED
tests/test_sweep.py::test_run_parameter_sweep_error_result_is_none                 PASSED
tests/test_sweep.py::test_run_parameter_sweep_metadata                             PASSED
tests/test_sweep.py::test_save_sweep_result_csv_creates_file                       PASSED
tests/test_sweep.py::test_save_sweep_result_csv_sample_warning_flag                PASSED
tests/test_sweep.py::test_save_sweep_result_csv_profit_factor_sentinel             PASSED
tests/test_sweep.py::test_run_parameter_sweep_real_ma_cross                        PASSED

========================= 29 passed =========================
```

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 29/29 sweep 測試 PASSED | **必須** |
| 不合法參數組合被過濾（回傳 list 中不含） | **必須** |
| 超過 200 組合時拋 ValueError | **必須** |
| `total_trades < 3` 的結果 `sample_warning=True` | **必須** |

#### 各測試驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_generate_param_grid_ma_cross_all_valid` | MA `short=[5,10,20]` × `long=[40,60,120]` → 9 組全合法；total_combos 為笛卡兒積大小（過濾前） | 回傳了過濾後的數量 |
| `test_generate_param_grid_ma_cross_filters_invalid` | MA `short=[5,20,50]` × `long=[10,30]` 中 `short>=long` 的 3 組被排除 | `_normalize_moving_average_params` 沒正確回傳 None |
| `test_generate_param_grid_rsi_filters_invalid` | RSI `oversold=[30,70]` × `overbought=[70,80]` 排除 `oversold==overbought` | normalize 函式的比較方向寫反 |
| `test_run_parameter_sweep_exceeds_limit_raises` | 15×15=225 合法組合 → `ValueError` 含「超過上限」 | 上限檢查在過濾前做（應在過濾後） |
| `test_run_parameter_sweep_correct_combo_count` | MA `short=[5,10]` × `long=[20,40]` → 4 筆結果，`valid_combos=4` | `_build_strategy` 對 float 型 params 不相容 |
| `test_run_parameter_sweep_sample_warning_set_when_few_trades` | `total_trades=2 < 3` → `SweepRunSummary.sample_warning=True` | 忘記在 `run_parameter_sweep` 中設定 flag |
| `test_run_parameter_sweep_captures_errors` | 引擎拋例外 → `error` 有值，`result=None`，`sample_warning=False` | except 範圍太窄 |
| `test_save_sweep_result_csv_creates_file` | CSV 包含參數欄 + metrics 欄 + `sample_warning` 欄 | 參數欄名不一致 |
| `test_parse_param_values_dedup_sort` | `"20,5,10,10"` → `[5.0, 10.0, 20.0]`（去重 + 升序） | 沒去重或排序 |
| `test_parse_param_values_floats` | `"1.5,2.0,2.5"` → `[1.5, 2.0, 2.5]` | 全部轉 int 導致精度損失 |

#### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 |
|---|---------|------|---------|
| 1 | 參數掃描 tab | 開啟回測頁，點選「參數掃描」tab | 顯示策略類型選擇 + 參數輸入 |
| 2 | 動態參數顯示 | 選 RSI 策略類型 | 顯示 RSI 週期、超賣門檻、超買門檻三個輸入框 |
| 3 | 組合數即時計算 | 輸入 `7,14,21` + `20,30` + `70,80` | 顯示「總組合數: 12 → 合法組合: 12」 |
| 4 | 不合法組合過濾 | MA Cross：`short=5,10,20` + `long=10,20,40` | 顯示合法組合數 < 9（排除 short>=long） |
| 5 | 超出上限警告 | 輸入大量參數使組合 > 200 | 顯示黃色警告，掃描按鈕不可按 |
| 6 | 掃描執行 | RSI `period=7,14,21` × `oversold=20,30` × `overbought=70,80` → 開始掃描 | 進度條顯示，完成後出現結果表 |
| 7 | 排序切換 | 在結果表上切換排序欄位 | 表格重新排序 |
| 8 | 樣本不足警告 | 查看 `total_trades < 3` 的結果列 | 交易次數欄顯示「⚠ 樣本不足」 |
| 9 | 回撤過深警告 | 查看 `max_drawdown > 50%` 的結果列 | 最大回撤欄顯示「⚠ 回撤過深」 |
| 10 | 選中展開圖表 | 選某筆結果 | 展開 Tearsheet + K 線圖 + 指標副圖 |
| 11 | 匯出掃描結果 | 點「匯出掃描結果」 | CSV 檔案建立，參數欄 + metrics 欄正確 |
| 12 | 其他 tab 不受影響 | 切回單次回測或策略比較 | 功能正常，不退化 |

#### 故障排除

| 問題 | 解法 |
|------|------|
| 參數輸入框不顯示 | 確認 `STRATEGY_META[type]["param_labels"]` 有值；DCA 被排除不顯示 |
| 掃描按鈕始終不可按 | 確認參數輸入框有值且合法組合 > 0 且 <= 200 |
| 掃描結果全為 error | 確認 `_build_strategy` 的 preset 格式含 `type` 和 `params` |
| 排序不生效 | 確認 DataFrame 排序欄位名稱與 selectbox 選項對應 |
| CSV 參數欄名不一致 | 確認 `SweepRunSummary.params` 的 key 與策略 `__init__` 參數名一致 |

---

### 7-D Walk-Forward Analysis

#### 測試對象

| 模組 | 測試檔 |
|------|--------|
| `src/backtest/walk_forward.py` | `tests/test_walk_forward.py` |
| `src/ui/pages/backtest.py`（Walk-Forward tab） | 手動驗收 |

#### 前置條件

- [x] Phase 7-C 全階段回歸通過
- [x] `src/backtest/walk_forward.py` 已實作 window splitter、WFA runner、aggregate、parameter stability、CSV export
- [x] `backtest.py` 已新增「Walk-Forward（滾動樣本外驗證）」tab
- [x] UI 中英文研究術語均有中文 label、help text 或 tooltip

#### 7-D-1 驗證紀錄（2026-05-09）

```bash
.\.venv\Scripts\python.exe -m pytest tests/test_walk_forward.py tests/test_sweep.py -v
```

結果：**62 passed, 1 warning**。

第一次一般權限執行時，8 個 CSV 相關測試在 pytest `tmp_path` fixture setup 階段遇到 Windows temp 目錄權限錯誤：

```text
PermissionError: [WinError 5] 存取被拒。: 'C:\Users\User\AppData\Local\Temp\pytest-of-User'
```

依本專案 Windows/OneDrive pytest 規則，使用同一 venv 與同一測試範圍 elevated 重跑後通過。測試本體無失敗。

`warning_count` 問題已補 regression test：

```text
tests/test_walk_forward.py::test_warning_count_includes_per_window_and_unstable_param PASSED
```

#### 7-D-2 驗收紀錄（2026-05-10）

7-D-2 UI tab + 文件 + 回歸已驗收完成。

已確認項目：

- Walk-Forward tab 已新增，且保留單次回測、策略比較、參數掃描、歷史結果 tabs。
- UI 中 Walk-Forward、IS、OOS、Sharpe Ratio、CV 等英文研究術語均有中文說明。
- 預設值顯示 IS=12 月、OOS=6 月、Step=6 月、Max windows=10。
- UI 會依日期區間、IS/OOS/Step 與合法參數組合數顯示回測次數預估。
- 合法組合數超過 200 時顯示警告，Run button disabled。
- `st.progress` 顯示目前執行到第幾段 WFA window。
- Summary metrics、window table、parameter stability table、視窗警告詳情與 CSV 匯出入口已具備。
- sweep 失敗視窗會標記「掃描失敗，跳過」，且不計入彙總。

驗證指令：

```bash
.\.venv\Scripts\python.exe -m py_compile src\ui\pages\backtest.py src\backtest\walk_forward.py
```

結果：**通過**。

第一次一般權限執行時因 Windows/OneDrive `__pycache__` 寫入權限失敗，使用同一 venv 與同一範圍 elevated 重跑後通過。

```bash
.\.venv\Scripts\python.exe -m pytest tests/test_strategies.py tests/test_strategy_config.py tests/test_batch.py tests/test_sweep.py tests/test_walk_forward.py -v
```

結果：**116 passed, 1 warning**。

#### 執行指令

**walk-forward 單元測試：**

```bash
uv run pytest tests/test_walk_forward.py -v
```

預期結果：

**33 passed**。

**與 7-C 聯合回歸：**

```bash
uv run pytest tests/test_sweep.py tests/test_walk_forward.py -v
```

預期結果：**62 passed**（29 sweep + 33 walk-forward）。

#### 通過標準

| 條件 | 必要性 |
|------|--------|
| 33/33 walk-forward 測試 PASSED | **必須** |
| rolling IS/OOS 視窗切割日期正確 | **必須** |
| 資料不足時阻止執行並顯示最低月數需求 | **必須** |
| WFA 視窗數 MVP 硬上限 10 段 | **必須** |
| IS sweep 使用 IS data，OOS 驗證使用 OOS data | **必須** |
| `profit_factor` 不可作為 optimize metric | **必須** |
| sweep 全失敗視窗 `skipped=True` 且不計入 aggregate | **必須** |
| `total_window_count` / `valid_window_count` / `skipped_window_count` 語意正確 | **必須** |
| `warning_count` 同時計入 per-window warnings 與 aggregate warnings | **必須** |
| CV 門檻分類 stable / moderate / unstable 正確 | **必須** |
| CSV 產出 window summary 與 parameter stability 兩份檔案 | **必須** |

#### 各測試驗證重點

| 測試名稱 | 驗證什麼 | 失敗常見原因 |
|----------|---------|-------------|
| `test_generate_walk_forward_windows_basic` | 以 12/6/6 月產生連續 rolling IS/OOS 視窗 | month offset 邊界含頭含尾處理錯 |
| `test_generate_walk_forward_windows_requires_enough_data` | 少於 `is + oos + (min_windows-1)*step` 時拋 ValueError | 只檢查一段視窗，沒檢查 min_windows |
| `test_generate_walk_forward_windows_max_windows_cap` | 長資料最多產生 10 段 | 忘記套 MVP hard cap |
| `test_required_months_for_wfa_formula` | 預設最低月數為 30 個月 | 公式少算 step 段數 |
| `test_run_walk_forward_uses_is_for_sweep_and_oos_for_validation` | IS/OOS 資料沒有重疊且傳給正確流程 | 切片日期 off-by-one |
| `test_run_walk_forward_selects_best_params_by_metric` | `sharpe_ratio` / `total_return` 等 metric 選最佳參數 | 對格式化字串排序，而非 raw float |
| `test_run_walk_forward_rejects_profit_factor_metric` | `profit_factor` 被拒絕 | sentinel 999.0 會污染排序 |
| `test_run_walk_forward_records_degradation` | `degradation = oos_metric - is_metric` | 用比率型公式或方向寫反 |
| `test_run_walk_forward_sets_low_trade_warning` | OOS `total_trades < 3` 時標記 warning | 只沿用 IS 的 sample_warning |
| `test_run_walk_forward_skips_failed_sweep_window` | IS sweep 無有效結果時跳過視窗 | 仍拿空 params 跑 OOS |
| `test_walk_forward_valid_window_count_excludes_skipped` | aggregate 只統計有效視窗 | skipped 視窗混入平均值 |
| `test_warning_count_includes_per_window_and_unstable_param` | per-window warnings 與 unstable param aggregate warnings 會一併計入 `warning_count` | unstable param warning 加入後覆蓋原 warning_count |
| `test_parameter_stability_stable` | CV < 0.15 → stable | 使用 max-min 而非 CV |
| `test_parameter_stability_moderate` | 0.15 <= CV < 0.40 → moderate | 門檻邊界寫錯 |
| `test_parameter_stability_unstable` | CV >= 0.40 或 mean=0 → unstable | mean=0 除以零 |
| `test_walk_forward_excludes_dca` | DCA 不支援 WFA | DCA 被列入可掃描策略 |
| `test_walk_forward_respects_max_combos` | 合法組合 > 200 時阻止 | WFA 繞過 7-C sweep 上限 |
| `test_save_walk_forward_csv_creates_files` | 建立 window + stability 兩份 CSV | 只輸出一份或欄位缺失 |

#### 手動驗收清單

| # | 檢查項目 | 步驟 | 預期結果 |
|---|---------|------|---------|
| 1 | Walk-Forward tab | 開啟回測頁，點選「Walk-Forward」tab | 顯示 WFA 控制項，且英文術語附中文說明 |
| 2 | 預設參數 | 進入 tab 不改設定 | IS=12 月、OOS=6 月、Step=6 月、Max windows=10 |
| 3 | 資料不足阻止 | 選資料少於 30 個月的區間 | 執行前阻止，顯示最低資料需求訊息 |
| 4 | 回測次數預估 | 輸入合法參數範圍 | 顯示「{windows} 段 × {combos} 組 = {total} 次回測」 |
| 5 | 超過 combo 上限 | 輸入合法組合 > 200 的範圍 | 顯示警告，Run button 不可按 |
| 6 | 進度條 | 執行 WFA | `st.progress` 顯示目前第幾段 window |
| 7 | Summary metrics | WFA 完成 | 顯示有效視窗數、OOS 勝率、平均 OOS return、平均 OOS Sharpe、最差 OOS drawdown、平均 degradation |
| 8 | Window table | 查看明細表 | 每段顯示 IS/OOS 起訖、best params、IS/OOS metrics、degradation、warnings |
| 9 | 參數穩定性 | 查看 stability table | 每個參數顯示 min/median/max/CV/status |
| 10 | sweep 失敗視窗 | 使用可觸發失敗的 mock/測試資料 | 該 window 標記「掃描失敗，跳過」，不計入彙總 |
| 11 | CSV 匯出 | 點下載/匯出 | 產出 window summary CSV 與 parameter stability CSV |
| 12 | 其他 tab 不受影響 | 切回單次回測、策略比較、參數掃描 | 原功能正常，不退化 |

#### 故障排除

| 問題 | 解法 |
|------|------|
| 視窗數少於預期 | 檢查 data index 日期範圍、timezone-aware 狀態與 month offset 邊界 |
| 明明有資料卻被判資料不足 | 確認 actual months 計算用資料首尾日期，而非 row count |
| WFA 跑太久 | 檢查 combo_count 與 window_count；MVP 最多 10 段、200 組 |
| OOS 結果全為 0 trade | 檢查策略在 OOS 區間是否有訊號；這是有效研究結果，不一定是 bug |
| aggregate 數字對不起來 | 確認 skipped=True 的視窗沒有被納入平均 |
| stability 顯示 unstable 過多 | 檢查最佳參數是否每段大幅跳動；也可能代表策略對參數敏感 |
| 英文欄位沒有中文說明 | UI label / help text / tooltip 必須補中文說明 |
| CSV 中文亂碼 | 使用 `utf-8-sig` 輸出 |

---

### Phase 7 全階段回歸測試

```bash
uv run pytest tests/test_strategies.py tests/test_strategy_config.py tests/test_batch.py tests/test_sweep.py tests/test_walk_forward.py -v
```

預期結果：**116 個測試全部 PASSED**（目前 Phase 7 驗收回歸實測結果：116 passed, 1 warning）。

Phase 1-7 全回歸：

```bash
uv run pytest tests/ -v -m "not integration" --tb=short
```

預期結果：**全部 PASSED**。

> Phase 7-D 完成後，Phase 7 全階段回歸需包含 7-A / 7-B / 7-C / 7-D。

---

## Phase 8：個股綜合分析儀表板

### 8-A 技術面自動判讀引擎

**新增測試檔：** `tests/test_technical_summary.py`

**執行指令：**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_technical_summary.py -v
```

**測試項目（13 個）：**

| 測試名稱 | 驗證內容 |
|------|------|
| `test_bullish_trend_fixture` | 多頭排列 fixture → trend/ma/kd/macd 欄位正確 |
| `test_bearish_trend_fixture` | 空頭排列 fixture → 正確空頭判讀 |
| `test_consolidation_fixture` | 盤整 fixture → 正確盤整判讀 |
| `test_short_term_score_range` | 分數在 0.0~1.0 範圍 |
| `test_short_term_score_weights` | 各組件權重正確加總（MA 30% + KD 25% + 量價 25% + 突破 20%） |
| `test_resistance_levels_found` | 壓力區正確計算（近 60/20 日高點） |
| `test_support_levels_found` | 支撐區正確計算（近期低點 + MA20 + MA60） |
| `test_volume_status_high` | 量 > 1.5x 5日均量 → "量能放大" |
| `test_volume_status_low` | 量 < 0.7x 5日均量 → "量能略縮" |
| `test_insufficient_data_5bars` | 僅 5 bar → MA60/KD/MACD 相關欄位標示資料不足，不 crash |
| `test_all_nan_data` | 全 NaN → 不 crash，欄位標示資料不足 |
| `test_ma_bias_calculation` | 乖離率 = (close - MA20) / MA20 × 100% |
| `test_chip_behavior_injection` | chip_behavior 參數正確注入 TechnicalSummary 輸出 |

**Fixture 構造要點：**

- 多頭：MA5 > MA20 > MA60，close 在均線上方，K > 50，DIF > DEA > 0
- 空頭：MA5 < MA20 < MA60，close 在均線下方，K < 50，DIF < DEA < 0
- 盤整：MA 交錯，close 在 MA20 附近
- 至少 60+ bar 確保所有指標可計算

**通過標準：** 13 個測試全部 PASSED。

---

### 8-B K 線型態辨識

**新增測試檔：** `tests/test_pattern.py`

**執行指令：**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_pattern.py -v
```

**測試項目（15 個）：**

| 測試名稱 | 驗證內容 |
|------|------|
| `test_detect_long_red_candle` | body > 2× avg body 且陽線 → 長紅 K |
| `test_detect_long_black_candle` | body > 2× avg body 且陰線 → 長黑 K |
| `test_detect_doji` | cdl_doji 正確辨識十字線 |
| `test_detect_hammer` | cdl_pattern hammer 正確辨識錘子 |
| `test_detect_engulfing` | cdl_pattern engulfing 正確辨識吞噬 |
| `test_no_pattern_detected` | 普通 K 棒不誤報（所有 detected=False） |
| `test_w_bottom_detected` | W 底 fixture（2 相近低點 + 中間反彈）→ formed=True |
| `test_w_bottom_not_formed` | 非 W 底數據 → formed=False，description 含「未形成」 |
| `test_m_top_detected` | M 頭 fixture → formed=True |
| `test_m_top_not_formed` | 非 M 頭 → formed=False |
| `test_tolerance_boundary` | 兩低點誤差剛好 3% → 邊界行為正確 |
| `test_multi_timeframe_bullish` | 日/週/月多頭 fixture → 三個 trend_direction 均為多頭 |
| `test_multi_timeframe_mixed` | 混合趨勢 fixture → 各週期獨立判讀 |
| `test_resample_ohlcv_correct` | 週 K resample：open=first, high=max, low=min, close=last, volume=sum |
| `test_insufficient_data_for_pattern` | < 20 bar → 不 crash，chart pattern 回傳 formed=False |

**Fixture 構造要點：**

- W 底：近 60 日有兩個低點（如 750, 755），中間反彈到 790+
- M 頭：近 60 日有兩個高點（如 850, 847），中間回落到 810-
- 多週期：至少 120 日資料確保月 K 有足夠 bars

**通過標準：** 15 個測試全部 PASSED。

---

### 8-C 籌碼分析管線

**新增測試檔：** `tests/test_chip_analysis.py`

**執行指令：**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_chip_analysis.py -v
```

**測試項目（14 個）：**

| 測試名稱 | 驗證內容 |
|------|------|
| `test_fetch_institutional_returns_wide_format` | pivot 後含 INSTITUTIONAL_COLUMNS 所有欄位 |
| `test_fetch_institutional_aggregates_foreign` | 外資 = Foreign_Investor + Foreign_Dealer_Self |
| `test_fetch_institutional_aggregates_dealer` | 自營商 = Dealer_self + Dealer_Hedging |
| `test_fetch_institutional_unit_is_shares` | 回傳單位為股（非張） |
| `test_fetch_margin_columns_correct` | 含 MARGIN_COLUMNS 所有欄位 |
| `test_fetch_margin_unit_is_lots` | 回傳單位為張 |
| `test_save_load_institutional_roundtrip` | save → load → DataFrame 完全一致 |
| `test_save_load_margin_roundtrip` | save → load → DataFrame 完全一致 |
| `test_incremental_fetch_only_missing_days` | 本地有 30 天，只呼叫 API 補 31 天後 |
| `test_chip_summary_concentrated` | 三大法人連續 N 日同方向買入 → "集中" |
| `test_chip_summary_dispersed` | 連續同方向賣出 → "分散" |
| `test_chip_summary_stable` | 多空交錯 → "穩定" |
| `test_chip_summary_label_format` | label 格式正確（如 "買超 36 張"） |
| `test_chip_summary_empty_data` | 空 DataFrame 不 crash |

**Fixture 構造要點：**

- 法人 long format mock：每天 5 筆 row（5 個 name 值），3~5 天
- 融資融券 wide format mock：每天 1 筆 row，3~5 天
- 增量測試：先存 30 天，再 mock API 回傳第 31 天起

**通過標準：** 14 個測試全部 PASSED。

**整合測試（需網路）：**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_chip_analysis.py -v -m integration
```

若有 `@pytest.mark.integration` 標記的 FinMind 真實 API 測試，需在有網路環境執行。

---

### 8-D 即時行情接入

**新增測試檔：** `tests/test_realtime.py`

**執行指令：**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_realtime.py -v
```

**測試項目（13 個）：**

| 測試名稱 | 驗證內容 |
|------|------|
| `test_parse_twse_response_normal` | 正常回傳 → RealtimeQuote 各欄位正確 |
| `test_parse_twse_response_no_trade` | z="-" → price 設為 yesterday_close |
| `test_parse_five_levels` | 五檔報價 "_" 分隔解析正確 |
| `test_parse_five_levels_partial` | 不足五檔（有空字串）→ 不 crash，取實際數量 |
| `test_change_calculation` | change = price - yesterday_close, change_pct 正確 |
| `test_cache_hit_within_ttl` | TTL 內第二次呼叫不觸發 HTTP request |
| `test_cache_miss_after_ttl` | TTL 過期後重新打 API |
| `test_market_map_tse` | type=twse → ex_ch 用 tse_ prefix |
| `test_market_map_otc` | type=tpex → ex_ch 用 otc_ prefix |
| `test_market_map_fallback` | market_map 查無此 symbol → fallback tse |
| `test_bid_ask_structure_buy_dominant` | 買方佔比 > 0.55 → "買盤較積極" |
| `test_bid_ask_structure_balanced` | 買賣方接近 → "多空均衡" |
| `test_is_market_open_detection` | 09:00-13:30 盤中 True，其餘 False |

**Fixture 構造要點：**

- Mock TWSE API response JSON（不真正打 API）
- 用 `monkeypatch` mock `requests.Session.get` 或內部 request helper，不新增 HTTP mock 套件依賴
- 快取測試用 `monkeypatch` 固定 `time.time()`，不新增 `freezegun` 依賴

**通過標準：** 13 個測試全部 PASSED。

**整合測試（需網路）：**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_realtime.py -v -m integration
```

真實打 TWSE MIS API 測試（盤中才有即時報價）。

---

### 8-E AI 綜合分析與操作劇本

**修改測試檔：** `tests/test_advisor.py`（追加）

**執行指令：**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_advisor.py -v
```

**新增測試項目（4 個）：**

| 測試名稱 | 驗證內容 |
|------|------|
| `test_dashboard_analysis_returns_correct_structure` | mock LLM → 回傳 DashboardAnalysis 所有欄位有值 |
| `test_dashboard_analysis_scenarios_count` | scenarios 恰好 3 個 TradingScenario |
| `test_dashboard_analysis_without_chip` | chip_summary=None → 仍正常回傳（不 crash） |
| `test_dashboard_analysis_ai_disabled` | ai.enabled=false → raise AIDisabledError |

**Fixture 構造要點：**

- Mock LLM 回傳 structured output JSON
- TechnicalSummary fixture 重用 8-A 測試的多頭 fixture

**通過標準：** 含既有 advisor 測試一同通過。

---

### 8-F 個股儀表板 UI

**新增測試檔：** `tests/test_dashboard_page.py`

**執行指令：**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_dashboard_page.py -v
```

**單元測試項目（8 個）：**

| 測試名稱 | 驗證內容 |
|------|------|
| `test_dashboard_page_imports` | `from src.ui.pages.dashboard import render_dashboard_page` 不 crash |
| `test_dashboard_page_render_no_symbol` | 未輸入代碼時顯示提示文字 |
| `test_dashboard_tab_overview_renders` | Tab 1 各區塊函式可呼叫不 crash |
| `test_dashboard_tab_chip_no_data` | chip=None 時不 crash |
| `test_dashboard_tab_ai_disabled` | ai.enabled=false 時顯示降級文字 |
| `test_dashboard_option_menu_entry` | app.py source 含「個股分析」字串 |
| `test_dashboard_page_not_ready_payload_does_not_render_tabs` | 缺本機日線資料時不進入 tabs、不觸發 KeyError |
| `test_dashboard_page_refresh_quote_updates_session_payload` | 「重新整理報價」更新 session payload quote/bid_ask 並 rerun |

**手動驗收項目（12 項）：**

1. 輸入 2330（上市）→ 4 個 tab 正確顯示
2. 輸入 6547（上櫃）→ 即時報價正確路由（otc_ prefix）
3. 盤後查詢 → 顯示「盤後資料」標籤
4. 「重新整理報價」按鈕 → 報價數字更新
5. Tab 2 → 法人近 5 日買賣超正確顯示
6. Tab 3 → K 線型態表有偵測結果
7. Tab 4 → 三情境操作劇本正確顯示
8. `ai.enabled=false` → Tab 4 降級為「請啟用 AI 功能」
9. 6 套主題切換 → 儀表板配色與文字均可讀
10. 不影響回測頁功能（跑一次回測驗證）
11. 不影響設定頁功能（切主題、儲存 preset）
12. 不影響 AI 問答頁功能（問一個問題）

**手動驗收指令：**

```powershell
.\.venv\Scripts\python.exe -m streamlit run src/ui/app.py
```

**通過標準：** 8 個單元測試 PASSED + 12 項手動驗收通過。

---

### 8-G 新手友善說明文字

**修改測試檔：** `tests/test_dashboard_page.py`（追加）

**執行指令：**

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_dashboard_page.py -v
```

**新增測試項目（8 個）：**

| 測試名稱 | 驗證內容 |
|------|------|
| `test_help_texts_has_all_required_keys` | `_HELP_TEXTS` dict 包含所有 23 個必要 key（6 技術面 + 2 壓力支撐 + 1 分數 + 4 籌碼法人 + 2 融資融券 + 1 買賣力道 + 3 量價結構 + 4 多週期含強度），無遺漏 |
| `test_pattern_details_has_all_10_candle_patterns` | `_PATTERN_DETAILS` dict 包含 10 種 K 棒型態名稱（長紅 K、長黑 K、十字線、錘子、吊人、吞噬、晨星、夜星、帶上影線、帶下影線） |
| `test_pattern_details_has_chart_patterns` | `_PATTERN_DETAILS` dict 包含 W底（雙底）和 M頭（雙頂） |
| `test_help_texts_values_are_nonempty_strings` | 所有 `_HELP_TEXTS` 值為非空字串（`isinstance(v, str) and len(v) > 0`） |
| `test_pattern_details_values_are_nonempty_strings` | 所有 `_PATTERN_DETAILS` 值為非空字串（`isinstance(v, str) and len(v) > 0`） |
| `test_tab_overview_renders_with_help_texts` | Tab 1 `_render_tab_overview()` 含 help 參數呼叫不 crash |
| `test_tab_chip_renders_with_help_texts` | Tab 2 `_render_tab_chip()` 含 help 參數呼叫不 crash |
| `test_tab_pattern_renders_with_details` | Tab 3 `_render_tab_pattern()` 含 detail caption 呼叫不 crash |

**Fixture 構造要點：**

- 重用既有 8-F 測試的 TechnicalSummary / CandlePattern / ChipSummary fixture
- dict 完整性測試直接 import `_HELP_TEXTS` 和 `_PATTERN_DETAILS` 常數
- render 不 crash 測試使用既有 Streamlit mock 機制

**手動驗收項目（6 項）：**

1. Tab 1 → 6 個 metric 的 `?` tooltip hover 後顯示完整說明
2. Tab 1 → 壓力區/支撐區/短線綜合分數下方有 caption 說明
3. Tab 2 → 三大法人/融資融券 metric tooltip 顯示正確
4. Tab 2 → 量價結構分析 3 行下方各有 caption 說明
5. Tab 3 → 偵測到的型態有 caption 詳細說明；未偵測到型態表有「詳細」欄
6. Tab 3 → 日/週/月線 metric tooltip 顯示正確、上方有強度說明 caption

**手動驗收指令：**

```powershell
.\.venv\Scripts\python.exe -m streamlit run src/ui/app.py
```

**通過標準：** 含既有 dashboard 測試一同通過（既有 + 8 新增全部 PASSED）+ 6 項手動驗收通過。

---

### Phase 8 全階段回歸測試

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_technical_summary.py tests/test_pattern.py tests/test_chip_analysis.py tests/test_realtime.py tests/test_advisor.py tests/test_dashboard_page.py -v
```

預期結果：**所有 Phase 8 新增測試 + 既有 advisor 測試全部 PASSED**。

Phase 1-8 全回歸：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/ -v -m "not integration" --tb=short
```

預期結果：**全部 PASSED**。

> Phase 8 完成後，全專案回歸需包含 Phase 1-7 既有測試 + Phase 8 新增測試。

---

## 全專案最終回歸測試

**所有 Phase 完成後，執行完整回歸：**

### 單元測試（不需網路）

```bash
uv run pytest tests/ -v -m "not integration" --tb=short
```

預期：**至少 60+ 個測試全部 PASSED**。

### 含整合測試

```bash
uv run pytest tests/ -v --tb=short
```

預期：**至少 67+ 個測試全部 PASSED**。

### 覆蓋率報告

```bash
uv run pytest tests/ --cov=src --cov-report=term-missing -m "not integration"
```

目標覆蓋率：

| 模組 | 目標 |
|------|------|
| `src/data/` | > 70% |
| `src/backtest/` | > 70% |
| `src/analysis/` | > 70% |
| `src/indicators/` | > 60% |
| `src/ai/` | > 50%（provider adapter 以 mock 為主，真實 API 走 integration） |
| `src/ui/` | 不計入（無法自動化測試） |
| **整體** | > 65% |

---

## 測試數量統計總覽

| Phase | 子階段 | 測試檔 | 單元測試數 | 整合測試數 | 小計 |
|-------|--------|--------|-----------|-----------|------|
| **1-A** | 資料源連線 | `test_fetcher.py` | 3 | 3 | 6 |
| **1-B** | Parquet 存儲 | `test_storage.py` | 5 | 0 | 5 |
| **1-C** | 資料品質 | `test_cleaner.py` | 6 | 0 | 6 |
| **1-D** | 除權息調整 | `test_cleaner.py`（追加） | 5 | 0 | 5 |
| **2-A** | 訊號生成 | `test_engine_vec.py` | 4 | 0 | 4 |
| **2-B** | 摩擦成本 | `test_cost.py` | 8 | 0 | 8 |
| **2-C** | 績效指標 | `test_metrics.py` | 6 | 0 | 6 |
| **2-D** | Tearsheet | `test_report.py` + `test_engine_vec.py` | 3 | 0 | 3 |
| **3-A** | 事件結構 | `test_events.py` | 7 | 0 | 7 |
| **3-B** | Account | `test_account.py` | 8 | 0 | 8 |
| **3-C** | 事件迴圈 | `test_engine_event.py` | 6 | 0 | 6 |
| **3-D** | 策略範例 | `test_engine_event.py`（追加） | 3 | 0 | 3 |
| **3-E** | 一致性驗證 | `test_consistency.py` | 4 | 0 | 4 |
| **4-A** | LLM Provider API | `test_advisor.py` | 4 | 1 | 5 |
| **4-B** | 指標工具 | `test_indicators.py` | 8 | 0 | 8 |
| **4-C** | Streamlit UI | （手動驗收 12 項） | — | — | 12 項手動 |
| **4-D** | 端到端 | `test_e2e.py` | 0 | 3 | 3 |
| **6-A** | 主題切換 | `test_themes.py` + `test_config_ui_section.py` | 5 | 0 | 5（+8 項手動） |
| **6-B** | 設定頁與側邊欄 UI 小修 | `test_themes.py` + `test_config_ui_section.py` + `test_strategy_config.py`（可追加 `test_settings_page.py`） | 待實作 | 0 | 待實作（+10 項手動） |
| **7-A** | 策略擴充 | `test_strategies.py` + `test_strategy_config.py` | 37 | 0 | 37（+9 項手動） |
| **7-B** | 策略研究工作台 | `test_batch.py` | 7 | 0 | 7（+12 項手動） |
| **7-C** | 參數掃描 | `test_sweep.py` | 29 | 0 | 29（+12 項手動） |
| **7-D** | Walk-Forward Analysis | `test_walk_forward.py` | 33 | 0 | 33（+12 項手動，已驗收） |
| **8-A** | 技術面自動判讀 | `test_technical_summary.py` | 13 | 0 | 13 |
| **8-B** | K 線型態辨識 | `test_pattern.py` | 15 | 0 | 15 |
| **8-C** | 籌碼分析管線 | `test_chip_analysis.py` | 14 | 0 | 14 |
| **8-D** | 即時行情接入 | `test_realtime.py` | 13 | 0 | 13 |
| **8-E** | AI 綜合分析 | `test_advisor.py`（追加） | 4 | 0 | 4 |
| **8-F** | 儀表板 UI | `test_dashboard_page.py` | 8 | 0 | 8（+12 項手動） |
| **8-G** | 新手友善說明文字 | `test_dashboard_page.py`（追加） | 8 | 0 | 8（+6 項手動） |
| | | **合計** | **265** | **7** | **272 + 83 項手動** |
