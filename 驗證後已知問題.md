# 已知問題追蹤

> 建立日期：2026-04-25
> 用途：記錄驗收過程中發現、**確定需要在後續 phase 處理**的問題。
> 不放：規格已明定不做的（請看 `未涵蓋資料項目.md`）、已自我修正後驗證不是問題的觀察。
>
> 每筆紀錄格式：
> - **位置**：哪個檔案 / 行號
> - **狀況**：問題是什麼
> - **風險**：不處理會發生什麼
> - **處理階段**：預計在哪個 phase 修
> - **發現於**：哪次驗收
>
> 標籤格式：`[P{phase}]` 表示預計處理階段，例如 `[P2-D]` 是 Phase 2-D。
> 處理完成後追加 `[✅ 已處理 @ commit-hash]`，並在條目底部加「處理摘要」。

---

## [P4-C] [✅ 已處理 @ cdff35b] 回測頁績效摘要缺少「交易次數」，容易誤解 0 報酬原因

- **位置**：`src/ui/pages/backtest.py` 回測結果摘要區
- **狀況**：目前摘要只顯示總報酬、年化報酬、最大回撤、Sharpe。當 MA Cross 在指定區間沒有成交時，所有績效值皆為 0，但使用者無法從摘要立即看出原因是 `交易次數 = 0`。
- **風險**：使用者可能誤以為回測引擎或績效計算壞掉，而不是策略在該期間沒有進場。
- **處理階段**：4-C（Streamlit UI）— 在回測頁績效摘要新增「交易次數」，並放在「總報酬」左邊。
- **發現於**：4-C 手動驗收，2330、2026/01/01–2026/04/26、MA 30/60。
- **處理摘要**：`src/ui/pages/backtest.py:237-238` 已將績效摘要改為 5 欄，並在最左側新增 `m0.metric("交易次數", f"{int(result.total_trades)}")`。驗證：`tests/test_backtest_page.py` 7/7 通過；目前未另加直接 assert metric 文字的 UI 測試。

---

## [P3-C/P4-C] [✅ 已處理 @ e6ffc19] 事件驅動引擎 MARKET BUY 可能因隔日開盤價與成本超出現金而失敗

- **位置**：`src/strategy/examples/ma_cross.py` 的下單數量計算、`src/backtest/engine_event.py` 的 MARKET order 撮合、`src/backtest/account.py` 的現金檢查
- **狀況**：策略在訊號日用當日 close 與當下現金計算可買股數，但事件驅動引擎採 next-bar open 成交，且 MARKET BUY 還會套用滑價與手續費。若隔日開盤價高於訊號日 close，實際買進成本可能超過現金，觸發 `Insufficient cash for BUY`。
- **重現案例**：4-C 手動驗收，2330、2025/01/01–2026/04/26、事件驅動引擎、MA 5/20。2025-08-28 產生 BUY 訊號時 close=1160，隔日 open=1180，實際 MARKET 成本超過帳戶可用現金。
- **風險**：使用者在 UI 選事件驅動引擎時會看到回測失敗；更深層問題是 order sizing 放在策略內，策略不知道實際成交價、滑價與成本，責任邊界不乾淨。
- **處理決策**：採方案 3 — 將 order sizing 從策略移到 broker / execution layer，由成交價與成本模型決定實際可買數量。策略只表達買賣意圖，不直接決定最終可成交股數。
- **處理階段**：事件驅動引擎修正階段（P3-C 延伸 / P4-C UI 暴露）。修正時需同步更新策略介面說明與事件引擎測試。
- **發現於**：4-C 手動驗收。
- **處理摘要**：`src/strategy/examples/ma_cross.py:16` 改以 `_BUY_INTENT_QUANTITY` 表達買進意圖，策略不再用訊號日 close 與現金自行決定最終買進股數；`src/backtest/engine_event.py:230-333` 在實際成交時用 next-bar open、滑價後成交價、commission 重新解析最大可買股數。驗證：`tests/test_engine_event.py::test_market_buy_is_resized_to_affordable_quantity`、`test_ma_cross_event_same_entry_points`、`test_event_engine_matches_vectorized` 均通過。

---

## [P2-D] [✅ 已處理 @ 451e2aa] 999.0 sentinel value 在 tearsheet 顯示時要轉成 N/A

- **位置**：`src/backtest/metrics.py` L62（sortino）、L67（calmar）、L87（profit_factor）
- **狀況**：當分母為零或無下方風險時，三個比率欄位回傳 `999.0` 作為 sentinel
- **風險**：
  1. Tearsheet 直接顯示 "999.00" 會誤導使用者以為是真實高分
  2. 多策略 ensemble / Walk-Forward 取均值時 999 會嚴重污染統計
- **處理階段**：2-D（tearsheet 渲染層）— 顯示前判斷 `== 999.0` 改為 "N/A" 或 "∞"；同時確認 3-B（Walk-Forward）若做平均要先過濾 999
- **發現於**：驗收 2-C
- **處理摘要**：`src/backtest/report.py:197-199` `_format_ratio` 判斷 `== 999.0` 回傳 `"N/A"`；測試 `test_tearsheet_renders_without_error` 用 `all_winning=True` 故意觸發 sentinel 並 `assert "N/A" in summary_values` 確認顯示正確。3-B Walk-Forward 階段需再次驗證做策略平均前先過濾 999（**留下 follow-up**）。

---

## [P2-D] [✅ 已處理 @ 451e2aa] MA cross 訊號在資料起點若已處於金叉狀態會誤發 +1

- **位置**：`src/strategy/examples/ma_cross.py` L40-48
- **狀況**：`trend` Series 預設值為 `-1`。當 rolling 還沒滿（前 N-1 筆 `has_ma=False`）時 trend 維持 `-1`。第 N 筆首次 `has_ma=True`，若 `ma_short > ma_long`，trend 從 `-1` 跳到 `+1`，`diff==+2` 觸發 buy signal — 但這不是真正的金叉，只是 warm-up 結束的偽訊號
- **風險**：實盤接資料時，若資料起點剛好處於多頭趨勢（`ma_short > ma_long`），會在第 60 個 bar 誤發一筆 BUY；回測會把這筆當成 valid entry 計入績效
- **處理階段**：2-D（向量化引擎整合層）— 引擎跑回測時應丟掉前 `max(ma_short, ma_long)` 筆 warm-up 資料；或在 strategy 層改用 `trend.shift(1).notna()` 屏蔽首次有效 bar
- **發現於**：驗收 2-A
- **處理摘要**：`src/backtest/engine_vec.py:177-184` `_drop_warmup_signals(strategy, signals)` 在 `run()` 取得 signals 後立即呼叫，把前 `max(ma_short, ma_long)` 筆設為 0。Strategy 層保留原樣（policy 與 engine 解耦）。

---

## [P2-D] [✅ 已處理 @ 451e2aa] CostCalculator 的 is_etf 旗標需要在事件引擎中注入

- **位置**：`src/backtest/cost.py` L39（`calculate(..., is_etf=False)`）
- **狀況**：`is_etf` 由 caller 顯式傳入，cost 模組本身不知道哪些 symbol 是 ETF。目前 fetcher / storage 都沒有 `is_etf` 欄位
- **風險**：事件引擎呼叫 `calculator.calculate()` 時若沒帶 `is_etf=True`，ETF 標的（0050、0056、006208 等）會被算成股票稅率 0.3% 而非 0.1%，回測高估 0.2% 賣出成本
- **處理階段**：2-D（事件引擎整合層）— 短期可在策略 / 引擎層硬寫 ETF symbol 集合（例如 `ETF_SYMBOLS = {"0050", "0056", "006208", ...}`）；長期看是否要在 `未涵蓋資料項目.md` 的「公司基本資料」加入 `is_etf` 欄位納入規格擴充
- **發現於**：驗收 2-B
- **處理摘要**：`src/backtest/engine_vec.py:16-23` 硬寫 `ETF_SYMBOLS = {"0050", "0051", "0056", "006208", "00878", "00919"}`；`_is_etf_symbol(symbol)`（L200-204）先查集合，再用「4 碼且開頭 `00`」的 fallback 規則涵蓋大部分 ETF。**長期 follow-up**：未涵蓋資料項目.md 的「公司基本資料」之後若擴規格，可改為從 metadata 讀 `is_etf` 欄位，移除硬寫集合。

---

## [P2-D] [✅ 已處理 @ 451e2aa] commission_discount 應從 config.yaml 注入而非寫死

- **位置**：`src/backtest/cost.py` L24（`commission_discount: float = 0.6` 是建構子預設值）
- **狀況**：個人券商議價折扣硬寫成 0.6。`config.yaml` 已有 `backtest.commission_discount: 0.6` 的設定，但 `CostCalculator` 沒從 config 讀
- **風險**：之後換券商或重新議價時要改兩處（config + 建構子預設值）才會一致；忘記改其中一處會導致回測與實盤手續費不一致
- **處理階段**：2-D（事件引擎初始化）— 引擎建構 `CostCalculator` 時從 `get_config()["backtest"]` 讀所有費率參數注入，建構子預設值僅作 fallback
- **發現於**：驗收 2-B
- **處理摘要**：`src/backtest/engine_vec.py:207-221` `_build_cost_calculator_from_config()` 從 `get_config()["backtest"]` 讀 `commission_rate / commission_discount / tax_rate / etf_tax_rate / slippage_ticks` 五個參數注入。建構子允許傳入自訂 `cost_calculator` 覆寫，否則一律經 config。

---

## [P2-D] [✅ 已處理 @ 451e2aa] avg_holding_days 是日曆日不是交易日，tearsheet 標題用詞要一致

- **位置**：`src/backtest/metrics.py` L177（`(exit_ - entry).dt.days`）
- **狀況**：`avg_holding_days` 用 `.dt.days` 取日曆日差。若策略持有期跨過長假（春節 9 天、五一連假），平均「交易日」會被高估約 30-40%
- **風險**：tearsheet 若標籤寫「平均持有交易日」會誤導；寫「平均持有日數」則 OK
- **處理階段**：2-D（tearsheet）— 二擇一：
  - (A) tearsheet 顯示為「平均持有日數」（不改實作）
  - (B) 改用台股交易日曆計算（需要 1-A 的 `MARKET_OPEN/CLOSE` 之外再補假日表，**這已超出規格**）
- **建議**：採 (A)，避免擴規格
- **發現於**：驗收 2-C
- **處理摘要**：採 (A)。`src/backtest/report.py:128` 標籤為 `"Average Holding Days"`，未含 "Trading"，與 `dt.days` 日曆日語意一致。

---

## 2-D 驗收新增觀察

### [P3-A] [✅ 已處理 @ 688fd17] 向量化回測 smoke test 太弱，未驗證精確 PnL

- **位置**：`tests/test_engine_vec.py` `test_vectorized_backtest_basic`
- **狀況**：目前只 assert `total_trades > 0` 與 `len(equity_curve) == len(df)`，沒驗證**任何具體數字**（總報酬、進出場日期、單筆 PnL、累計成本）
- **風險**：引擎邏輯 bug（例如 PnL 公式漏扣 buy_cost、訊號往前看一格、最終強制平倉計算錯）會被這個測試漏接。Phase 3 walk-forward 在 unstable engine 上跑會放大誤差
- **處理階段**：3-A（Walk-Forward 開始前）— 補一份手算的小型 fixture（5-10 bar，1 進 1 出），逐欄驗證：
  1. trade_records 的 `entry_date / exit_date / entry_price / exit_price / pnl` 完全對得上手算
  2. equity_curve 在進場日、持有期、出場日的具體數值（含手續費滑價扣抵）
  3. final equity == initial_capital + sum(pnl)
- **發現於**：驗收 2-D
- **處理摘要**：`tests/test_engine_vec.py` 新增 `test_vectorized_backtest_hand_calculated_fixture`：6 bar fixture + `_DeterministicStrategy(signals=[1,0,0,-1,0,0])`，逐欄 assert `entry_price=101.0`、`exit_price=107.0`、`pnl=40510.44`、進場日/持有期/出場日的 equity 具體值，並驗證 `final_equity == initial + pnl` 收支平衡。順便補了 `_DeterministicStrategy` 子類，未來其他精確驗證測試可重用。

---

### [P3-?] [✅ 已處理 @ 95b241e] tearsheet HTML 測試寫到 `data/backtest/`，不夠乾淨

- **位置**：`tests/test_report.py` L55-57（`Path("data/backtest/test_tearsheet_unit.html")`）
- **狀況**：測試直接寫到專案 `data/` 子目錄而非 `tmp_path`。`.gitignore` 有 `/data/` 不會 commit，但會留垃圾檔
- **風險**：低 — 不影響正確性，只是測試衛生問題；多次跑測試會在 `data/backtest/` 累積檔案
- **處理階段**：低優先（任意 phase）— 改用 `tmp_path` fixture
- **發現於**：驗收 2-D
- **處理摘要**：`tests/test_report.py::test_tearsheet_save_html` 已改用 `tmp_path / "tearsheet.html"`，不再寫入 `data/backtest/`。驗證：`tests/test_report.py` 通過。

---

### [P3-?] 引擎不支援加倉 / 分批進出

- **位置**：`src/backtest/engine_vec.py` L116-119
- **狀況**：`signal == 1 and quantity == 0` 才執行買進；已有部位時新的 +1 訊號被忽略。同理 sell
- **風險**：MA cross 這種「全進全出」策略沒影響，但 Phase 3 後若做動量加倉、金字塔加碼、分批停利就不能用
- **處理階段**：未定（取決於 Phase 3 之後新增的策略類型）— 若需要，要把訊號從「+1/-1/0」擴充為「目標部位比例」或「加減倉股數」
- **發現於**：驗收 2-D

---

## 3-A 驗收新增觀察

### [P3-C → P3-D] [✅ 已處理 @ 2237951] FillEvent BUY 必須傳 tax=0.0，total_cost 才會正確

- **位置**：`src/backtest/events.py` L172-177（`FillEvent.total_cost` property）
- **狀況**：`total_cost` 對 BUY 計算 `base + commission`，**不加 tax**；對 SELL 計算 `base - commission - tax`。但 `FillEvent` 欄位本身允許 BUY 帶非零 tax（驗證只擋負數），若 caller 在台股場景誤傳 BUY 的 tax > 0，total_cost 會偷偷少算（沒把該 tax 加進去）
- **風險**：3-C 事件迴圈建構 FillEvent 時若忘記台股 BUY 一律 `tax=0.0`，回測淨入帳金額對不起來。對 shellpig 版（純台股）OK，但對未來跨市場（V3.0 美股 / 加密貨幣有買進稅）會踩雷
- **處理階段**：3-C（事件迴圈整合）— 引擎建構 BUY 的 FillEvent 時硬寫 `tax=0.0`；或更嚴謹：在 `FillEvent.__post_init__` 校驗「BUY 且 tax > 0」直接拋 `ValueError`
- **發現於**：驗收 3-A
- **3-C 處理結果（2026-04-25 驗收）**：⚠️ **慣例上正確、型別層仍未守**
  - `engine_event.py:150-155` 透過 `cost_calculator.calculate(...)` 取得 `cost.tax`，由 `cost.py:55-56` 保證 BUY tax=0；engine 端不會踩到
  - 但 `FillEvent.__post_init__` 沒加「BUY+tax>0 必拋 ValueError」的不變式
  - **轉派 P3-D**：3-D 起跑前在 `events.py` 加 invariant 檢查（一行 if），徹底關上這個洞
- **3-D 處理結果**：`events.py:170-171` 加入 `if side == "BUY" and tax > 0: raise ValueError("BUY fill must have tax=0.0.")`。`tests/test_events.py::test_fill_event_buy_tax_must_be_zero` 驗證 BUY+tax>0 確實拋 ValueError。

---

### [P3-C → P3-D] [✅ 已處理 @ 2237951] SimpleAccount.apply_fill BUY 不檢查現金是否足夠，會無聲變負

- **位置**：`src/backtest/account.py` L74（`self.cash -= (fill_price * quantity) + commission`）
- **狀況**：BUY 時直接從 cash 扣除花費，不檢查 cash 是否足以支付。若 strategy 算錯下單金額（例如忘記扣已用倉位、未考慮現有未平倉成本），cash 會變負，account 進入無效狀態
- **風險**：3-C 事件迴圈整合後，無聲變負的帳戶會讓後續 PnL、equity curve、勝率全部錯誤；error 不會在當下浮現，而是在最終 tearsheet 數字看起來怪怪的時才被發現
- **處理階段**：3-C（事件迴圈整合）— 二擇一：
  - (A) `apply_fill` 自己擋：BUY 計算後若 `self.cash < 0` 則 `raise ValueError("Insufficient cash")`，並回滾倉位變動
  - (B) 事件迴圈在 dispatch OrderEvent 前先檢查 `account.get_cash() >= price × qty + estimated_commission`
- **建議**：採 (A) — 把不變式（cash >= 0）守在 account 層，事件迴圈不必信任策略
- **發現於**：驗收 3-B
- **3-C 處理結果（2026-04-25 驗收）**：❌ **完全未處理**
  - `engine_event.py:72` 仍直接呼叫 `account.apply_fill(fill)`，沒做任何 cash 檢查
  - `account.apply_fill` 也沒加守衛
  - **轉派 P3-D**：採建議 (A) — 在 `account.py:74` 後加：`if self.cash < 0: raise ValueError("Insufficient cash for BUY: cash={...}, cost={...}")`，並在錯誤訊息附上現金餘額與訂單成本以利除錯
  - **同時**：補一個 `tests/test_account.py::test_buy_insufficient_cash_raises` 測試 BUY 超過現金會被擋
- **3-D 處理結果**：採建議 (A)。`account.py:70-73` 在扣款前計算 `total_buy_cost`，若 `> self.cash` 則 `raise ValueError("Insufficient cash for BUY: cash=..., total_cost=...")`；且確保失敗時部位不會被更改（守衛在 `self.cash -=` 之前）。`tests/test_account.py::test_buy_insufficient_cash_raises` 驗證 ValueError 被拋、現金和部位均未變動。

---

### [P3-C → P3-D] [✅ 已處理 @ 2237951] BarEvent.freq 是字串自由格式，事件迴圈分流時要小心拼字

- **位置**：`src/backtest/events.py` L73、L86（`freq: str`，僅校驗非空字串）
- **狀況**：`freq` 接受任意非空字串。`"1min"`、`"1m"`、`"1"`、`"daily"`、`"1d"` 都會通過驗證，但下游若依字串路由（例如 3-C 引擎決定要不要消費這個 bar、4-D dashboard 過濾顯示）會踩拼字 bug
- **風險**：寫入端與讀取端用不同字串會無聲分歧；測試覆蓋不到時，bug 只會在實盤切換週期時才浮現
- **處理階段**：3-C（事件迴圈接 BarEvent 之前）— 在 `src/core/constants.py` 加白名單常數 `VALID_BAR_FREQS = {"1min", "5min", "15min", "30min", "60min", "daily"}`，`BarEvent.__post_init__` 改用白名單校驗
- **發現於**：驗收 3-A
- **3-C 處理結果（2026-04-25 驗收）**：⚠️ **慣例上正確、型別層仍未守**
  - `engine_event.py:181-196` `_infer_freq` 產出 canonical 字串（`"1day"` / `"Nmin"` / `"60min"`），engine 端只發合規 freq
  - 但 `BarEvent.__post_init__` 仍接受任意非空字串
  - **轉派 P3-D**：在 `src/core/constants.py` 加 `VALID_BAR_FREQS = {"1min", "5min", "15min", "30min", "60min", "1day"}`（注意 `_infer_freq` 用 `"1day"` 不是 `"daily"`，白名單要對齊），並在 `BarEvent.__post_init__` 加 `if self.freq not in VALID_BAR_FREQS: raise ValueError(...)`
- **3-D 處理結果**：`constants.py:16` 加 `VALID_BAR_FREQS = {"1min", "5min", "15min", "30min", "60min", "1day"}`；`events.py:89-90` `BarEvent.__post_init__` 加白名單校驗（`"daily"` 等舊字串會拋 ValueError）。`tests/test_events.py::test_bar_event_invalid_freq_raises` 以 `"daily"` 觸發驗證確認。

---

## 3-C 驗收新增觀察

### [P3-C] [✅ 已處理 @ 2258c27] 🚨 事件引擎沒建構 round-trip trades，導致 win_rate / profit_factor / max_single_loss 全壞

- **位置**：`src/backtest/engine_event.py` L74（`trade_records.append(fill.to_dict())`）+ L91（`_build_result(...)` 把 fill_records 直接餵給 `calculate_metrics`）
- **狀況**：事件引擎把每筆 `FillEvent` 的 dict 當成「一筆 trade」塞進 trade_records，但 `metrics.py` 期待的是 round-trip trade（含 `entry_date / exit_date / entry_price / exit_price / pnl`）。fill_dict 沒這些欄位，`calculate_metrics` 走 fallback 路徑後：
  - `total_trades = len(fills)` — BUY+SELL 各算一筆，是真實 round-trip 的兩倍
  - `pnl` 欄被建空 Series，所有筆 pnl=0
  - `win_rate = 0%`（沒 winning trade）
  - `gains=0, losses=0` → `profit_factor = 999.0`（被當無虧損誤判為「無敵」）
  - `max_single_loss = 0`
- **風險**：**所有用事件引擎跑出來的 tearsheet 績效報表都是錯的**。權益曲線正確（mark-to-market 有做），但 trade-level 指標全失真。3-D 端到端範例策略跑完看到的 win_rate/profit_factor 會嚴重誤導
- **處理階段**：**3-C（必修，不該推到 3-D）** — 引擎自己要追蹤未平倉部位、在 SELL 成交時建構 round-trip：
  1. 仿照 `engine_vec.py:82-110` 的 `_OpenTrade` dataclass 與 PnL 累加邏輯
  2. SELL fill 後計算 `pnl = (exit_price - entry_price) × qty - buy_cost - sell_cost`
  3. trade_records 改 append `{"entry_date", "exit_date", "side", "quantity", "entry_price", "exit_price", "pnl"}`
  4. 若同 symbol 有多次加倉，採 average-cost 配對（與 `SimpleAccount.cost_basis` 一致）或 FIFO；規格沒指定，但要與向量化引擎一致以便 3-D 對照
- **發現於**：驗收 3-C
- **建議補測試**：3-C 的 6 個 spec 測試只驗證「形狀」沒驗證「數字」。修這條的同時補：
  - `test_round_trip_pnl_matches_hand_calculated` — 6-bar fixture，BUY @ bar[1].open、SELL @ bar[4].open，逐欄 assert entry_price/exit_price/pnl
  - `test_total_trades_counts_round_trips_not_fills` — 一次 BUY + 一次 SELL → `total_trades == 1`
  - `test_event_engine_matches_vectorized` — 同 fixture、同決定論 strategy 跑兩個引擎，`total_return` 與 `total_trades` 應一致
- **處理摘要（2026-04-25 驗收）**：`engine_event.py:103-160` 新增 `_OpenPosition` 與 `_update_round_trip_records`，支援平均成本加倉與部分賣出按比例分攤費用。`tests/test_engine_event.py` 已加前兩個建議測試。**第 3 個 `test_event_engine_matches_vectorized` 沒加** — 驗證時發現兩引擎是不同設計典範（vec strategy 只給信號、引擎決定股數；event strategy 自帶 quantity），原始版本的測試怎麼寫都會 FAIL。詳見下方 [P?-?] 兩引擎下單典範分歧條目；建議改寫成 per-share PnL 比對（**留下 follow-up**）。

---

### [P?-?] 兩引擎是不同設計典範（signal-based vs order-based），結果不可直接比對

- **位置**：
  - `src/backtest/engine_vec.py:186-192` `_calculate_order_quantity`：vec 引擎自己用 `cash // price` 算「能買多少就買多少」，strategy 只給 +1/-1 信號
  - `src/backtest/engine_event.py:78` `strategy.on_bar(bar, account)` 回傳 `OrderEvent` list，**strategy 自帶 quantity**
  - `src/strategy/base.py:21-25` 文件聲稱「同一策略可被兩種引擎呼叫」
- **狀況**：跨引擎一致性驗證實測（同 8-bar fixture、同 cost、邏輯等價的 strategy）：
  - 兩引擎 `total_trades=1`、`entry_price=101`、`exit_price=104`、**per-share pnl 完全一致 (2.51275)**
  - 但 vec 用 1,000,000/101 算出買 9,000 股，event 用 strategy 寫死 1,000 股 → **總 PnL 差 9 倍**
  - 兩個 strategy 邏輯一樣（「在 t=0 BUY、t=3 SELL」）但無法寫成同一個類別
- **風險**：
  1. 規格書 §「同一策略兩引擎可跑」承諾**事實上沒做到**，目前是兩套無法互通的策略 API
  2. 4-D Streamlit 整合時若沒明確選一個引擎跑、又顯示兩種結果，使用者會以為「兩引擎跑出不同數字 = 引擎有 bug」
  3. Walk-forward / robustness 測試若想用兩引擎交叉驗證會誤判
- **處理階段**：**短期保留現狀**（採決策 (C)：把分歧記錄為已知設計，不修）
  - 長期可選 (D)：加 adapter 層讓同一份策略邏輯適配兩引擎，但這要 Phase 4+ 才有空做
- **發現於**：驗收 3-C 跨引擎比對（2026-04-25）
- **約束（必須遵守）**：
  - **4-D Streamlit dashboard 必須明確只用一個引擎**（建議用 `EventDrivenBacktester`，因為 strategy 控制 quantity 比較貼近實盤行為），UI 上不顯示「另一引擎結果」以免誤導
  - 文件層面：`src/strategy/base.py` docstring 要修正「同一策略可被兩種引擎呼叫」的說法，改為「`generate_signals` 給 vec 引擎用、`on_bar` 給 event 引擎用，兩條路是並行設計而非可互換」
  - 跨引擎測試若要寫，**只能驗 per-share PnL**（即 `vec_pnl/vec_qty == event_pnl/event_qty`），不能驗總 PnL

---

### [P3-?] [✅ 已處理 @ 95b241e] engine_event 與 engine_vec 程式碼重複，refactor 候選

- **位置**：
  - `engine_event.py:19-26` 與 `engine_vec.py:16-23`：`ETF_SYMBOLS` 集合
  - `engine_event.py:198-202` 與 `engine_vec.py:200-204`：`_is_etf_symbol`
  - `engine_event.py:204-219` 與 `engine_vec.py:206-221`：`_build_cost_calculator_from_config`
- **狀況**：兩個引擎各複製一份，加新 ETF symbol 或改 config 鍵值要兩邊改
- **風險**：低 — 不影響功能；但加新 ETF 時忘記改一邊會導致兩個引擎的 ETF 認定不一致，**事件引擎與向量化引擎跑同一份 0050 資料會收不同稅**
- **處理階段**：低優先（任意 phase）— 抽到 `src/backtest/_helpers.py` 共用模組
- **發現於**：驗收 3-C
- **處理摘要**：新增 `src/backtest/_helpers.py`，集中 `ETF_SYMBOLS`、`is_etf_symbol()`、`build_cost_calculator_from_config()` 與 `calculate_max_buy_quantity()`；`engine_event.py` 與 `engine_vec.py` 已改用共用 helper。驗證：`tests/test_engine_event.py tests/test_engine_vec.py tests/test_consistency.py` 通過。

---

## 3-D 驗收新增觀察

### [P3-?] [✅ 已處理 @ e6ffc19] MACrossStrategy.on_bar 買入量採 `(cash // price) // 1000 * 1000`，與 engine_vec 邏輯相同但沒共用

- **位置**：`src/strategy/examples/ma_cross.py:104-107`（`_calculate_buy_quantity`）與 `src/backtest/engine_vec.py:187-192`（`_calculate_order_quantity`）
- **狀況**：兩處各寫了一份「全額買、整千張」邏輯，語意完全相同
- **風險**：低 — 修改其一（例如改為 100 股零股規則）而忘記同步另一，導致兩引擎在同 strategy 下下單量不同
- **處理階段**：低優先（任意 phase）— 可抽到 `src/backtest/_helpers.py` 的 `calculate_buy_quantity(cash, price)` 公用函式
- **發現於**：驗收 3-D
- **處理摘要**：`src/strategy/examples/ma_cross.py` 已移除策略端自行 sizing 的責任，BUY 改送 `_BUY_INTENT_QUANTITY`，由事件引擎在成交層解析實際可買股數；因此原本「MA Cross event 路徑與 engine_vec 各寫一份 cash // price sizing」的重複已不存在。驗證：`tests/test_engine_event.py::test_ma_cross_event_same_entry_points`、`test_event_engine_matches_vectorized` 均通過。注意：`engine_vec.py` 仍有自己的 `_calculate_order_quantity`，且「向量化引擎買進張數未預留交易成本」仍是獨立待修項。

---

### [P3-?] [✅ 已處理 @ 95b241e] reset_runtime_state 目前只在 engine_event 呼叫，engine_vec 沒呼叫

- **位置**：`src/backtest/engine_event.py:64`（有呼叫）；`src/backtest/engine_vec.py`（沒呼叫）
- **狀況**：`engine_vec.run()` 呼叫 `strategy.generate_signals(df)` 前沒有呼叫 `strategy.reset_runtime_state()`。對純向量化策略（state 全在 `generate_signals` 內部）沒差，但若策略同時實作 event 路徑且有共用狀態（如 `_close_history`），先跑 event 再跑 vec 會帶著髒狀態
- **風險**：目前 `MACrossStrategy` 的 event/vec 狀態互相獨立，不會互污；但設計上不一致，未來有人加「兩路共享 state 的策略」時會踩雷
- **處理階段**：低優先（任意 phase）— 在 `engine_vec.run()` 開頭也加 `strategy.reset_runtime_state()` 一行
- **發現於**：驗收 3-D
- **處理摘要**：`src/backtest/engine_vec.py` 在 `run()` 取得非空資料後、呼叫 `generate_signals()` 前已加入 `strategy.reset_runtime_state()`，與事件引擎行為一致。驗證：`tests/test_engine_vec.py` 與 `tests/test_consistency.py` 通過。

---

## 紀律

1. 每次驗收若有「**確定**會在某 phase 阻擋的行為」→ 加進來
2. 純規格選擇題（兩種實作都 OK）→ 不加
3. 規格已明定不做（如即時 tick 資料）→ 不加，去 `未涵蓋資料項目.md`
4. 進到對應 phase 處理完，**改本檔案標記為 ✅ 已處理（含 commit hash）**，不刪除（保留歷史可追溯）
5. 處理時若衍生 follow-up，在「處理摘要」段註明，避免把問題藏進 commit message

## 2026-04-26 Code Review 新增待修項目

### [P1] [✅ 已處理 @ TBD] ParquetStorage 的 symbol 可造成路徑穿越寫檔

- **狀況**：`src/data/storage.py:81-88` 直接把 `symbol` 接進路徑，例如 `..\..\escape` 會讓輸出位置離開預期的 `data/raw/tw/{symbol}` 或 `data/processed/tw/{symbol}`。
- **風險**：若 `symbol` 來自 UI、API 或外部資料，可能造成任意檔案寫入或覆蓋非預期位置。
- **建議處理**：限制 `symbol` 格式，例如台股代號白名單 regex，並在 `resolve()` 後確認目標仍位於 `data_dir` 之下。
- **預計處理 phase**：近期安全修正。
- **處理摘要**：`storage.py:81-110` 新增 `_validate_market()`（只接受 `"tw"`）、`_validate_symbol()`（拒絕 `..`、`/`、`\`、絕對路徑與 drive anchor）、`_build_market_path()`（`resolve(strict=False)` + `relative_to(data_dir)` 防逃逸）。所有 6 個 public 方法（save/load × daily/minute/adjusted）加入 `market: str = "tw"` 參數並經驗證。`tests/test_storage.py` 新增 `test_storage_rejects_path_traversal_symbol`（9 種 traversal 向量）、`test_storage_uses_default_tw_market_path`、`test_storage_rejects_unknown_market`，16/16 通過。**低優先 follow-up**：`_validate_symbol` 目前被 public 方法和 `_daily_path` 等內部方法各呼叫一次（冪等但冗餘），可考慮集中到 public 方法層。

### [P1] [✅ 已處理 @ 70c3db4] 事件引擎無法跑 1min / 5min 資料

- **狀況**：`src/backtest/engine_event.py:253-268` 只處理 pandas 舊頻率別名 `T`。目前 pandas 對分鐘資料可能回傳 `min` / `5min`，導致 `BarEvent.freq` 驗證失敗；`len(index) < 3` 時 `pd.infer_freq()` 也可能直接丟錯。
- **風險**：Phase 3 的分鐘 K 事件驅動回測會被阻斷，尤其是 1 分鐘與 5 分鐘資料。
- **建議處理**：支援 `min`、`5min`、`h` 等新 alias；對資料長度不足或無法推斷頻率時穩定 fallback 到 `1day` 或呼叫端指定頻率。
- **預計處理 phase**：Phase 3 事件引擎修正。
- **處理摘要**：`engine_event.py:282-302` `_infer_freq` 已支援 pandas 新舊別名：`T`/`5T`（舊）→ `1min`/`5min`、`MIN`/`5MIN`（新）→ `1min`/`5min`、`H`/`2H`（舊新）→ `60min`；`len(index) < 3` 時穩定 fallback 到 `1day`；`constants.py:17` `VALID_BAR_FREQS` 白名單與 `BarEvent.__post_init__` 校驗確保只產出合規 freq。

### [P2] [✅ 已處理 @ 95b241e] pending order 會用下一根 bar 的 symbol 成交

- **狀況**：`src/backtest/engine_event.py:229-230` 建立 `FillEvent` 時使用 `match_ctx.symbol`，不是 `order.symbol`。多標的資料或策略送出非當前 bar symbol 的訂單時，訂單可能被記成下一列資料的 symbol。
- **風險**：多標的回測會把成交、稅率、持倉與 PnL 歸到錯誤標的，績效報表失真。
- **建議處理**：`FillEvent.symbol` 應使用 `order.symbol`；撮合前應確認目前 bar 的 symbol 與 order symbol 相符，不相符時延後、跳過或依多標的資料結構查找正確 bar。
- **預計處理 phase**：多標的支援前必修。
- **處理摘要**：`engine_event.py` 已改為只在 `order.symbol == bar_event.symbol` 時撮合 pending order；不符者保留在 deferred pending list，等對應 symbol 的 bar 到來才成交。`FillEvent.symbol` 與 ETF 判定也改用 `order.symbol`。mark-to-market 改用 `latest_prices` 追蹤各持倉 symbol 最新價格，避免多標的情境缺價。驗證：臨時多標的 repro 中，`0050` 訂單不再用下一根 `2330` bar 的 open 成交，而是延後到下一根 `0050` bar 成交；`tests/test_engine_event.py` 通過。

### [P2] [✅ 已處理 @ 95b241e] 買進張數未預留交易成本

- **狀況**：`src/backtest/engine_vec.py:72-82` 用 `cash // price` 決定買進數量，之後才扣手續費與滑價；`src/strategy/examples/ma_cross.py:104-107` 的 event 路徑也只用現金與價格計算下單量。
- **風險**：向量化引擎可能出現負現金並形成不真實槓桿；事件引擎則可能在成交時因 `SimpleAccount` 現金不足而丟錯。
- **建議處理**：抽出共用 sizing helper，計算最大可買數量時把 commission、slippage 與最低手續費納入，並讓 vectorized / event strategy 使用同一套邏輯。
- **預計處理 phase**：Phase 3-D / 回測一致性修正。
- **現況補充（3-D 驗收）**：`account.py:70-73` 已加 cash guard，BUY 超過現金時拋 `ValueError("Insufficient cash for BUY")`，失敗模式從「無聲變負」升級為「明確中斷」。但 sizing 公式（`cash // price` 取整千）仍未預留 commission，**剛好滿倉時會因手續費超出而被 guard 擋住、回測中斷**。共用 sizing helper 仍是正解。
- **處理摘要**：`src/backtest/_helpers.py::calculate_max_buy_quantity()` 已集中最大可買股數計算，會把 BUY commission 納入 affordability check，優先整千股、再 fallback 零股。`VectorizedBacktester._calculate_order_quantity()` 與事件引擎 `_resolve_buy_quantity()` 已改用此 helper。驗證：`tests/test_engine_event.py::test_market_buy_is_resized_to_affordable_quantity`、`tests/test_engine_vec.py`、`tests/test_consistency.py` 通過。

---

## 2026-04-27 0050 1 拆 4 驗證新增待修項目

> 背景：元大台灣 50（0050）於 **2025-06-13~17 停牌**、**2025-06-18 以拆分後價格復牌**，比例 **1 拆 4**（before_price=188.65、after_price=47.16，ratio≈4.00）。
>
> ⚠ **2026-04-27 二次驗證更新**：原以為 [P1-A] / [P1-B] 都是待修問題，二次驗證才發現 splits 管線與 `require_adjusted` 防呆**早已在 working tree 實作完成、未 commit**。下面兩條條目仍保留原始問題描述（供追溯），但「建議處理」段已被「二次驗證實況 + 剩餘待辦」段取代；只要把現有 unstaged diff commit 並對 0050 跑一次 rebuild 即可關閉。
>
> ✅ **2026-04-30 收尾**：splits 管線與 `require_adjusted` 防呆於 `bf32571` commit；0050 已實際 rebuild（`data/processed/tw/0050/adj_daily.parquet` 存在）。後續發現的 splits 測試 tz/unit 不一致於 `7058e17` 修復，全套 133 passed。本次更新將 [P1-A] / [P1-B] 結案。

### [P1-A] [✅ 已處理 @ bf32571] 拆股事件未進入復權管線（0050 2025-06 案例）

> **2026-04-27 二次驗證實況**（推翻原本的「需要新增」假設）：
> - `compute_adjustment_factors` 簽章已是 `(daily_df, dividends_df, splits_df=None)`；[src/data/cleaner.py L223-253](src/data/cleaner.py:223) 已實作 `factor *= after_price / before_price` 並與 dividend factor 同日累乘
> - `adjust_prices` 已透傳 `splits` 參數
> - `FinMindFetcher.fetch_splits(symbol, start_date)` 已實作（呼叫 `TaiwanStockSplitPrice`），含 `_normalize_splits` 正規化
> - `core/constants.py` 已加 `SPLITS_COLUMNS`
> - `ParquetStorage` 已加 `save_splits` / `load_splits` / `_splits_path`
> - [src/data/maintenance.py L55-94](src/data/maintenance.py:55) `rebuild_symbol` 與 `rebuild_adj_factors` 已串入 `_fetch_splits` + `save_splits` + meta `freq="splits"` 寫入
> - 對應測試（`test_cleaner` / `test_fetcher` / `test_maintenance` / `test_storage`）均有 unstaged 修改
> - **End-to-end 實證**：以現有 fetcher / cleaner 對 0050 跑 `adjust_prices(raw, divs, splits=splits)`，2025-06-10 close=46.18 → 2025-06-18 close=46.58 完全連續無斷層；adj_factor 從 0.244783 跳到 0.979182，比例 1/4 正確
>
> **狀態**：✅ 已收尾（2026-04-30）。
>
> **處理摘要**：
> - `bf32571 fix: rebuild adjusted data during update and add split pipeline` 將 splits 管線（`fetch_splits` / `SPLITS_COLUMNS` / `compute_adjustment_factors(splits_df=...)` / `adjust_prices` 透傳 / `save_splits` / `load_splits` / `rebuild_symbol` 串入 `_fetch_splits` + meta `freq="splits"`）連同對應測試一併 commit，淨增修 541 行。
> - 0050 已實際跑過 `rebuild_symbol`，`data/processed/tw/0050/adj_daily.parquet` 已落地。
> - splits 測試在 pandas 2.x + pyarrow round-trip 下出現 `datetime64[ns]` vs `[us]`、tz-aware/naive 比對 IndexError 兩個尾巴問題，於 `7058e17 test: fix splits roundtrip tz/unit assertions` 修復。
> - 回歸：133 passed, 8 deselected。
> - 剩餘可選 follow-up：在回測頁實際選 0050 + 跨 2025-06-13~18 區間做人工 equity curve smoke check（不影響本條目關閉）。
>
> ---
>
> 以下保留原始（事前）問題描述，供追溯。

- **位置**：
  - `src/data/cleaner.py:155-234` `compute_adjustment_factors`（公式只認 cash_dividend / stock_dividend，沒讀拆股事件）
  - `src/data/fetcher.py:185-202` `fetch_dividends`（FinMind `TaiwanStockDividend` 不收拆股）
  - `src/data/maintenance.py:50-74` `rebuild_symbol` / `rebuild_adj_factors`（沒抓 splits、沒餵進 cleaner）
- **狀況**：
  - 已在 2026-04-27 驗證：`data/raw/tw/0050/daily.parquet` 在 2025-06-10 close=188.65、2025-06-18 open=47.50，中間停牌 5 天，**raw daily 有 -75% 斷層**。
  - FinMind `TaiwanStockDividend` 對 0050 自 2024 起 5 筆紀錄全部 `stock_dividend=0.0`，**未收錄拆股**。2025-07-21 那筆 `cash_dividend=0.36` 是拆後金額。
  - FinMind 另有 `TaiwanStockSplitPrice` dataset 收錄此事件，回傳 `{date: '2025-06-18', before_price: 188.65, after_price: 47.16, open_price: 47.16}`，可直接由 `before_price / after_price` 算出 ratio≈4.00。
  - 目前 `compute_adjustment_factors` 完全沒讀這個 dataset，所以即使對 0050 跑 `rebuild_adj_factors`，產出的 adj_daily 也會在 2025-06-18 留下 4 倍斷層。
- **風險**：
  - **任何跨越 2025-06-13 → 2025-06-18 的 0050 回測都嚴重失真**：max drawdown、Sharpe、單筆 max loss、勝率全錯。
  - 事件引擎會在拆分日把持倉估值瞬間打成 1/4，可能觸發 stop-loss 或現金檢查的奇怪行為。
  - 不只 0050，未來任何拆股 / 反拆股的 ETF / 個股（00878 拆分傳聞、美股反拆股等）都會踩同樣的洞。
- **建議處理（採 B 方案）**：
  1. **`src/data/fetcher.py`** 新增 `fetch_splits(symbol, start_date)`，呼叫 FinMind dataset `TaiwanStockSplitPrice`，正規化欄位為 `["date", "before_price", "after_price", "symbol"]`，`date` 採除權交易日（資料中的 `date` 欄就是恢復交易日）。空回傳走 `_empty_splits_dataframe()` 模式比照 dividends。
  2. **`src/core/constants.py`** 加 `SPLITS_COLUMNS = ["date", "before_price", "after_price", "symbol"]`。
  3. **`src/data/cleaner.py`** `compute_adjustment_factors(daily_df, dividends_df, splits_df=None)` 多收 `splits_df`：在事件迴圈把每筆 split 的 `factor *= after_price / before_price`（即 `1 / ratio`）疊乘進 `event_factor_by_idx[idx]`，與 cash/stock dividend 的 factor **同日累乘**。`adjust_prices` 對應加參數透傳。
  4. **`src/data/maintenance.py`** `rebuild_symbol` 與 `rebuild_adj_factors` 都要 `fetch_splits` + `save_splits`（若決定落地）+ 餵進 `adjust_prices`。元資料用 `freq="splits"` 寫進 `DuckDBMeta`（與 dividends 對稱）。是否落地存檔可選，最小可行作法是「現抓現用、不落地」，但建議落地以利離線重算。
  5. **跑驗證**：對 0050 跑一次完整 `rebuild_symbol("0050")`，確認 `data/processed/tw/0050/adj_daily.parquet` 在 2025-06-18 前後 close 連續（拆前歷史價會被乘上 0.25 左右，從 ~188 變成 ~47）。
  6. **測試**（`tests/`）：
     - `tests/fixtures/splits_0050.csv` 加 1 筆 2025-06-18 fixture
     - `tests/test_fetcher.py` 加 `test_fetch_splits_normalizes_schema`（mock FinMind 回傳，驗欄位）
     - `tests/test_cleaner.py` 加 `test_compute_adj_factor_with_split`（手算 fixture：split-only、split+dividend 同日、split-only 跨多年累乘）
     - `tests/test_maintenance.py` 加 `test_rebuild_symbol_applies_split`（end-to-end 0050 mini fixture）
  7. **文件**：
     - `未涵蓋資料項目.md` 把「拆股」相關項目移除（目前該檔沒明列拆股，但 §F 的「漲跌停實際觸發旗標」段附近可加一段聲明已涵蓋拆股事件）
     - `量化交易系統規格書_shellpig版.md` 在資料管線章節補「拆股事件處理」說明
     - 本檔此條目改 `[✅ 已處理 @ <commit>]` 並寫處理摘要
- **預計處理 phase**：近期資料正確性修正（建議排在 P5-C 之前，否則任何後續策略對 0050 的回測都帶錯）。
- **發現於**：2026-04-27 使用者反映 0050 拆股未復權；以 raw daily inspect + FinMind API 驗證確認。

### [P1-B] [✅ 已處理 @ bf32571] 回測頁 silent fallback 到未復權 raw daily，掩蓋資料缺口

> **2026-04-27 二次驗證實況**（推翻原本「silent fallback 仍存在」的假設）：
> - [src/ui/pages/backtest.py L256-268](src/ui/pages/backtest.py:256) 已加 `_load_backtest_data(..., require_adjusted: bool = True)` 預設參數
> - 找不到 adjusted 直接 `raise FetcherError("{symbol} adjusted data is missing. Please run rebuild_adj_factors (or rebuild_symbol) before backtest.")`
> - UI 呼叫端 [backtest.py L104-112](src/ui/pages/backtest.py:104) 顯式傳 `require_adjusted=True`，例外攔成 `st.error("讀取資料失敗：...")`
> - Silent fallback 已不再是 default 行為；保留 `require_adjusted=False` 旁路供測試 / debug
> - `tests/test_backtest_page.py` 同樣在 unstaged diff 內，已覆蓋 raise / 不 raise 兩條路徑
>
> **狀態**：✅ 已收尾（2026-04-30）。
>
> **處理摘要**：
> - 與 [P1-A] 同一個 `bf32571` commit。`_load_backtest_data(..., require_adjusted: bool = True)` 預設啟用嚴格模式，找不到 adjusted 直接 `raise FetcherError`，UI 呼叫端攔成 `st.error`；保留 `require_adjusted=False` 旁路供測試 / debug。
> - `tests/test_backtest_page.py` 涵蓋 raise / 不 raise 兩條路徑。
>
> **未採用的替代方案**（記錄供未來參考）：把 raise-if-missing 策略下放到 `storage.load_adjusted` 層 — 目前選 UI 層擋，避免 storage 過度耦合 UX 訊息。
>
> ---
>
> 以下保留原始（事前）問題描述，供追溯。


- **位置**：`src/ui/pages/backtest.py:251-257` `_load_backtest_data`
- **狀況**：
  ```python
  df = storage.load_adjusted(symbol)
  if df.empty:
      df = storage.load_daily(symbol)   # ← silent fallback
  ```
  - `storage.load_adjusted` 在檔案不存在時走 `_load_or_empty`（`storage.py:155-157`）silent 回空 DataFrame、不 raise。
  - 結果：任何**未跑過 `rebuild_adj_factors` 的標的**（例如 0050，目錄 `data/processed/tw/0050/` 根本不存在）會被悄悄退到 raw daily，使用者完全看不出來。
  - 配 [P1-A]：0050 回測就是這條 fallback 命中、踩 raw daily 拆股斷層才會看起來「有資料但結果荒謬」。
- **風險**：
  - 把「資料管線沒跑完」掩蓋成「回測正常但結果失真」，比資料缺失更糟（看不出錯）。
  - 即使 [P1-A] 修好，未來只要有人新增標的忘記跑 `rebuild_adj_factors`，又會落入同樣的 silent fallback。
- **建議處理**（兩段式，二選一即可，建議 (B)）：
  - (A) **降級警告**：fallback 時 `st.warning(f"{symbol} 找不到復權檔，使用 raw daily，績效可能失真。請先跑 rebuild_adj_factors。")` — 最小改動，但仍允許錯誤回測產出。
  - (B) **直接擋下**（推薦）：`load_adjusted` 為空時 `st.error(...)` 並 `return pd.DataFrame()`，回測流程顯式退出。同時 `_load_backtest_data` 簽章加 `require_adjusted: bool = True` 旗標，給未來測試 / debug 場景可繞過。
  - **不建議**沿用現行 silent fallback。
- **配套**：`src/data/storage.py` `load_adjusted` 也可考慮新增 `load_adjusted(symbol, *, raise_if_missing=False)` 旗標，UI 層改用 `raise_if_missing=True`，把策略放在 storage 層（更乾淨）。
- **預計處理 phase**：與 [P1-A] 同 commit 處理（一起做才完整）。
- **發現於**：2026-04-27 驗證 [P1-A] 時連帶發現。

---

## 2026-05-07 Phase 7-A 驗收新增待修項目

### [P7-A] [✅ 已處理 @ e993cba] 回測頁策略下拉選單尚未暴露 6 個新策略 preset

- **位置**：
  - `config.yaml` `strategies[]`
  - `src/ui/pages/backtest.py` 策略下拉選單（由 `get_strategy_presets(get_config())` 讀取 preset）
- **狀況**：
  - Phase 7-A 核心實作已完成並通過驗證：`tests/test_strategies.py tests/test_strategy_config.py` 為 `46 passed`；全非 integration 回歸為 `178 passed, 8 deselected`。
  - 6 個新策略類別、`strategy_config.py` 正規化與 metadata、回測頁分派皆已存在。
  - 但目前 `config.yaml` 的 `strategies[]` 只有 `MA20_MA60` 與 `定期定額`，因此 UI 策略下拉選單無法直接選到 `RSI_14`、`KD_Cross`、`MACD_Cross`、`BB_20`、`BIAS_20`、`Donchian_20_10`。
- **風險**：
  - 使用者在回測頁手動驗收 Phase 7-A 時看不到 6 個新策略，會誤以為策略尚未實作或 UI 分派未完成。
  - 自動測試會通過，但手動驗收清單中 6 個新策略回測項目無法從 UI 入口直接執行。
- **建議處理**：
  - 在 `config.yaml` 補上 6 個 Phase 7-A 預設策略 preset，使用規格建議名稱與預設參數：
    - `RSI_14` / `rsi`
    - `KD_Cross` / `kd_cross`
    - `MACD_Cross` / `macd_cross`
    - `BB_20` / `bollinger_band`
    - `BIAS_20` / `bias`
    - `Donchian_20_10` / `donchian_breakout`
  - 補一個輕量驗證，確認 `get_strategy_presets(get_config())` 回傳 8 種策略類型（既有 2 種 + 新增 6 種）。
- **預計處理 phase**：Phase 7-A 收尾。
- **發現於**：2026-05-07 Phase 7-A 驗證。
- **處理摘要**：
  - `config.yaml` 已補上 6 個 Phase 7-A 預設策略 preset：`RSI_14`、`KD_Cross`、`MACD_Cross`、`BB_20`、`BIAS_20`、`Donchian_20_10`。
  - `tests/test_strategy_config.py` 已新增 `test_config_yaml_exposes_all_8_strategy_types`，確認 `get_strategy_presets(get_config())` 回傳既有 2 種 + 新增 6 種，共 8 種策略類型。
  - 驗證結果：`tests/test_strategy_config.py` 為 `17 passed`。
  - 已於 `e993cba feat: expose all 8 strategy presets in config.yaml (Phase 7-A)` commit。

---

## 2026-05-10 Phase 6-B 驗證新增待修項目

### [P6-B] [✅ 已處理 @ TBD] Streamlit `use_container_width` 參數已棄用，終端機持續輸出 warning

- **位置**：
  - `src/ui/pages/backtest.py`：`st.dataframe(...)`、`st.plotly_chart(...)` 共 12 處
  - `src/ui/pages/settings.py`：`st.button(...)` 共 5 處
  - `src/ui/pages/data_management.py`：`st.button(...)`、`st.dataframe(...)` 共 4 處
- **狀況**：
  - Streamlit 1.56.0 執行 UI 時持續輸出：
    - `` `use_container_width` will be removed after 2025-12-31. ``
    - `For use_container_width=True, use width='stretch'. For use_container_width=False, use width='content'.`
  - 目前 `src/` 中仍有 21 處 `use_container_width=True`。
- **風險**：
  - 短期：終端機 warning 洗版，干擾驗證與錯誤排查。
  - 中期：Streamlit 後續版本移除該參數後，相關按鈕、表格或圖表呼叫可能直接失敗。
- **建議處理**：
  - 將所有 `use_container_width=True` 改為 `width="stretch"`。
  - 若未來出現 `use_container_width=False`，改為 `width="content"`。
  - 修正後跑 UI 相關測試與 Streamlit smoke check，確認 warning 消失。
- **預計處理 phase**：Phase 6-B 收尾小修，或 Phase 6-C Streamlit API 相容性清理。
- **發現於**：2026-05-10 Phase 6-B 驗證與使用者截圖回報。
- **處理摘要**：
  - `src/ui/pages/backtest.py`、`src/ui/pages/settings.py`、`src/ui/pages/data_management.py` 中 21 處 `use_container_width=True` 已改為 `width="stretch"`。
  - 驗證：`src/` 已無 `use_container_width`；UI 相關回歸 `tests/test_themes.py tests/test_config_ui_section.py tests/test_strategy_config.py tests/test_settings_page.py tests/test_backtest_page.py` 為 `41 passed, 1 warning`（elevated 重跑通過；剩餘 warning 為 pandas-ta，與本項無關）。
  - Streamlit smoke check：`exceptions 0`，未再出現 `use_container_width will be removed after 2025-12-31`。

### [P6-B/相容性] pandas-ta 匯入時觸發 pandas `mode.copy_on_write` deprecation warning

- **位置**：
  - `.venv/Lib/site-packages/pandas_ta/__init__.py:37`
  - 由 UI / 回測相關測試匯入 `pandas_ta` 時觸發，例如 `tests/test_backtest_page.py` 與策略/指標相關模組。
- **狀況**：
  - 測試輸出持續出現：
    - `Pandas4Warning: The 'mode.copy_on_write' option is deprecated. Copy-on-Write can no longer be disabled (it is always enabled with pandas >= 3.0), and setting the option has no impact. This option will be removed in pandas 4.0.`
  - 目前觀察是第三方套件 `pandas_ta` 匯入階段設定 pandas option 造成；不是 QuantTrader 自有程式直接設定。
- **風險**：
  - 短期：測試 warning 噪音，容易掩蓋真正的新 warning。
  - 中期：若 pandas 4.0 移除該 option 且 pandas-ta 未更新，匯入 `pandas_ta` 可能從 warning 升級成錯誤，影響指標計算、策略回測與 UI 圖表。
- **建議處理**：
  - 先確認目前鎖定的 `pandas` / `pandas-ta` 版本與上游是否已有相容版本。
  - 若上游已修，評估升級 `pandas-ta` 並跑指標、策略、回測相關回歸。
  - 若上游未修，短期可在測試設定中針對這條第三方 warning 做明確 filter，但不得忽略 QuantTrader 自有程式的新 warning。
  - 長期可評估替代維護狀態較好的 technical indicators 套件，或將必要指標封裝在本專案內。
- **預計處理 phase**：相容性清理 / pandas 4.0 升級前。
- **發現於**：2026-05-10 Phase 6-B 與 Streamlit API 相容性驗證。
- **2026-05-10 驗證更新**：
  - 已在 `pyproject.toml` 加入 pytest warning filter：`ignore::pandas.errors.Pandas4Warning:pandas_ta`。
  - 驗證：`tests/test_backtest_page.py tests/test_indicators.py tests/test_strategies.py` 為 `45 passed`，測試輸出不再出現 pandas-ta 的 `Pandas4Warning`。
  - 一般匯入驗證：`import pandas_ta` 可正常執行，終端未顯示 warning。
  - 已在 `src/ui/app.py` 的 `from src.ui.pages import ...` 之前加入 app 層 warning filter，module regex 為 `pandas_ta(\..*)?$`，讓 `streamlit run src/ui/app.py` 的終端機也不顯示這條第三方 warning。
  - app 匯入驗證：
    ```powershell
    .\.venv\Scripts\python.exe -W default -c "import runpy; runpy.run_path('src/ui/app.py', run_name='__not_main__')"
    ```
    結果不再出現 pandas-ta `copy_on_write` warning；僅剩 Streamlit bare mode 的 `missing ScriptRunContext` 測試訊息。
  - 但嚴格模式仍可重現根因：
    ```powershell
    .\.venv\Scripts\python.exe -c "import warnings, pandas as pd; warnings.simplefilter('error', pd.errors.Pandas4Warning); import pandas_ta"
    ```
    仍會在 `pandas_ta/core.py` 的 `pd_options.mode.copy_on_write = True` 拋出 `pandas.errors.Pandas4Warning`。
  - **判定**：目前已處理「pytest warning 噪音」與「Streamlit app 終端 warning 噪音」，尚未處理「第三方套件 pandas-ta 對 pandas 4.0 的相容性根因」。本條不可標記為完全已處理；待上游 pandas-ta 修正、升級套件，或改用替代指標實作後，才能結案。

### [P6-C] [✅ 已處理 @ TBD] 回測頁日期輸入欄位排列不直覺，結束日期應放在開始日期右邊

- **位置**：
  - `src/ui/pages/backtest.py` `_input_symbol_and_dates(...)`
  - 影響單次回測、策略比較、參數掃描、Walk-Forward 共用的股票代碼 / 日期輸入區。
- **狀況**：
  - 目前 helper 用 `st.columns(2)`，左欄放「股票代碼」與「開始日期」，右欄只放「結束日期」。
  - 實際畫面會呈現第一列左側是「股票代碼」、第一列右側是「結束日期」，第二列左側才是「開始日期」。
  - 使用者預期日期區塊應以時間順序並排：開始日期在左、結束日期在右。
- **風險**：
  - 使用者容易先看到結束日期、再往下找開始日期，增加操作認知負擔。
  - 多個回測 tab 共用同一 helper，因此這個排版問題會同時出現在單次回測、策略比較、參數掃描與 Walk-Forward。
- **建議處理**：
  - 將輸入區拆成兩列：第一列只放「股票代碼」，第二列用兩欄並排「開始日期」與「結束日期」。
  - 保留既有 key naming（`{key_prefix}_symbol`、`{key_prefix}_start`、`{key_prefix}_end`），避免破壞 session state 與既有測試。
  - 若後續有 UI 測試 helper，補一個輕量測試或手動驗收項目確認日期欄位順序。
- **預計處理 phase**：Phase 6-C 回測頁 UI 細節整理。
- **發現於**：2026-05-10 使用者截圖回報（`Snipaste_2026-05-10_12-03-39.jpg`）。
- **處理摘要**：
  - `src/ui/pages/backtest.py` `_input_symbol_and_dates(...)` 已改為第一列只顯示「股票代碼」，第二列用兩欄並排「開始日期」與「結束日期」。
  - 保留原本 key naming：`{key_prefix}_symbol`、`{key_prefix}_start`、`{key_prefix}_end`。
  - 驗證：相關 UI / 設定 / report 測試範圍 `22 passed`（elevated 重跑通過）。

### [P6-C] [✅ 已處理 @ TBD] `midnight_blue` 主題下回測結果 metric card 文字幾乎不可見

- **位置**：
  - `src/ui/pages/backtest.py` `_render_tearsheet_metrics(...)`
  - `src/ui/pages/backtest.py` DCA 結果 metric 區
  - `src/ui/themes.py` `midnight_blue` 主題與 metric container CSS
  - `streamlit-extras` 的 `style_metric_cards()` 樣式套用路徑。
- **狀況**：
  - 在 `midnight_blue` 且 `ui.use_extras=true` 時，回測結果摘要卡片背景呈白色，卡片文字也接近白色，正常狀態下幾乎看不到「交易次數 / 總報酬率 / 年化報酬率 / 最大回撤 / Sharpe」與數值。
  - 使用滑鼠反白文字後才可辨識內容，表示文字實際存在但前景色 / 背景色對比錯誤。
- **風險**：
  - 使用者無法直接閱讀回測結果的核心績效摘要，會誤以為 UI 沒有輸出或結果卡片壞掉。
  - 因 metric card 是回測頁主要摘要元件，問題會影響單次回測結果；若 DCA 或其他結果區也套用 `style_metric_cards()`，可能同樣受影響。
- **建議處理**：
  - 檢查 `style_metric_cards()` 是否在暗色主題下覆蓋 metric card 背景與文字色。
  - 在 `render_theme_css("midnight_blue")` 或呼叫 `style_metric_cards()` 後追加更高優先級 CSS，確保 `div[data-testid="metric-container"]`、label、value、delta 文字使用暗色主題可讀色。
  - 針對 `midnight_blue` + `use_extras=true` 做 Streamlit smoke check，確認 metric card 正常狀態下不用反白即可讀。
- **預計處理 phase**：Phase 6-C 回測頁 UI 細節整理。
- **發現於**：2026-05-10 使用者截圖回報（`Snipaste_2026-05-10_12-04-49.jpg`、`Snipaste_2026-05-10_12-04-56.jpg`）。
- **2026-05-10 驗證更新**：
  - 已嘗試在 `src/ui/themes.py` 新增 `render_metric_card_override_css(...)`，並於 `src/ui/pages/backtest.py` 的回測 summary / DCA summary 呼叫 `style_metric_cards()` 後追加覆蓋 CSS。
  - 自動驗證通過：`py_compile` 通過；相關測試範圍 `22 passed`。
  - 但使用者截圖 `Snipaste_2026-05-10_12-43-33.jpg` 顯示 `midnight_blue` 下 metric card 仍是白底 + 近白文字，問題依舊。
  - **當時判定**：此條仍未處理完成。現有 CSS selector 可能未覆蓋 `streamlit-extras` 實際產生的 card wrapper，或 override 注入順序 / specificity 不足。後續修正需以實際 Streamlit 畫面截圖確認，不得只靠 unit test。
- **2026-05-10 再驗證更新**：
  - `src/ui/pages/backtest.py` 已改為在呼叫 `style_metric_cards(...)` 時直接傳入目前主題色：`background_color=palette["surface"]`、`border_left_color=palette["primary"]`、`border_color=palette["surface"]`。
  - 此作法不再依賴先前失效的額外 CSS override selector；回測 summary 與 DCA summary 兩處 metric card 都套用相同主題色路徑。
  - 對比估算：`midnight_blue` metric value `#F8FAFC` 對 card surface `#1E293B` 約 `13.98:1`，metric label `#94A3B8` 對 card surface 約 `5.71:1`，均高於一般文字 `4.5:1`。
  - 驗證：`py_compile` 通過；相關 UI / 設定 / report 測試範圍 `22 passed`（elevated 重跑通過）。
  - **判定**：此條已處理完成；仍建議下次手動 smoke test 時補一張 `midnight_blue` 回測結果截圖作為視覺佐證。

### [P6-C] [✅ 已處理 @ TBD] `warm_sepia` / `arctic_light` 主題下 Plotly 圖表與 Performance Summary 文字對比過低

- **位置**：
  - `src/backtest/report.py` Performance Summary 表格產生 / Plotly table 樣式
  - `src/ui/pages/backtest.py` 股價定勢、均線與成交點位圖 / monthly returns heatmap 顯示區
  - `src/ui/pages/backtest.py` 回測結果 tearsheet 顯示區
  - `src/ui/themes.py` `warm_sepia`、`arctic_light` 主題色與 chart / table 文字色套用邏輯
- **狀況**：
  - 在 `warm_sepia` 主題下，回測結果的 `Performance Summary` 區塊背景偏淺，標題與表格文字也接近白色或低對比淺色。
  - 截圖中 `Performance Summary` 標題、`Metric` / `Value` 欄名，以及 Total Return、Annual Return、Max Drawdown、Sharpe Ratio 等列的文字幾乎不可讀。
  - 使用者後續回報 `arctic_light` 也有同樣 Performance Summary 文字看不清楚問題，代表問題可能不是單一主題設定，而是 Plotly table / tearsheet 對淺色主題沒有套用足夠高對比文字色。
  - 2026-05-10 後續截圖顯示 `arctic_light` 不只 Performance Summary，股價定勢圖與 monthly returns heatmap 的 title、legend、axis label、tick label、colorbar label 等 Plotly 文字也偏白，白底上幾乎不可讀。
  - 表格資料實際有渲染，但前景色與背景色對比不足，正常狀態下需要非常仔細看才看得到。
- **風險**：
  - 使用者無法直接閱讀回測績效摘要，會誤以為 tearsheet 沒有輸出或數值消失。
  - Performance Summary 是回測結果的核心摘要，若在 `warm_sepia` / `arctic_light` 不可讀，會影響單次回測、DCA 或其他共用 tearsheet 顯示流程。
  - 這類問題可能影響所有淺色 / 低對比主題的 Plotly table。
- **建議處理**：
  - 檢查 `TearsheetReport` 的 Plotly table header / cell `font_color`、`fill_color` 是否固定使用不適合淺色主題的顏色。
  - 將所有 Plotly 圖表文字色改為依目前主題選擇高對比色；`arctic_light` 白底 / 淺底文字應直接使用深色（例如 `#111827` / `#1f2937`），不要使用白色或過淡灰色。
  - 同時檢查 table header、body row、alternate row、chart background、paper background、title font、legend font、axis tick font、axis title font、annotation font、colorbar tick / title font 的對比。
  - 建議加一個主題 smoke check：分別切換 `warm_sepia` 與 `arctic_light` 後跑單次回測，確認 Performance Summary、股價定勢圖、monthly returns heatmap 的標題、欄名、座標軸、legend、metric 名稱與數值不用反白即可閱讀。
  - 若後續處理 `midnight_blue` metric card 對比問題，建議一併盤點所有主題下回測 summary / tearsheet / dataframe 的對比。
- **預計處理 phase**：Phase 6-C 回測頁 UI 細節整理。
- **發現於**：2026-05-10 使用者截圖回報（`Snipaste_2026-05-10_12-21-48.jpg`；後續補充 `arctic_light` 也同樣發生）。
- **2026-05-10 驗證更新**：
  - 已嘗試在 `src/backtest/report.py` `_apply_theme(...)` 中對 Plotly table trace 的 header / cells 套用 theme 色。
  - 自動驗證通過：`py_compile` 通過；相關測試範圍 `22 passed`。
  - 但使用者截圖 `Snipaste_2026-05-10_12-42-57.jpg`、`Snipaste_2026-05-10_12-43-05.jpg` 顯示 `arctic_light` 下多個 Plotly 圖表文字仍然過淡。
  - **當時判定**：此條仍未處理完成。需要改成全 Plotly 圖表文字高對比策略，而不只是 Performance Summary table trace。
- **2026-05-10 再驗證更新**：
  - `src/backtest/report.py` `_apply_theme(...)` 已對所有 tearsheet Plotly figure 套用 `font={"color": palette["text"]}`，並同步套用 `paper_bgcolor`、`plot_bgcolor`、Plotly table header / cells fill 與 font 色。
  - `src/ui/pages/backtest.py` `_render_price_and_indicator_panel(...)` 已對股價定勢、均線與成交點位圖套用 `font={"color": palette["text"]}`，涵蓋 title、legend、axis label、tick label 等主要 Plotly 文字。
  - 對比估算：`arctic_light` 圖表文字 `#2E3440` 對白底 `#FFFFFF` 約 `12.49:1`；muted `#4C566A` 對白底約 `7.38:1`；`warm_sepia` 圖表文字 `#586E75` 對背景 `#FDF6E3` 約 `4.99:1`。
  - 已知限制：`warm_sepia` 的 `Performance Summary` header 若使用 `surface #EEE8D5` 搭配 `text #586E75`，估算約 `4.39:1`，略低於嚴格 WCAG 一般文字 `4.5:1`。本次判定已處理原先「白字 / 極淡字幾乎不可讀」問題；若後續要做完整無障礙對比標準，可另拆主題色微調項。
  - 驗證：`py_compile` 通過；相關 UI / 設定 / report 測試範圍 `22 passed`（elevated 重跑通過）。
  - **判定**：此條已處理完成；建議下次手動 smoke test 仍補看 `warm_sepia` Performance Summary header，確認視覺觀感可接受。

### [P6-C] [✅ 已處理 @ TBD] `arctic_light` 主題下目前策略清單的「清除」按鈕文字看不清楚

- **位置**：
  - `src/ui/pages/settings.py` 目前策略清單 / 清除策略按鈕
  - `src/ui/themes.py` `arctic_light` 主題 button foreground / background / disabled 或 hover 狀態色
- **狀況**：
  - 在 `arctic_light` 主題下，設定頁「目前策略清單」每列左側的「清除」按鈕背景為深色，但按鈕文字也偏深或對比不足。
  - 截圖中多個「清除」按鈕文字幾乎融入深色按鈕背景，正常狀態下不易閱讀。
  - 策略名稱與參數說明本身可讀，主要問題集中在按鈕文字色 / 按鈕背景色對比。
- **風險**：
  - 使用者難以辨識刪除 / 清除操作入口，尤其在目前策略清單有多列時，會降低設定頁可用性。
  - 若按鈕 CSS 是全域套用，`arctic_light` 其他頁面的 primary / secondary / disabled buttons 也可能有同樣對比問題。
- **建議處理**：
  - 檢查 `arctic_light` 主題下 button 的文字色、背景色、border 色、hover 色與 disabled 色。
  - 確保「清除」按鈕在正常、hover、focus、disabled 狀態下都有足夠對比；至少正常狀態不用反白即可讀。
  - 若「清除」是危險操作，建議採用一致的 danger / destructive button 樣式，避免只靠低對比深色背景。
  - 做設定頁 smoke check：切換 `arctic_light`，檢查「目前策略清單」所有「清除」按鈕文字可讀，並確認其他主題沒有退化。
- **預計處理 phase**：Phase 6-C 回測頁 / 設定頁 UI 細節整理。
- **發現於**：2026-05-10 使用者截圖回報（`Snipaste_2026-05-10_12-24-25.jpg`）。
- **2026-05-10 驗證更新**：
  - 已嘗試在 `src/ui/themes.py` 以 `.stButton > button` 全域覆蓋 button 背景與文字色。
  - 自動驗證通過：`py_compile` 通過；相關測試範圍 `22 passed`。
  - 但對比估算顯示 `arctic_light` 目前 button `primary #5E81AC` 背景搭配 `background #ECEFF4` 文字約為 `3.50:1`，低於一般文字建議的 `4.5:1`。
  - **當時判定**：此條仍未處理完成。建議改用深色文字 / 更深按鈕背景，或針對 destructive button 定義明確高對比配色。
- **2026-05-10 再驗證更新**：
  - `src/ui/themes.py` button CSS 已改為淺色主題使用 `palette["text"]` 作為 button background / border，並用 `palette["background"]` 作為 button 文字色。
  - CSS selector 已加上 `[data-testid="stAppViewContainer"]` 前綴，並同時覆蓋 button 本體、button 內的 `p` 與 `span`，避免全域文字色規則蓋掉按鈕文字。
  - 對比估算：`arctic_light` button 文字 `#ECEFF4` 對 button 背景 `#2E3440` 約 `10.84:1`，高於一般文字 `4.5:1`。
  - 驗證：`py_compile` 通過；相關 UI / 設定 / report 測試範圍 `22 passed`（elevated 重跑通過）。
  - **判定**：此條已處理完成；仍建議下次手動 smoke test 在設定頁確認 hover / focus 狀態沒有退化。

### [P6-C] [✅ 已處理 @ TBD] 策略比較表最右側「備註」欄文字被裁切

- **位置**：
  - `src/ui/pages/backtest.py` `_render_batch_result(...)`
  - 策略比較 tab 的 `st.dataframe(df_compare, width="stretch", ...)`
  - `df_compare` 最右側欄位：`備註`。
- **狀況**：
  - 策略比較結果表欄位較多，最右側「備註」欄可視寬度不足。
  - 當某策略有備註文字（例如定期定額策略不適用一般批次比較流程）時，文字只露出前段，右側內容被表格邊界裁切，使用者無法完整閱讀。
- **風險**：
  - 使用者看不到策略失敗或被略過的完整原因，容易誤判策略比較結果。
  - `備註` 欄通常承載 error / skip reason，若不可讀會降低批次比較 tab 的診斷價值。
- **建議處理**：
  - 為 `備註` 欄設定較寬的 column config，或改用可顯示完整內容的文字欄 / tooltip / 展開區。
  - 確認策略比較表在 `midnight_blue` 與淺色主題下，最右欄文字都能完整閱讀或可透過互動查看完整內容。
  - 若保留 `st.dataframe`，可評估調整欄位順序，把 `備註` 移到較容易閱讀的位置，或縮短其他數值欄位寬度。
- **預計處理 phase**：Phase 6-C 回測頁 UI 細節整理。
- **發現於**：2026-05-10 使用者截圖回報（`Snipaste_2026-05-10_12-14-04.jpg`）。
- **處理摘要**：
  - `src/ui/pages/backtest.py` 策略比較表 `st.dataframe(...)` 已加入 `column_config={"備註": st.column_config.TextColumn("備註", width="large")}`。
  - 驗證：相關 UI / 設定 / report 測試範圍 `22 passed`（elevated 重跑通過）。

### [P7-D-2/Hotfix] [✅ 已處理 @ TBD] Walk-Forward 執行完成後寫入 `st.session_state["wfa_symbol"]` 會撞到 widget key

- **位置**：
  - `src/ui/pages/backtest.py` `_render_walk_forward_tab(...)`
  - `src/ui/pages/backtest.py` `_input_symbol_and_dates("wfa")`
  - 具體衝突 key：`wfa_symbol`
- **狀況**：
  - 在回測頁切到「Walk-Forward（滾動樣本外驗證）」tab，設定股票、日期、策略與參數後，按「開始 Walk-Forward 分析」。
  - WFA 計算完成後，程式嘗試執行：
    ```python
    st.session_state["wfa_result"] = summary
    st.session_state["wfa_symbol"] = symbol
    ```
  - 但同一頁稍早已透過 `_input_symbol_and_dates("wfa")` 建立股票代碼輸入框：
    ```python
    st.text_input("股票代碼", value="2330", key=f"{key_prefix}_symbol")
    ```
  - 因 `key_prefix="wfa"`，該 widget 的 key 也是 `wfa_symbol`。
  - Streamlit 規則是不允許 widget 建立後再手動修改同名 `st.session_state` key，因此拋出：
    ```text
    streamlit.errors.StreamlitAPIException: st.session_state.wfa_symbol cannot be modified after the widget with key wfa_symbol is instantiated.
    ```
- **重現步驟**：
  1. 啟動 UI：
     ```powershell
     .\.venv\Scripts\streamlit.exe run src\ui\app.py
     ```
  2. 進入「回測」頁。
  3. 切到「Walk-Forward（滾動樣本外驗證）」tab。
  4. 股票代碼使用 `2330`，日期選足夠產生 WFA 視窗的區間（例如最近 3 年）。
  5. 使用預設或小範圍參數，按「開始 Walk-Forward 分析」。
  6. 執行完成後頁面報錯，結果區無法顯示。
- **根因判定**：
  - `wfa_symbol` 同時被當成 Streamlit widget key 與 WFA 結果暫存 key。
  - 這不是 WFA 核心引擎問題；`run_walk_forward_analysis(...)` 已完成後才在 UI session state 寫入階段失敗。
- **風險**：
  - 使用者無法在 UI 完成 Walk-Forward 手動驗收，因為結果計算後會被 Streamlit session state 例外中斷。
  - CSV 匯出入口與 summary/window/stability tables 都無法到達。
  - 7-D-2 雖然核心回歸測試通過，但實際 UI 操作會失敗，屬高優先級 hotfix。
- **建議處理**：
  - 不要覆寫 widget key `wfa_symbol`。
  - 將 WFA 結果用的 symbol 暫存 key 改名，例如：
    ```python
    st.session_state["wfa_result_symbol"] = symbol
    ```
  - 結果渲染處同步改為：
    ```python
    _render_wfa_results(
        wfa_summary,
        symbol=st.session_state.get("wfa_result_symbol", ""),
    )
    ```
  - 或者更簡單：不要另存 symbol，直接使用目前 widget 回傳的 `symbol` 渲染結果；但若未來允許切換輸入後保留舊結果，建議使用 `wfa_result_symbol` 才能清楚區分「目前輸入」與「結果所屬標的」。
- **驗收方式**：
  - 重新執行 Walk-Forward tab，按「開始 Walk-Forward 分析」後不應再出現 `StreamlitAPIException`。
  - WFA 完成後應顯示 summary metrics、window table、parameter stability table。
  - 結果標題應顯示正確股票代碼。
  - 切換其他 tab 後再回到 Walk-Forward tab，結果仍能正常顯示或以既定行為清除，不得因 session state key 衝突報錯。
- **預計處理 phase**：Phase 7-D-2 hotfix（Walk-Forward UI 驗收修正）。
- **發現於**：2026-05-10 Phase 7 手動驗證，使用者按下「開始 Walk-Forward 分析」後回報 traceback。
- **處理摘要**：
  - `src/ui/pages/backtest.py` 已將 WFA 結果暫存 key 從 `wfa_symbol` 改為 `wfa_result_symbol`。
  - 結果渲染處同步改為讀取 `st.session_state.get("wfa_result_symbol", "")`。
  - 靜態檢查確認目前 `src/ui/pages/backtest.py` 不再手動寫入 `st.session_state["wfa_symbol"]`。
  - 驗證：`py_compile src\ui\pages\backtest.py` 通過。尚需使用者或後續 UI smoke test 實際點擊 Walk-Forward tab，確認不再出現 `StreamlitAPIException` 且結果表可顯示。

---

## 2026-05-10 Phase 8-F 儀表板 UI 驗收新增待修項目

### [P8-F] [✅ 已處理 @ 5bcac6e] 日 K 線圖可讀性不佳（與參考設計落差大）

- **位置**：`src/ui/pages/dashboard.py:254-310`（`_render_price_chart` 函式）
- **第一輪修正**（2026-05-10）：
  1. ✅ **Y 軸**：圖高改為 650px；`yaxis.range` 鎖定至 `[low.min - 5% padding, high.max + 5% padding]`。
  2. ✅ **K 棒寬度**：MA 計算後先 `.tail(250)` 切片，顯示近 250 根 K 棒，Plotly 自動分配合適棒寬。
  3. ✅ **MA 均線**：三條 Scatter 均加 `line={"width": 1}`，由預設 2px 改為 1px。
  4. ✅ **支撐壓力線**：移除 `add_hline`，改用 `add_shape`（最後 20 根 K 棒長度的短線段）+ `add_annotation`（右側文字標註 `壓 xxx.x` / `撐 xxx.x`）。
- **第二輪待修**（2026-05-10 手動截圖對比）：
  5. **K 棒改實心並加寬**：目前紅綠 K 棒為空心描邊，與參考設計（實心填充）落差大。棒寬仍偏窄，左右間隙過大。需將 Candlestick 的 `increasing_fillcolor` / `decreasing_fillcolor` 設為實心，並調整棒寬或進一步縮減顯示根數。
  6. **撐/壓標籤與 MA legend 重疊**：`add_annotation(x=1, xref="paper")` 的文字（撐 xxx / 壓 xxx）與右側 legend（MA20 / MA60）位置重疊。需將標籤左移（如 `x=0.98`）或調整 legend 位置。
- **驗證**：`tests/test_dashboard_page.py` 10 tests passed。
- **預計處理階段**：Phase 8-F UI 細節修正。
- **處理摘要**：K棒已設定 `increasing_fillcolor="#ef4444"` / `decreasing_fillcolor="#22c55e"` 為實心並設定框線顏色一致；K線顯示數量縮減至 `tail(120)` 以加寬棒體；撐壓標籤移至 `x=0.98` 並使用 `xanchor="right"` 避免與 MA legend 重疊。

---

### [P8-A] [✅ 已處理 @ 5bcac6e] 壓力/支撐價位計算 bug：`_last_value` 取最後一天而非極值

- **位置**：`src/analysis/technical_summary.py:290-291, 312`
- **狀況**：
  - `_find_resistance_levels` 第 290-291 行使用 `_last_value(h.tail(60))` / `_last_value(h.tail(20))`，`_last_value` 只取序列最後一筆值（最新一天的 high），**不是** 近 60/20 日的最大值。導致「近60日高點」實際上是最新一天的最高價。
  - `_find_support_levels` 第 312 行使用 `_last_value(l.tail(20))`，同理取的是最後一天的 low，**不是** 近 20 日最低點。
  - 正確寫法：壓力應為 `h.tail(60).max()` / `h.tail(20).max()`；支撐應為 `l.tail(20).min()`。
- **風險**：
  - 壓力/支撐價位完全錯誤，影響儀表板「關鍵價位」顯示。
  - 連帶影響 `_score_breakout` 的短線綜合分數計算（breakout 權重 20%）。
  - 測試未抓到此 bug，因為 fixture 使用 `np.linspace` 等差遞增，最後一筆恰好接近極值。
- **處理階段**：Phase 8-A hotfix。
- **發現於**：2026-05-10 Phase 8-F 手動驗收，使用者質疑「壓力區近60日高點: 1050.00」數值明顯偏低。
- **處理摘要**：已將 `_find_resistance_levels` 與 `_find_support_levels` 中誤用的 `_last_value` 修改為 `float(h.tail(60).dropna().max())` 與 `float(l.tail(20).dropna().min())`，正確找出現部極值點。
